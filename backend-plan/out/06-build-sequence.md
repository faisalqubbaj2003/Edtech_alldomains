# CAROS backend · Pass 6 · Build sequence

Written 2026-09-23 against `index.html` at commit `fb28217` (10,048 lines), `backend-plan/CONTEXT.md`, `PRODUCT.md`, `PHASES.md`, and the five earlier passes (`out/01` to `out/05`). Every tooling, platform and vendor claim that no earlier pass had already verified was checked on 2026-09-23 against a named page and is cited where it is made and again under Sources; claims that could not be checked are labelled as assumptions. Session counts and calendar figures in this pass are **planning assumptions**, not commitments, and are marked so where they appear.

**Revised 2026-09-24 by pass 8, session 3**, to apply Davide's decisions on the pass 7 review (`07-review.md`) and the handoffs of sessions 1 and 2 (`08-changelog.md`). The revision keeps every section number. Its three largest changes: the build is now planned for **Davide building alone**, with **G-REAL in the first fortnight of the spring term 2027 (April 2027)** and August 2027 as the fallback, counted in sessions rather than weeks (§2.3, decision on F39); **Opus 5.5 under the zero-data-retention organisation writes every task and Fable 5.1 reviews diffs only**, with a B0 bake-off (§4, decision on C7); and **a light process while one person builds**, with the trigger for the full one (§2.4, §2.5, decision on C8). §0.5 lists everything else. Every change is in `08-changelog.md` under Session 3.

This pass turns the five earlier passes into an order of work. It does not redesign anything they settled; where it found them disagreeing with each other it says so (§12) and plans on the later pass.

## 0. Read this first

### 0.1 Departures from section 3 of CONTEXT.md

**None from section 3.** Every decision in section 3 is planned on, as CONTEXT.md now records it after pass 8 (the declared email inventory of pass 4 §5.8 under decisions C2 and C3, the `school_admin` and `staff` roles, the lexicon-only safety screen and no headline rephrase). **One departure from section 2, by Davide's decision (F39, C8):** CONTEXT §2 says there is no fixed deadline and that teammates work in parallel. The revision plans on a dated target, G-REAL in the first fortnight of the spring term 2027, with Davide as the only builder until a second regular builder joins, and states what that date costs (§2.3). The streams of §2.1 are kept as package boundaries and as the structure the full process switches on to; they are not staffed. Three things this pass does are narrowings a reader must know before the phases make sense:

1. **The complete schema is applied in slices, and the slices are set here.** Pass 1 designed the full schema (135 tables in 19 schemas in its first version, more after pass 8: 01 §0.3) and its §5.6 says a module's migrations land in the phase that first serves it. Passes 2 to 5 numbered their schema deltas D1 to D75 "in migration order" *within each pass*. Those numbers are design identifiers, not the order the migrations are applied: pass 4's `auth.school_domain` (D37) is needed at the first sign-in, long before pass 3's engine tables (D17 to D32) exist. §1.12 gives the applied order, phase by phase, and is the single source: every D-number and every object pass 8 added to pass 1 lands exactly once, and each task's migration list is generated from it (F40).
2. **Pass 1's first default permission rows are never applied.** Pass 4 §3.6 replaced them and its D36 deletes them. The first migration that creates `auth.role_permission` seeds pass 4's matrix directly, generated from `packages/contracts/authz/matrix.json` through pass 1 §2.4a, so no screen is ever built against a matrix that is later replaced and no qualifier ships without its mechanism and its negative test (F02).
3. **"The thin slice" is agreed in content and split into four phases** (B0 to B3, §1.13), because as one phase it is about a hundred agent sessions with no checkable "done" in the middle, and because pass 1's narrative seed layer means the first screens do not have to wait for the engine. For the April 2027 target the counselor-facing half of B3 moves behind G-REAL (§2.3): shadow mode shows counselors no tiers, so the sheet, the case file and Demo 1 are needed by the first reveal session and G-LIVE, not by the first real record.

### 0.2 Where the code and the documents disagree

The code wins. This pass read the router, the navigation and the write-side handlers of the prototype to fix the port checklist (§6.2), and found these differences with CONTEXT.md §8 and §9:

| Topic | Document says | `index.html` does | Consequence for the build |
|---|---|---|---|
| Counselor views (CONTEXT §8) | `command` and `si-cases` listed as two views ("command centre", "priority queue") | the router maps both to `vCommand` (`index.html:9010`, `:9011`), and `si-interventions` to `vWork` (`:9012`) | one screen with a filter, not two; the router's chain has 25 counselor route keys (plus the ten dimension keys and the open case or file), rendered by 21 distinct view functions, and the port counts screens, not keys |
| The caseload sheet (CONTEXT §8) | four views `cohort-schedule`, `cohort`, `cohort-all`, `today` | all four render `vSheet` (`:9003`, `:9007`) | one screen with four modes |
| The dimension pages | "the ten dimension pages (`DIMS`)" | `DIMS[S.cv]` dispatches to one `vDim(S.cv)` (`:9002`, `:3263`) | one screen component, ten rule sets from `config.rule_set_version 'dimensions'` (pass 3 §9.3) |
| Student and parent navigation (CONTEXT §6) | "grade-conditional (`roleNav`, `index.html:1898`)" | confirmed at `:1898` to `:1947`; the student list has 11 possible entries across grades and tracks, the parent list 8; Grade 10's `stuclass` entry is the selection form itself, not a record | the port carries the grade and track conditions as data on the tenant (`sis.year_group.stage`, `sis.programme.family`), never as literal grade numbers (pass 1 DR-8 second-school checklist) |
| Audit events written (CONTEXT §9) | the mutation inventory lists an event per action | `logAudit(...)` is called with 37 distinct action keys when calls with a computed actor are counted (the first version said 33, F72); three of them (`PRIORITY_CHANGE`, `CASE_REASSIGNED`, `CASE_CLOSED`) from more than one handler; several actions in the inventory write nothing (`FILE_ACCESS`, bulk actions, mentor actions, roadmap XP); the seed carries `CONCERN_SUBMIT` (no such key) and an `AI_DRAFT` "Letter draft generated" entry | the port writes pass 1's taxonomy plus the keys of passes 2, 4 and 5 through `audit.record()`, one list with no duplicates (§12.16), not the prototype's subset; the seed maps `CONCERN_SUBMIT` to `TEACHER_FLAG` and drops the `AI_DRAFT` letter entry (pass 1 DR-8); every domain function names its action in its session brief |
| Route-states | not counted | `PHASES.md` P0 verified "all 98 route-states render without a console error" | 98 is the port's completeness count; the checklist in §6.2 is organised by those states |
| Write-side handlers (CONTEXT §9: `bind()` at `:9141`) | confirmed | 479 `data-*` attribute occurrences across roughly 100 distinct handler kinds | each write handler maps to one domain function (pass 1 DR-2's single path); the mapping table is the first artefact of each screen's port (§6.1) |
| Demo apparatus (CONTEXT §11.18) | role switcher, grade preview and track preview are not product | `roleBar()`, `S.previewGrade`, `gradeSwitcher()` (`:9090`) | not ported; the Next.js app has one acting role per session (pass 4 §2.4); the synthetic tenants carry real memberships for the demo personas instead |

### 0.3 What passes 1 to 5 handed to this pass

This paragraph records what the first versions of passes 1 to 5 asked for on 2026-09-23. Pass 8 changed several requests (the RISC receiver is withdrawn, F18; pass 4 asked for D33 to D61, not D33 to D60, F40; the safety screen has no model layer, C1); what pass 8 asks of this pass is in `08-changelog.md`'s handoff and is applied throughout. Pass 1 §7 asked for: the order in which §2's schemas are migrated (§1.12); the thin slice and its tables (§1.1 to §1.4); the RLS suite and the second-school checklist as week-one tests (B0, §5.4); the restore drill before real data (B4, §7.6); the Container Apps Job for migrations (§7.3); `docs/PLAN.md` and `CLAUDE.md` content (§3.2, §3.3); the UAE Central access request as a week-one task (B0). Pass 2 §13 asked for: the Studio's four screens in the thin slice (B1); the fixture packs as week-one tests (B1, §5.5); migrations D1 to D16 in its order (§1.12); the SFTP endpoint in Terraform (B4, open decision on Blob SFTP verified in the portal, §7.2); connector adapters after four green weeks (B9); the "four green weeks" gate before auto-approval (§1.11); the `expected_cadence` panel (B3). Pass 3 asked for: `packages/engine` as a pure library with the scenario suite as its acceptance test, built before any screen and before the narrative layer is retired (B2); the calibration script beside it (B2); D17 to D32 in order (§1.12); the alert rules and `/health/sweep` in the sweep's definition of done (B2, §7.4); the elicitation session before the first configuration version (§1.11); shadow mode as the pilot's first phase with the counselor log in the thin slice (B2, §1.11); the backtest as a gate when history exists (§1.11); Fable 5.1 for the statistics and the tiering rows, and a review by a different model (§4). Pass 4 asked for: the week-one items that cannot be retrofitted (B0); the §9.13 list as a phase gate (B4, §8); the Google Workspace and safeguarding-route onboarding checklists (§9); the RISC receiver early (B1); the DPA, DPO and notices as non-code deliverables (B4, §8); the penetration test and tabletop before the first real record (B4, §8); D33 to D60 (§1.12); the support console in the thin slice (B1); the `no-real-data` and log-scanner tests from the first commit (B0); the SFTP verification (§7.2); six runbooks (§7.7). Pass 5 asked for: the week-one non-code items (the ZDR conversation, the organisation and workspaces, the smoke test) (B0 and B7); the build order inside the AI phase (B7); D62 to D75 (§1.12); the eval runner as a CI gate from the first AI commit (B7, §5.6); labelling sessions before each feature's acceptance (§8); the Extended Essay guide decision before the IB module is built for a 2027 cohort (B6, §13); the cost script's home (`packages/ai/tooling/cost.py`, B7).

### 0.4 The build sequence on one page

Ten phases, each an integration milestone on `main` with a checkable "done". Five gates sit on the sequence, and none can be skipped.

| Phase | Name | What exists when it is done | Gate at exit |
|---|---|---|---|
| **B0** | Foundations | the repository, CI with both synthetic tenants and the CI-only `wellesmere-shifted` tenant, staging in UAE North with CMK and geo-redundant backup set at creation, one login role per deployable, the spine tables (core, auth, config, audit, events, privacy, doc, notify, reporting, jobs), `withTenant()`, `auth.allowed()` and `auth.protect()` with every matrix qualifier's mechanism, pass 4's matrix, the RLS and authz suites with D61's tests, the tenant CLI, the plan file and `CLAUDE.md`, and the model bake-off's verdict | **G-CI**: every PR runs the RLS suite against both tenants |
| **B1** | Facts and identity | `packages/ingest` end to end, the Mapping Studio's import, identity and cadence screens, both fixture packs (with the history packs) imported through the real pipeline, Google sign-in for staff, sessions and step-up (after the `auth_time` spike), the hourly Directory read, the support console with operator- and table-bound grants | fixtures.spec and roster-security green on both tenants |
| **B2** | Engine and sweep | `packages/engine` with S1 to S26 and the property tests, shadow cases on the live lifecycle, the sweep jobs over schools holding data, the two watchdogs and the alert rules, thresholds versioning with the activation path, the counselor log and the shadow view | `engine-reproduces-seed` equals the lists the rules generate for ACS, Wellesmere and `wellesmere-shifted`; a staging sweep completes on schedule for seven nights |
| **B3** | The counselor's morning | **before G-REAL:** the escalation end to end (counselor and teacher referrals, the pin, category routes, the reporter's countdown, the reference-only email, the CPO's acknowledgement), the outbox and templates of pass 4 §5.8, the teacher register and flag modal, the flags inbox, the shadow-mode caseload list with the counselor log, access logging and the audit screen, in-app notices; **after G-REAL, before the first reveal and G-LIVE:** the caseload sheet in four modes, the case file with its evidence chain, cover and break-glass, the command centre, `si-why`, the dimension pages, Demo 1 | **G-DEMO**: Demo 1 on staging (§6.5), now during shadow mode |
| **B4** | Pilot readiness | privacy tooling (notices, the DPIA's evidence, SAR pack, erasure with restore replay, restriction), production in UAE North, WAF, the canary as a UAE North job, the pen test against production and its fixes, the review pack, the restore drills in their own subscription, on-call with an escalation job, the onboarding runbook and CLI, the elicitation session | **G-REAL**: pass 4 §9.13 complete; the first real record may move; target the first fortnight of the spring term 2027 |
| **B5** | The application season | the university module with sectioned statements and UCAS reference formats, the parent portal (activation per SIS contact, magic links, messages, meetings), the student record surfaces, and the lexicon safety screen with the first student-facing screen | Demo 2 |
| **B6** | The Diploma | IB subject selection with its gates, the demand sheet, CAS and the Extended Essay (the ManageBac read-only mirror where the school keeps ManageBac, `ee_2027` by default), the coordinator views | Demo 2 (with B5) |
| **B7** | AI | the gateway, pseudonymiser, validator and leak scanner; the registry and prompts; the quarantined reader, lazy per record kind; then the five features in pass 5 §9.6's order, each behind its eval bar, the school's written acceptance and ADEK's consent | **G-AI** per feature |
| **B8** | Discovery, mentors, reporting | the discovery flow in both modes with the approval gate, XP, the mentor path (vetting, passkeys, coordinator-made pairings, held messages, in-person sessions only), per-tenant reporting aggregates | Demo 3 |
| **B9** | Connectors, second school, scale | the Veracross, ManageBac (rosters and grades; the CAS and EE mirror is B6's) and Classroom adapters in the order ACS's answers dictate, the iSAMS adapter, the second real school, the year-1 items | the second school onboarded with a human at seven steps only (pass 2 §9) |

**The April 2027 target** (decision on F39). G-REAL lands in the first fortnight of the spring term 2027, with Davide building alone. That holds only with the cuts §2.3 names: the counselor-facing half of B3, the thresholds what-if, the offboarding drill, the load test and the shadow comparison report move behind G-REAL (they are needed by the first reveal session or G-LIVE, not by the first real record); B5 and B6 no longer land during shadow mode but after G-LIVE, in the 2027-28 year; B7 waits for G-LIVE, the ZDR arrangement and ADEK's consent. Nothing in pass 4 §9.13 is cut. Three checkpoints decide whether the date holds; if the second or third fails, the target becomes the first fortnight of the 2027-28 year, **August 2027** (§2.3).

The **pilot overlay** (§1.11) runs from G-REAL: onboarding (pass 2 §9), at least twenty weeks of history imported, then shadow mode with teacher flags live and no visible tiers, the reveal sessions at shadow weeks 4 and 8, then **G-LIVE** on pass 3 §14.5's pooled, pre-registered criteria, planned for the start of the 2027-28 year, then the AI features one at a time as the school accepts each and ADEK consents.

**Three demos.** Demo 1 (the rest of B3, during shadow mode) is the first build a school can be shown that the prototype cannot match: real sign-in, the real engine over the weekly ACS packs and their history, an escalation that goes somewhere, and the same product running the British school. Until then the frozen prototype remains the sales demo and its script skips §6.3's lines. Demo 2 (end of B6) adds the family, the student and the Diploma. Demo 3 (end of B8) adds the AI features on synthetic data with their labels and evidence panels. §6.5 says exactly what each shows and what it does not.

**Streams.** While Davide builds alone every task is his; the four streams of §2.1 (D spine, A facts, B engine, C programmes) remain the package boundaries the tooling enforces and the ownership a second regular builder would take on (§2.5).

### 0.5 What pass 8 changed in this pass

Session 3 of pass 8 revised this file in place on 2026-09-24. In summary (every change is in `08-changelog.md`, Session 3):

- **Schedule and process.** Week counts are replaced by session counts; G-REAL targets April 2027 with Davide alone, every external gate is back-scheduled from it, the cuts are stated, three checkpoints and the August 2027 fallback are named (§2.3, F39). The light process of C8 while one person builds, and the trigger for the full one (§2.4, §2.5). Capacity is planned against usage and rate limits, not dollars (§4.4).
- **Models.** Opus 5.5 under the ZDR organisation writes every task; Fable 5.1 reviews diffs of code and synthetic data only; B0.16 is the bake-off on the `auth.allowed()` rewrite; §4.1 to §4.3 are regenerated from the task tables, and every silent-error task F38 lists gains a Fable review (C7, F38).
- **Tenancy and authorization.** Per-deployable login roles with `NOINHERIT` and the tier in the policy expression (F01); the matrix through pass 1 §2.4a with every qualifier's negative test and positive verb tests (F02); narrow-only widening with `allow_counselor_widening` (F47); the `school_admin` and `staff` roles, capability grants under four eyes, break-glass confirmed by the lead (F16); pastoral assignments (F33); support grants bound to operator and table, and CAROS writes only under school-approved grants (F35); one status model, `onboarding → shadow → live`, with enumeration through ID-only functions and pg-boss under its own login (F36).
- **Identity.** The RISC receiver is removed; the Directory read is mandatory and hourly for staff (F18); step-up proves `auth_time` in a B1 spike, with a staff passkey fallback (F19).
- **Engine.** Pass 3's revised engine: attendance in days over fortnight blocks, the detuned pilot defaults, no common-cause cap, the escalation pin, raises never held, shadow cases on the live lifecycle, S24 to S26, and the seed expectation of 8 of 15 authored tiers (F05 to F08, F24 to F26, C9).
- **Safeguarding.** Teacher referrals and the disclosure rule in the flag modal (F10); the reporter's countdown from escalation, category routes confirmed by the CPO, 999 and the Principal on immediate risk, no clause numbers on screen (F09, F55); the lexicon-only safety screen moves from B7 to B5 (C1, F03); the CPO's "alert unopened" notice (F12); only pass 4 §5.8's emails, each content-free and content-tested (F28, F60, C2, C3).
- **Removed from v1.** The headline rephrase (F52), the safety screen's model layer (C1), the "AI read" score (F66), mentor video sessions (C6), fairness labels in the pilot (C11).
- **Residency and development tooling.** Read and Bash hooks, no production credential on agent machines, and the canary, backtest and restore checks as jobs in UAE North (F23); the cold copy as a real choice with ADEK's consent (F22, F04); ADEK consent as a named gate (F04).
- **Bookkeeping.** §1.12 is the single migration source with D61 and D75 placed and pass 8's new objects landed (F40); O11, O33 and O35 closed (F72); §13 and §14 rebuilt; §14 now points to `ACS-IT-QUESTIONS.md`, where every question is tagged with the gate it blocks (F78).

---

## 1. The phases

### 1.0 Conventions

- **A phase is an integration milestone on `main`**, not a calendar block. It is done when its acceptance criteria are checked (by someone other than the builder of the last piece once the full process is on; in solo mode, by the Fable review and the required checks, §2.4), its tests are in CI, its `docs/PLAN.md` entry reads `complete <date> (commit …)`, and `CLAUDE.md` has been rewritten from the build (§3.6). While Davide builds alone, phases run in sequence except where §2.3 says a later phase's first tasks start early.
- **A task is one agent session**: two to four hours, one branch, one pull request, one model, a written brief (§3.1). The task tables below give each task's stream (D, A, B, C, per §2.1: the package boundary and the future owner; in solo mode every task is Davide's), its model and effort, and a **Rev** column naming the model that must review the diff. **Decision C7:** every task is written by Opus 5.5 under the ZDR organisation (`O`), at the effort given; `F` in the Rev column means Fable 5.1 reviews the diff, which carries code and synthetic data only, never tenant data. Where the first version had Fable 5.1 write a task, the row now reads `O max` with `F` review. The B0 bake-off (B0.16) may move a category of tasks back to Fable as writer (§4.2). Session counts per phase are planning assumptions.
- **Model lines** follow `PHASES.md`: `**Model:** … · **Why this model:** …`. The review rule is in §4.2 and §4.3.
- **Migrations** are named `NNNN_<phase>_<subject>.sql` and applied in the order of §1.12, which is the single source: a task's migration list is generated from it, and a task row that disagrees with §1.12 is wrong. A D-number from passes 2 to 5 lands once, in the phase named there. A phase never creates a table nothing in that phase reads or tests (pass 1 §5.6).
- **C-numbers.** "Decision Cn" is one of pass 7's judgment calls as Davide decided them; "pass 6 decision Cn" is one of this pass's own challenges (§11); "counsel Cn" is a question for counsel (§14.8); a bare C1 to C25 inside the `signals` module is one of pass 1 DR-7.1's case transitions.
- **"Both tenants"** always means the ACS and Wellesmere synthetic tenants (pass 1 DR-8); the CI-only `wellesmere-shifted` tenant (Europe/London, a Friday and Saturday weekend) runs every suite that touches time. A screen or rule is not done until the Wellesmere checklist assertions for it pass, including that no "ADEK" string appears on any Wellesmere screen, template or email (F31).
- **Cut or deferred** in a task row means the April 2027 plan moves the task behind G-REAL (§2.3); the row keeps its number and its acceptance criteria.
- Line references are to `index.html` at `fb28217` and drift; the port checklist (§6.2) carries function names so it survives drift.

### 1.1 B0 · Foundations

**Status:** not started · **Depends on:** nothing · **Model:** Opus 5.5, high effort for the scaffold, CI, infrastructure and UI kit; **Opus 5.5, max effort** for the four tenancy pieces (roles and spine migration, `withTenant()` and the migrator, `auth.allowed()` and `auth.protect()` with the matrix mechanisms, the RLS and authz harness) · **Reviewed by:** Fable 5.1 on every tenancy piece, the Terraform module, the hash chain, the demonstration marker and the logger; the bake-off (B0.16) runs on B0.7
**Why this model:** most of B0 is scaffolding Opus 5.5 does quickly and well; the tenancy layer is written once, every later line trusts it, and a hole in it is invisible until a school reads another school's child, so it gets maximum effort, the other model's review, and the bake-off that tests whether Opus 5.5 is good enough for it (C7).

**Scope.** The repository of pass 1 DR-3 with every package present (stubs where the phase does not fill them); the toolchain (pnpm 12, Turborepo 2.11, Node 24, TypeScript strict, Biome, `dependency-cruiser` with DR-3's import matrix, Vitest 5, Playwright, Drizzle 0.45 pinned with `drizzle-kit generate` and `drizzle-kit check`, pg-boss 12); the supply-chain settings of pass 4 §9.8 from the first commit (`minimumReleaseAge = 10080` for production dependencies, lifecycle-script allowlist, gitleaks, Renovate, Trivy, pinned Actions); the GitHub organisation on a **GitHub Team** plan, because rulesets, branch protections and deployment environments on a private repository need it ([about rulesets](https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-rulesets/about-rulesets): rulesets are for "customers on GitHub Team and GitHub Enterprise plans"; [environments](https://docs.github.com/en/actions/how-tos/deploy/configure-and-manage-deployments/manage-environments): "Organizations with GitHub Team and users with GitHub Pro can configure environments for private repositories"); the solo-mode ruleset and the anti-overwrite checks of §2.4; CI (§5.8); the Terraform module of pass 1 DR-9 applied to **staging** in UAE North with the two creation-time settings that cannot be retrofitted made non-optional in the module: customer-managed keys on the server and the storage accounts (pass 4 §9.2) and geo-redundant backup (pass 1 DR-1); the UAE Central access request submitted; the Azure Monitor action group with email, SMS and voice to +971 numbers ([action groups](https://learn.microsoft.com/en-us/azure/azure-monitor/alerts/action-groups) lists country code 971 under both SMS and voice support; voice calls to the UAE come from a US number); GitHub Actions authenticating to Azure by OIDC federated credentials with no stored secret ([Azure Login with OIDC](https://learn.microsoft.com/en-us/azure/developer/github/connect-from-azure-openid-connect)), for staging only (GitHub-hosted runners never hold a production credential or read production, F23); the **spine** of the schema (below); **one login role and managed identity per deployable** (`caros_web`, `caros_worker`, `caros_support`, `caros_migrator`, `caros_jobs`), all `NOINHERIT`, with `caros_owner` unable to log in (pass 1 DR-4, F01); `withTenant()`, the migrator as a Container Apps Job with its second, non-transactional step for files marked `-- no-transaction` (F40), `auth.allowed()` with the tier evaluated in the policy expression and passed in as an argument (F01), `auth.protect()` with the class expression, row kind, addressee, pairing and verbs (F02), the ID-only definer lookups (`auth.school_for_domain`, `core.schools_due_for_sweep`, `core.schools_holding_data` and the others of pass 1 §2.4, F36), `notify.enqueue()`, `audit.record()` and `events.emit()` with their per-tier allowlists (F75), `audit.chain()`, `core.ensure_month_partitions()`; pass 4's permission matrix generated from `packages/contracts/authz/matrix.json` through pass 1 §2.4a, with the projection views; the RLS and authz harness, including D61's tests; the tenant CLI (`tooling/tenant`, pass 2 §9 step 3) creating the synthetic `core.school` rows (`purpose = 'demo'`, a status the sweep visits) with modules, calendars, year groups, programmes and programme families, timetable periods, a regulator profile each (ADEK for ACS, an explicit empty profile for Wellesmere, F31) and the demo personas' memberships and capability grants; the UI kit ported from `DESIGN.md` into `packages/ui` (the three token layers, the tier marks `U C R M P`, the run cells, `tally`, `empty`, the letterhead from `core.school.brand`, the demonstration marker driven by `core.school.demonstration_marker`, fonts self-hosted with `font-src 'self'`, F76); the app shell with role navigation as data; tRPC mounted at `/api/trpc` with a health router; `/health` and `/health/sweep`; the worker with its heartbeat line and the partition job; pg-boss under its own login `caros_jobs` with no tenant privilege, payloads validated as ids, completed jobs deleted after seven days and registered as a non-tenant store (F36); `CLAUDE.md`, `docs/PLAN.md`, `docs/decisions/`, `docs/spec/` (frozen copies of `CONTEXT.md`, `PRODUCT.md` and `out/01` to `out/08` with their commit hash), the runbook skeleton, `.claude/rules/` and the hooks (§3.4); the documentation checks (§3.6); the model bake-off (B0.16).

**The spine (tables B0 creates).** `core.school` (with `purpose`, the one status model and its transition trigger, D36's `allow_counselor_widening`; classification, purpose and branding immutable, F42, F36, F67), `core.school_module` (with D10 and D60 keys documented), `core.person`, `core.staff_profile`; all `auth.*` tables of pass 1 §2.4 as revised, including `auth.capability_grant` and `auth.pastoral_assignment` (F16, F33), with D33 (sensitivity), D34 (`export`), D35 (`mentor_coordinator`), D36 (the matrix and `auth.forbid_widening()`), D37 (`auth.school_domain` with DNS proof), D38 (no JIT), D39 (session columns), D44 (it lands with `auth.allowed()`, whose support branch reads it; D42 and D46 land with the tables they alter, in B1 and B3, each in the same migration as the scope predicate that reads it, §1.12); `config.rule_set_version` and `config.vocabulary` with D7 and D27's attribute schemas, D49's vocabularies, D8 `config.grade_scale`, `config.vocabulary_history` (F48); `ref.source`, `ref.grade_scale`, `ref.programme_family`, `ref.destination_system`, `ref.regulator_profile` (F65, F31); `sis.academic_year`, `sis.term`, `sis.calendar_period`, `sis.school_day`, `sis.year_group`, `sis.programme`, `sis.timetable_period` (the tenant module's own); `notify.template` (D47 allowlists for exactly pass 4 §5.8's templates), `notify.outbox`, `notify.preference`, `notify.in_app`; `audit.action` (pass 1's keys plus D9, D48 and D71, one list, `PREVIEW_ACCESS` once) and `audit.entry` with its school foreign key; `events.name` (pass 1's events plus D9 and D28, `sweep.completed` once) and `events.event`; the `reporting` schema, empty (F34); `privacy.retention_class` (pass 1's rows of pass 4's 27 classes, updated by D50, every number marked proposal), `privacy.retention_override`, `privacy.table_registry`, `privacy.non_tenant_store`, `privacy.erasure_request`, `privacy.erasure_log`. `doc.file` lands in B1 with D13's kinds, and `core.meeting` in B3 with the tables its foreign keys name (F40). Everything else lands in the phase that serves it (§1.12). Two things are hand-written SQL beside the generated files: the policies and the `SECURITY DEFINER` helpers owned by `caros_policy` with `EXECUTE` revoked from `PUBLIC` (F75), and the roles. The migrator logs in as `caros_migrator` (credential in Key Vault, used only by the migration job), runs `SET ROLE caros_owner` as its first statement and sets `app.actor_kind = 'migration'` (§12.2).

**Tasks (planning estimate: 22 sessions plus Fable reviews).**

| # | Task | Stream | Model | Rev | Done when |
|---|---|---|---|---|---|
| B0.1 | Workspace scaffold: every package and app of DR-3, tsconfig, Biome, `dependency-cruiser` rules, Vitest, Playwright, `turbo.json`; `pnpm ci` runs a trivial test in every package | D | O high | | a fresh clone runs `pnpm i && pnpm ci` green; the boundary check fails a deliberately bad import in a test |
| B0.2 | `ci.yml` with the jobs of §5.8 and a PostgreSQL 17 service container; required checks named in the ruleset | D | O high | | a PR that breaks any job cannot merge |
| B0.3 | GitHub organisation, the solo-mode ruleset on `main` (§2.4), environments `staging` and `production` (required reviewer on production), the wholesale-replacement check (§2.4), the repository hooks (§3.4, including the Read and Bash guards of F23) | D | O medium | | force-push refused; a PR replacing 80% of a file without the label fails; a PR with green checks merges without a second person; a session cannot read a path outside the repository or run `az` or `psql` against production |
| B0.4 | Terraform module with CMK, geo-redundant backup, ZRS and immutable containers, per-tenant encryption scopes, Container Apps environment (internal), ACR, Key Vault (Premium, purge protection, RBAC), Log Analytics, ACS Email, action group, OIDC federation for staging; a separate drill subscription (F43); applied to staging; state in a UAE North storage account | D | O high | F | `terraform plan` clean on main; both apps answer `/health` on staging; the action group test SMS arrives; UAE Central request ticket recorded in `PLAN.md` |
| B0.5 | Migrations `0001_b0_roles_extensions` and `0002_b0_spine` (the policies are B0.7's `0003`, the global seeds `0004`): extensions, the per-deployable login roles and tier roles (`caros_t_teacher` and `caros_t_ai` included, F75, F44), `NOINHERIT` on every login role and the tier roles `NOLOGIN NOBYPASSRLS`, schemas, the spine tables, vocabularies in place of CHECK lists, append-only and `updated_at` triggers, partitions, the classification, purpose, status and reclassification triggers | D | O max | F | applies from zero in CI; every spine table registered; the registry and convention tests pass, including the materialised-view ban (F34) and "no definer executable by `PUBLIC`" (F75) |
| B0.6 | `withTenant()` (pass 1 DR-4 verbatim contract, `SET LOCAL ROLE` to the tier), the un-exported pool, the migrator image with its non-transactional step, and the Container Apps Job | D | O max | F | a query without context returns zero rows on every tenant table; the pool cannot be imported (lint test); the job applies a migration on staging before the revision swap; a `-- no-transaction` file runs outside the transaction |
| B0.7 | `auth.allowed()` (tier argument, table, row kind, addressee, pairing, the support branch by operator and table) and `auth.protect()`, the scope predicates and `auth.scope_rank()`, the matrix seed generated from `matrix.json` through pass 1 §2.4a with the projection views, `auth.forbid_widening()` implementing pass 4 §3.1 exactly (narrow-only; `allow_counselor_widening` accepted only for counselor `signal` and `case_note` reads at school scope, never `safeguarding`), `notify.enqueue()`; **the bake-off task** (B0.16) | D | O max | F | policy-presence test: every `school_id` table has `tenant_isolation` and `role_read` with `FORCE`; every qualifier of pass 1 §2.4a has its mechanism and a negative fixture; the narrowing trigger refuses a wider school row and any `safeguarding` row, and accepts the widening rows only when the flag is set (F47) |
| B0.8 | The RLS and authz harness in `packages/db/test/{rls,authz,narrowing,jobs,purpose}` (D61): generated from `auth.role_permission` and a fixture map; the eight "see nothing" cases; column cases; positive and negative tests for every verb through RLS (F02); `caros_owner` with `FORCE`; the deployable-role tests (the web identity cannot `SET ROLE` to system, support or owner; `NOINHERIT` holds; a web connection claiming `actor_kind = 'system'` under a user tier sees nothing, F01); a job carrying another school's ids writes nothing (F36); a flagged fixture per role (F13); both tenants | D | O max | F | a matrix row without fixtures fails the build; the suite runs on every PR in under five minutes |
| B0.9 | `audit.record()` and `events.emit()` with per-tier action allowlists, `audit.chain()` with the per-school advisory lock, `core.ensure_month_partitions()`, the per-action `detail` schemas in `packages/contracts/audit/` (D48) | D | O high | F | an entry with an unknown key, a key the caller's tier may not write, or a 65-character value is refused; the chain verifies over 10,000 seeded entries; partitions exist a quarter ahead |
| B0.10 | `packages/contracts` skeleton: jsonb shapes for the spine, the event and audit payloads, the `StudentEvaluationInput` and `EvaluationResult` types from pass 3 §13.1 as revised (so B2 can start), the connector types from pass 2 §7.1 (`AdapterKind`, `ACCESS_CHANGING`), `matrix.json` | D | O medium | | every package compiles against them; a change to the package names its consumers in the PR |
| B0.11 | `tooling/tenant` CLI and `packages/seed` layer one for people and calendar: the two synthetic `core.school` rows (ACS `real_institution`, marker on; Wellesmere unbranded, marker off; both `purpose = 'demo'`) and the CI-only `wellesmere-shifted`, modules, year groups with stages, programmes with families and scales, terms, periods, school days, regulator profiles, demo personas with memberships and capability grants (Ms Haddad `caseload_lead`; a **fictional** IB coordinator, never the real ACS name, F29; the CPO persona on a `staff` membership with `safeguarding_lead`; a `school_admin` pair each; Wellesmere's DSL on a teacher membership with the capability, its tutors and Head of Year as pastoral assignments) | D | O high | | `pnpm seed --tenant acs --anchor <date>` is idempotent; the seed refuses a `real` tenant; the `no-real-data` test passes with every real person's name in `PRODUCT.md` and the prototype on its blocklist (F29) |
| B0.12 | `packages/ui` from `DESIGN.md` and the app shell: tokens, tier marks, `tally`, `empty`, the letterhead, the demonstration marker (a generated column, not a flag), role navigation from data, reduced-motion rules, 375px, self-hosted fonts; the run cells land with the sheet (B3.9) | D | O high | F | a visual check page renders every primitive for both tenants; the marker shows for ACS and not for Wellesmere and cannot be separated from the letterhead (F67); `web-design-guidelines` review clean |
| B0.13 | `apps/worker` bootstrap: pg-boss under `caros_jobs`, `jobs.enqueue()` validating payloads as ids, the heartbeat log line every five minutes, `partition.maintain`, the structured logger with the allowlisted serializer (pass 4 §9.4) and the lint rule | D | O medium | F | the log-scanner test finds no seed name in captured logs; the heartbeat appears in Log Analytics on staging; a job payload with a free-text field is refused |
| B0.14 | Documentation: `CLAUDE.md` (§3.3), `docs/PLAN.md` with B0 to B9, the April schedule and the B1 briefs, `docs/decisions/DR-01` to `DR-16` extracted from the passes, `docs/spec/` copies, `docs/runbooks/README.md`, `tooling/docs/verify-claude-md.ts` and the generated-docs jobs (§3.6) | D | O high | | `verify-claude-md` passes; the docs job fails on an unresolvable identifier planted in a test |
| B0.15 | Week-one non-code checklist (§2.3 has the dates): UAE Central access; GitHub Team; Azure subscriptions (staging, production, drills) and PIM; the ZDR organisation for build sessions (C7) and the conversation with Anthropic on ZDR for the product's own organisation, with `production` and `staging` workspaces pinned to `us` (pass 5 §8.6); a Google Cloud project for the sign-in client and its brand verification submitted; counsel engaged, with the 7.1.3.a hosting question first; DPA drafting started from pass 4 §1.4; DPIA outline started; DPO search opened; penetration-test firm shortlist (CREST-accredited or DESC Cyber Force listed, §8); cyber-liability insurance quote; the ACS name-on-demo consent letter sent, naming third-party sales demos (question 49, F29, F43); the "ask now" questions sent to ACS (`ACS-IT-QUESTIONS.md`: 149, 150, 165 and the others marked); the KHDA directory check of "Wellesmere" | D | none | | each item has a date and a reference in `PLAN.md` |
| B0.16 | **The bake-off (decision C7).** One brief (B0.7's `auth.allowed()` rewrite with its acceptance tests) given to Opus 5.5 at max effort under the ZDR organisation and to Fable 5.1 at max effort outside it, in separate worktrees, on synthetic data only; each model then reviews the other's diff blind; Davide judges on the acceptance tests first (pass or fail per test), then on defects found in cross-review, by category (correctness of the tier and verb logic, policy-expression safety, test coverage of qualifiers, SQL clarity) | D | O max and F max | cross | the verdict is a decision record naming, per category, the model that writes it from now on; the default is Opus 5.5 for every category; a category moves back to Fable 5.1 only where Fable is clearly better (it passes acceptance tests Opus fails, or cross-review finds a defect class in Opus's diff that Fable's lacks); the winning diff lands as B0.7 |

**Acceptance criteria.** CI green on a fresh clone; the RLS suite proves tenant A sees and writes nothing of tenant B for every tier role and every spine table, with no context, and as the owner under `FORCE`; the web identity cannot become the worker, support or the owner; the matrix suite is generated and covers every default row and every qualifier with a positive and a negative fixture, and every verb positively through RLS; `drizzle-kit check` passes; the registry, convention and no-second-copies tests pass; staging serves both apps from `main` with migrations applied by the job; the drift check job runs nightly on staging; the action group delivers to a phone; `verify-claude-md` passes; the week-one items are dated; the bake-off's decision record exists. **Done includes** every test named above running on every pull request.

### 1.2 B1 · Facts and identity

**Status:** not started · **Depends on:** B0 (G-CI) · **Model:** Opus 5.5, high effort for the pipeline, the transforms, the Studio and the record module; **Opus 5.5, max effort** for the identity ladder, sign-in and sessions, the step-up spike, the Directory read and the support-grant path · **Reviewed by:** Fable 5.1 on each of those, on the roster and security section, on forbidden columns and on access logging
**Why this model:** the pipeline is large and well specified, which suits Opus 5.5 at high effort; the ladder decides who a person is, sign-in decides who is in the building, the Directory read decides when a leaver stops being in it, and the grant path is the only way a CAROS person ever reaches real data, so those get maximum effort and the other model's eyes.

**Scope.** `packages/ingest` exactly as pass 2 §2 to §5 as revised: the canonical envelope, the mapping profile schema and the closed transform set of nineteen operations with property tests (`const` is a binding, not a transform), the state machine (D1) with real-tenant imports refused until the tenant is `shadow` or `live`, the jobs (`ingest.scan`, `parse`, `map`, `validate`, `diff`, `commit`, `after_commit`, `rollback`, `purge`), validation and the reason codes of pass 2 §2.6 (including `unknown_counselor`, `under13_consent_missing`, `erased_subject`, `reactivation_required`, `restriction_imported`, `register_disagreement`, `day_derived:<rule>`, `fixture_unsigned`, `approver_is_uploader`, `conflict:selection.level`, `conflict:cas.hours_required`), the dry-run preview with the non-collapsible security section listing guardian-link gains one by one with restrictions and reactivations, caseload and pastoral changes, the rejection report, idempotent re-import, rollback with `signals.reopenAfterRollback` stubbed until B2, the identity ladder with candidates and merges (erased check at rung 1; guardians never merged in CAROS), normalisation of grade scales (`config.grade_scale` before `ref.grade_scale`, IGCSE scales included), attendance (the master register in days, the declared day rule with every derived day marked, reason-code coverage in `import.summary`), behaviour, rosters, contacts (one person and one set of links per SIS contact, imported restrictions), calendar and subjects; the `caseload` and `pastoral` import kinds and `sis.enrolment_programme`; the under-13 check at import with `ingest.consent_decision` and D58's consent column (F56); uploader and approver different for every access-changing kind, which never auto-approves (F57); fixtures loaded only from a CI-signed manifest; the record tables and views (§1.12); both fixture packs with `expected.json` and the `2025-26-history/` packs, generated deterministically under `packages/seed/src/fixtures/<tenant>/`; the Mapping Studio under `apps/web/app/(staff)/admin/ingest/` for the `school_admin` role: Sources, Imports, Identity and consent decisions, Cadence (with reason coverage), and Profiles as a read-only view with dry run and activation (the three-pane editor is cut behind G-REAL: profiles are authored as JSON in the repository, validated by the schema, and installed under a school-approved write grant, §2.3); pass 4's identity for people the school already knows: Google OIDC with PKCE and `openid email profile` only, `hd` validated in the token against `auth.school_domain` (DNS-proved, school-approved) before any tenant query, mapping on `sub`, binding by email only for a person with no stored Google subject (F75), nothing created at sign-in, one acting role per session, server-side sessions with the lifetimes of pass 4 §2.4, **step-up proved by Google's `auth_time`** requested through the `claims` parameter and rejected when older than ten minutes, after a spike that tests it against a live Google session, with a CAROS-held staff passkey as the fallback (F19), anomaly rules, the daily expiry job; **the Directory read**, a `google_directory` resource on the delegated reader account, over the schools `core.schools_with_accounts()` lists (every status but `offboarded`, so onboarding tenants' `school_admin` holders are covered), hourly for staff and nightly for students, mandatory, ending sessions and memberships for suspended or removed accounts (F18; the RISC receiver is withdrawn because Google sends no Cross-Account Protection events for Workspace accounts); the support console as a separate app under `caros_support` (grant request naming mode, tables, location and ticket; school approval under step-up; masked views only; grants bound to operator and table; write grants executed through named domain functions, `WRITE_GRANT_EXECUTED`; `SUPPORT_ACCESS`; F35, F61); the admin screens People and Memberships with capability grants under four eyes and never to oneself (F16); `record` read functions for the student file tabs (academic, attendance, behaviour, transcript) so later screens can render them; `PREVIEW_ACCESS`, `FILE_ACCESS` and `DIRECTORY_SEARCH` written by the read functions.

**Not in B1.** Parent and mentor sign-in (B5, B8): parents need the activation-code flow and nothing to look at yet; students sign in through the same Google path but see nothing until B5, so student sign-in is tested in B1 and switched on for real students at onboarding step 18. Live connectors (B9), except the Directory read, which is identity, not a data connector. The SFTP endpoint (B4).

**Tasks (planning estimate: 29 sessions plus Fable reviews; the Profiles editor's two sessions are cut behind G-REAL).**

| # | Task | Stream | Model | Rev | Done when |
|---|---|---|---|---|---|
| B1.1 | Migrations for `sis.student` (with D58's columns), `enrolment`, `enrolment_programme`, `external_identity`, `subject`, `section`, `section_teacher`, `section_membership`, `assessment`, `grade`, `attendance_event` (with `derived_rule`), `behaviour_event`, `transcript`, `tutor_group`, `tutor_group_member`, `engagement.activity_event` (partitioned, with D56), `family.guardian_link` (with D42's columns), `core.person_name_history`, `doc.file` (D13 kinds); `ingest.*` with D1 to D6 and `ingest.consent_decision`; D12 views (`security_invoker`), D16 registry entries, D59; exactly the B1 row of §1.12 | A | O high | F | registry, RLS and no-second-copies tests extend automatically; `drizzle-kit check` clean; the task's list equals §1.12's B1 row (generated) |
| B1.2 | Canonical records and the profile schema (`MappingProfileSchema`, bindings, expectations, natural keys) in `packages/contracts/ingest`; the 19-op transform set with property tests | A | O high | | a profile using an unknown op is refused; every op has a property test |
| B1.3 | `ingest.scan`, `ingest.parse` (encodings, BOM, XLSX as text, ZIP with `manifest.csv`), chunked staging; the signed-manifest check for fixtures | A | O high | | the ACS and Wellesmere encoding defects produce their codes; an unsigned fixture is refused (`fixture_unsigned`) |
| B1.4 | Identity ladder, `identity_candidate`, `identity_merge` with 30-day reversal, `core.person.match_key`; the erased check at rung 1 | A | O max | F | rung tests: `4412` vs `004412` resolves by rung 1; **the two guardians who share an email stay two persons, one per SIS contact** (F57); a name-only match never auto-resolves; an erased subject is refused |
| B1.5 | `ingest.map` and `ingest.validate`: row and cross-row checks, expectations, `forbidden_columns` before staging, the under-13 check, the register disagreement, SIS email change as `reactivation_required` | A | O high | F | every reason code in pass 2 §2.6, including the pass 8 codes, is produced by at least one fixture row |
| B1.6 | `ingest.diff` with the security section (guardian links one by one, restrictions, reactivations, caseload, pastoral and EE-supervisor changes); `ingest.commit` in dependency order in 2,000-row chunks with `lock_timeout = 5s`; `import_change`; `ingest.after_commit` (recompute `normalised_pct`, refresh school days, derive day rows, mark final results for pass 3's offer check, pass 1's C22 closures stubbed to a domain hook) | A | O high | F | the week-44 correction produces exactly two supersedes; a truncated roster fails `max_end_ratio` and commits nothing; every access-changing change appears in the security section |
| B1.7 | `ingest.rollback` (the `school_admin` under step-up, or support only under a school-approved write grant naming the function) and `ingest.purge`; `IMPORT_*` audit actions and events | A | O high | | rollback refuses when a later import holds the rows; reason under 20 characters refused |
| B1.8 | Normalisers: grade scales (IGCSE included) and predicted versus achieved, attendance in days from the master register with the declared day rule (`half_days`, `all_periods`, `majority_periods`) and reason coverage, behaviour categories, rosters as a security property (pass 2 §5.4), contacts and relationships (no emergency contacts; restrictions imported), calendar periods, canonical subjects, the `pastoral` and `caseload` kinds | A | O high | F | `roster-security.spec.ts` passes on Wellesmere; a GCSE `9` is not 9%; an imported pastoral assignment grants nothing until a capability is granted |
| B1.9 | Fixture generators and `expected.json` for both tenants and `wellesmere-shifted` (term-start pack, twelve weekly packs, the `2025-26-history/` pack of at least twenty in-band weeks, reporting pack; Michaelmas, Lent term-grades-only, correction), reproducing pass 3 §7.2's re-authored series in days missed and late days, ACS defects 19 to 25, signed manifests | A | O high | | `fixtures.spec.ts` imports every pack in order against an ephemeral database and matches `expected.json` exactly; regeneration is byte-identical |
| B1.10 | Studio: Sources, and Profiles as read-only view, dry run on a sample, and activation by the `school_admin` under step-up; lookups pre-seeded from the regulator profile's reason family. **The three-pane editor is cut behind G-REAL** (§2.3) | A | O high | | Playwright: install a profile from JSON under a write grant, dry-run it against the sample, activate it, on both tenants |
| B1.11 | Studio: Imports (upload by single-use SAS, batch assembly, progress, preview with the security section above the fold, approve with the summary hash by a second `school_admin` for access-changing kinds, rejection report CSV, rollback) and Cadence with reason coverage | A | O high | | Playwright: the full round trip as `school_admin`; an access-changing batch cannot be approved by its uploader (`approver_is_uploader`); a stale preview must be re-run |
| B1.12 | Studio: Identity and consent decisions queue (link, create, skip; the under-13 consent decision) re-queuing validation | A | O medium | | deciding a candidate or a consent re-runs `ingest.validate` for the batch |
| B1.13 | Google sign-in: OIDC with PKCE, `hd` and `nonce` validation, `auth.school_for_domain()`, `auth.map_domain()` with DNS proof and approval, person mapping (email only when no Google subject is stored), "no access" page, `IDENTITY_BOUND`, `SIGN_IN_DENIED`, `DOMAIN_MAPPED`, no JIT | D | O max | F | a token with an unknown `hd` is rejected before `withTenant()`; a person with no membership sees "no access"; **staging maps only CAROS test domains**, and a CAROS test domain signs a tester in as Ms Haddad on the synthetic ACS tenant (F35, F43) |
| B1.14 | **Step-up spike first** (one session): request `auth_time` through `claims` on a live Google session and prove whether Google forces re-authentication; then sessions: server-side rows, `__Host-caros_s`, lifetimes per population, acting-role choice, re-authentication on switch, step-up by `auth_time` within ten minutes or a staff passkey, CSRF, revocation, the expiry job, anomaly alerts | D | O max | F | the spike's result is a decision record; session tests including "same id from two ASNs"; step-up required for every action in pass 4 §2.4's list (table-driven test); a token without a fresh `auth_time` never satisfies step-up |
| B1.15 | **The Directory read** (replaces the RISC receiver, F18): the delegated reader account, the `google_directory` resource, hourly for staff and nightly for students, outside the preview path, suspended or deleted accounts ending sessions and memberships, the leaver runbook step "revoke in CAROS" | D | O max | F | a suspended test account's session ends at the next hourly run; a truncated Directory response ends nothing (the 14-day guard); every change is audited |
| B1.16 | Support console as its own app under `caros_support`: grant request (mode, tables, location, ticket) and school approval under step-up, masked views only, grants bound to operator and table, write grants through named functions, `SUPPORT_ACCESS`, `WRITE_GRANT_EXECUTED`, the monthly review query; admin screens People and Memberships with `auth.capability_grant` under four eyes, never to oneself | D | O max | F | no grant → zero rows; masked grant → pseudonyms; another operator's grant → zero rows; a table outside the grant → zero rows; expired grant → zero rows in the same session; a capability granted to oneself is refused; `AUDIT_ACCESS` written on the audit screen |
| B1.17 | `record` module read functions for the student file tabs and the teacher register roster, writing `FILE_ACCESS` and `DIRECTORY_SEARCH` in the read transaction; tRPC routers for `record` and `ingest` | A | O high | F | authz suite extended for `academic`, `attendance`, `behaviour`, `directory`, `ingest`; a teacher's attendance read goes through `sis.v_attendance_teacher` under `caros_t_teacher` and has no reason text |
| B1.18 | Hardening: content-type by magic bytes; Defender for Storage scan gate (`scan_status = 'clean'`); the export-injection fixture test; 250 MB limit; a 1,300-student roster resolves in under a second | A | O medium | | the upload tests (EICAR, polyglot, oversize) and the perf test pass |

**Acceptance criteria.** Both fixture sets, with their history packs, import through the real pipeline with `expected.json` matched exactly; `roster-security.spec.ts` passes; the Studio round trip runs in Playwright on both tenants under `school_admin`, with a second approver for access-changing kinds; staff sign in through Google on a CAROS test domain and land on their imported person; a token from an unmapped domain never reaches a tenant query; step-up is proved by `auth_time` or a passkey; the Directory read ends a suspended account's session within the hour; the support path behaves as pass 4 §3.8's support cases require; every read of a student's file writes its access entry; the authz suite covers the new classes. **Done includes** `fixtures.spec.ts`, `roster-security.spec.ts`, the profile specs, the identity-ladder tests, the session and step-up tests, the Directory-read test and the upload tests in CI.

### 1.3 B2 · Engine and sweep

**Status:** not started · **Depends on:** B0.10 (the contracts) for the engine package, which may start before B1 ends; B1 (facts and `sis.v_current_*`) for the sweep orchestration · **Model:** **Opus 5.5, max effort** for `packages/engine` (the series and band, the detectors, levels, cold start, suppression, the combination table, hysteresis and the pin, the templates, the `engine.tiering` rows); Opus 5.5, high effort for the calibration port, the domain loader and writer, the sweep jobs, the watchdogs and alert rules, the thresholds activation path, the counselor log and shadow view · **Reviewed by:** Fable 5.1 on the engine (pass 3 asked for a review by a model other than the writer), the calibration port, the parameter bounds, the writer's transaction and deterministic-id policy, the sweep jobs, the alert rules and the thresholds guards
**Why this model:** the statistics are the product's mechanism and their errors are silent (a floor set wrong is a quiet student never seen, or a queue nobody opens), so the writer works at maximum effort and the other model reviews every engine diff; the orchestration is well specified and testable.

**Scope.** `packages/engine` as pass 3 §13.1 as revised: `evaluateStudent(input): EvaluationResult`, pure, total, deterministic, versioned; the four JSON schemas published (D31, with `measures.<key>` carrying register, unit and window, `cusum.z_cap`, `attendance.max_level`, `typical_absence_share`, and without `min_history_full`, `exit_h_factor` or `common_cause_share`); pass 3's revised measures: attainment points per assessment with the two-year carry, attendance in days from the master register over non-overlapping fortnight blocks with the day-level floor and a minimum meaningful change of 1.5 days, one lateness measure in late days, floors at the full typical noise, the z cap of 3 inside the CUSUM, strong shocks through their own rule, one persistence definition (`persist = 3` consecutive weeks of data with the CUSUM signal), the chart restarted when a case opens, the ordinal one-step hold, exit and recovery on levels and the mean since opening, attendance held at weak until a configuration version cites measured reason-code coverage, the year-to-date chronic-absence floor, `relapse_watch`, the termly backfill cap and the retrospective rules; **no common-cause cap** (F26); the escalation pin (the engine may raise and never lower or auto-close a case, or the student's shadow case, while an escalation is unreleased, F07); the rate limit on lowerings and repeated reversals only, a raise into `checkin` or `urgent` never held (F08); vocabulary attributes snapshotted into `rule_hits` and the evidence (F48); the scenario suite S1 to S26 with generators and expectations in `packages/engine/test/scenarios/`, and the Wellesmere expected results W1 to W8; the property tests (determinism, monotonicity, suppression never raises, strict no comparison, hysteresis, the pin, within-tier order never moved by other students' data, vocabulary snapshots); the golden-file test; the lint bans on `Date.now()`, `Math.random()` and the environment; `packages/engine/tooling/calibrate_tiers.py` committed from pass 3's appendix and reproducing its published output (F06); the engine tables and D17 to D32 (§1.12); the domain loader `packages/domain/signals/load.ts` (one query per table batched by student ids, through `sis.v_current_*`, per-domain `as_of` from `ingest.expected_cadence` and the newest fact, restricted domains from D58); the writer (one transaction per student; snapshots content-addressed; `uuid_generate_v5` ids for signals and evidence; one evaluation per run, student and evaluated week under D17's key; `outside` written on every series point; the case open or move through the trigger; events and audit); **shadow cases on the live lifecycle** (`signal.case.shadow = true`, one open live and one open shadow case per student, evidence stored against the evaluation, every run trigger included, read as `signal_shadow`, F24); the sweep jobs of pass 3 §13.2 (`sweep.schedule` over `core.schools_due_for_sweep()`, which lists schools holding data, `run`, `batch`, `finalize`, `event`, `backfill`, `whatif`, `watchdog` at 05:45) with the deadlines and retries; the external watchdog as a Container Apps Job; the Azure Monitor scheduled-query alert rules of pass 3 §13.8, carrying counts and never student ids, keyed on `purpose` so canary and demo tenants never page as customers (F60, F42), and `/health/sweep`; `signals.reopenAfterRollback`; the `ingest.after_commit` hooks (dirty marking of `(student, measure, week)` on supersede; backfill enqueue for scheduled packs and the history import; pass 1's C22 closure; the offer-condition check on final results, F64); the thresholds activation path (propose, validate against every bound, activate as `caseload_lead` under step-up, guards 1 to 6, the disabled-domain banner and `config.domain_disabled`); the platform default rule sets version 1, and ACS's and Wellesmere's version 1 **starting from the platform defaults**, not from the prototype's `S.thresh` (whose `acad 2` maps below pass 3's bound; the seed note records that it is not ported); the counselor log table and API, the shadow view, `signal_shadow` and `counselor_log` data classes in the matrix and the authz suite. **Cut behind G-REAL:** the thresholds screen's what-if run and its UI beyond the activation path (B2.15); the reveal sessions use it, so it lands before the first reveal (§2.3).

**Tasks (planning estimate: 28 sessions plus Fable reviews; B2.15's what-if half, about one and a half sessions, is cut behind G-REAL).**

| # | Task | Stream | Model | Rev | Done when |
|---|---|---|---|---|---|
| B2.1 | Series builder and the personal band: school weeks, assessments and fortnight blocks as points, "weeks of data", the baseline window, median and MADn widened by `sqrt(1 + 1/H)`, the floors at the full typical noise (percentage 4.0 points, the day-level binomial, own-rate floors), `phase` (cold, self-starting, band, full), the run cells from the `outside` flag | B | O max | F | S4, S6, S13, S14 pass; the band never reads another student (the strict no-comparison property) |
| B2.2 | Detectors: band, shock with the strong-shock rule, one-sided CUSUM with onset, the z cap, the chart restart, the ordinal one-step hold, Theil–Sen for the sentence; levels 0 to 3 per measure and per domain with the three caps | B | O max | F | S1, S3 pass; the ARL check reproduces pass 3 §2.3's computed figures |
| B2.3 | Cold start and the self-starting CUSUM from three points; the `baseline_established` monitor case; the two-year carry | B | O max | F | S5 passes; a transfer's first month surfaces a genuine collapse at `monitor` only |
| B2.4 | Suppression: calendar kinds, absence spans in days and blocks, section changes, counselor and teacher context, vocabulary snapshots in the checks; every check recorded fired or not; **no common-cause guard** (F26) | B | O max | F | S9, S10, S11 and S12 as revised pass; a school-wide dip changes no student's level unless a `calendar_period` is declared (the strict property) |
| B2.5 | Domains: academic (per assessment, mocks judged against the section's band, `missing` per fortnight block), attendance (days from the master register, fortnight blocks, held at weak until coverage is measured, the year-to-date floor), the one lateness measure, behaviour (weekly floor and shock), teacher concern with corroboration (a `safeguarding` flag is a referral, never an engine input, F10), university rules as inputs with calendar-day clocks outside term and the offer-condition check, positive change, recovery and relapse with `relapse_watch`; the termly backfill cap and the retrospective rules | B | O max | F | S2, S7, S8, S16, S21, S26 pass; a test that only a configuration version citing measured coverage releases the attendance hold |
| B2.6 | Combination table and persistence, the tiering rows as data in the closed grammar, hysteresis (exit on levels and the mean since opening, the 14-day person hold, the reversal cap on lowerings only), the escalation pin, auto-review never closing a pinned case, the daytime asymmetry as a flag on the result | B | O max | F | S17, S18, S19 (with a raise that is never held), S22, S24, S25 pass; the hysteresis and pin properties hold over generated series |
| B2.7 | Templates: every rule's headline and evidence sentence from `template_args` restricted to numbers, dates and enumerated labels; the move-why template; no model wording anywhere | B | O high | F | a template cannot bind a free-text field (schema test); every S-scenario's final evidence sentences match; no regulator name in any engine template (test) |
| B2.8 | The JSON schemas D31 with every bound of pass 3 §10.1 as revised; `engineVersion`, the changelog rule, the golden files | B | O high | F | a value outside a bound is refused with the bound in the message; a withdrawn key is refused |
| B2.9 | `calibrate_tiers.py` committed from pass 3's appendix with its seed, its output reproduced within rounding; the known-parameter ARL check; `pnpm test --filter engine` under one second | B | O high | F | the numbers in pass 3 §3.6 and §11.1 are regenerated within rounding by the committed script, and any change to a level threshold or tiering row re-runs it in CI |
| B2.10 | Migrations for `signal.*` (all but `escalation`, `teacher_flag` and `v_last_contact`), exactly the B2 row of §1.12: D17 with its new unique key, D18 to D25 (D22's `triggering_state`; D25 rewritten as `security_invoker` over shadow cases and every run trigger), D26 created with no application grant and unused in the pilot, D29, D31, D32, D60, `signal.case.shadow`, `signal.case_link`, (the database's `signal.case_escalation_pin` trigger lands with `signal.escalation` in B3; the engine's own pin is B2.6), the `sweep_run` nightly index, `signal.week_run()` aligned by `week_start` (`signal.teacher_flag` and `signal.v_last_contact` land in B3) | D | O high | F | registry and RLS suites extend; the counselor log and shadow view are invisible to teachers, students, parents, mentors and out-of-caseload counselors (test) |
| B2.11 | Domain loader and writer: `load.ts`, the input DTO, one transaction per student, deterministic ids, snapshots, the case trigger path for live and shadow cases, events, audit | D | O high | F | a replayed write is an upsert with no duplicates; a crash mid-batch leaves no half-written student (fault-injection test); a backfill writes one evaluation per evaluated week under D17's key |
| B2.12 | `sweep.schedule` over `core.schools_due_for_sweep()`, `sweep.run`, `sweep.batch`, `sweep.finalize` with the DR-6 assertions, the overnight comparison and the per-tenant reporting aggregates written after the cases (`reporting.agg_tier_daily`, tier counts per school, counselor and day, and `reporting.agg_event_weekly`, the event counts the four metric families need; tables under RLS with the sweep as their single writer, never materialised views, F34); `sweep.event` with the daytime asymmetry and the five-minute coalescing; `sweep.backfill` for weekly packs, termly files and the history import; `sweep.whatif` | D | O high | F | the event test: a flag raises within five minutes and never lowers except on context; the backfill test S20; a school in `onboarding` is never swept |
| B2.13 | Watchdogs and alerts: `sweep.watchdog` at 05:45 local, the Container Apps cron job, the scheduled-query alert rules (no completion by 05:30 for a school holding data, partial, duration over two hours, heartbeat absent 20 minutes, `students_failed`, `domain.quiet`, `budget.exceeded`, churn over 10%, assertion failures), counts only, keyed on `purpose`; `/health/sweep`; the non-dismissible banner | D | O high | F | on staging: a stopped worker pages within 20 minutes; a deliberately failed run raises the banner; `/health/sweep` reports per school without identifiers; a canary tenant's failed sweep pages as a canary, never as a customer |
| B2.14 | `ingest.after_commit` hooks: dirty marking on supersede, backfill enqueue for scheduled packs and the history import, pass 1's C22 closure through the domain, `signals.reopenAfterRollback`, the offer-condition mark | A | O high | | S15 passes end to end through a corrected import |
| B2.15 | Thresholds: the activation path (propose, validate, activate as `caseload_lead` under step-up, guards 1 to 6, the version note with the engineer's acknowledgement in shadow, the disabled-domain banner); **cut behind G-REAL, needed by the first reveal session:** the thresholds screen's sections for the three rule sets and the what-if run | B | O high | F | the mass-change guard and the disabled-domain guard fire in tests; activation needs `caseload_lead`; (deferred half) the what-if delta guard fires and the what-if run returns in seconds at pilot scale |
| B2.16 | `engine-reproduces-seed` over ACS, Wellesmere and `wellesmere-shifted`: the history pack and the weekly packs imported in sequence, backfill evaluations, the result compared with **lists generated by running the rules**, not written by hand; for ACS under the pilot defaults that list is pass 3 §7.2's: 8 of the 15 authored tiers match, Maryam's `good` two weeks late, Ahmed and Priya one tier lower, Hana at `monitor`, Zayd, Maya and Fahad with no case (F06, F31) | B | O high | F | the diff equals the generated lists for all three tenants; the narrative layer for signals is then retired from the seed; Davide's decision on whether the sales narrative follows the engine is recorded (§13) |
| B2.17 | Seven staging nights: the sweep runs on schedule for the synthetic tenants; the alert rules stay silent; the drift check and `tier_yesterday` assertion pass | D | none | | seven consecutive `sweep.completed` lines by 05:30 in each tenant's timezone |

**Acceptance criteria.** The scenario and property suites (S1 to S26, W1 to W8) pass and run in under a second; the engine imports nothing from `fairness` or the database (tests); the seed reproduction equals the generated lists for all three tenants; shadow cases run the live lifecycle; the sweep completes on seven consecutive staging nights inside its window with the alert rules silent; a stopped worker pages within twenty minutes; the event path raises and never lowers except on context; a raise into `checkin` or `urgent` is never held; the thresholds activation path requires `caseload_lead`. **Done includes** the scenarios, the property tests, the golden files, the loader and writer fault tests, the sweep smoke test (§5.7), the watchdog test, the RLS additions and the calibration check in CI or, for the staging nights, in the drill log.

### 1.4 B3 · The counselor's morning

**Status:** not started · **Depends on:** B1 and B2 · **Model:** Opus 5.5, high effort for the screens and the case, intervention, flag and meeting mutations; **Opus 5.5, max effort** for the escalation (the state machine, the pin, the route and category routes, the reminders, the acknowledgement scope, teacher referrals), cover and break-glass, and the template renderer with its allowlists · **Reviewed by:** Fable 5.1 on the escalation, cover, the renderer, email delivery, access logging and the table-driven "every mutation writes its audit action" test
**Why this model:** the screens are a port of a specification that exists and renders, and the mutations are a state machine pass 1 wrote out transition by transition; the escalation is the one act in the product that is consequential in the world, and the email that carries it must be provably empty of the child.

**B3 is split for the April 2027 target (§2.3).** Shadow mode shows counselors no tiers, so the first real record needs only what shadow mode and safeguarding use: the escalation end to end, teacher flags and referrals, the flags inbox, a caseload list with the counselor log, the outbox, access logging, the audit screen and in-app notices. Those tasks are **B3 before G-REAL**. The counselor's morning proper (the sheet in four modes with tiers, the case file, cover, the command centre, `si-why`, the dimension pages) is **B3 after G-REAL**, built during shadow mode; the case file, the what-if run (B2.15) and the comparison report (B4.15) must exist by the first reveal session at shadow week 4, and everything else by G-LIVE. Demo 1 moves with it.

**Scope.** The `signals` module: every transition of pass 1 DR-7 (C1 to C25, I1 to I6) as a named domain function that begins with `assertAllowed`, writes its audit action and its event in the same transaction, and is reachable only through its tRPC procedure; the escalation transitions (C15 with one open escalation per case plus an idempotency key and `previous_escalation_id`; C23, a teacher's safeguarding referral, opening a case if none is open; C24, information added to an open escalation; C25, linking cases across students; the pin trigger `signal.case_escalation_pin` released only by a person's act after the outcome) come first; merge only within one student (C12); the teacher surfaces: the class register (first), my classes, the flag modal with kinds (including `safeguarding`, a referral that goes to the route), tags and lesson period, the "who will see this" line and the school's disclosure rule ("If a student told you something that suggests harm, report it to [the coordinator] now. This form is not a safeguarding report", with the coordinator's name from the route), flags for students taught outside a roster (subject teachers and Extended Essay supervisors, F10), and "what I've logged" showing only `routed_key` phrases that never reveal a case (F58); the flags inbox with attach and dismiss writing `routed_key`; the flag text screened by the safety lexicon once B5.12 lands (F10); the escalation end to end as pass 4 §5 as revised: the route configuration screen (`school_admin` under step-up with a `safeguarding_lead` co-signature, `school_route_history`), `safety_alert_routes` by category confirmed by the CPO (F09), `core.school.safeguarding_contacts` from the regulator profile with `verified_at` per number, the structured referral form with step-up (counselors and, through the flag kind, teachers), **the reporter's own countdown shown from the moment of escalation** and labelled as the reporter's duty, with the direct-report route beside it from the start (F09), `immediate_risk = yes` showing 999 and the Principal's contact and notifying the whole route at once, the reference-only email through `notify.enqueue()` naming only a role (F60), the in-app status line, reminders at 60 and 120 (deputy, by email) and 240 minutes (oversight, in-app only), the acknowledgement screen for any staff member holding `safeguarding_lead` under the `ESC` scope, updates while open, the outcome vocabulary from the regulator profile, **no clause numbers in any UI text** (the policy by name; numbers live in the school's configuration, F55), and 80085 labelled as the Ministry of Education's Child Protection Unit line; the outbox delivery worker with the Azure Communication Services Email adapter (Mailpit locally), send-time idempotency by outbox id, delivery status polled rather than taken from Event Grid (F60, F77), the in-app channel, and the template renderer that refuses undeclared variables and any variable holding a person's name, for exactly pass 4 §5.8's templates (F28); the shadow-mode caseload list (the counselor's students from the `caseload` import, no tiers, the counselor log row and Friday prompt, the day's flags); the audit screen (`caseload_lead` and `school_admin`, step-up, `AUDIT_ACCESS`); in-app notifications. **After G-REAL:** the caseload sheet in four modes with the run, "what changed overnight" from `tier_yesterday`, the missed-night line and the `expected_cadence` panel with reason coverage, ordered by tier then time raised and nothing else (F50); bulk acknowledge and reassign; the command centre and priority queue as one screen; signal intelligence (`si-why`: descriptive cohort tiles, the matrix by domain level, no ranking); the case file (`vCase`) with level, breadth and window, the evidence chain with source labels and as-of dates, the baselines tab, interventions and steps, staff-only notes as superseding rows, counselor context with re-evaluation, the "who has looked at this file" panel, and the escalate action; cover and break-glass (practising counselors only, two in seven days, confirmed by the lead the same school day or lapsed, never self-assigned by anyone who administers cover, timestamps for validity, F16); interventions and meetings (`core.meeting` classed `contact_record`, staff-only, F15); the five student-intelligence dimension pages; the `arch` page ported as an honest description of the sweep and the pull pipeline; Demo 1.

**Not in B3.** The `prove` reporting page and the `pilot` page (B8); the co-pilot (B7); the five application dimensions (B5); parent, student and mentor surfaces (B5, B6, B8); the lexicon safety screen (B5.12, with the first student-facing screen; teacher flag text is screened from then).

**Tasks (planning estimate: 36 sessions plus Fable reviews: 19 before G-REAL, 17 after, of which B3.11 and its evidence panel must land by the first reveal session).**

| # | Task | Stream | Model | Rev | Done when |
|---|---|---|---|---|---|
| B3.1 | Migrations, exactly the B3 row of §1.12: `signal.escalation` with D46, `signal.escalation_update`, `signal.teacher_flag`, the pin trigger and `auth.esc_covers()` in the same migration, D45's columns on `core.school` with `core.school_route_history`, `core.meeting`, `family.contact_log`, `signal.v_last_contact`, D43 on `auth.caseload_assignment`; the synthetic tenants' own rows for the vocabularies whose kinds and platform defaults landed in B0 with D27 and D49 (`flag_tag` with severities and versions, `teacher_flag_routed`, `intervention_type`, `meeting_kind`, `meeting_topic`, `context_kind` with effects, `behaviour_category`, `absence_reason`, `access_purpose`, `escalation_outcome` from the regulator profile) | D | O high | F | registry, RLS, matrix and no-second-copies tests extend; `flag_tag` attribute schema validated on insert |
| B3.2 | **Before G-REAL:** the transitions escalation needs (manual open, C15, C23, C24, C25) and the flag transitions (C13 attach with `routed_key`, dismiss); **after G-REAL:** C2, C5 to C12 (merge within one student only), C14, C17, C18 and interventions I1 to I5; bulk acknowledge and reassign | D | O high | F | a table-driven test proves every domain function in `packages/domain/signals` writes exactly its named audit action and event; C18 refuses while an intervention is open; a merge across students is refused |
| B3.3 | **After G-REAL.** Cover and handover: planned cover, break-glass (practising counselors only, 20-character reason, until the end of the school day, step-up, two in seven days, the lead's same-day confirmation or lapse, notices in-app to owner and lead, weekly digest), reassignment; nobody who administers cover assigns it to themselves; `purpose = 'cover:<id>'` on reads | D | O max | F | the eight "see nothing" cases include ended cover, lapsed break-glass and reassignment; a `school_admin` cannot cover; the digest lists every break-glass use |
| B3.4 | Escalation: route configuration and history, `safety_alert_routes`, contacts with `verified_at`, the structured form under step-up, C15 and C23 in one transaction, the reference generator (random, non-sequential), `notify.enqueue()` rows, `ESCALATED_SAFEGUARDING` with identifiers only, the pin | D | O max | F | **one open escalation per case** holds (the partial unique index) and a second submission with the same idempotency key is a no-op, while a new concern about an escalated student is recorded as an update or a linked escalation (S25); the engine never lowers or closes a pinned case (S24); names never enter `audit.entry` (test); the Wellesmere tenant shows its own safeguarding title and no "ADEK" string (test) |
| B3.5 | Escalation clocks: the reporter's countdown from the moment of escalation (`reporter_deadline_at` from the regulator profile), reminders at 60, 120 (deputy) and 240 (oversight, in-app) minutes, `unacknowledged_24h` with `DIRECT_REPORT_RECORDED`, the `school_hours` clock option, `immediate_risk` routing, outcome closing with the vocabulary, the pin's release by a person, `ESC` scope until 90 days after outcome, nightly route validation (F77) | D | O max | F | timers tested with a fake clock; the countdown and the direct-report route show from the first minute; oversight receives only the fact, never content; the `ESC` scope expires in the authz suite; a departed recipient fails the nightly validation |
| B3.6 | Acknowledgement screen for `safeguarding_lead` holders on any staff role: sign-in, step-up, the case under `ESC`, acknowledge, the recipient's private note, updates, close with outcome | D | O high | F | a lead on a `staff` membership sees nothing but escalations (matrix test); acknowledgement tells the raiser at once; oversight holding `safeguarding_oversight` reads status only |
| B3.7 | Outbox delivery worker, the ACS Email adapter with the custom domain, Mailpit locally, the in-app channel, send-time idempotency by outbox id, retries and `not_before`, delivery status polled, bounce handling, the 30-per-minute new-domain limit respected | D | O high | F | a queued row is delivered within one minute on staging; a retry after an unclear provider response never sends twice; a bounced address is `suppressed` |
| B3.8 | Template renderer with allowlists (D47) for exactly pass 4 §5.8's templates, among them `safeguarding_escalation` with exactly its six variables, `raised_by_role` among them, `escalation_reminder`, `safety_alert_notice`, `alert_unopened_notice`, `parent_update_waiting` (which also carries the parent's meeting confirmation, pass 4 §5.8), `student_deadline_nudge`, `magic_link` and `grooming_alert` (the parent activation code is never emailed; `safety_response` is the in-app student message, B5.12); `packages/notify/test/templates.spec.ts` | D | O max | F | the escalation and reminder emails contain their reference and URL, and every rendered email contains none of the fixture student's name, initials, year, tier, headline, reason or evidence, and no person's name (roles only, CPO and safeguarding lead as "the school"); an undeclared variable fails the CHECK |
| B3.9 | **Before G-REAL:** the shadow-mode caseload list (students from the `caseload` import, no tiers, the counselor log row and Friday prompt, the day's flags), `CASELOAD_VIEW` per render. **After G-REAL:** the sheet in four modes, the run cells from `signal.week_run()`, overnight column from `tier_yesterday` and `moved_why`, the missed-night line, the `expected_cadence` panel, ordered by tier then time raised | D | O high | | Playwright on both tenants; Wellesmere shows Years and the DSL label; the marker shows on ACS only; (after G-REAL) the ordering test: changing any other student's data never moves a row within its tier (F50) |
| B3.10 | **After G-REAL.** Command centre and priority queue (one screen), signal intelligence (`si-why`) with the matrix by domain level and the descriptive cohort tiles | D | O high | | the ordering test of B3.9 covers every list on these screens; no screen orders students by any score |
| B3.11 | **After G-REAL, by the first reveal session.** Case file: header (tier, level, breadth, window, `cold_start`), evidence chain with source labels and as-of, baselines tab with the band and the run, interventions and steps, notes (superseding), context (with re-evaluation status), the access panel, the escalate action, `CASE_ACCESS` and `NOTE_ACCESS` in the read transaction; the shadow case read under `signal_shadow` for reveal sessions | D | O high | F | a file open writes `FILE_ACCESS` with `purpose` when the reader is not the owner; the evidence sentence is pass 3's stored `summary`, never recomputed |
| B3.12 | **After G-REAL.** Interventions view, meetings view and `core.meeting` recording with actions; `family.contact_log` entry and `PARENT_CONTACT` | D | O medium | | `signal.v_last_contact` updates on a recorded meeting; parents and students read no meeting row (authz) |
| B3.13 | Flags inbox: attach (C13) with `routed_key`, dismiss, merge (C12, after G-REAL); the "what I've logged" teacher view showing only the routed vocabulary | D | O high | | a teacher never sees a tier, `case_id`, a case status or a phrase that reveals a case (authz and rendered-page tests) |
| B3.14 | Teacher register and my classes from roster rows only; concern paths for subject and supervision students outside a roster; the flag modal (kinds with `safeguarding` as a referral, tags with severity, lesson period, the "who will see this" line, the disclosure rule); `TEACHER_FLAG`; the event evaluation enqueued; 375px | A | O high | | a teacher after a roster move loses the student (authz suite); a flag with a safeguarding-relevant tag evaluates within five minutes (S7 end to end); a `safeguarding` flag reaches the route as a referral and never enters the engine |
| B3.15 | **After G-REAL.** The five student-intelligence dimension pages over the `dimensions` rule set; `vDim` as one component | D | O medium | | the three formerly hard-wired dimensions are computed from series (no student-id literals anywhere: lint test) |
| B3.16 | Audit screen, the file access panel and in-app notification centre before G-REAL; the honest `arch` page after | D | O medium | | every audit-screen query writes `AUDIT_ACCESS` and shows it |
| B3.17 | Accessibility and copy pass on every B3 screen as it lands: keyboard, focus, `aria-live` for the sheet, reduced motion, the six type roles from `DESIGN.md`, no severity by colour alone, `spellcheck="false"` on the escalation form and notes (F76) | D | O medium | | `web-design-guidelines` review with no blocking finding; Playwright axe checks pass |
| B3.18 | **After G-REAL.** Demo 1 script and rehearsal on staging with both tenants (§6.5); route-state count for counselor and teacher against the 98 | D | none | | every counselor and teacher route-state renders with zero console errors on both tenants; the script runs in twenty minutes |

**Acceptance criteria.** *Before G-REAL:* the escalation runs end to end on staging for both tenants from a counselor and from a teacher's safeguarding flag, its email is provably free of the child and of every name, its timers fire on a fake clock, the reporter's countdown shows from the first minute, its acknowledgement needs the capability and step-up, one escalation per case is open at a time with linked updates, and the engine cannot lower a pinned case; every mutation writes its audit action and event (table-driven test); every sensitivity-3 read writes its access entry; the routed phrase never reveals a case; the Wellesmere checklist assertions for these screens pass. *After G-REAL:* the Demo 1 script runs end to end on staging for both tenants; every counselor and teacher route-state renders without error; the ordering test holds; the accessibility review passes. **Done includes** the Playwright flows (teacher flag to attachment; teacher referral to acknowledgement; escalation to acknowledgement; after G-REAL, the morning ritual, cover and break-glass), the template content test, the timer tests, the audit-coverage test and the ordering test in CI.

### 1.5 B4 · Pilot readiness

**Status:** not started · **Depends on:** B3 before G-REAL (G-DEMO no longer precedes it, §2.3) · **Model:** Opus 5.5, high effort for the tooling, the runbooks, the review pack and the production Terraform; **Opus 5.5, max effort** for erasure with restore replay and offboarding key destruction (irreversible by design), for the legal-basis and status gates, and for authorization findings from the penetration test · **Reviewed by:** Fable 5.1 on those, on the production Terraform apply, on the retention purge jobs and on the shadow comparison report
**Why this model:** this phase is where the irreversible things happen: keys, erasure, production. Most of it is procedure and tooling, which Opus 5.5 does well at high effort; the irreversible pieces get maximum effort and the other model's review.

**Scope.** Pass 4's §9.13 list, in order, as a phase gate (none of it is cut for April), plus the pilot's non-code work. Privacy tooling: the three notices as versions with acknowledgement on first sign-in (D51), stating the truth about free text, both AI retention periods and "inference in the United States" (F20, F63, F76); the **legal-basis attestation trigger** that keeps a `real` tenant in `onboarding` until it exists, so that `onboarding → shadow` happens only at G-REAL (D45, F36); the **data protection impact assessment** drafted by CAROS for ACS as controller, from the passes, with the evidence it needs generated from the registry and the matrix (F37; a document, Davide's, not a session, §8 G26); the SAR request and pack generator from `privacy.table_registry` under decision C4 (professional records, AI generations and anything linked to an escalation or safety alert excluded by default; any request about a student with an escalation routed to the safeguarding lead; per-item inclusion as the school's explicit act; after 18 only the student requests), delivered in the portal only, watermarked (D52, F14); the erasure job with `erased_external_identity` (D52), pseudonymised tombstones and the restore replay step in the restore runbook; `processing_restriction` with the engine hook (D58); the retention jobs per class and per-school override, each number a proposal; the weekly audit anchor to a locked immutable container and the verification job; `pgaudit` for DDL and ROLE. Production: the Terraform module applied to `caros-prod` (server created with CMK and geo-redundant backup, zone-redundant HA, the second Key Vault in UAE Central, Application Gateway WAF v2 in detection mode from the day production exists and in prevention mode before the first real import, PIM with no standing access, the release pipeline with the production environment approval, the drift check, the Microsoft support plan with Customer Lockbox, F61); **the canary pair**: two synthetic, unbranded tenants with `purpose = 'canary'`, AI off and no deliverable email addresses, and the canary itself as a Container Apps job in UAE North that calls domain functions through `withTenant()` as a canary membership, refuses any non-canary tenant, and asserts that every row it reads carries its own school id; no GitHub-hosted runner holds a production credential (F42, F23); the SFTP endpoint if the pilot uses it (pass 2 §9 step 9: SSH key per tenant, or for Veracross's Data Export Package an Azure-generated password in Key Vault rotated every 90 days with Veracross's egress IPs allow-listed; Azure Blob SFTP needs a hierarchical-namespace account, authenticates local users and keeps port 22 open even when disabled, [SFTP support](https://learn.microsoft.com/en-us/azure/storage/blobs/secure-file-transfer-protocol-support); the page lists no regions, so the UAE North check is done in the portal and the fallback is pass 2's container) and the watcher job that moves each overwriting export into the immutable container. Drills: all five of pass 1 DR-9 run once before real data, with the erasure replay, **in their own subscription** (F43), their checks running as jobs in UAE North, recorded in the drill log the release pipeline reads. The onboarding runbook (`docs/runbooks/tenant-onboarding.md`) and the tenant's onboarding checklist record covering pass 2 §9's twenty steps as revised, the Google Workspace configuration checklist of pass 4 §2.1 (Limited for the staff and student units, under-18 designation confirmed, the Directory read scope, CAROS owning the OAuth client), the leaver runbook's "revoke in CAROS" step, and the safeguarding route and category routes set up with a test referral delivered to the CPO. The review pack organised by the regulator profile's checklist (for ACS, ADEK Digital 5.5.1 a to h, pass 4 §9.12): the ASVS 5.0 Level 2 self-assessment (ASVS 5.0.0 was released in May 2025 with three verification levels per secondary sources; [OWASP ASVS](https://github.com/OWASP/ASVS)); the whitepaper from pass 4 §§2 to 7; Annex 1 generated from the registry; the DPA with annexes and the 7.1.3.a clause signed; the DPO appointed; the notices approved by the school; **the penetration test by an independent firm against production while it holds only the canary tenants** (pass 6 decision C5, F41), scoped to the web application, the API, authentication and tenant isolation, highs fixed and retested within 30 days, application-layer fixes retested on staging and production-layer fixes (WAF, TLS, network, keys) on production while it still holds only canaries (CREST-accredited firms operate in the UAE under the Dubai Cyber Force scheme run by CREST with the Dubai Electronic Security Center, [CREST](https://www.crest-approved.org/membership/dubai-cyber-force-program/)); the incident tabletop with ACS IT and the CPO; the support-grant path exercised once with the school; cyber-liability insurance; the on-call rota, the paging escalation job (§7.5) and the sweep-failed runbook; the school's monthly report skeleton; the elicitation session (pass 3 §10.5) with the four counselors, producing ACS's `engine.thresholds` and `engine.tiering` version 1 for shadow with every departure from the platform default traced to an answer, and Part F's two G-LIVE floors. **Cut behind G-REAL (§2.3):** the offboarding drill (B4.7), the morning-burst load test (B4.11) and the shadow comparison report (B4.15), all needed by the first reveal session or G-LIVE, not by the first real record.

**Tasks (planning estimate: 24 sessions of code plus the external lead times, of which 18 before G-REAL; the external items decide the calendar, §2.3).**

| # | Task | Stream | Model | Rev | Done when |
|---|---|---|---|---|---|
| B4.1 | Notices (D51): three audiences, versions, approval fields, acknowledgement on first sign-in and on each new version; the matrix rendered from `auth.role_permission` inside the notice; the free-text truth, both retention periods and "inference in the United States" in the text | D | O high | | a person with an unacknowledged current notice is routed to it before any screen |
| B4.2 | Legal-basis attestation and the status gate (D45's trigger): a `real` tenant cannot move `onboarding → shadow` without the attestation; fixture and real classification gates re-verified; no import into a real tenant before `shadow` | D | O max | F | the move is refused without the attestation; the seed refuses a `real` tenant; a fixture import into a real tenant is refused |
| B4.3 | SAR request, pack generator (JSON and PDF from the registry, access history without staff names, the default exclusions of decision C4), the per-item inclusion act with `sar_inclusion_reason`, routing to the safeguarding lead when an escalation exists, portal delivery with watermark, 30-day expiry | D | O high | F | with an escalated-student fixture: the default pack contains no `signal` row, note, AI generation, escalation-linked item, safety alert or read under the escalation scope, and every other registered table where they are subject; a request about the escalated student reaches the safeguarding lead, not the caseload lead (F14) |
| B4.4 | Erasure: request, 30-day schedule, the job (pseudonymised tombstone, scrub `pii_columns`, delete or anonymise by class, blob purge, pseudonym maps, external identities hashed, non-tenant stores including pg-boss), `ERASURE_EXECUTED`, the destruction statement; the restore replay script | D | O max | F | after erasure, a full-text search of every registered table, every non-tenant store and the blob store for the person's strings finds nothing; a restored server replays the erasure before use (drill 1) |
| B4.5 | Restriction and objection: `processing_restriction`, the engine skip, the frozen-case banner, exclusion from co-pilot and exports; parental access at 18 continuing by default with `student_objection_18`, pending ACS's answer to question 148 (decision C5) | D | O high | F | a restricted student is skipped by the sweep and excluded from a SAR of another student's data; a student's objection at 18 ends their parents' access |
| B4.6 | Retention jobs per class and per-school override with the proposal numbers, `RETENTION_PURGE`; the weekly audit anchor and verification (P1 on mismatch); `pgaudit` settings in Terraform | D | O high | F | the anchor blob lands in the locked container; a tampered entry in a test database is detected; a purge never deletes a row inside its override's period (test) |
| B4.7 | **Cut behind G-REAL, needed by G-LIVE.** Offboarding: exit pack in the portal, `offboarding` status and the read-only month, key destruction in Key Vault, `offboarded` kept as a row, `TENANT_OFFBOARDED`, the destruction certificate; the restore runbook's offboarding replay | D | O max | F | a drill in the drill subscription offboards a synthetic tenant and the raw-import container becomes unreadable |
| B4.8 | Production Terraform: apply to `caros-prod`; WAF in detection mode; PIM; the support plan and Customer Lockbox; the release workflow with the production environment, deployment-branch rule (tags only), the drill-log check, the staging soak check; the canary pair and the canary job in UAE North | D | O high | F | a release deploys by tag with Davide's approval; rollback to the previous revision works in a rehearsal; the canary refuses a non-canary tenant; every report, indicator, cost board and alert keys on `purpose` (test) |
| B4.9 | SFTP: portal check of Blob SFTP in UAE North; per-tenant local user with `Create` and `Write` only, SSH key or the Data Export Package's password path, egress IP allowlist, host keys published; else the container; the watcher moving each overwriting export into the immutable container within fifteen minutes and creating the import | A | O high | | a dropped file becomes a `received` import; the tenant user cannot list or read; an overwritten export is never lost |
| B4.10 | Restore drills 1 to 5 scripted in `tooling/restore-drill/`, run in the drill subscription with their checks as UAE North jobs, the drill log, the CI check that blocks a production deploy after a quarter without a drill | D | O high | | all five drills pass with the erasure replay; the log records time and point achieved |
| B4.11 | **Cut behind G-REAL, needed by G-LIVE.** Load test of the morning burst and the sweep at pilot and year-1 shapes; Container Apps scale ceilings set | D | O medium | | p95 under the feature timeouts at 3× pilot concurrency (assumption for the bar) |
| B4.12 | Onboarding runbook and checklist record; `tooling/tenant` completed for a real tenant under the one-time bootstrap grant (classification `real`, `purpose = 'customer'`, the first `school_admin`, the first DNS-proved domain, the route; pass 2 §9 step 3, which refuses to start unless the DPA names two different people, the signatory and the first `school_admin`, and records the signatory's confirmation of the second as `SECOND_ADMIN_CONFIRMED`); `core.enable_sweep()` and `core.record_g_live_signoff()` for the caseload lead; the Google Workspace configuration checklist; the safeguarding route and category routes with the test referral | D | O high | F | a dry onboarding of a synthetic tenant in the drill subscription runs steps 3 to 20 with a human at the steps pass 2 §9 lists; CAROS writes nothing outside the bootstrap grant or a school-approved write grant (test) |
| B4.13 | Review pack: ASVS L2 self-assessment with evidence links to CI, the whitepaper, Annex 1 generated, the CI control list (pass 4 §9.11) rendered from the test names, the checklist from the regulator profile | D | O high | | every control in pass 4 §9.11 links to a passing test in the latest CI run |
| B4.14 | Penetration test: scope, firm, **production target while it holds only the canary tenants**, findings triage, fixes (authorization findings at max effort with Fable review), retests (application-layer on staging, production-layer on production before G-REAL); the rules of engagement for the yearly tests after G-REAL (canary tenants only, no real-row exfiltration, redacted evidence, the firm named in the DPA) | D | O high | F | highs closed and retested before G-REAL; the report and the rules of engagement in the pack |
| B4.15 | **Cut behind G-REAL, needed by the first reveal session.** Shadow comparison report over shadow cases and every run trigger (overlap, lead time, engine-only, counselor-only with `data_gap` versus `engine_miss`, noise per detector), the blinded judgement queue mixing engine cases with matched unflagged students (question 161), stored in `signal.shadow_judgement` (pass 3 D24, landed in B4 by §1.12), the pooled G-LIVE statistics of pass 3 §14.5 (Wilson lower bound, matched-control difference), masked in the support console | B | O high | F | the report runs over the synthetic tenants' log rows; a judge cannot tell an engine case from a matched control in the queue; the G-LIVE numbers are reproduced by a second implementation in the test |
| B4.16 | Elicitation session with the ACS counselors (Davide, no model): the twelve vignettes in days, Parts B to F, the two G-LIVE floors; ACS `engine.thresholds` and `engine.tiering` version 1 for shadow with the mapping table in the version note | D + B | none | | the version note traces every departure from the platform default to an answer; disagreements recorded; the floors are pre-registered in the pilot agreement |
| B4.17 | Non-code gate items with evidence (§2.3 has the dates): DPO appointed; DPA with the 7.1.3.a clause signed with annexes; the DPIA accepted by the school; notices approved; attestation recorded; insurance bound; tabletop held; support grant exercised with the school; on-call rota published; the CPO's test referral acknowledged and the category routes and helplines confirmed | D | none | | the §9.13 checklist in `PLAN.md` has a date and a reference per line |

**Acceptance criteria.** Every line of pass 4 §9.13 has evidence; the five drills are logged; the pen test against production has its highs closed and retested; production deploys by tag with approval and rolls back in a rehearsal; the SAR (with the escalated-student fixture) and erasure tests pass; the canary job runs nightly in UAE North against the canary pair only; ACS's thresholds version 1 exists for shadow. **Done includes** the SAR, erasure, restriction, anchor and drill tests, the purpose test, and the CI drill-log check; the offboarding, load-test and comparison-report items follow before G-LIVE.

### 1.6 B5 · The application season

**Status:** not started · **Depends on:** B3; under the April plan it is built after the G-LIVE items, from the end of the 2026-27 year, to be on production before the 2027-28 application deadlines (§2.3) · **Model:** Opus 5.5, high effort; **Opus 5.5, max effort** for the reach, match and safety classification rule and the fee-status rule (numbers a family sees, invariant 3), for parent activation and magic links (identity for people the school does not know), and for the lexicon safety screen and its routing · **Reviewed by:** Fable 5.1 on those, on the parent portal's scoping and on the letter records
**Why this model:** the module is broad and mostly a faithful port; the classification and fee-status rules decide what a family reads about money and chances, the parent link decides which adult sees which child, and the safety screen decides whether a child's words reach an adult.

**Scope.** The university module: `uni.*` and the per-school and global reference tables with `ref.source` on every row; the classification rule (pass 5 §2.3) as `config.rule_set_version 'uni.classification'`, recomputed by the sweep and on predicted-grade or requirement change, with `classification_abstention` when nothing is on file and the inputs printed; `ref.admission_statistic` for published institution-wide rates, **shown labelled on the counselor's file only and never per target on a student or parent screen** (F77; a capped classification reads "a highly selective institution" to families, with no figure); the reference-data curation tool (`tooling/ref`) so that every institution, course, requirement, cost and deadline row a school can see carries a source URL and retrieval date; **`seed://` rows exist in staging only, never render a number ("not on file" instead), and a production check refuses any** (F30); the course catalogue as per-school rows, not global `ref.*` (F65); deadlines per destination system and the internal reference deadline per school (`uni.school_deadline`); the student nudges at T-21, T-14, T-7 and T-2 through the outbox with `dedupe_key` and the content-free `student_deadline_nudge` template of pass 4 §5.8 (F60); document requests with the reference SLA chases, delivered in-app, not by email (decision C3); **statements per destination and entry year, sectioned per `ref.statement_format`** (UCAS's three questions from 2026 entry), completeness from `uni.statement_complete()`, the counselor review queue (F32); **references per `ref.reference_format`** (the UCAS reference's centre statement as a versioned `uni.centre_statement`, extenuating circumstances flagged `generation_forbidden`, supporting information), the predicted-grade snapshot taken at submission, the reference-letter records of pass 5 §3.10 (D72: activity records, teacher comments, reference notes, consent columns, voice samples with `format_key` and `section_key`) with human-written letter versions only; AP exam registrations; the five application dimension pages over `uni.v_application_pack` (completeness, not characters); the counselor screens `app-season`, `app-flow`, `app-letter` (as a human-written versions screen), `psreviews` (no "AI read" score, F66), and the file's university tab; the student screens my subjects, subject record, my progress, university targets (no probability, no admit rate), Personal Statement Lab (sectioned, no score), AP exams, the high school guide and the IB-or-AP explainer with its synthetic alumni content labelled, each shown by the tenant's **portal rule set**, never by a literal grade (F65); the parent portal: activation codes (D41) issued by a `school_admin` and handed over off-email through the school's chosen channel, **one activation per SIS contact** enforced in `auth.is_guardian_of()` (later links pending until the import that adds them is approved; restrictions suspend one contact only; reactivation on address change), browser-bound single-use magic links sent by the content-free `magic_link` template, the child switcher, overview, messages (in-app, scoped by sender and recipient), meet the counselor with slots, requests and the content-free `parent_update_waiting` notice, grades and transcripts by the portal rule, university requirements, AP exams, the guide, the Parents admin screen; student sign-in switched on for the synthetic tenants; the student's "I need to talk to someone" channel writing an `urgent_request` reflection, never extracted; **the safety screen, `packages/safety`, as a lexicon only with no model** (decision C1; B7.9 in the first version): every hit reaches a person, one open alert per student with further hits appended, routing by category from `safety_alert_routes` confirmed by the CPO, the counselor's reference-only safety-alert email, the CPO's reference-only "alert unopened" notice (never an automatic referral, F12), the alert in the staff-only `safety_alert` class with its lexicon version and span (F13, F48), teacher flag text screened (F10), the school-approved `safety_response` template with the helplines split into "someone to talk to now" (24/7 lines only) and "to report that a child is being harmed", each with hours and a last-verified date and confirmed by the CPO before any student sees it (F11); the student surfaces in deterministic mode where a later model feature would sit (statement read absent; discovery in B8).

**Tasks (planning estimate: 38 sessions plus Fable reviews).**

| # | Task | Stream | Model | Rev | Done when |
|---|---|---|---|---|---|
| B5.1 | Migrations, exactly the B5 row of §1.12: `uni.*` including `uni.centre_statement` and `uni.school_deadline`, the remaining `ref.*` (`ref.statement_format`, `ref.reference_format`), `family.message`, `family.meeting_slot`, `family.meeting_request`, `auth.activation_code`, D11, D41, D68, D72, D73 (except archetype columns), the `uni.classification` and `safety.lexicon` rule-set kinds and their schemas, `probability`, `chance`, `likelihood` on the forbidden list | C | O high | F | registry, RLS, matrix and no-second-copies tests extend |
| B5.2 | Reference-data curation tool and the first sourced set for the pilot's demo targets; `ref.source` rows with URL and retrieval date; the UI source label; `seed://` rows staging-only, rendered as "not on file", refused in production | C | O high | F | no `ref` row renders without a source label; no `seed://` number renders anywhere; the production check fails on a planted `seed://` row |
| B5.3 | The classification rule and its recompute paths; fee-status rules by passport with the Canada demonstration; abstentions | C | O max | F | the rule never reads nationality, sex or a fairness label (test); P4's two students differ only on the cost page; classification inputs print on every target |
| B5.4 | Applications, offers, document requests with `chase_count`, the reference SLA rule, `document.chased` events, chases delivered in-app | C | O high | | a document past T-14 is chased once per cadence with a `dedupe_key`; no chase enters the email outbox |
| B5.5 | Deadlines per destination system and the school's internal deadlines; student nudges (T-21, 14, 7, 2) through the outbox; `notify.preference`; `application.deadline_nudge_sent` | C | O high | | the nudge email names no institution, date, student or topic (the content test); the detail is in-app |
| B5.6 | Statements: per destination and entry year, sectioned per `ref.statement_format`, versions, `uni.statement_complete()`, `psreviews`, the Personal Statement Lab in deterministic mode with no score, `PS_SUBMITTED` (no score in the audit detail), `PS_REVIEWED` | C | O high | | a UCAS statement is complete only when every section meets its minimum within the total; a submitted version reaches the queue; the lexicon runs on every saved version |
| B5.7 | Reference formats and letter records (D72): `ref.reference_format`, the centre statement, extenuating circumstances `generation_forbidden`, the predicted-grade snapshot, the internal deadline, the `app-letter` screen as human-written versions with the evidence panel from citation rows; `student_consent_at` | C | O high | F | a version with `produced_by = 'model'` is refused before B7 (test); a generation request for a `generation_forbidden` section is refused |
| B5.8 | Counselor screens `app-season`, `app-flow`, the five application dimension pages over `uni.v_application_pack`, the file's university tab with the labelled institution-wide rates | C | O high | | the ten dimension pages now all exist; Wellesmere shows its own destination systems' deadlines (UCAS and Common App, per its seed) |
| B5.9 | Student record surfaces by the portal rule set: my subjects, subject record, my progress, targets without probability or admit rate, AP exams, the guide, the IB-or-AP explainer with the synthetic label | C | O high | | Grade and Year labels and every tab from the tenant; the probability meter and any per-target admit rate are absent (DOM test) |
| B5.10 | Parent activation (D41) per SIS contact, magic links (256-bit, hashed, 15 minutes, browser-bound, rate limits), reactivation on address change, restrictions per contact, the Parents admin screen | D | O max | F | a forwarded link fails on another device; an unactivated or restricted contact reads nothing (enforced in the database); three links an hour per address |
| B5.11 | Parent portal: child switcher over `linked_children`, overview, messages in-app scoped by sender and recipient, meeting slots and requests, confirmed by the content-free `parent_update_waiting` notice, grades and transcripts by the portal rule, university requirements, AP exams, the guide; uniform not-found for non-linked ids; no meeting notes, contact log, safety alerts or XP | D | O high | F | Playwright: a guardian with two children in different years sees each under its navigation and never a sibling's row from the other context; one guardian never reads the other guardian's thread |
| B5.12 | `packages/safety`, the lexicon screen only: bilingual lexicon with Arabic script, the pattern screen under 50 ms on every student free text, teacher flag text and (with B6) Extended Essay rationales; one open alert per student; routing by category; the counselor safety-alert email and the CPO's "alert unopened" notice; the `safety_response` template with the CPO-confirmed helplines; the "I need to talk to someone" action; `SAFETY_ALERT_RAISED`, `SAFETY_ALERT_ROUTED`, `SAFETY_ALERT_DISPOSED` | D | O max | F | every lexicon hit on the corpus reaches a person and no alert closes without a person's disposition (test); a student, parent or mentor reads no alert and no `safety_flag` (the per-role flagged fixture); no model is called (test); no helpline renders before the CPO's confirmation |
| B5.13 | Student sign-in enabled on the synthetic tenants; the student notice; the under-13 gate for the student surfaces (the record gate is B1's import check) | D | O medium | | an under-13 synthetic student without recorded consent has no record and no sign-in |
| B5.14 | Accessibility and 375px pass on every parent and student screen; `spellcheck="false"` in the Personal Statement Lab; Demo 2 script part one | C | O medium | | axe checks pass at 375px; the demo script runs |

**Acceptance criteria.** Every family and university route-state renders on both tenants; the classification and fee-status rules pass their tests and never read a protected column; every reference figure a family sees carries a source label and no `seed://` number renders; statements and references follow their formats; the parent flows pass in Playwright with activation per contact enforced in the database; every email template passes the content test; every lexicon hit reaches a person; the Wellesmere checklist assertions for these screens pass. **Done includes** the rule tests, the outbox dedupe tests, the parent identity tests, the safety corpus and routing tests and the Playwright flows in CI.

### 1.7 B6 · The Diploma

**Status:** not started · **Depends on:** B3 (case file, meetings), B1 (rosters and `feeder_set_key`), the record-system decision per school (decision C10: at ACS, ManageBac stays the record for CAS and the Extended Essay and CAROS mirrors it read-only), and the coordinator's confirmation of the guide per round and its word limits and marks (question 133) before the essay part runs for a real cohort; under the April plan it follows B5, in the 2027-28 year, ahead of the Grade 10 selection round (§2.3) · **Model:** Opus 5.5, high effort; **Opus 5.5, max effort** for the two gates (the approval trigger of invariant 6 and the capacity stamp of invariant 8) · **Reviewed by:** Fable 5.1 on the `selCheck` port, the gates, the capacity stamp, the union test, the Extended Essay scope and the ManageBac mirror's write-scope check
**Why this model:** the module is the most literally specified part of the prototype (its rule engine `selCheck` is code today), so a faithful port suits Opus 5.5; the two gates are the module's reason to exist and a wrong port would be invisible on screen.

**Scope.** `ib.*` and D70; the catalogue and its per-level periods, prerequisites and subject teachers as per-school rows, seeded from the prototype for the synthetic ACS tenant and absent for Wellesmere (modules off); the selection round and the S1 to S10 state machine with `selCheck` ported into `packages/domain/ib/selCheck.ts` returning `block`, `ask` and `ok` findings exactly as `index.html:4671` does, the version bump on submission, sign-offs deleted when a pick changes, every state change through a definer transition function with column-level grants only (F17); **the approval gate as the `ib.check_approval` trigger** (the review meeting belongs to this student, is of kind `selection_review`, was held after the current version was submitted, has non-empty notes, and `review_meeting_version` equals `version`) and the domain guard; teacher sign-off only through `ib.sign_off()`, on HL picks in an open round, signed as the acting teacher, from the narrow `ib.v_signoff_card` (name, prerequisite mark, the student's reason, and no band or other engine output, F58); imports never write a pick (a SIS or ManageBac level that differs from an approved pick is a `conflict:selection.level` row for the coordinator); the demand and viability views, counted per subject; the coordinator views (`vSelQueue`, `vSelStudent`, `vSelDemand`), the teacher sign-off queue (`vTSelReview`, `vTSelCard`), the choosing-stage student form (`vSelForm`, `vSelChooser`, `vSelSubmittedPanel`) with the review-slot request, shown by the tenant's stage and portal rules; the review conversation recorded as a `core.meeting` of kind `selection_review` by the coordinator for the whole cohort; the subject-choice engine stated as IB-only, a known limitation to generalise to A levels when a British school signs (F65); **CAS and the Extended Essay under the school's record system**: where the school keeps ManageBac (ACS), the read-only mirror over the v2p3 API (CAS experiences with approval status, hours per strand, supervisor, CAS-project flag and dates; hours required compared with the school's expectation and never written into it; EE projects with research question, supervisor, status and reflection dates, not reflection text) arriving as `source = 'managebac_mirror'` rows through the ingest pipeline, with CAROS's EE transitions E1 to E10 disabled and the counselor views ManageBac lacks (the coordinator's cohort, "drifting", supervision load) built over the mirror (C10, F27; moved here from B9.3); where CAROS is the record (Wellesmere has no IB; a future school may choose it): the ledger with `imported_balance` rows, reflections with the seven outcomes, `ib.v_cas_totals`, and the Extended Essay rounds carrying `ee_guide_key` (**`ee_2027` by default, one reflective statement; `ee_2018` for resits only**, F62), milestones as data per round, real due dates per year group, the E1 to E11 state machine, `ee_stamp_capacity` recording load and over-capacity and never blocking, the teacher queue with load on every card, reflections, drafts as `doc.file`, milestone-overdue events and in-app nudges; in both modes, the student activities page and the Diploma core tabs (CAS, TOK described and not tracked, EE), the coordinator aggregate and the near-duplicate check with `pg_trgm` for the coordinator only; word limits and marks shown to students only once the coordinator has confirmed them (F77); module gating so that Wellesmere shows no IB navigation, queue or CAS page, and non-IB students keep the activities page when CAS is off (F65). The prototype's CAS "Ask to join" action (`index.html:9311`) is not ported (§6.3, F72).

**Tasks (planning estimate: 32 sessions plus Fable reviews).**

| # | Task | Stream | Model | Rev | Done when |
|---|---|---|---|---|---|
| B6.1 | Migrations, exactly the B6 row of §1.12: `ib.*` with `review_meeting_version`, `ib.check_approval`, `ib.sign_off()`, `ib.v_signoff_card`, the transition functions, D10 with its mirror columns on `ib.cas_entry` and `ib.ee_essay`, D70 (`ref.ee_guide`, `ref.ee_criterion`, the two seed guides with descriptors left for the school, confirmation columns); the catalogue seed per school for the synthetic ACS tenant | C | O high | F | registry, RLS and matrix tests extend; the `ib_cohort` scope is exercised in the authz suite; no tier holds table-wide `UPDATE` on `ib.*` (grant test) |
| B6.2 | `selCheck` port with findings `block`, `ask`, `ok`: group coverage, HL count, period clashes (including `sis.section_meeting` week B), prerequisites against `feeder_set_key`, goal alignment, viability | C | O high | F | a golden test replays the prototype's seeded selections through the port and matches its findings one for one |
| B6.3 | Selection state machine S1 to S10 through definer functions, the version bump, sign-off deletion on change, `ib.check_approval` and the domain guard, `ib.sign_off()`, `SUBJECTS_SENT`, `SUBJECT_SIGNOFF`, `SUBJECT_REVIEW`, `SUBJECTS_APPROVED`, `SUBJECTS_RETURNED` | C | O max | F | approval is refused at both layers when the meeting is another student's, of another kind, held before the current version, without notes, or recorded for an earlier version; a student cannot supply a meeting or person id; a teacher can sign off only HL picks of their own subjects in an open round, as themselves |
| B6.4 | Demand and viability views and the demand sheet; a subject's viability counted once, never per level | C | O medium | | the prototype's "five at-risk courses, not nineteen" case is a test |
| B6.5 | Coordinator queue and student detail; the conversation recording for the whole cohort; return with a note; the round's open and close and S10; the `conflict:selection.level` queue | C | O high | | Playwright: submit, sign off, record conversation, approve, on ACS; the queue is absent on Wellesmere; an import never changes an approved pick |
| B6.6 | Student form and chooser at the tenant's choosing stage (`stage = 'pathway_choice'`, never the literal 10), the reason per pick, the block and ask findings rendered, the slot request | C | O high | | a Wellesmere Year 11 sees no selection form (test) |
| B6.7 | CAS where CAROS is the record: catalogue, interests captured where used, the ledger, reflections and outcomes, `imported_balance`, `ib.v_cas_totals`, in-app nudges, `CAS_ENTRY`, `CAS_REFLECTION`, `CAS_NUDGE`, `CAS_REVIEWED` | C | O high | | the ledger total equals the prototype's `hrs` and the recent log equals its `entries` (port test) |
| B6.8 | CAS coordinator cohort and detail views over either record system; the student activities page and the Diploma core tabs; TOK described only | C | O medium | F | the Diploma cohort view never totals with the caseload (a query test that no function returns their union); the cohort is not sorted by gap (§6.3) |
| B6.9 | EE rounds, guides (`ee_2027` default, `ee_2018` for resits), milestones as data for both reflection models, due dates per year group, the coordinator's confirmation of word limits and marks | C | O high | F | the coordinator picks the guide per round; milestones and their owners render from the round; no limit or mark shows to a student before confirmation |
| B6.10 | EE state machine E1 to E11 with `ee_stamp_capacity`, wait days, the stalled rule, `EE_PROPOSED`, `EE_SUPERVISOR`, `EE_REFINE`, `EE_PASSED`, `EE_ASSIGNED`, `EE_REFLECTION`; disabled where the record system is ManageBac | C | O max | F | an assignment over the guide succeeds and records it; a proposal is stalled only past `wait_days`; no CAROS transition runs on a mirrored essay |
| B6.11 | Teacher EE queue with load on the card, accept, sharper question, pass to a colleague; the supervision view; reflection recording | C | O high | F | `ee_party` scope holds in the authz suite; a teacher sees only essays targeted at them |
| B6.12 | Coordinator aggregate and student essay page; the near-duplicate check; drafts as files with the scan gate | C | O medium | | duplicates are shown to the coordinator only |
| B6.13 | Student EE propose and status pages, reflections view, the nudge | C | O medium | | 375px pass |
| B6.14 | Milestone-overdue events and in-app nudges; the coordinator's "drifting" list | C | O medium | | `ee.milestone_overdue` emitted once per milestone per day; no email is sent (pass 4 §5.8 has no EE email) |
| B6.15 | Accessibility pass; Demo 2 script part two; Wellesmere module-off assertions for every IB screen | C | O medium | | every IB route-state renders on ACS and is absent on Wellesmere |
| B6.16 | **The ManageBac mirror** (moved from B9.3): the adapter's CAS and EE resources on v2p3 with a read-only, scope-checked token (`verify()` refuses any write-capable scope), the `cas_mirror` import kind through the pipeline, mirror rows as `managebac_mirror`, a moved EE supervisor held as an access-changing batch for a second approver, token rotation each school year; the NAT gateway's static egress address from B6 if ACS restricts the token by IP (question 78), otherwise from B9 | A | O high | F | a token with write reach fails `verify()`; the ACS fixture's mirror pack produces the expected CAS totals and EE states; a supervisor change is never auto-approved |

**Acceptance criteria.** The `selCheck` golden test matches the prototype; the approval gate holds at both layers against every forged-meeting case; capacity warns and records; the Diploma cohort and the caseload are never totalled; CAS and the essay run under either record system; every IB route-state renders on ACS and is absent on Wellesmere; the EE round runs under either guide. **Done includes** the golden test, the gate tests, the union test, the mirror's `verify()` test, the Playwright flows and the module-off assertions in CI.

### 1.8 B7 · AI

**Status:** not started · **Depends on:** B3 (cases and evidence), B4 (production and the school's acceptances), B5 for the parent draft, B6 for the Extended Essay feedback, G-LIVE (pass 4 open decision on AI during shadow: off), and **two external gates before any student-derived generation on a real tenant: the written ZDR arrangement and ADEK's consent under Digital Policy 7.1.3.a** (G5, G26; F04); under the April plan the mechanisms can be built in 2027-28 on synthetic data, and no feature runs for real before both gates · **Model:** **Opus 5.5, max effort** for the pseudonymiser and final scan, the validator, the leak scanner, the co-pilot's plan compiler (grammar to parameterised SQL) and the quarantined reader; Opus 5.5, high effort for the gateway plumbing, the provider adapter, prompts, envelope builders, screens, the eval runner and reports; the eval judge as pass 5 §10.1 (a model other than the generator) · **Reviewed by:** Fable 5.1 on every max-effort piece, the gateway's retrieval, the provider adapter, the prompt constitution, each feature's envelope allowlist, the failure handling and the promotion gate
**Why this model:** the mechanisms that keep a child's name inside the country and keep a model's sentence honest are exactly the subtle, expensive, hard-to-see class; the features themselves are mostly plumbing and prose.

**Scope.** Pass 5 §3.0's gateway as revised, one path in `packages/ai`: the policy step checks `adek_consent_ref`, the ZDR evidence and `purpose <> 'canary'` (F04, F42); the pseudonymiser and the final scan (R4) with the name rule revised by pass 8 (no length floor, Arabic script and transliteration, school-recorded nicknames, people outside the directory from kinship and role cues), the school fingerprint stripped from the envelope, single-student envelopes that never carry a second student, statements treated as not pseudonymisable (F20, F77); typed minimisation from `ai.feature_policy.allowed_fields` (R5); `caros_t_ai` granted only to `caros_web` and `caros_worker`, `NOINHERIT`, with `SELECT` grants generated from the union of the allowlists and a CI test that it can select nothing else (D55, F44); note bodies only through a view that reads the school's policy row; re-identification only through `ai.reidentify()` at render; no `metadata.user_id` (F76); the validator V1 to V10 with one repair round, the canary and its monthly rotation, plain-text rendering; the registry (D62) seeded with pass 5 §9.1's rows, `covered_model` and `zdr_eligible` living there only (§12.9); `ai.model_config` (D64) with `active_needs_eval` and a thinking allowance per feature; `ai.eval_run` (D65), `ai.thread` and the generation columns (D63, `initiated_by` for pre-warming), `ai.provider_account` (D53, D74) with the ZDR and ADEK consent evidence, `ai.feature_policy` rows all off (D74), the `AI_*` audit actions (D71); the prompt repository layout and the constitution; the Anthropic adapter over the official SDK exposing only `messages.create`, `messages.stream`, `messages.parse` and `messages.count_tokens` (provider-surface test), `inference_geo: "us"` on every call, `cache_control` on the stable layers, no Batch or Files API (R13); the smoke test that structured outputs, `inference_geo`, caching at the feature's prefix length and streaming compose on Opus 5.5 and Sonnet 5 (a 400 here changes pass 5 §3.0.2), with thinking tokens measured per feature (p50 and p99) and `max_tokens` set from the p99 (F70); the injection suite's measurement of the two envelope deliveries in the phase's first week (pass 5 open decision on envelope delivery); **the quarantined reader** with its own policy row, `allowed_record_kinds`, extracting lazily only for the three kinds an enabled writer reads (teacher flags, parent messages, Extended Essay rationales), never urgent requests, never mentor messages, reflections or statement drafts, with the test that fails when an extraction column fills while no consuming feature is enabled (F21); then the **five features** in pass 5 §9.6's order, each behind both gates, its eval bar and the school's written acceptance: the meeting brief with pre-warming as the case owner under `caros_t_ai` (`actor_kind = 'user'`, `initiated_by = 'prewarm'`, schools holding data only, never canaries, F44, F36), the co-pilot (threads, plan, execute, write, the compiled filter rendered above every answer and editable, raw-text search, the coverage rule, `copilot.max_students`, the six promises as assertions, F53), the parent email draft (the parent role's allowlist, the bilingual pair, the parent's messages through the reader, delivered in-app with the content-free `parent_update_waiting` notice, C2), the Extended Essay feedback (criteria from D70 under `ee_2027`, coordinator-confirmed limits, model labels kept off the supervisor's and coordinator's decision screens, the ManageBac-held essay per pass 5 open decision 21), and the discovery model mode, which lands in B8 when the deterministic flow exists; the eval runner writing `ai.eval_run` and an HTML report, the deterministic tier on every PR touching `packages/ai`, `packages/contracts/ai` or a prompt, the judged tier nightly; the suites and the adversarial profiles of pass 5 §10.2 and §10.3 as revised, with the pseudonymiser recall corpus written in-country by people outside the team and its 0.99 bar on directory names as a gate before G-AI (question 164); counselors' eval questions collected in the tenant and rewritten onto synthetic personas in-country by a named person (question 132, F23); the cost script `packages/ai/tooling/cost.py` with the thinking allowances; the daily ceiling, scaled with the school from the second school; the circuit breaker with the Claude Platform on AWS fallback row (only with its own ZDR evidence, R15), the degraded-mode matrix (every feature has a deterministic result); the labelling sessions with the school scheduled before each feature's acceptance (questions 129, 132, 133, 164); the AI label rendered from the generation row on every surface. **Not in v1** (decisions C1 and on F52): the safety screen's model layer (the lexicon screen is B5.12; an add-only model layer is revisited after one term of lexicon data) and the headline rephrase (the rule headline stands alone).

**Tasks (planning estimate: 33 sessions plus Fable reviews; the school's acceptances and the two external gates decide when each feature runs for real).**

| # | Task | Stream | Model | Rev | Done when |
|---|---|---|---|---|---|
| B7.1 | Migrations, exactly the B7 row of §1.12: D53 to D55, D62 to D65, D67, D69 (three record kinds), D74, `ai.generation`, `ai.model_config`, `ai.pseudonym_map`, `ai.reidentify()`; the registry seed with `terms_verified_at`; `ai.feature_policy` rows off; `caros_t_ai` grants generated from the allowlist; D75's tests | D | O high | F | the refusal test refuses `claude-fable-5-1` (Covered) and `claude-haiku-4-5` (no geo pin) for every student-derived feature; `caros_t_ai` can select nothing outside the allowlist (test) |
| B7.2 | Pseudonymiser: per-generation tokens, directory scan with name history and nicknames, Arabic script, people outside the directory, identifier patterns, the fingerprint stripped, single-student envelopes, the reverse map encrypted per tenant with 30-day expiry, re-identification only through `ai.reidentify()` at render | D | O max | F | the leak scanner runs every envelope builder over both tenants with zero hits; the recall corpus meets its bar (directory names 0.99, others 0.90); P4's twins produce byte-identical envelopes |
| B7.3 | Validator V1 to V10: citations resolve, token fidelity, coverage, no comparison, no directory string, no URL or markup, one repair round; `AI_OUTPUT_FLAGGED` | D | O max | F | a planted out-of-envelope number, a ranking phrase and a canary each fail the validator (tests) |
| B7.4 | Gateway steps with policy checks (ZDR evidence, `adek_consent_ref`, `purpose`), `assertAllowed` before retrieval (the `dependency-cruiser` rule), retrieval under `caros_t_ai` inside the user's transaction, `AI_PROMPT_BUILT` with `input_refs`, storage pseudonymised, `usage.inference_geo` check with `refused_post_hoc` and suspension, no `metadata.user_id` | D | O max | F | `copilot-scope` test: a plan naming another caseload's student returns zero rows; support cannot run the co-pilot; a feature on a tenant without ADEK consent evidence is refused |
| B7.5 | Provider adapter on the official SDK, the surface test, `inference_geo`, structured outputs via `messages.parse`, streaming, cache breakpoints; the smoke test on both models; the prefix-length measurement; thinking tokens measured per feature | D | O high | F | the smoke test passes in the staging workspace; the measured prefixes exceed the cache minimums; the measured p99 thinking replaces the assumed allowances in `ai.model_config` and the cost script |
| B7.6 | Prompt repository: `_constitution/1.0.0.md`, per-feature templates and changelogs, `prompt_sha256`, the tenant layer reduced to vocabulary keys and date | D | O high | F | a prompt PR without a version bump, changelog line and `eval_run` id fails the check |
| B7.7 | Eval runner: cases in `packages/ai/evals/<feature>/cases/`, gold before model, splits, the three grader tiers, `ai.eval_run`, the HTML report, CI gating of the deterministic tier, nightly judged tier; the `no-real-data` test extended to the eval directories and to student names | B | O high | | a case without gold fails the runner; the judge is never the generator's model (assertion) |
| B7.8 | Quarantined reader: its own policy row, `allowed_record_kinds`, lazy extraction for the three kinds, one untrusted text per call, the fixed schema, `extraction` columns revoked from the teacher, student, guardian and mentor tiers, `instructions_to_ai`, the 300-text corpus with 60 injection strings | D | O max | F | the corpus bars hold; a hostile teacher note becomes a flag the answer must mention; an extraction column fills only when a consuming feature is enabled (test); urgent requests are never extracted |
| B7.9 | **Moved to B5.12 (decision C1).** The safety screen is a lexicon with no model and ships with the first student-facing screen. This row now holds only the revisit after one term of lexicon data: a written proposal for an add-only model layer (it may add an alert or raise urgency, never clear or lower one), for Davide and the CPO to decide | D | none | | the proposal cites the lexicon's false-positive rate by entry and the CPO's view |
| B7.10 | Injection harness: 120 strings planted in every untrusted surface on both tenants; the envelope-delivery comparison; the canary; expectations under the lexicon screen and for unread surfaces | D | O high | F | 0 changed facts, 0 canary leaks, 0 cross-student mentions, 0 actions; the better delivery recorded in the decision log |
| B7.11 | **Dropped from v1 (decision on F52).** The headline rephrase is not built; the rule headline stands alone; revisit after the pilot | | | | nothing in `packages/ai` names a headline feature (test) |
| B7.12 | Meeting brief: deterministic body, model synthesis, `MeetingBrief` schema, pre-warming as the meeting's counselor under `caros_t_ai` after the sweep for schools holding data, regeneration on dirty facts, `brief_generation_id`, the meetings-view and case-file entry points | D | O high | F | 46-case suite; the opening line contains no fact token; note bodies arrive only through the policy view and default to excluded |
| B7.13 | Co-pilot: `ai.thread`, the plan call, the grammar compiler to parameterised SQL, the cap, the compiled filter rendered above every answer and editable, raw-text search, the write call, `CopilotAnswer`, the coverage rule, the fixed ranking refusal, `COPILOT_QUERY` and `CASELOAD_VIEW`, the screen (`vCopilot` port with the boundaries card as tests) | D | O max | F | the 150 + 60 + 40 suite; the six promises hold; nothing the model emits is executed as SQL (test); the filter is visible on every answer |
| B7.14 | Parent email draft (CONTEXT's feature name; delivered in-app): the parent-role allowlist envelope, `ParentDraft`, promise-word list, length, the bilingual pair, the parent's messages through the reader, consumption into `family.message` in-app with the `parent_update_waiting` notice, `PARENT_CONTACT` | D | O high | F | the 120-case suite; no fact outside `facts_used`; no draft enters the email outbox; the notice email is content-free |
| B7.15 | Extended Essay feedback: `EEFeedback`, criteria citations from the round's guide (`ee_2027`), never a rewritten question, `feedback_generation_ids`, labels visible to the student only and kept off the supervisor's and coordinator's decision screens, the ManageBac-held essay per pass 5 open decision 21 | C | O high | F | the 160-case suite labelled by the coordinator; an eight-word quoted candidate question is refused; no model label renders on a decision screen (DOM test) |
| B7.16 | Failure handling: timeouts per feature, the circuit breaker and the fallback account with R15 evidence, the daily ceiling with the 80% and 100% behaviours, refusal and validator rate alarms, `max_tokens` stops counted, the degraded-mode matrix rendered as banners | D | O high | F | each row of the matrix has a test that flips the failure and asserts the screen's behaviour |
| B7.17 | Cost script with the measured thinking allowances, `cost_usd` on every generation from the registry, the school's monthly AI line, the 14-day cost-anomaly alert, the registry-age alarms | D | O medium | | the script reproduces pass 5 §11.3 within rounding with the assumed allowances, and is re-run with B7.5's measured ones |
| B7.18 | Promotion protocol tooling: terms check, shape smoke, suite at neighbouring efforts, sign-off fields, `AI_MODEL_SWITCHED`, the 14-day watch with automatic rollback | D | O high | F | a promotion without a passing `eval_run` is refused by `active_needs_eval` |
| B7.19 | Labelling sessions with the school (questions 129, 132, 133, 164) and the human samples; the per-feature acceptance records (`school_accepted_residual_retention_at`) and the ADEK consent reference | D | none | | each feature's 30-item sample is rated before its policy row is enabled |
| B7.20 | Demo 3 preparation on synthetic data: every feature with its label, evidence panel and "built from N records" line | D | none | | the demo runs with the AI on against the synthetic tenants only |

**Acceptance criteria.** Pass 5 §10.4's bars hold for every shipped feature, including the pseudonymiser recall bar; the leak scanner and injection zeros hold; the provider-surface, schema-hygiene, refusal, AI-tier allowlist, extraction-consumer and `copilot-scope` tests pass; the smoke test passes on both models; no feature runs on a real tenant until its policy row is enabled by the school in writing, the ZDR evidence and ADEK's consent are recorded and its eval run is signed; the degraded-mode matrix is tested row by row. **Done includes** every suite in pass 5 §10.2 as revised with its runner in CI (deterministic tier per PR, judged tier nightly) and the human samples recorded in `ai.eval_run`.

### 1.9 B8 · Discovery, mentors, reporting

**Status:** not started · **Depends on:** B5 (student surfaces, the lexicon safety screen); B7 for the discovery model mode only; the reporting half may start once G-LIVE produces events · **Model:** Opus 5.5, high effort; **Opus 5.5, max effort** for mentor credentials (passkeys, the password-plus-TOTP fallback), the pairing controls and the message holds (an adult in contact with a minor) · **Reviewed by:** Fable 5.1 on the mentor pieces, the discovery approval gate, the XP single-writer rule, the archetype claim render rule, the mentor contact columns and the reporting aggregates
**Why this model:** the discovery flow is a port of code that runs today; the mentor path is the one place an outsider adult reaches a child, and its credential and hold logic must be right the first time.

**Scope.** Discovery: `discovery.*` with D66, `ref.discovery_question` (folding `ref.onboarding_question`), `ref.archetype` version 1 and `ref.archetype_claim` with D73's kinds and verification (a sentence with an unsourced claim does not render; the Stanford GSB undergraduate claim removed; named programmes and medical durations hidden until sourced), archetype steps conditioned on programme family (F65); `packages/discovery` as a pure package with the ported scorer, goal classifier, queue and the differential tests; the survey with a real "skip for now", the chat in deterministic mode with canonical questions and keyword extraction as **a floor the model may later add to with quoted spans and never remove**, the student seeing and correcting the tags, a session ended only by code or by the student, never by the model's `answer_kind` (F53); narrowing, the analysis page with the honest flat-profile text, the "Closest fit" stamp and `discovery.closest_fit_margin` (never "match", F51) and the alumni precedent counts under small-cell suppression (from `uni.destination` when the school supplies it), proposals and submission of any archetype (P1), the counselor approvals screen with approve, amend inline and send back with structured reasons that become constraints (P2 to P6), approved pathways with the roadmap snapshot, roadmap progress and the XP ledger in its own student-only class written only by the discovery module (P7, invariant 10, F77), the celebration on the student side only; then the model mode of pass 5 §3.4 and §3.5 as revised (tag additions with spans, the phrased next question, `PathwayAnalysis`, the lexicon screen first on every turn, `paused_safety` ending only on a person's disposition) behind `feature_policy.discovery_chat` and `pathway_analysis`, both gates of B7, R8 acceptance and the under-13 gate, and the differential evals P1 to P10. Mentors: `mentor.*` with D40 and D57; the `mentor_coordinator` screens (vetting record with expiry, invitations whose code is handed over off-email, pass 4 open decision 21; pairings created only by the coordinator's function; held messages); the mentor identity path (7-day invitation, passkey, the fallback of a 12-character password checked against the Pwned Passwords range API and stored with Argon2id plus mandatory TOTP, 12-hour and 30-minute sessions); the student's find-a-mentor and network pages, the mentor's requests and mentees pages (own pairing only, first names by default, F59, F76), append-only messages screened by the lexicon with the pattern hold and the grooming alert to the coordinator by the reference-only email and to the lead in-app (C3), twenty messages a day per student, **sessions in person on school premises only in v1** (decision C6: video on the school's platform waits for ADEK 6.4 consent and approval), the parent's "a pairing exists" line with the mentor's headline and nothing else; consent recorded on the pairing, naming mentors who may live abroad. Reporting: **per-tenant aggregate tables under RLS with one writer**, written by `sweep.finalize` and never materialised views (F34); the `prove` page from `events.event` with the four metric families and an honest "not enough events yet" state, never a target shown as a measurement; the `pilot` page; the regulator profile's indicators (for ADEK, the CU Guidance Policy's four, each counted over one population and never totalled across the caseload and the Diploma cohort, F77, F31); the school's monthly report (head hash, grants, rotations, AI lines); the per-teacher concern-rate report for the `caseload_lead` only; every report keyed on `purpose` so canary and demo tenants never count (F42).

**Tasks (planning estimate: 26 sessions plus Fable reviews).**

| # | Task | Stream | Model | Rev | Done when |
|---|---|---|---|---|---|
| B8.1 | Migrations, exactly the B8 row of §1.12: `discovery.*`, D66, `ref.discovery_question`, `ref.archetype` and claims with D73 columns and programme families; `mentor.*` with `mentor.pairing` and `auth.is_pairing_mentor()`, D40, D57; `uni.destination`; the remaining `reporting` aggregate tables | C | O high | F | registry, RLS and matrix tests extend; only the discovery module can write the XP ledger (grant and boundary test); no materialised view exists (convention test) |
| B8.2 | `packages/discovery`: scorer, goal classifier, queue, the "enough signal" rule, keyword tags as the floor, differential and deterministic-mode tests | C | O high | | P3's twins differ in exactly one tag; P1 ends with `ranked_archetypes = null` |
| B8.3 | Survey with skip, deterministic chat, the student's tag corrections, narrowing, the analysis page with "Closest fit", precedent counts with suppression, submission of any archetype | C | O high | | Playwright on both tenants; a bank question naming IB is absent on Wellesmere; the copy lint finds no "match" outside reach, match and safety |
| B8.4 | Counselor approvals: approve, amend inline, send back with structured reasons, regeneration under constraints, `PATHWAY_*` audit | C | O high | F | send-back without reasons is refused at both layers; the regenerated analysis differs from the previous one |
| B8.5 | Roadmap with programme-family steps, progress, XP once per step in its own class, the celebration on the student surface only | C | O medium | F | a second completion awards nothing; no XP code exists outside the discovery module (boundary test); parents read no XP (authz) |
| B8.6 | Discovery model mode: tag additions with quoted spans, phrased questions, `PathwayAnalysis`, the lexicon first and `paused_safety`, the evals P1 to P10 | C | O high | F | the discovery suite bars of pass 5 §10.4; the model never removes a keyword tag and never ends a session (tests) |
| B8.7 | Archetype claims: kinds, sources, verification status, `safe_text`; the render rule; the removal list from pass 5 §2.5 | C | O medium | F | an unverified claim never renders (DOM test) |
| B8.8 | Mentor identity: invitation with the code handed over off-email, passkey registration and assertion, the password-plus-TOTP fallback, sessions, `MENTOR_INVITED` | D | O max | F | a breached password is refused; TOTP is mandatory on the fallback; recovery only by re-invitation; no credential ever sits in the outbox |
| B8.9 | Vetting record and expiry, pairings created only by the coordinator's function, `share_scope` changed only through the student's, ending a pairing, `PAIRING_ENDED` | D | O max | F | `active` requires `verified` and an arranger; a mentor sees only their own paired students' first name, year and goal (column tests); a student cannot insert a pairing |
| B8.10 | Messages append-only with the lexicon and the pattern hold, the coordinator's held-message review, the grooming alert, the rate limit, thread visibility to counselor, coordinator and lead with the header line; sessions in person on premises only | D | O high | F | a message with a phone number is held; the thread header names who can read it; a session with any mode but `in_person_on_premises` is refused |
| B8.11 | Student find-a-mentor and network pages; mentor requests and mentees pages; the parent's pairing line | C | O medium | F | no email or phone column exists in `mentor.*` (schema test); a parent reads only that a pairing exists and the mentor's headline |
| B8.12 | Reporting: per-tenant aggregate tables written after the sweep, the `prove` page with the four families and the honest empty state, the `pilot` page, the regulator profile's indicators over one population each, the monthly school report, the teacher concern-rate report for the lead; everything keyed on `purpose` | D | O high | F | no figure on `prove` comes from anything but `events.event`; the page says "targets, not measurements" until events exist; no indicator adds the caseload and the Diploma cohort; a canary tenant never appears in a count |
| B8.13 | Demo 3 script; accessibility pass on the new surfaces | C | O medium | | the script runs; axe passes at 375px |

**Acceptance criteria.** The approval gate holds at both layers; XP has one writer and its own class; the flat profile ends honestly; the model never removes a tag or ends a session; the mentor path passes its credential, column, hold and in-person tests; reporting shows only computed figures, per tenant, keyed on `purpose`. **Done includes** the discovery differential tests, the mentor identity tests, the boundary tests and the reporting tests in CI.

### 1.10 B9 · Connectors, second school, scale

**Status:** not started · **Depends on:** the pilot's four green weeks (pass 2 §9 step 16); ACS's answers to questions 70, 77 and 81 for the adapter order; pass 4 §6.6 and question 14 before the Classroom adapter runs for a real tenant · **Model:** Opus 5.5, high effort · **Reviewed by:** Fable 5.1 on every adapter's `verify()` (write-scope and sensitive-scope detection) and credential handling, the connector runtime, the isolated-deployment rehearsal and the PgBouncer test
**Why this model:** adapters are I/O against documented APIs; the one subtle failure is a token that can write, or read what it must not, which `verify()` must catch.

**Scope.** `VeracrossDataApiConnector` (OAuth client credentials with list and read scopes only, `verify()` refusing write scopes and the student-alert and Health scopes, `on_or_after_update_date` cursors, the rate limit of 300 requests per 3 minutes per access token, the NAT egress IP), unless ACS's weekly pack runs on Veracross's **Data Export Package**, which needs no adapter (B4.9's password path and watcher, F54); `ManageBacConnector` for rosters and grades on **v2p3** (read-only token with configurable scopes, `verify()` refusing any write-capable resource, `modified_since` where the resource has it and full reads where it does not); the CAS and Extended Essay mirror is B6.16's, not B9's (C10); `GoogleClassroomConnector` (the school-created reader account, a domain administrator or a teacher of each course, under domain-wide delegation, read scopes, nightly polling, the engagement domain's conditions of pass 4 §6.6, `occurred_on` from the tenant's timezone); the Directory read is B1.15's; in the order ACS's answers dictate; `ISAMSBatchConnector` (XML to envelopes) for the second school, with its pastoral import from forms and year groups; the OneRoster profile family (Veracross API Plus is read by its own adapter, not by the CSV profile family); the Maia CSV profile run weekly by a counselor; the `uni.destination` import for alumni counts; auto-approve policies per source for fact kinds only, access-changing kinds and a mirror batch that moves a supervisor always waiting for a second approver; connector runs, cursors, `CONNECTOR_VERIFIED`, `CREDENTIAL_ROTATED`; the second real school onboarded with a CAROS human at steps 1, 2, 8, 11, 14, 15 and 19 only (pass 2 §9), measured as under three profile versions per import kind before four green weeks; the isolated single-school deployment rehearsed once from the same module with `deployment_mode = "single"` so the promise is real before a school asks; the Profiles three-pane editor cut from B1 (§2.3), needed before the second school; the year-1 items: PostgreSQL 18 review once the extension list is clean, Drizzle 1.0 after two GA minor releases, `apps/api` (Hono and `@hono/zod-openapi`) only when a non-TypeScript caller exists, the PgBouncer transaction-pooling test in the RLS suite before any pooler, the in-country inference review with counsel's answers and the eval scores (pass 5 open decision 1), Sonnet 5.5 and Haiku 5.5 as `candidate` rows through pass 5 §9.5, SOC 2 Type I within six months of the first real record then Type II, ISO/IEC 27001 within twenty-four (timing assumptions, pass 4 §9.12); the letter engine's generation stays deferred until real letters exist to draft and time and counsel has answered the letter question.

**Tasks (planning estimate: 30 sessions plus Fable reviews; spread over the pilot's live period and year 1).**

| # | Task | Stream | Model | Rev | Done when |
|---|---|---|---|---|---|
| B9.1 | Connector runtime: `RateLimitedHttp`, run budgets, cursors, JSON Lines raw artefacts, pause after three failures, the Studio's connector screens, credential rotation with re-verify | A | O high | F | a run whose budget is spent ends `partial` and advances only completed resources |
| B9.2 | Veracross Data API adapter with `verify()` refusing write scopes and the student-alert and Health scopes, rate-limited per access token; field bindings read off the real pages at onboarding | A | O high | F | a token with a write or Health scope fails `verify()`; the ACS weekly pack becomes a nightly connector without a profile change |
| B9.3 | ManageBac adapter for rosters and grades on v2p3 with the scope check (the CAS and EE mirror moved to B6.16) | A | O high | F | a token that can write fails `verify()`; a roster never writes an IB pick (a conflict row instead) |
| B9.4 | Google Classroom adapter under the engagement conditions; the engagement module flag; the engine's `occurred_on` read | A | O high | F | no time-of-day column is readable by the engine (test) |
| B9.5 | iSAMS Batch adapter (XML to envelopes) with the pastoral import and the OneRoster profile family; the Maia CSV profile; `uni.destination` | A | O high | | the Wellesmere fixture also imports through the XML path, pastoral assignments included |
| B9.6 | Second real school onboarding with the runbook; the profile-version metric; the regulator profile for its regulator (a KHDA profile for a Dubai school, analysed before it signs) | D | none | | a human at the seven steps only; under three profile versions per kind |
| B9.7 | Isolated single-school deployment rehearsal in a throwaway subscription from the same module | D | O max | F | the app refuses to start in `single` mode with more than one school that is not `offboarded`; RLS unchanged; torn down after the drill |
| B9.8 | Year-1 platform items: PostgreSQL 18 review, Drizzle 1.0, the PgBouncer test, `apps/api` if a caller exists | D | O high | F (PgBouncer) | each has a decision record |
| B9.9 | Certification work: SOC 2 Type I evidence from the CI control list and the runbooks; the audit firm engaged | D | O medium | | the Type I report within six months of the first real record (assumption) |
| B9.10 | The Profiles three-pane editor cut from B1.10, before the second school | A | O high | | Playwright: author a profile against a sample, activate it, on both tenants |

**Acceptance criteria.** Each adapter passes `verify()` against a token with write or sensitive reach (negative test) and pulls its fixture into the same pipeline as a file; the second school onboards at the target; the isolated deployment rehearsal passes the RLS suite as one tenant. **Done includes** the adapter tests, the isolation rehearsal record and the year-1 decision records.

### 1.11 The pilot overlay

The pilot is not a phase; it is a sequence of the school's own steps laid over the build, gated by G-REAL at its start and G-LIVE in its middle. The tenant follows pass 1's one status model (F36): created `onboarding` under the bootstrap grant, holding no student data; `onboarding → shadow` at G-REAL, which the attestation trigger refuses without the school's legal-basis attestation; `shadow → live` at G-LIVE with a recorded sign-off. Sweeps, connectors and data alerts key on "holds data" (`shadow` or `live`), and the nightly sweep starts only when the caseload lead enables it after the backfill sweep is reviewed (pass 2 §9 step 15), so no sweep runs on a half-imported history. Weeks are counted from G-REAL and are planning assumptions; the calendar column assumes G-REAL on the first school day after the 2027 spring break, taken as early April 2027 until ACS answers question 165. Pass 2 §9 gives the twenty steps and pass 3 §14 the shadow protocol.

| Week | Calendar (assumed) | What happens | Gate or output |
|---|---|---|---|
| before G-REAL | January to March 2027 | Onboarding steps 1 to 6 while the tenant is `onboarding` and holds no student data: agreement and DPA with the data delivery schedule as `ingest.expected_cadence` and the history import in it; the discovery call; the tenant created under the bootstrap grant with `purpose = 'customer'`, which cannot start unless the DPA names two different people, its signatory and the first `school_admin` (the signatory's written confirmation of the second `school_admin` is recorded as `SECOND_ADMIN_CONFIRMED`); the school's Workspace administrator sets CAROS to Limited, confirms the under-18 designation and approves the Directory scope on the delegated reader account, and each domain is mapped on DNS proof; the two `school_admin` holders; counselor memberships and `caseload_lead` granted by the school under four eyes; the calendar entered by hand. The elicitation session (B4.16) and the CPO's route, category routes, helplines and test referral are done here too | the §9.13 list complete (B4.17) |
| 0 | first fortnight of the spring term, April 2027 | **G-REAL**: the attestation recorded, the tenant moves to `shadow`. Steps 7 to 9: the pseudonymised sample with free text blanked, discarded after preview; profiles version 1 installed under a write grant and activated by the `school_admin`; the delivery path (upload, the Data Export Package over password SFTP, or SFTP by key) verified | the tenant is `shadow` |
| 0 to 2 | April 2027 | Steps 10 to 13: the history import, oldest first, **at least twenty weeks before shadow's first sweep** (the 2026-27 year to date gives about twenty-five school weeks by April; two years where ACS has them, question 83), month by month where the export's five-minute limit requires it, access-changing batches approved by a second `school_admin`; the data-quality review with reason coverage; identity checks; the `caseload` import | previews approved by the school's `school_admin` holders |
| 2 | late April 2027 | Step 14: ACS's thresholds and tiering version 1 from the elicitation activated **in shadow** by the caseload lead; step 15: the backfill sweep and the retrospective validation report reviewed; the caseload lead enables the nightly sweep (`core.enable_sweep()`, `SWEEP_ENABLED`), and shadow week 1 counts from the first nightly sweep after that act; **the backtest** (pass 3 §15.2) as a UAE North job over the history if the counselors' dated handled-case records exist, with only aggregates leaving, otherwise recorded as not possible | the backtest report or the written statement; shadow's first sweep |
| 2 onward | from late April 2027 | Step 16: weekly packs arrive; the `school_admin` approves every preview for four weeks, then auto-approval for the fact kinds only; step 18, teacher rollout: flags and safeguarding referrals are live throughout shadow because they need no engine; the counselor log begins (thirty seconds a day); no tiers are visible to anyone | **four green weeks** gate fact auto-approval and the connectors (B9) |
| shadow weeks 4 and 8 | about late May and late June 2027 | Reveal sessions (pass 3 §14.4), counted from shadow's first sweep (§12.17): engine cases mixed blind with matched unflagged students and judged, then walked through with the evidence chain; counselor-only students classified `data_gap` or `engine_miss`; threshold changes become new **shadow** versions. The case file (B3.11), the what-if run (B2.15) and the comparison report with the blinded queue (B4.15) must exist by week 4 | the comparison report (B4.15) per counselor and pooled |
| end of 2026-27 | late June 2027 | If pass 3 §14.5's criteria hold (at least 40 blindly judged engine cases at `review` or above, the one-sided 95% lower bound at or above the pre-registered floor, engine cases judged `yes` more often than matched controls, every miss classified and answered, four weeks of on-time sweeps, the four counselors agreeing), the G-LIVE decision is taken now and recorded; otherwise shadow resumes in August up to the pre-registered longest period (question 162). Over the summer the rest of B3, B4.7 and B4.11 are finished, then B5 begins | the G-LIVE decision, or the recorded reason it waits |
| start of 2027-28 | August 2027 | **G-LIVE**: the caseload lead activates the first live version; `shadow → live`, which the status trigger refuses until `g_live_signoff` is recorded (`G_LIVE_SIGNED_OFF`); the first live nightly sweep on the first school week; the marker is off because the tenant is `real`. Going live starts live cases fresh, each linked to its shadow history | steps 19 and 20 |
| autumn 2027 | September to December 2027 | B5 on production behind its module flag before the application deadlines: students sign in; parents once guardian links are activated per contact and codes handed out through the school's chosen channel (question 124); Demo 2 for ACS leadership on the real tenant with real data only for staff who already hold it. The counselor log continues one more term | Demo 2 |
| spring 2028 | January to April 2028 | B6 before the Grade 10 selection round and the Extended Essay proposals; connectors after four green weeks in the order ACS's answers dictate (B9); B7's features one at a time, only after G-LIVE, the ZDR arrangement and ADEK's consent, as the school accepts each in writing and its eval run is signed | **G-AI** per feature |

Two things the overlay makes explicit. **The pilot can run for months with the AI off and lose nothing that makes CAROS CAROS** (pass 4 §6.1); shadow mode and the first live term are that time. **The demonstration marker has two real branches**: it is on for the synthetic ACS tenant used in demos and training, and off for the real one; the two are different tenants, and the classification and purpose triggers stop either from becoming the other. No fairness audit runs in the pilot and no special-category labels are collected; the pilot documents say fairness is untested (C11).

**If the fallback applies** (§2.3): G-REAL moves to the first fortnight of the 2027-28 year (August 2027); the history import covers 2026-27 in full; shadow runs from September to about November 2027, during the Grade 12 application season, when counselors are busiest; G-LIVE falls in November or December 2027; B5 competes with the reveal sessions for the same autumn, so the application-season module reaches families for the 2028-29 cycle rather than 2027-28.

### 1.12 The migration map

**This table is the single source for what lands when** (F40). Each phase's migration task (B0.5, B1.1, B2.10, B3.1, B5.1, B6.1, B7.1, B8.1) lists exactly its row, generated from this table into `docs/PLAN.md`, so a task row cannot drift from it. The D-numbers of passes 2 to 5 land once each, in the phase named here, and so do the objects pass 8 added to pass 1's DDL (01 §0.3). File names are illustrative; the sequence within a phase is the pass's own order unless a dependency forces otherwise. Every migration is a pull request reviewed as SQL (pass 1 DR-2), and `drizzle-kit check` guards the history against two branches generating in parallel ("extremely useful when you have multiple developers working on the project and altering database schema on different branches", [drizzle-kit check](https://orm.drizzle.team/docs/drizzle-kit-check)).

**The migrator.** `drizzle-orm/migrator` (0.45 and the 1.0 release candidates) runs every pending migration in one transaction, and PostgreSQL refuses `CREATE INDEX CONCURRENTLY` inside one (F40). The migration job therefore has a second, non-transactional step for files marked `-- no-transaction`, run after the transactional step and idempotent by construction. At pilot scale no index needs `CONCURRENTLY`; the step exists for the first large table, and every file that uses it says why.

**Scope predicates land with the tables they read.** Pass 4 asks that D42, D44 and D46 land in the same migration as the `auth.allowed()` predicates that read their columns. `auth.allowed()`, `auth.protect()` and the predicates whose tables exist in B0 land in B0; `auth.is_guardian_of()` is created with `family.guardian_link` and D42 in B1; `auth.is_pairing_mentor()` with `mentor.pairing` in B8; `auth.esc_covers()` with `signal.escalation` and D46 in B3. Until its predicate exists, a matrix row that needs it resolves to false, and the authz suite's fixtures for it are marked pending, not skipped.

| Phase | Migrations | Creates or changes | D-numbers landed |
|---|---|---|---|
| B0 | `0001_b0_roles_extensions`, `0002_b0_spine`, `0003_b0_policies`, `0004_b0_seed_global` | the per-deployable login roles (`caros_web`, `caros_worker`, `caros_support`, `caros_migrator`, `caros_jobs`) all `NOINHERIT`, and the tier roles, `NOLOGIN NOBYPASSRLS`; the schemas including `reporting` and the pg-boss schema under `caros_jobs`; the spine tables of §1.1 with `core.school.purpose`, the status model and its trigger, `status_before_suspension`, `sweep_enabled_at` with `core.enable_sweep()` (`SWEEP_ENABLED`) and `g_live_signoff` with `core.record_g_live_signoff()` (`G_LIVE_SIGNED_OFF`), the trigger refusing `shadow → live` without the sign-off; `auth.capability_grant`, `auth.pastoral_assignment`; `ref.programme_family`, `ref.destination_system`, `ref.regulator_profile`; `config.vocabulary_history`; `privacy.retention_override`, `privacy.non_tenant_store`; `auth.protect()`, `auth.allowed()`, the B0 predicates and `auth.scope_rank()`, `core.school.sweep_hour` (D29, landed here because `core.schools_due_for_sweep()` reads it), the ID-only lookups (`auth.school_for_domain`, `auth.school_for_magic_link`, `core.schools_due_for_sweep`, `core.schools_holding_data`, `core.schools_with_accounts`, `notify.schools_with_due_outbox`), `notify.enqueue()`, the projection views of pass 1 §2.4a whose tables exist; global seeds (`auth.data_class` with sensitivity and pass 3's three classes, inserted once by D30 with pass 4's keys and given their sensitivity by D33; `auth.role` with `school_admin` and `staff`; `auth.capability`; the matrix from `matrix.json`; `audit.action` with every key of passes 1 to 5 once; `events.name` once; `privacy.retention_class` with all of pass 4 §7.1's classes under its keys, seeded by pass 1 and updated, never re-inserted, by D50, before any registry row references them; `notify.template` versions for pass 4 §5.8; `config.vocabulary` platform defaults with attribute schemas; `ref.grade_scale`; the regulator profiles) | D7, D8, D9 (keys), D27, D28, D29, D30, D33, D34, D35, D36, D37, D38, D39, D44, D47, D48, D49, D50, D61 (tests), D71 (keys); D15 is subsumed by the matrix rows of pass 4 §3.6 and is not applied separately; D14 is withdrawn (staged rows are pass 4's `raw_import` class, 30 days) and its key kept so no number shifts |
| B1 | `0005_b1_record`, `0006_b1_ingest`, `0007_b1_views` | the `sis` record tables with `sis.enrolment_programme`, `sis.tutor_group`, `sis.tutor_group_member`, `sis.student`'s restriction and under-13 consent columns, `sis.attendance_event.derived_rule`; `engagement.activity_event`; `family.guardian_link` with D42 and `auth.is_guardian_of()`; `core.person_name_history`, `core.person.match_key`, `sis.section_meeting`; `doc.file` with D13's kinds; `ingest.*` complete with `ingest.consent_decision`; `sis.v_current_*`, `sis.v_attendance_teacher` | D1, D2, D3, D4, D5, D6, D12, D13, D16 (registry entries and tests), D42, D56, D58, D59 |
| B2 | `0008_b2_signal`, `0009_b2_config_engine`, `0010_b2_fairness` | `signal.*` except `escalation`, `teacher_flag` (it references `signal.escalation`) and `v_last_contact` (it reads `core.meeting` and `family.contact_log`), all three B3's; with `signal.evaluation` created under D17's key and `backfill_of` (both now in pass 1's DDL; D17 no longer adds them, and the D10, D68 and D72 columns shown in pass 1's DDL are still migrated by those deltas in their own phases), `signal.case.shadow`, `signal.case_link`; the engine deltas; `signal.counselor_log`, `signal.v_shadow_queue` (`security_invoker`); `signal.week_run()` by `week_start`; `reporting.agg_tier_daily` and `reporting.agg_event_weekly`, written by `sweep.finalize`; `fairness.group_label` with no application grant, unused in the pilot; the engagement module flag | D17, D18, D19, D20, D21, D22, D23, D24, D25, D26, D31 (schemas in the package), D32, D60 |
| B3 | `0011_b3_escalation`, `0012_b3_cover_contact` | `signal.escalation` restructured with the partial unique index and the idempotency key, `signal.teacher_flag`, `signal.v_last_contact`, `signal.escalation_update`, `auth.esc_covers()`, the `signal.case_escalation_pin` trigger; `core.school` safeguarding and safety-alert route columns and route history, the legal-basis column (its trigger in B4); `core.meeting` (its foreign keys' tables now exist); `auth.caseload_assignment` cover columns; `family.contact_log` | D43, D45 (columns), D46 |
| B4 | `0013_b4_privacy`, `0014_b4_gates` | `privacy.notice_version`, `notice_ack`, `sar_request`, `processing_restriction`, `erased_external_identity`; the attestation trigger on `onboarding → shadow`; retention numbers reconfirmed | D45 (trigger), D51, D52; and pass 3 D24's blind-judgement table `signal.shadow_judgement`, created here for B4.15 |
| B5 | `0015_b5_ref`, `0016_b5_uni`, `0017_b5_family`, `0018_b5_safety` | the remaining `ref.*` with `ref.statement_format`, `ref.reference_format`, `ref.admission_statistic`; `uni.*` except `uni.destination`, with `uni.centre_statement` and `uni.school_deadline`, D11 and D72; `family.message`, `meeting_slot`, `meeting_request`; `auth.activation_code`; `signal.safety_alert` and `signal.student_reflection.urgent_request`; the `uni.classification` and `safety.lexicon` rule-set kinds; the forbidden-column additions | D11, D41, D68, D72, D73 (except the archetype columns) |
| B6 | `0019_b6_ib`, `0020_b6_ee_guides` | `ib.*` with `review_meeting_version`, `ib.check_approval`, `ib.sign_off()`, `ib.v_signoff_card`, the transition functions and column grants; D10's mirror columns on `ib.cas_entry` and `ib.ee_essay`; `ref.ee_guide`, `ref.ee_criterion`, the round's guide key, `feedback_generation_ids` | D10, D70 |
| B7 | `0021_b7_ai_registry`, `0022_b7_ai_tier`, `0023_b7_ai_features` | `ai.model`, `ai.model_config`, `ai.provider_account`, `ai.feature_policy`, `ai.generation`, `ai.pseudonym_map`, `ai.reidentify()`, `ai.thread`, `ai.eval_run`; the generation columns; `caros_t_ai`'s generated grants; `core.meeting.brief_generation_id`; the extraction columns on three record kinds | D53, D54, D55, D62, D63, D64, D65, D67, D69, D74, D75 (tests) |
| B8 | `0024_b8_discovery`, `0025_b8_mentor`, `0026_b8_reporting` | `discovery.*`, `ref.discovery_question`, `ref.archetype` and claims with the D73 columns; `uni.destination` (introduced by this pass, because pass 5 §2.5 asks for alumni precedent counts and no earlier DDL defines it: `school_id`, graduation year, institution, course family, country, count; counts only, no student link, small cells suppressed at read; B8's precedent counts read it and B9 imports into it); `mentor.*` with `mentor.pairing` and `auth.is_pairing_mentor()`, `auth.credential`, the vetting and hold columns; the remaining `reporting` aggregate tables (`reporting.agg_indicator` for the regulator profile's indicators) and their single writer | D40, D57, D66, D73 (archetype columns) |
| B9 | adapter-specific configuration only | no new tenant tables for the adapters (`ingest.connector_run`, `source_cursor` exist since B1) | none |

Four deviations from the passes' own numbering, each for a dependency, recorded in pass 2's D-table and in the "For other passes" notes of passes 3 to 5 (§12.18): D11 (columns on `uni.*`) waits for the `uni` tables in B5; D15's permission row is part of the matrix seed in B0; D56 and D58 land with the tables they alter in B1 (the import's under-13 check reads D58's consent column, F56); D60 lands with the engine that reads it in B2. Two duplicate seed keys are resolved once: `PREVIEW_ACCESS` is seeded by pass 2's D9 only, and `sweep.completed` by pass 1's event list only (§12.16).

### 1.13 The thin slice as proposed, and what this pass changes

The prompt's thin slice is right about content and this pass keeps every item in it. It changes five things, openly.

1. **It is four phases, not one.** Tenancy, identity, ingest, the engine and sweep, the queue, the file, the teacher loop, audit and the escalation are about a hundred agent sessions. A phase with no checkable "done" for three months is how documentation drifts and how a bad merge goes unnoticed for weeks. B0 to B3 each end in something that can be verified.
2. **It grows by five items the passes showed are not optional.** The Mapping Studio's import and identity screens (the seed cannot run without a profile, pass 2 DR-11; the profile editor waits, §2.3); the counselor log and the shadow view (the pilot's first phase is shadow mode, pass 3 §14); the support console (onboarding runs through a grant, pass 4 §3.7); **the hourly Directory read** (the first version added the RISC receiver here; Google sends no Cross-Account Protection events for Workspace accounts, so the mandatory Directory read and the leaver runbook's "revoke in CAROS" step are what end a leaver's access, F18); the CPO's acknowledgement screen (an email that says "sign in to acknowledge" without a screen to acknowledge on is a false promise, pass 4 §5.3).
3. **"Identity" in the slice means staff.** Students sign in through the same path but have nothing to see until B5; parents need an activation flow that is B5's; mentors are B8's. Building the parent and mentor paths in the slice would delay the pilot by the two riskiest identity pieces for no gain.
4. **The first screens do not wait for the engine to be finished.** Pass 1 DR-8's narrative seed layer exists so the screens can be built before the engine is; B2's `engine-reproduces-seed` retires it, and Demo 1 (§6.5) is shown with the real engine.
5. **For the April 2027 target, the slice's counselor-facing half follows G-REAL** (§2.3). Shadow mode shows counselors no tiers, so the sheet, the case file and the command centre are built during shadow, with the case file due by the first reveal session. The escalation, the teacher loop, the flags inbox, audit and access logging stay before G-REAL, because they are live from the first day of shadow.

One thing the prompt's list implies and this pass does not do: it does not put the nightly sweep's operations (alerting, the watchdogs, on-call) after the slice. They are inside B2's definition of done, because a sweep nobody is told about has not been built.

---
## 2. Parallel workstreams, the April 2027 schedule, and the process

### 2.1 Four streams and their ownership

Pass 1 DR-3 fixed the package ownership and §4 the module ownership. This pass turns them into four streams. **While Davide builds alone (decision C8), every stream is his**: the streams stay as package boundaries the tooling enforces, and as the structure the full process switches on to when a second regular builder joins (§2.5). The assignment of Faisal, Qasim and Taha to A, B and C is then Davide's call.

| Stream | Owner now | Owns (packages, modules, screens) | Busiest in |
|---|---|---|---|
| **D · spine** | Davide | `packages/db`, `packages/contracts`, `apps/worker`, `infra/`, the `tenant`, `identity`, `signals` (domain and screens), `notify`, `audit and reporting`, `privacy` modules, `packages/ai` gateway, `packages/ui` and the app shell, `docs/` | every phase |
| **A · facts** | Davide (a teammate later) | `packages/ingest`, the `record` module and its read functions, the Mapping Studio, the fixtures, the teacher register (B3.14), the SFTP watcher, the ManageBac mirror (B6.16), the connectors (B9) | B1, B9 |
| **B · engine** | Davide (a teammate later) | `packages/engine`, the calibration script, the thresholds screen, the counselor log and shadow tooling, the comparison report, the eval runner (B7.7) | B2, B4, B7 |
| **C · programmes** | Davide (a teammate later) | the `university`, `ib`, `discovery` and `mentor` modules (domain and screens), the reference-data tool, the EE feedback feature | B5, B6, B8 |

Rules that keep the streams apart, all enforced by tooling rather than memory, and kept in solo mode so that a second builder can join without a rewrite: a stream edits only its own packages and its own folders under `packages/domain/<module>`, `packages/api/<module>` and `apps/web/app/(role)/<module>`; cross-module reads go through the other module's `index.ts` (the `dependency-cruiser` rule of DR-3 fails the build otherwise); `packages/contracts` changes are proposed by the stream that needs them and merged before the work that uses them; `packages/db/migrations` accepts one migration per pull request, `drizzle-kit check` in CI; `packages/ui` is D's, and a screen that needs a new primitive proposes it there first.

### 2.2 Contracts and integration points

Every seam between streams is a typed artefact in `packages/contracts` with a test that proves both sides honour it. The six seams:

| Seam | Producer → consumer | The contract | The test that proves it | Phase |
|---|---|---|---|---|
| Envelopes | A (`packages/ingest`) → A (`record`) | `CanonicalEnvelope<K>` and `CanonicalRecord[K]` Zod schemas; natural keys per kind (pass 2 §3.4, §7.1) | `fixtures.spec.ts` against `expected.json`; the `sis.v_current_*` views' row counts after commit | B1 |
| Facts to the engine | A (`record`) → B via D's loader | `StudentEvaluationInput` (pass 3 §13.1 as revised): per-domain `as_of`, cadence and register, calendar, sections, series, events, university rule outputs, case state with the escalation pin and recent moves, restricted domains, vocabularies, configuration | `packages/domain/signals/test/load.spec.ts`: the loader over both fixtures produces inputs that validate against the schema; `engine-reproduces-seed` end to end | B2 |
| Engine to signals | B (`packages/engine`) → D (`signals` writer) | `EvaluationResult` with drafts for snapshots, signals, evidence, suppressions, rule hits with vocabulary snapshots and the proposal; `inputHash` | the writer's replay test (an upsert, no duplicates); S23 | B2 |
| Signals to notify | D (`signals`) → D (`notify`) | rows written only through `notify.enqueue()` with `template_key`, `template_version`, `dedupe_key` and a payload validated against the template's allowlist (identifiers and roles only, never a name or a credential) | `packages/notify/test/templates.spec.ts`; the outbox dedupe and send-time idempotency tests | B3 |
| Server to browser | every module's `packages/api` router → `apps/web` | the inferred tRPC router type; no hand-written client type; procedures validate with the `contracts` schemas | the type check itself; Playwright per screen; the `'use server'` lint rule (no Server Actions for mutations) | every phase |
| Records to the model | every module's read functions → D (`packages/ai`) | the per-feature envelope builders with accessors generated from `ai.feature_policy.allowed_fields` (an out-of-allowlist field is a compile error) | the leak scanner over both tenants; `envelope-allowlist`; `schema-hygiene`; the AI-tier allowlist test | B7 |

Two more seams are contracts by convention rather than by type: the rule-set parameter schemas (`packages/engine/schemas/*.schema.json`, D31) that the thresholds screen (B) and the domain layer (D) both read, and the audit `detail` schemas (`packages/contracts/audit/`, D48) that every module writes against.

### 2.3 The April 2027 schedule

**The target** (decision on F39): G-REAL in the first fortnight of the spring term 2027, with Davide building alone. Every date below counts back from **T, the first school day after ACS's 2027 spring break**, assumed to be **Monday 5 April 2027** until ACS answers question 165; if T moves, every date moves with it. The build is assumed to start on **Monday 28 September 2026**. All session counts and dates are planning assumptions, re-planned at each checkpoint from measured numbers.

**How capacity is counted.** In sessions, not weeks and not dollars. Under decision C7 build sessions run on Opus 5.5 through the zero-data-retention API organisation, so their limit is that organisation's rate-limit tier and spend limit (both raised with Anthropic in week one) and, in practice, Davide's own attention: each session needs a brief before it and a read of its diff after it. Fable 5.1 review sessions cannot run under zero data retention (Fable 5.1 is a Covered Model) and run under a separate account whose weekly usage limit, reached fastest by max-effort sessions, is the tighter of the two limits. **Planning assumption: eight Opus 5.5 build sessions and four Fable 5.1 review sessions a week, sustained, measured from week one.** Non-code work (counsel, the DPA, the DPIA, ACS meetings, the elicitation) is Davide's too and is what caps the build rate below the models' limits.

**The critical path, in Davide's sessions.** One critical path, because one person builds. Before G-REAL:

| Phase | Build sessions planned | Before G-REAL | Moved behind G-REAL |
|---|---|---|---|
| B0 Foundations | 22 | 22 | none |
| B1 Facts and identity | 31 | 29 | the Profiles editor (2; now B9.10) |
| B2 Engine and sweep | 29.5 | 28 | the thresholds screen's what-if and UI (1.5) |
| B3 The counselor's morning | 36 | 19 | the sheet's four modes, the case file, cover, the command centre, `si-why`, meetings and interventions, the dimension pages, the `arch` page, Demo 1 (17) |
| B4 Pilot readiness | 24 | 18 | offboarding (2), the load test (1), the comparison report and blinded queue (2), rehearsal (1) |
| **Total** | **142.5** | **116** | **26.5** |

To the 116 add a rework allowance of 20% for sessions that end with the task not done (the attempt counter of §3.1 expects them): **about 139 Opus 5.5 sessions on the critical path to G-REAL**, and about 50 Fable 5.1 reviews (every `F` task before G-REAL, plus the bake-off). About 9 of the 139 (the restore drills, the onboarding dry run, the review pack, the pen-test fixes) fall after the pen test starts, so **about 130 must be done by the code freeze at T minus 7 weeks, Monday 15 February 2027**. From 28 September 2026 to 15 February 2027 is 20 weeks; less two weeks of low capacity (UAE National Day on 2 and 3 December, the winter break, illness) that is 18 weeks, **144 sessions at eight a week**. The margin is about 14 sessions, one tenth. The fifty Fable reviews need about 13 weeks at four a week, inside the same window.

**What the date holds on, stated plainly.**
- **At eight sessions a week with rework at or under 20%**, April holds with a tenth to spare. At nine it is comfortable. At seven (126 sessions) it holds only by taking every contingent cut below. Below seven, or if rework runs above 30%, it does not hold.
- **The committed cuts** (already in the tables): the counselor's morning proper and Demo 1 move from before G-REAL to shadow mode; the thresholds what-if, the comparison report with the blinded queue, the offboarding drill and the load test move behind G-REAL; the Profiles editor moves to B9. **B5 and B6 no longer land during shadow mode**: they follow in 2027-28 (B5 before the application deadlines, B6 before the Grade 10 selection round). **B7 cannot start for real before G-LIVE, the ZDR arrangement and ADEK's consent**, so no AI feature runs in 2026-27. Demo 1 therefore happens during shadow mode, and until then the frozen prototype stays the sales demo with its script skipping §6.3's lines (decision C4 applies "from Demo 1").
- **The contingent cuts**, taken in this order at a checkpoint that finds the rate short (about 9 sessions in all): (1) the termly retrospective mode and S21 wait for a school that needs them, if ACS commits to the weekly pack (question 70), about 2; (2) the `pastoral` import kind waits for the second school, since ACS has no form tutors, while its tables and scopes stay in B0 and are seeded directly for Wellesmere, about 1.5; (3) B1.18's performance test (the scan gate stays), 0.5; (4) the in-app notification centre shrinks to the escalation status line, about 1; (5) the SAR pack generator (B4.3) lands by T plus 4 weeks, a request before then answered by the runbook's manual procedure under a school-approved write grant, about 2.5; (6) restriction and objection (B4.5) likewise, about 1.
- **What cannot be cut:** anything on pass 4 §9.13's list; both synthetic tenants on every screen and every test; the engine's full scenario suite and the calibration script; the escalation with teacher referrals, the pin and the reporter's countdown; the Fable reviews on every `F` task; the anti-overwrite checks.

**Three checkpoints.**

| Checkpoint | Date | Must be true | If not |
|---|---|---|---|
| **CP1** | Friday 23 October 2026 (end of week 4) | the measured rate and rework over weeks 1 to 4; B0 done or within 4 sessions of done; the bake-off's decision record; counsel engaged; the "ask now" questions sent to ACS and questions 49, 149 and 165 answered or chased | below eight a week: take contingent cuts now; below six: tell ACS now that the target is August 2027 |
| **CP2** | Thursday 17 December 2026 (before the winter break) | B1 complete; B2's engine package green on S1 to S26; the pen test booked for the week of 15 February; counsel's answer on whether in-country Microsoft hosting is "sharing" under ADEK 7.1.3.a (counsel question C25); the DPA draft back from counsel; ACS's answers on the weekly pack and its delivery path (70, 73), the calendar (165), the IT review's content (36, 113) and the history export (83) | **the target moves to August 2027** |
| **CP3** | Monday 15 February 2027 (T minus 7 weeks) | the G-REAL scope code-complete: B0, B1, B2, B3's before-G-REAL tasks and B4's application tasks; production applied with the WAF in detection mode; the canary job running; the pen test starting; if C25 said hosting is "sharing", ADEK's consent requested by ACS and not refused | **the target moves to August 2027** |

**Every external gate, back-scheduled from T.** Latest dates; earlier is better. G-numbers are §8's.

| Gate | Latest date (T = 5 April 2027) | Why that date |
|---|---|---|
| ACS asked the "ask now" questions (49 consent letter, 149 ADEK 7.1.3.a with other vendors, 150 the ADEK request, 165 calendar, 30 and 31 DPA, 36 and 113 IT review, 70 and 73 weekly pack, 83 history, 19 and 141 Google app approval, 32 and 109 Records 1.5.1) | sent week 1 (2 October 2026); answers by 30 October 2026 | every later date depends on them; F04's decision says raise 7.1.3.a this term |
| G2 consent letter for the ACS name, naming third-party sales demos | 30 October 2026 | the synthetic ACS tenant is already shown |
| Counsel engaged; C25 (Microsoft hosting and 7.1.3.a) answered | engaged week 1; answer by 27 November 2026 (T minus 18 weeks) | if the answer is "sharing", ADEK's consent becomes a G-REAL gate and needs the rest of the calendar |
| **G26 ADEK consent** under Digital Policy 7.1.3.a | if C25 says hosting is sharing: request and evidence pack submitted by ACS by 15 January 2027 (T minus 11 weeks), consent by CP3 or the target moves; otherwise: before G-AI and before any EU cold copy, with the request drafted this term and submitted by ACS in 2026-27 | ADEK's response time is unknown (an assumption); eleven weeks is the most the April date allows |
| G7 DPA with the 7.1.3.a clause | CAROS draft to counsel 6 November 2026; counsel's comments 11 December; to ACS 8 January 2027; signed with annexes 12 March 2027 (T minus 3) | the DPO, the DPIA and the attestation all reference it |
| G6 DPO appointed | 8 January 2027 | named in the DPA sent to ACS |
| **G27 DPIA** drafted by CAROS, accepted by ACS as controller | draft 29 January 2027 (after CP2 fixes the engine's defaults); accepted 19 March 2027 (T minus 2) | PDPL Article 21; pass 4 §9.13 |
| G8 counsel's G-REAL answers (§14.8's G-REAL questions) | 12 February 2027 (T minus 7) | the notices and the DPA annexes rest on them |
| G9 notices approved and the attestation | drafted 12 February; approved 19 March; attestation at G-REAL | the status trigger refuses `shadow` without it |
| G12 penetration test | shortlist week 1; booked by CP2 (four to six weeks' lead time, an assumption); fieldwork from 15 February 2027 on production; report by 1 March; highs fixed and retested by 22 March (T minus 2) | pass 6 decision C5: production before it holds a real record |
| Production applied, WAF in detection | 12 February 2027; WAF in prevention by 29 March | the pen test needs the real WAF, TLS and network |
| G14 insurance | quote week 1; bound 19 March 2027 | insurers may ask for the pen-test report (an assumption) |
| G11 ACS IT review | its content known by 30 October 2026; the pack delivered 5 March 2027 (after the pen-test report); sign-off 26 March | the review needs the report |
| G13 tabletop | the week of 8 March 2027 | before G-REAL, after the runbooks exist |
| G4 and G10 Google | brand verification submitted week 1; ACS's Workspace configuration (Limited, under-18, the Directory scope, DNS proof) by 19 March 2027 | staff sign-in on the real tenant |
| G15 CPO route, category routes, helplines, test referral | route and helplines confirmed on the tenant (still `onboarding`, no student data) by 12 March; test referral acknowledged by 19 March | teacher referrals are live from the first day of shadow |
| G16 elicitation | the week of 25 January 2027, before Ramadan (which begins about 8 February 2027, an assumption; question 165 asks for Ramadan hours); fallback the fortnight after Eid al-Fitr | thresholds version 1 must exist at shadow week 2 |
| G17 weekly pack and its delivery path | agreed by CP2 | B4.9 builds the path by February |
| Restore drills in the drill subscription | 12 March 2027 | pass 4 §9.13 |
| Support-grant path exercised with the school | 26 March 2027, on the onboarding tenant | pass 4 §9.13 |
| G5 ZDR for the product's organisation | conversation opened week 1 | not on the April path; needed before G-AI |

**The fallback: August 2027.** If CP2 or CP3 fails, G-REAL moves to the first fortnight of the 2027-28 year. The build continues at its measured rate; the deferred B3 and B4 items move back before G-REAL, so G-LIVE needs no summer catch-up; shadow runs from September to about November 2027 in the Grade 12 application season, when counselors are busiest; G-LIVE falls in November or December 2027; B5 then reaches families for the 2028-29 cycle. The fallback costs a school year of live use and buys a complete morning at G-REAL. Davide tells ACS at the checkpoint, not later.

### 2.4 Branch and review discipline

The failure this repository has already suffered was a merge that replaced a file wholesale and a documentation set that described a build that no longer existed. The discipline below makes both structurally hard, and every rule is a machine check, not a request. **Decision C8:** while Davide is the only builder, every automated check stays and human review never blocks a merge (solo mode); the full process switches on when a second regular builder joins (§2.5).

**Repository shape.** Many small files, one concern each; no file over 800 lines except generated ones (a lint rule); generated files (`packages/db/migrations/meta/`, the tRPC types, the generated docs) are never hand-edited (a CI check regenerates and diffs).

**Trunk and branches.** One long-lived branch, `main`. Every change on a short-lived branch named `<stream>/<phase>.<task>-<slug>` (for example `a/b1.6-commit-chunks`), living days not weeks. Squash merge only, so `main` has one commit per pull request and a linear history. Agent sessions run in git worktrees, one session per worktree (`claude --worktree <name>` creates an isolated checkout, [Claude Code worktrees](https://code.claude.com/docs/en/common-workflows)); two sessions never share a working copy.

**The ruleset on `main`, solo mode** (GitHub rulesets, a Team-plan feature): require a pull request; require the `ci` checks listed in §5.8 to pass; require conversation resolution; require linear history; block force pushes; restrict deletions. **No required approvals**, because GitHub does not let an author approve their own pull request and a required approval with one person stops every merge ([about code owners](https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/about-code-owners); F46). The bypass list stays empty: nobody, including the organisation owner, skips the checks. The branch-protection option list is GitHub's ([about protected branches](https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches/about-protected-branches)).

**`CODEOWNERS`.** Present from B0, naming Davide on every path, so ownership is documented and the switch to the full process is a one-line change per path; not enforced by the ruleset in solo mode.

**Reviews in solo mode.** Three layers, none of which blocks on a part-time person. (1) **The different-model review**: every task marked `F` in its row gets a Fable 5.1 review session before merge; the session's brief names the model, attaches the diff and the acceptance list, and ends in a written verdict on the pull request; a "request changes" verdict blocks the merge until it is addressed or Davide overrules it in writing on the pull request. (2) **The required checks**, which are the real gate. (3) **Asynchronous human review with a service level**: a teammate reviews merged pull requests on `D`-stream paths within seven days when one is available; findings become new tasks, never reverts, and the review log in `docs/PLAN.md` shows what is unreviewed. Rule ledgers are kept for every ported screen (§6.1).

**The wholesale-replacement check.** A CI job (`tooling/ci/wholesale.ts`) computes, for every file changed in the pull request, the share of its lines that are deleted and re-added. A file over 200 lines whose diff replaces more than 60% of it fails the job unless the pull request carries the label `wholesale-rewrite` and a one-line reason in its description. The label is applied by a human, never by the agent (a hook refuses `gh pr edit --add-label wholesale-rewrite` from an agent session). This is the check that would have caught commits `0daf069` and `c045a6f`.

**Conflicts.** An agent never resolves a merge conflict by taking one side of a file wholesale; `CLAUDE.md` says so and the wholesale check enforces it. A conflicting branch is rebased in a session whose only task is the rebase, with the conflict list in the brief.

**Migrations.** One migration per pull request; `drizzle-kit generate` output reviewed as SQL; hand-written policy SQL in the same numbered sequence; `drizzle-kit check` in CI; a merged migration is immutable (a hook refuses edits to any file under `packages/db/migrations/` that exists on `main`; a correction is a new migration); expand-and-contract with the drop two releases later (pass 1 DR-9); `drizzle-kit push` refused by a hook unless `DATABASE_URL` points at `localhost`. Every migration touching `auth.*` or `audit.*` has a Fable review (pass 4 §4.2 under decision C8).

**The pull request template** carries: the task id from `docs/PLAN.md`, the model and effort used, the reviewer model and verdict, the tests added, the acceptance line it satisfies, and "docs touched" with the `PLAN.md` line.

**Secrets and data.** No secret in the repository (gitleaks in CI, push protection on). No real tenant data anywhere in the repository, in CI or in a session (F23): the `no-real-data` test covers seeds, fixtures and eval cases with every real person's name on its blocklist; CI runs on GitHub-hosted runners, which GitHub hosts "on virtual machines in Microsoft Azure" with no region choice ([about GitHub-hosted runners](https://docs.github.com/en/actions/using-github-hosted-runners/using-github-hosted-runners/about-github-hosted-runners)), so CI holds nothing that is not synthetic and never a production credential; the canary, the backtest and the restore-drill checks run as jobs in UAE North (§5.8); no machine that runs an agent holds a production credential or a signed-in production browser profile; the Read and Bash hooks refuse paths outside the repository and `az`, `psql` or `kubectl`-style commands against production (§3.4); no agent session ever holds a support grant (`CLAUDE.md`, and the support console rejects an automation user agent); build sessions run under the ZDR organisation (C7), so that if real data ever did reach one, no Covered Model would retain it.

**Releases.** A release is a tag `vYYYY.MM.DD-n` on `main` after a staging soak (§7.1); the changelog is generated from the squash commits' pull-request titles, so the title is the changelog line.

### 2.5 Davide alone, and when that changes

**Now.** The order is B0, B1, B2, B3 before G-REAL, B4, strictly in sequence, with two exceptions: the engine package (B2.1 to B2.9) may start as soon as B0.10's contracts exist, interleaved with B1, because it needs no database; and the external gates run in parallel with everything, on the dates of §2.3. After G-REAL: B3 after G-REAL (the case file first, for the first reveal session), B2.15's what-if, B4.15, B4.7 and B4.11, then B5, B6, B8 and B7 as the gates allow, then B9. The plan file, the contracts, the briefs and the tests keep their value because they are what lets one person put work down and pick it up, and what lets a second person join without a rewrite.

**The trigger for the full process** (decision C8). A **second regular builder** exists when a person other than Davide authors pull requests merged into `main` in at least three of any four consecutive weeks, or commits in writing to at least two days a week for a whole phase. When that happens, in one pull request with a decision record: the four streams are staffed and `PLAN.md`'s owner column changes; `CODEOWNERS` names two owners on every path and the ruleset requires code-owner review; two approvals are required on `packages/contracts/**`, migrations touching `auth.*` or `audit.*`, and `packages/ai/prompts/_constitution/**`; human review gets a 24-hour service level on the critical path; the on-call rota becomes two people (§7.5). A second builder takes stream A first (B5 and B6's domain work, then the connectors), because A's work has the cleanest seam (§2.2) and moves C's modules forward in 2027-28.

---
## 3. Working with a coding agent across many sessions

### 3.1 Session-sized tasks

A session is one task from `docs/PLAN.md`, two to four hours, one worktree, one branch, one pull request. The task tables in §1 are the first cut; `PLAN.md` carries the live list and each task's **brief**. A brief is short and answers five questions, in this order, because the agent reads it before anything else:

```
### B3.4 · Escalation: raise, route, reference, outbox rows
Stream: D · Model: Opus 5.5, max (ZDR organisation) · Reviewer: Fable 5.1 (diff only) · Attempts: 0 · Status: ready
Reads first: docs/spec/04-security-privacy-compliance.md §5.1 to §5.3 and §5.8; docs/decisions/DR-14-escalation.md;
             packages/contracts/audit/ESCALATED_SAFEGUARDING.json; packages/domain/signals/README.md
Produces:    packages/domain/signals/escalation.ts (raise, teacher referral, update, link),
             packages/api/signals/escalation.ts, migrations/0011_b3_escalation.sql (D45 columns, D46,
             the pin trigger, auth.esc_covers()), packages/domain/signals/test/escalation.spec.ts
Done when:   one open escalation per case (the partial unique index) and a repeated idempotency key is a no-op;
             a new concern on an escalated student is recorded as an update or a linked escalation (S25);
             the engine cannot lower or close the pinned case (S24); a teacher's safeguarding flag raises a
             referral and opens a case if none is open (C23); the form refuses observed < 40 and why_now < 20
             characters; step-up within 10 minutes is required; one transaction writes the escalation, the case
             move to urgent/act, notify.enqueue() rows per order-1 recipient, ESCALATED_SAFEGUARDING with
             identifiers only, and case.escalated; the reporter's deadline is set from the regulator profile;
             names never appear in audit.entry or in any email (test); the Wellesmere tenant shows its own
             safeguarding title and no "ADEK" string (test)
Out of scope: reminders and the 24-hour banner (B3.5); the acknowledgement screen (B3.6); the templates (B3.8)
Updates last: PLAN.md status and attempts; docs/decisions if a decision was made; the PR template fields
```

Rules for briefs: a task that cannot be described in this shape is two tasks; "done when" lists checks, never adjectives; every task names the spec sections it implements so that the pass outputs stay the specification and the code stays the truth; the brief is written by the stream owner before the session, and a session that finds the brief wrong stops and rewrites the brief rather than the scope.

**The attempt counter.** `Attempts` counts sessions that ended with the task not done (acceptance still red, or the reviewer rejected). At two, the task is re-planned: the owner reads both sessions' closing notes, narrows or splits the task, and the third attempt runs on Opus 5.5 at max effort with both notes attached and a Fable 5.1 review of the two failed diffs in its brief (decision C7: Fable reviews diffs and writes no task, unless the bake-off moved that task's category back to it, §4.2). At four, the task goes to a person without an agent for a day, because the problem is usually the specification.

### 3.2 `docs/PLAN.md`

The plan file is the one document every session reads after `CLAUDE.md` and updates before it ends. It is short at the top and long at the bottom.

```
# CAROS backend · plan
Updated: 2026-11-03 by <person> in <PR>.   Current phase: B1.   Next milestone: CP2 (2026-12-17), then G-REAL (T).

## Now
- B1.6 commit chunks (in progress, PR #142; Fable review pending)
- B2.4 suppression (ready; the engine package interleaves with B1, §2.5)

## Blocked
- B4.9 SFTP: waiting on the portal check of Blob SFTP in UAE North (Davide, by 2026-11-10)
- B1.15 Directory read: waiting on ACS IT's approval of the Directory scope (question 110)

## Gates
- G-CI passed 2026-10-12 (commit …)
- CP1 passed <date> (measured rate <n> a week, rework <n>%) · CP2: 2026-12-17 · CP3: 2027-02-15
- G-REAL: not yet (target T, 2027-04-05; §9.13 items open, each with its latest date) · G-DEMO: during shadow · G-LIVE: not yet · G-AI: not yet

## External gates (§2.3's back-schedule: gate, latest date, owner, status, reference)
...
## Week-one items (B0.15)
| item | owner | date | reference |
...

## Phases
### B0 · Foundations · complete 2026-10-12 (commit …)
### B1 · Facts and identity · in progress
#### Tasks
| # | task | owner | model | rev | attempts | status | PR |
...
#### Briefs
### B3.4 · …   (the brief format of §3.1)
...
## Decisions log (one line each, newest first, linking docs/decisions/)
## Session notes (one line per session: date, task, outcome, what the next session should know)
```

Three rules. The **Now** list never holds more than two tasks (in solo mode one person cannot supervise more); a third "in progress" line means a review has not been done. A task's line changes only in the pull request that does the work, so the plan and the code move together and a reviewer sees both. The **Session notes** line is the hand-off between sessions and is written even when the session failed, because the next attempt reads it first.

### 3.3 `CLAUDE.md` for the backend repository

Written by B0.14 and rewritten from the build at the end of every phase (§3.6). Under 200 lines, because Claude Code loads it into every session and its own guidance is to keep the file short and specific ("target under 200 lines per CLAUDE.md file", [How Claude remembers your project](https://code.claude.com/docs/en/memory)). No line numbers, because they drift. The draft:

```
# CAROS backend: working instructions

This is the production backend and Next.js app for CAROS (Counselor OS). The prototype in the
EdTech repository (index.html) is frozen and is the behavioural specification; nothing is ported
from it without first writing down the rule it encodes (docs/screens/<screen>.md).

This file describes the build as it stands after phase <N> (<date>). It is rewritten from the
code at the end of every phase. If a name here does not exist in the code, the code is right and
this file is stale: fix the file in the same PR. `pnpm docs:verify` checks every identifier here.

## Read first, update last
1. Read docs/PLAN.md. Find your task under "Now". Read its brief. Do only that task.
2. Read the spec sections the brief names (docs/spec/ holds frozen copies of the plan passes).
3. Before ending: update the task line and attempts in docs/PLAN.md, add a session note, and
   fill the PR template. If you decided something, add a docs/decisions/ file.

## The shape (pass 1 DR-2, DR-3)
- pnpm workspace, Turborepo. apps/web (Next.js App Router) and apps/worker (pg-boss) both call
  packages/domain. packages/contracts is the only package everyone may import.
- Every mutation: tRPC procedure → domain function → withTenant() transaction → audit.record()
  and events.emit() in the same transaction. Server Actions are not used for data. There is no
  way to run a query outside withTenant(); do not look for one.
- Every domain function starts with assertAllowed(class, action, subjects). A query before it
  fails the build (dependency-cruiser).
- packages/engine is pure: no I/O, no database, no Date.now(), no Math.random(). It exports
  evaluateStudent(input): EvaluationResult. The domain layer loads and writes.
- Derived values are views or functions, never columns. The forbidden-column test lists them.
- Migrations: drizzle-kit generate; hand-written SQL for policies; one migration per PR;
  a merged migration is never edited; drizzle-kit push is refused outside localhost.

## Invariants (CONTEXT §4, quoted verbatim; the mechanism that enforces each is beneath it)
1. "The five triage tiers and their order. urgent (route now under school policy) → checkin
   (within 2 school days) → review (this week) → monitor (no action required) → good (positive
   change, acknowledge). Never collapsed, never reordered, never re-themed or renamed per school."
   Mechanism: the tier enum and PRIO order are platform seeds no school row can change.
2. "Signals are personal, never comparative. Deviation is measured against the student's own
   baseline band. Never rank students against each other, anywhere, including in AI answers."
   Mechanism: the engine's strict no-comparison property; lists order by tier then time raised
   only (the ordering test); the co-pilot's fixed ranking refusal.
3. "The trust spine: rules decide, the model writes. Deterministic tables own entry requirements,
   prerequisites, fee status, tuition, deadlines and reach/match/safety classification. The model
   only interprets, names, phrases and converses. Nothing that gates a decision or a submission
   is model output. Every number a user sees traces to a source."
   Mechanism: rule sets in config.rule_set_version; the validator; no seed:// number renders;
   the safety screen is a lexicon; model tags are a floor-plus, never a gate.
4. "Provenance over authority. Every signal shows the evidence that fed it. A past alert must
   remain explainable after the rules or thresholds that produced it have changed."
   Mechanism: evidence snapshots, rule-set versions and vocabulary snapshots on every signal.
5. "Safeguarding escalation is a real, consequential, logged act. It routes to a named Child
   Protection Officer (ACS's term; British schools say Designated Safeguarding Lead, and the
   term is a per-school field). It requires a reason, cannot double-fire, and is audited."
   Mechanism: one open escalation per case plus an idempotency key; the pin; audit with
   identifiers only; the email names a role and nothing about the child.
6. "The conversation is a gate. An IB subject selection cannot be approved until a dated,
   attributed record of the review meeting exists."
   Mechanism: the ib.check_approval trigger (student, kind, held after the current version,
   notes, review_meeting_version) and the domain guard.
7. "Two populations, never totalled. The counseling caseload (one counselor's students across
   Grades 9 to 12) and the Diploma cohort (the school's IB students across Grades 10 to 12)
   answer different questions. No query, report or screen adds them together."
   Mechanism: the union test; indicators counted over one population each.
8. "Supervision capacity warns and never blocks. An Extended Essay assignment over the school's
   guide is allowed and records that it was over."
   Mechanism: ee_stamp_capacity.
9. "No fabrication. No invented testimonials, customers, outcomes, benchmarks, pricing or press.
   While a real school's name is applied to synthetic data, a visible demonstration-data marker
   is a shipping requirement."
   Mechanism: the marker is a generated column bound to the letterhead; no-real-data blocklist.
10. "Gamification exists on the student side only. Counselor, parent, teacher and mentor
    surfaces are ungamified."
    Mechanism: only the discovery module writes the XP ledger, in its own student-only class.
11. "Mentor contact is in-platform only, for safeguarding. The word match is reserved for
    reach/match/safety and is never used for mentor pairing."
    Mechanism: no contact columns in mentor.*; in-person sessions on premises only in v1; the
    copy lint for "match".

## Tenancy and data
- Every tenant table has school_id first and composite foreign keys; RLS with FORCE on all of
  them; the tenant policy is RESTRICTIVE; a query with no context sees nothing.
- Two synthetic tenants, ACS (marker on) and Wellesmere (marker off, British, no IB), plus the
  CI-only wellesmere-shifted (another timezone and weekend). A screen is not done until it passes
  on both. Never write "Grade", a year number, "Child Protection Officer", "ADEK", a regulator's
  clause number or any ACS name as a literal; read them from the tenant and its regulator profile.
- Real tenant data never enters this repository, a test, a fixture, a log line or a session.
  You never hold a support grant or a production credential; hooks refuse reads outside the
  repository and az or psql against production. If you see a real name where a synthetic one
  should be, stop, and say so in the PR.
- Nothing personal leaves the UAE except through packages/ai's gateway, pseudonymised, to a
  model on the registry with covered_model = false and zdr_eligible = true, inference_geo "us".

## How to work
- One task per session, in a worktree (claude --worktree <task>), one PR. Never merge; never
  force-push; never resolve a conflict by taking one side of a file. Never edit generated files.
- Tests are the definition of done. Add the test the brief names before the code it tests.
- Prefer the spec's names for tables, columns, jobs, audit actions and events. Do not invent
  parallel vocabulary. If the spec is wrong, say so in the PR and the decisions log; do not
  silently diverge.
- Copy in this repository is written to be read aloud: no em dashes, short sentences.
- Do not add a dependency without the cooldown (pnpm minimumReleaseAge) and a line in the PR.

## Commands
- pnpm ci            everything CI runs, locally (typecheck, lint, boundaries, unit, db, e2e)
- pnpm db:migrate    apply migrations to DATABASE_URL (localhost only from a session)
- pnpm seed --tenant acs|wellesmere --anchor <date>
- pnpm test --filter engine        the scenario and property suites (under a second)
- pnpm test --filter db            the RLS, authz, registry and convention suites
- pnpm docs:verify                 every identifier in CLAUDE.md and docs/ resolves
- pnpm dev                         web + worker + Postgres + Azurite + Mailpit via Compose

## Where things are
- docs/PLAN.md (the plan), docs/decisions/ (DR-01…), docs/spec/ (frozen plan passes and
  CONTEXT.md, PRODUCT.md), docs/screens/ (rule ledgers per ported screen), docs/runbooks/,
  docs/generated/ (schema, API, audit actions, events; never hand-edited).
- packages/db/migrations (SQL), packages/db/src/schema (Drizzle), packages/engine/test/scenarios
  (S1…S23), packages/seed/fixtures/<tenant>/<pack>, packages/ai/prompts/<feature>/<semver>.md.

## Models and reviews (docs/PLAN.md carries the per-task line)
- Opus 5.5, under the zero-data-retention organisation, writes every task (decision C7), at the
  effort on the task line: max for RLS and auth.allowed(), safeguarding routing, engine
  statistics and tiering rows, irreversible migrations, the pseudonymiser and validator.
- Fable 5.1 reviews diffs only (code and synthetic data), on every task marked F, and never
  sees tenant data. The B0 bake-off record says if any category moved back to Fable.
```

Two things about this file. It repeats nothing that `docs/generated/` can produce (the table list, the API, the audit actions), because a hand-maintained inventory is the first thing to drift. And the paragraph about the demonstration data and real data is in the invariants, not in a footnote, because the prototype's own `CLAUDE.md` learned that lesson.

### 3.4 `.claude/rules/` and hooks

Claude Code loads `.claude/rules/*.md` alongside `CLAUDE.md` and scopes a rule to files by a `paths` frontmatter ([memory docs](https://code.claude.com/docs/en/memory)), so the long, path-specific guidance lives there and does not cost every session its context:

| Rule file | `paths` | Carries |
|---|---|---|
| `db.md` | `packages/db/**` | migration naming, the immutability rule, the policy pattern (`auth.protect()` call per table), the registry entry, the conventions test list, expand-and-contract |
| `engine.md` | `packages/engine/**` | purity, determinism, the scenario file format, the changelog rule, "a template binds numbers, dates and labels only", the no-`fairness`-import rule |
| `domain.md` | `packages/domain/**` | `assertAllowed` first, the audit action and event per function, one transaction, the state-machine tables of pass 1 DR-7 as the source of transition names |
| `ingest.md` | `packages/ingest/**` | the closed transform set, no tenant slug in code, rosters are permissions, adapters never import the database |
| `ai.md` | `packages/ai/**`, `packages/contracts/ai/**` | the eleven steps, the envelope rules, the prompt PR rule (version, changelog, eval run), no tenant string in a schema, the model registry is a migration |
| `web.md` | `apps/web/**`, `packages/ui/**` | rules live in the domain, never in a component; the rule ledger before a port; the marker and letterhead; both tenants; 375px; no severity by colour alone; the AI label from the generation row |
| `notify.md` | `packages/notify/**` | allowlisted variables, identifiers only, the content test per template |
| `copy.md` | `**/*.tsx`, `**/*.md` | the writing rules from `PRODUCT.md` and `DESIGN.md`: register per audience, no eyebrow above a heading, no em dashes |

Hooks (`.claude/settings.json`), because "Claude treats them as context, not enforced configuration. To block an action regardless of what Claude decides, use a PreToolUse hook" ([memory docs](https://code.claude.com/docs/en/memory); hook events and the blocking exit code are in [Hooks](https://code.claude.com/docs/en/hooks)):

| Event | Hook | Blocks or does |
|---|---|---|
| `PreToolUse` on `Bash` | `hooks/guard-git.sh` | exits 2 on `git push --force`, `git push -f`, `git merge` into `main`, `git rebase` of `main`, `gh pr merge`, `gh pr edit --add-label wholesale-rewrite`, `drizzle-kit push` unless `DATABASE_URL` matches `localhost`, `terraform apply` outside CI |
| `PreToolUse` on `Bash` | `hooks/guard-prod.sh` | exits 2 on `az`, `psql`, `pg_dump` or any database client whose target is not `localhost` or the CI service container, and on any command naming the production subscription, server or Key Vault (F23) |
| `PreToolUse` on `Read`, `Grep` and `Glob` | `hooks/guard-paths.sh` | exits 2 on any path outside the repository's worktree, so a session cannot read a downloaded export, a support-console file or a browser profile (F23) |
| `PreToolUse` on `Edit` and `Write` | `hooks/guard-files.sh` | exits 2 on a path under `packages/db/migrations/` that exists on `main`, on `docs/generated/**`, on `packages/db/migrations/meta/**`, and on any write whose content contains a string from the real-names blocklist |
| `SessionStart` | `hooks/session-start.sh` | prints the "Now" block of `docs/PLAN.md` and the brief of the task the worktree branch name points at |
| `Stop` | `hooks/session-end.sh` | if the worktree has changes and `docs/PLAN.md` is untouched, prints a reminder that the task line and session note are missing (it cannot block, and should not) |
| `PostToolUse` on `Bash` matching `pnpm test` | `hooks/record-test.sh` | appends the test summary to the session note draft, so the closing note is written from evidence |

### 3.5 What every session reads first and updates last

Reads first, in order: `CLAUDE.md` (loaded automatically), the `Now` block and the task's brief in `docs/PLAN.md` (printed by the hook), the spec sections the brief names under `docs/spec/`, the module's `README.md` in `packages/domain/<module>/` (one screen of text: the tables it owns, its public functions, its audit actions), and the rule ledger `docs/screens/<screen>.md` when the task ports a screen. Nothing else is read up front; the agent reads code as it needs it.

Updates last, in the same pull request: the task line in `docs/PLAN.md` (status, attempts, PR), one session note, a decision record if a decision was made, the module `README.md` if a public function was added or changed, the rule ledger if a screen rule was implemented, and the PR template's five fields. A pull request whose diff touches `packages/` and not `docs/PLAN.md` fails a CI check (`tooling/ci/plan-touched.ts`) unless labelled `chore`.

### 3.6 Keeping the documentation from drifting

The prototype repository's own `CLAUDE.md` records that its documentation twice described a build that no longer existed, and that reading it as truth cost real work. Five mechanisms, all of them checks:

1. **Generate what can be generated.** `docs/generated/schema.md` from the Drizzle schema plus `privacy.table_registry` (table, class, subject column, retention class, the `auth.protect()` arguments); `docs/generated/api.md` from the tRPC routers (procedure, input schema, the domain function it calls); `docs/generated/audit-actions.md` and `events.md` from `audit.action` and `events.name`; `docs/generated/runbooks.md` from the directory; `docs/generated/jobs.md` from the worker's queue registrations. CI regenerates and fails on a diff, so a hand edit of a generated file cannot land.
2. **Verify every named identifier.** `pnpm docs:verify` (`tooling/docs/verify-claude-md.ts`) extracts every backticked identifier from `CLAUDE.md`, `docs/PLAN.md`, `.claude/rules/*.md`, `docs/decisions/*.md` and `packages/*/README.md`, and checks that a path exists, an export exists (through the TypeScript program), a table or column exists (through the schema snapshot), a job name is registered, or an audit action or event key is seeded. An identifier that does not resolve fails CI unless it is followed by `(planned)`; a `(planned)` marker older than one phase fails too. This is the check the prototype repository did not have.
3. **Rewrite `CLAUDE.md` from the build at the end of every phase.** The last task of each phase is a documentation session whose brief is "describe the build as it stands; delete every sentence that describes intention". The prototype's P8 did exactly this and its `CLAUDE.md` became an authority again.
4. **Specification stays frozen and labelled.** `docs/spec/` holds the pass outputs with their commit hash and a header saying they describe a design, not a build; the rule ledgers and decision records are where the design and the build are reconciled, and the code wins.
5. **No line numbers in prose.** A rule ledger cites the prototype by function name (`vCase`, `selCheck`) and the build by file path and export, never by line; a lint on `docs/` fails on `:\d+` after a file name.

---
## 4. The model and effort for each phase

**Decision C7 (2026-09-24).** Opus 5.5 under the zero-data-retention API organisation writes every task, including those the first version reserved for Fable 5.1; Fable 5.1 reviews diffs only (code and synthetic data, never tenant data), keeping the different-model review rule; B0's bake-off (B0.16) decides whether any category of work moves back to Fable as writer. The reason is F23: if real data ever reached a build session despite the controls of §2.4, no Covered Model would retain it. **This section is generated from the task tables of §1** (F38): `tooling/plan/models.ts` reads the Model and Rev columns of `docs/PLAN.md` and writes §4.1 and §4.3, and CI fails when they differ, so the tables and this section cannot disagree again.

### 4.1 The table

| Phase | Writer and effort | Why | Reviewed by Fable 5.1 |
|---|---|---|---|
| B0 Foundations | Opus 5.5 high; **max** for B0.5 to B0.8 (roles and spine, `withTenant()` and the migrator, `auth.allowed()` and `auth.protect()`, the harness); the bake-off B0.16 on both models | scaffolding is Opus 5.5's ground; the tenancy layer is written once and every later line trusts it | B0.4, B0.5 to B0.9, B0.12, B0.13; B0.16 cross-reviewed |
| B1 Facts and identity | Opus 5.5 high; **max** for B1.4, B1.13 to B1.16 (the ladder, sign-in, sessions and step-up, the Directory read, the support console) | the pipeline is large and specified; the ladder, sign-in, the Directory read and the grant path decide who a person is and who may look | B1.1, B1.4, B1.5, B1.6 (the security section, formerly a second Opus session), B1.8, B1.13 to B1.17 |
| B2 Engine and sweep | Opus 5.5 **max** for the engine (B2.1 to B2.6); high for templates, schemas, calibration, loader, writer, jobs, watchdogs and thresholds | the statistics fail silently; the orchestration fails loudly | every task from B2.1 to B2.13, and B2.15, B2.16 |
| B3 The counselor's morning | Opus 5.5 high; **max** for cover (B3.3), the escalation (B3.4, B3.5) and the renderer (B3.8) | a port of a rendered specification; the escalation is the one consequential act | B3.1 to B3.8, B3.11 |
| B4 Pilot readiness | Opus 5.5 high; **max** for the status gate (B4.2), erasure (B4.4), offboarding (B4.7) and authorization findings from the pen test | procedure and tooling, with irreversible pieces | B4.2 to B4.8, B4.12, B4.14, B4.15 |
| B5 The application season | Opus 5.5 high; **max** for classification and fee status (B5.3), parent identity (B5.10) and the safety screen (B5.12) | a broad port; three pieces decide what a family reads, which adult sees which child, and whether a child's words reach an adult | B5.1, B5.2, B5.3, B5.7, B5.10, B5.11, B5.12 |
| B6 The Diploma | Opus 5.5 high; **max** for the approval gate (B6.3) and the capacity stamp (B6.10) | the most literally specified module; `selCheck` is code today | B6.1, B6.2, B6.3, B6.8 to B6.11, B6.16 |
| B7 AI | Opus 5.5 **max** for the pseudonymiser, validator, gateway, reader and plan compiler (B7.2, B7.3, B7.4, B7.8, B7.13); high elsewhere | the mechanisms that keep a child's name in the country and a sentence honest | B7.1 to B7.6, B7.8, B7.10, B7.12 to B7.16, B7.18; evals judged by a model other than the generator (pass 5 §10.1) |
| B8 Discovery, mentors, reporting | Opus 5.5 high; **max** for mentor credentials and pairings (B8.8, B8.9) | a port, plus the one path where an outside adult reaches a child | B8.1, B8.4 to B8.12 |
| B9 Connectors, second school, scale | Opus 5.5 high; **max** for the isolation rehearsal (B9.7) | I/O against documented APIs | B9.1 to B9.4, B9.7, B9.8 |

Effort settings, stated once because Opus 5.5's API default is `medium` (pass 5 §9.1) and a session that inherits it does less than the task needs: **high** for every build task unless its row says otherwise; **max** for every task marked `max` in its row, which is every task the first version reserved for Fable 5.1; **medium** for documentation, accessibility passes, scaffolding and rehearsals; **max** for every Fable 5.1 review session. The first version's `xhigh` tier is withdrawn: its rows said `high` and the tables are the source (F38). No task runs at `low`; research sub-agents inside a session may.

### 4.2 The review rule and the bake-off

**Writers.** Every task is written by Opus 5.5 under the ZDR organisation. The effort is the row's. There is no reserved list of Fable-written tasks, unless the bake-off creates one.

**The bake-off (B0.16).** The first version's most demanding reserved task, the `auth.allowed()` rewrite of F01, is given as one brief to both models. Each works in its own worktree on synthetic data only, Opus 5.5 under the ZDR organisation and Fable 5.1 under its own account. Each then reviews the other's diff without being told which model wrote it. Davide judges by category: whether the acceptance tests pass (the deciding test), then the defects each review found (tier and verb logic, policy-expression safety, qualifier test coverage, SQL clarity). The default for every category is Opus 5.5. A category moves back to Fable 5.1 as writer only where Fable is clearly better: it passes acceptance tests Opus fails, or its diff lacks a class of defect that review finds in Opus's. The verdict is a decision record; any category that moves back runs under §2.4's data controls, because Fable 5.1 keeps prompts for thirty days. The categories are those the first version reserved: authorization and row-level security; safeguarding routing; the engine's statistics and tiering rows; hard-to-reverse schema changes; identity and credentials; the AI trust mechanisms.

**Reviews.** Fable 5.1 reviews every task whose Rev column says `F`. That includes every `max` task, every migration that touches `auth.*` or `audit.*`, every piece that implements an invariant or a gate, and every silent-error task F38 lists. The same rule applies to any later pull request that changes `auth.allowed()`, `auth.protect()`, a `SECURITY DEFINER` helper, a policy, the safeguarding route handling, a template, an engine default, a detector, the combination table or hysteresis. It also applies to any migration that drops or renames a column.

**Failures.** After two failed attempts, the third runs on Opus 5.5 at max effort, with both closing notes and a Fable review of the two failed diffs in its brief (§3.1).

The rule is written into `docs/PLAN.md`'s task lines and the pull request template, so a task run on the wrong model, or merged without its review, is visible at review.

### 4.3 Review by a different model

Generated from the Rev columns; 98 tasks carry a Fable 5.1 review, about 50 of them before G-REAL:

B0.4, B0.5, B0.6, B0.7, B0.8, B0.9, B0.12, B0.13, B0.16 (cross); B1.1, B1.4, B1.5, B1.6, B1.8, B1.13, B1.14, B1.15, B1.16, B1.17; B2.1 to B2.13, B2.15, B2.16; B3.1 to B3.8, B3.11; B4.2 to B4.8, B4.12, B4.14, B4.15; B5.1, B5.2, B5.3, B5.7, B5.10, B5.11, B5.12; B6.1, B6.2, B6.3, B6.8, B6.9, B6.10, B6.11, B6.16; B7.1 to B7.6, B7.8, B7.10, B7.12 to B7.16, B7.18; B8.1, B8.4 to B8.12; B9.1 to B9.4, B9.7, B9.8.

Every silent-error task pass 7's F38 names is on this list (B2.9, B2.13, B2.8, B2.15, B4.15, B4.6, B7.5, B7.16, B7.18, B8.12, B0.12, B0.13, B1.5, B1.17, B3.7, B5.11, B6.8, B6.11, B8.7, B8.11), and B1.6's security section is reviewed by Fable 5.1 rather than a second Opus session, which was the same model. A review session's brief names the model, attaches the diff and the acceptance list, and ends in a written verdict on the pull request; the writer's model never approves its own work. Pass 3's requirement that the engine be reviewed by a model other than the one that wrote it is B2's Rev column; pass 7's adversarial review by Opus 5.5 applied to this plan itself.

### 4.4 What the agent costs, and what limits it

**The binding limits are usage, not money** (F39). Build sessions on Opus 5.5 run through the ZDR API organisation, whose rate-limit tier and monthly spend limit are raised with Anthropic in week one; Fable 5.1 review sessions run under a separate account whose weekly usage limit, reached fastest by max-effort sessions, caps the reviews. §2.3 plans on eight build sessions and four reviews a week and measures both from week one; a week that hits a limit is recorded in `PLAN.md`, because it moves the checkpoints. (Pass 7's review was itself interrupted twice by a session limit.)

**The money, for completeness.** Pass 5 verified Opus 5.5 at $4 input and $20 output per million tokens with cache reads at $0.20, and Sonnet 5 at $2 and $10 (pass 5 §11.2, [pricing](https://platform.claude.com/docs/en/about-claude/pricing)); pass 7 confirmed Fable 5.1 at $10 and $50 on the same page. An order-of-magnitude estimate, easily wrong by a factor of three: about 330 build sessions to the end of B9 (the sum of §1's estimates with the rework allowance), each reading a few million tokens mostly from cache and writing a few hundred thousand including thinking, plus about 100 review sessions, puts the agent's bill in the low thousands of dollars for the whole plan, well under the penetration test, the DPO and the insurance. CONTEXT §2 says budget is not the binding constraint, and it is not.

---

## 5. Tests

### 5.1 The suites, where they live and when they run

| Suite | Package and path | Proves | Runs |
|---|---|---|---|
| RLS | `packages/db/test/rls/` | tenant isolation for every tier role and every tenant table, fail-closed, `FORCE`, the eight "see nothing" cases and pass 4 D61's additions; the deployable roles (the web identity cannot become system, support or owner; `NOINHERIT`); both tenants | every PR |
| Authz matrix | `packages/db/test/authz/`, `narrowing/`, `jobs/`, `purpose/` (generated) | every (role or capability, class, action, scope, row kind) cell with a positive and a negative fixture; every qualifier of pass 1 §2.4a with its negative fixture; every verb positively and negatively through RLS; column cases; a flagged fixture per role; narrowing (pass 4 §3.1); support by operator and table; cross-school jobs; every report keyed on `purpose` | every PR |
| Registry and conventions | `packages/db/test/schema/` | every tenant table registered, protected, composite-keyed, timestamped; no forbidden derived column; no display strings; the policy-presence check; `drizzle-kit check` | every PR |
| Engine scenarios and properties | `packages/engine/test/` | S1 to S26 and Wellesmere's W1 to W8; determinism, monotonicity, suppression never raises, strict no comparison, hysteresis, the escalation pin, within-tier order, vocabulary snapshots; golden files; no `fairness` or database import; no regulator name in a template | every PR (under a second) |
| Seed reproduction | `packages/seed/test/engine-reproduces-seed.spec.ts` | the engine over the fixture facts, history included, equals the lists the rules generate, for ACS, Wellesmere and `wellesmere-shifted` | every PR touching `engine`, `ingest`, `seed` or `signals` |
| Fixtures and profiles | `packages/ingest/test/fixtures.spec.ts`, `profiles/<tenant>/<kind>.spec.ts`, `roster-security.spec.ts`, transform property tests, parser fuzz | both export shapes through the real pipeline against `expected.json`; rosters as a security property | every PR |
| Domain contracts | `packages/domain/*/test/` | every mutation writes its audit action and event; state-machine transitions and guards; the loader's inputs validate; the writer replays as an upsert; caseload and cohort never union; only discovery writes XP | every PR |
| API structure | `packages/api/test/structure.spec.ts` | every procedure validates with a `contracts` schema and calls exactly one domain function with no other logic (a ts-morph structural test); no `'use server'` outside the allow-list | every PR |
| Notify | `packages/notify/test/templates.spec.ts` | every template of pass 4 §5.8 against its content rule (no names, no child, no topic; roles only); undeclared variables refused; outbox dedupe and send-time idempotency | every PR |
| Safety | `packages/safety/test/` | the lexicon corpus with Arabic script; every hit reaches a person; no model is called; routing by category; one open alert per student; the "alert unopened" notice; the per-role flagged fixture; teacher-flag and urgent-request rules; the helplines never before the CPO's confirmation | every PR |
| AI mechanisms | `packages/ai/test/{leak-scanner, refusal, validator, injection, copilot-scope, schema-hygiene, envelope-allowlist, provider-surface, ai-tier-allowlist, extraction-consumer, pseudonymiser-recall}` | pass 5 §10.4's hard zeros and the recall bar | every PR touching `packages/ai`, `packages/contracts/ai` or a prompt |
| AI evals | `packages/ai/evals/<feature>/` with the runner | the suites of pass 5 §10.2 and the profiles of §10.3 | deterministic tier every PR; judged tier nightly on `main`; full run for a promotion |
| Web | `apps/web/test/` (Playwright, axe) | every route-state renders on both tenants; the demo flows; the Wellesmere checklist assertions, including no "ADEK" on any Wellesmere screen, template or email; the ordering test; the copy lint for "match"; 375px; the AI label present | every PR (smoke) and nightly (full) |
| Logs and data hygiene | `packages/web/test/log-scanner`, `packages/seed/test/no-real-data` | no seed name or email in captured logs; no real person's name (the blocklist covers every real name in `PRODUCT.md` and the prototype, F29) and no real student name in seeds, fixtures or eval cases | every PR |
| Sweep smoke | `apps/worker/test/sweep-smoke.spec.ts` | §5.7 | every PR (fake clock) and nightly on staging (real clock) |
| Production checks | the canary job in UAE North, the drift check, the `tier_yesterday` assertion, the chain verification, the drill log check | the running system still holds the properties the suites proved; none runs on a GitHub-hosted runner | nightly, weekly, quarterly, per deploy (§7.4, §7.6) |

### 5.2 Signal engine unit tests

The adversarial synthetic set is pass 3 §15.1 as revised, and it is the acceptance test for every engine change: S1 the strong student quietly declining, S2 the struggling student genuinely recovering, S3 the single outlier, S4 the term break, S5 the transfer in, S6 missing weeks, S7 the teacher concern only, S8 relapse inside the window, S9 the exam period suppressed for one year group, S10 the subject change, S11 the authorised absence, S12 the school-wide bad week (no student's level changes without a declared calendar period), S13 the tiny wobble, S14 the ordinal series, S15 the corrected import, S16 the positive-only student, S17 and S18 the counselor downgrade with and without new evidence, S19 hysteresis at the edge with a raise that is never held, S20 the weekly backfill, S21 the retrospective term, S22 the threshold change, S23 the fifteen authored cases, S24 the escalated case with quiet data for 60 days, S25 a second concern on an escalated case, S26 results day and the offer-condition check; and Wellesmere's W1 to W8. Each scenario file carries its generator (points per assessment, week or fortnight block per measure with a named noise seed, calendar periods, absence spans in days, flags, context, case state, escalations) and its expectation per week (level per measure, rule hits, proposed tier, onset week) and its final evidence sentences. The property tests run over generated series. The golden-file test pins byte-identical output for the same input. `calibrate_tiers.py`, committed from pass 3's appendix, reproduces pass 3 §3.6 and §11.1 and the known-parameter ARL figures, and any change to a level threshold or a tiering row re-runs it. `engine-reproduces-seed` is the bridge to the fixtures: the history pack and the weekly packs imported in sequence, the backfill evaluations, and the result equal to lists generated by running the rules; for ACS under the pilot defaults that is pass 3 §7.2's: 8 of the 15 authored tiers match, Maryam's `good` two weeks late, Ahmed and Priya one tier lower, Hana at `monitor`, and Zayd, Maya and Fahad with no case.

### 5.3 Contract tests between modules

One test per seam in §2.2, plus the structural tests that keep the seams the only path: `packages/contracts` schemas round-trip every fixture record; the loader produces inputs that validate against `StudentEvaluationInput` for every student in both tenants; the writer replays an `EvaluationResult` as an upsert with no duplicate signal or evidence ids; `packages/api`'s structural test proves every procedure is `input(schema) → one domain function`; the `dependency-cruiser` rules fail a build in which a module imports another module's internals, an adapter imports the database, `packages/ai` imports the worker's system context, or a domain function queries before `assertAllowed`; the template allowlists and envelope allowlists are tests, not reviews; the rule-set and audit `detail` schemas are validated on insert and tested with an out-of-bound value and an unknown key.

### 5.4 Authorization tests

The RLS suite and the generated matrix suite run against **both** synthetic schools on every pull request (pass 1 DR-4, pass 4 §3.8): for each tier role and each tenant table, tenant A with tenant B's data present sees and writes nothing; no context sees nothing; the owner under `FORCE` sees nothing; every `auth.perm_scope` is exercised with a student who is in the caseload, in the roster, self, a linked child, a paired mentee, and none of those; the eight "see nothing" cases (no context; expired session; ended membership; ended cover; ended guardian link; ended pairing; teacher after a roster move; counselor after reassignment); the column cases (mentor revoked columns; teacher attendance view without reasons; guardian tier without nationality); the verb cases, positive and negative through RLS (`approve`, `escalate`, `configure`, `export` accepted for exactly the roles whose row carries them and refused for every other role, even when `read` exists, F02); the narrowing trigger on pass 4 §3.1's rule, narrow-only by default, with the `allow_counselor_widening` rows accepted only when the school's flag is set and never for `safeguarding` (F47); the support cases, by operator and table (F35). The suite is generated from `auth.role_permission` and a fixture map, so a new row without fixtures fails the build, and a school override in a test tenant is exercised too. The Wellesmere checklist (pass 1 DR-8) is the second-school half: no "Grade" literal, no ordinal assumption, no IB navigation when the modules are off, the safeguarding label and counselor title from the tenant, scales through `ref.grade_scale`, the calendar from the tenant, deadlines per destination system (its seed carries UCAS and Common App targets, so "UCAS-only" is withdrawn, F31), no ACS string in code, no "ADEK" on any screen, template or email, its pastoral assignments and the DSL on a teacher membership, the marker off; the CI-only `wellesmere-shifted` tenant runs every suite that touches time. Two module-boundary invariants are tests: no function returns the union of the caseload and the Diploma cohort, and no code outside the discovery module writes `xp_ledger`. In production, the canary (pass 4 T1) is a Container Apps job in UAE North that calls domain functions through `withTenant()` as a canary membership, refuses any non-canary tenant, asserts that every row it reads carries its own school id, and alerts on any query plan without `school_id`; it never runs from a GitHub-hosted runner and holds no credential outside Key Vault (F42, F23).

### 5.5 Import tests against both export shapes

`fixtures.spec.ts` imports the Veracross-shaped ACS packs (term start, twelve weekly, one reporting) and the iSAMS-shaped Wellesmere packs (Michaelmas, Lent, the correction) in order through the real pipeline against an ephemeral database and asserts `expected.json` exactly: counts per status and code, the people summary, the security section, the rows the correction supersedes, and the `sis.v_current_*` views after `after_commit`. The deliberate defects of pass 2 §8 (the first version's 26 plus ACS defects 19 to 25: the imported restriction, the reactivation, the individual link, the under-13 student, the register disagreement, the approver who uploaded, the unsigned fixture) are each a line in the oracle (the locale date, the padded id, the name change, the transfer and withdrawal, the shared guardian email, the cover teacher, the teacherless section, the set move, the duplicates, the unmapped code, the grade without membership, the stale pack, the truncated roster, the two-score correction, the forbidden column, the missing submissions, the exam period; the `N` to `I` recode, the leaver in a set, the DSL who teaches, the emergency contact, the two guardians who share an email and stay two persons, the Windows-1252 file, the GCSE `9` and A-level `A*`, the `UPN` column, the two-week timetable). `roster-security.spec.ts`, the profile specs, the transform property tests, a parser fuzz test over malformed CSV and XLSX, the export-injection fixture, the 250 MB limit and the scan gate complete the set. The test that fails the build if any adapter or profile references a tenant slug is the one that keeps the second school cheap.

### 5.6 AI evals

Pass 5 §10 is applied as revised: the suites (co-pilot 150 + 60 + 40 refusals; brief 46; parent draft 120; discovery 60 profiles × 3 seeds on both tenants; EE 80 × 2 guides; reader 300 over the three record kinds; injection 120 × surfaces; pseudonymiser recall on a corpus written in-country by people outside the team; leak scanner over both directories; no headline suite and no safety model suite, since neither feature exists in v1, and the safety lexicon's corpus is the Safety suite above), the ten adversarial profiles, and the pass bar (§10.4) recorded as `suite_version` thresholds. The runner refuses a case without a gold answer written before the model ran, keeps train, validation and test splits, and treats a case a prompt change breaks as a regression until a person re-labels it. The deterministic tier gates every pull request that touches the AI packages or a prompt; the judged tier runs nightly on `main` and in full for a promotion; the human sample (30 items per feature, 27 accepted) is labelled by the school's people (questions 132, 133, 164) and blocks shipping; counselors' questions reach the eval set only through the in-tenant rewrite onto synthetic personas (question 132, F23). The promotion protocol (pass 5 §9.5) is tooling, not a document: `active_needs_eval` refuses a configuration without a passing run, `AI_MODEL_SWITCHED` records the switch, and the 14-day watch rolls back on a doubling of flagged outputs or refusals. Before any suite runs for real, the week-one smoke test proves structured outputs, `inference_geo`, caching and streaming compose on Opus 5.5 and Sonnet 5.

### 5.7 The smoke test of the nightly sweep

Three levels, because the sweep is the product's ritual and its failure mode is silence.

1. **In CI, on every pull request, with a fake clock.** An ephemeral database with both tenants seeded; the clock advanced past each tenant's `sweep_hour`; `sweep.schedule` enqueues `sweep.run` once per tenant that holds data and has its sweep enabled, never for a tenant in `onboarding`, and never twice (the nightly index); the batches run; `sweep.finalize` sets `succeeded`, `students_evaluated` equals `students_in_scope`, `tier_yesterday` equals the recomputation from `case_tier_history`, `sweep.completed` is emitted and the structured log line is written, `/health/sweep` reports today for both tenants; then the failure paths: a batch made to throw leaves `partial` with `students_failed` and yesterday's tier on the sheet with its line; a run that cannot start is `failed`; with the worker stopped, `sweep.watchdog` emits `sweep.missed` and the banner appears; a daytime flag runs `sweep.event` within the coalescing window and never lowers a tier; a scheduled pack commit runs `sweep.backfill` and only the final week opens cases.
2. **On staging, every night, with the real clock.** The cron runs for the synthetic tenants at their `sweep_hour` in their own timezones; the alert rules (§7.4) stay silent; the seven-night acceptance of B2.17 is the first pass and the check is permanent afterwards; once a month a deliberate stop of the worker proves the page arrives within twenty minutes.
3. **In production, from B4 onward.** The canary tenant pair runs the sweep every night before and after the first real school, so the path that matters most has run many times before it runs for a child; its alerts are keyed on `purpose` and never page as a customer's.

### 5.8 CI gates and their order

`ci.yml` runs on every pull request and on `main`. Jobs, in the order they are required to pass, with durations as planning assumptions:

| Job | Contents | Required |
|---|---|---|
| `check` (2 min) | typecheck, Biome, `dependency-cruiser` boundaries, the `'use server'` lint, `docs:verify`, generated-docs diff, `plan-touched`, the wholesale-replacement check, gitleaks, `npm audit signatures`, the lockfile and cooldown check | yes |
| `unit` (3 min) | Vitest across packages, including the engine suite and the AI mechanism tests | yes |
| `db` (6 min) | migrations from zero, `drizzle-kit check`, seed both tenants, the RLS, authz, registry and convention suites, the domain contract tests, `fixtures.spec.ts`, `engine-reproduces-seed`, the sweep smoke with a fake clock, the log scanner, `no-real-data` | yes |
| `e2e` (8 min) | Playwright smoke on both tenants with axe; the full Playwright set nightly | yes (smoke) |
| `images` (5 min, `main` only) | build `web` and `worker`, Trivy scan, attestations, SBOM, push to the registry | for deploy |
| `evals-deterministic` (varies) | only when `packages/ai`, `packages/contracts/ai` or a prompt changed | yes when triggered |
| nightly | the judged eval tier, the full Playwright set, the staging sweep check, the staging drift check, the `tier_yesterday` assertion; the production canary and production drift check run as UAE North jobs and report into the same board, never from a runner | reported, pages on failure where §7.4 says so |
| weekly | the audit chain verification, the Renovate batch, the external attack-surface scan | reported |
| quarterly | the restore drill log freshness (blocks a production deploy when stale) | pre-deploy |

---
## 6. The frontend rebuild

### 6.1 Principles of the port

The prototype is the specification and it is frozen, so the Next.js app is the only place a school will see anything new. Five rules keep the port from losing what the prototype encodes.

1. **Rules move to the domain before the screen is ported.** Every rule the prototype enforces in a view or a handler (`selCheck` gating a submission, the approval button disabled without `rec.talk`, `teacherQueue()` filtering to HL picks of the teacher's own subjects, `casSum` reading `hrs` and `entries` as different numbers, the routed note vocabulary, the escalation's confirm-and-reason) becomes a domain function or a rule set first, with a test, and the component calls it. A component in `apps/web` contains no rule; a lint on `apps/web` fails on imports from `packages/db` and on any arithmetic over record fields that is not display formatting.
2. **A rule ledger precedes every screen.** `docs/screens/<screen>.md`, written in the port session's first half: the prototype function(s) and route-states; every rule the screen encodes, by function name; where each rule now lives (domain function, view, rule-set key); the test that pins it; the handler map (each `data-*` action → domain function → audit action → event); what is deliberately not ported and which pass says why. The ledger is the reviewer's checklist and the reason a later change to the screen cannot silently drop a rule.
3. **Derived values are read, never recomputed in the browser.** The run comes from `signal.week_run()`, the counts from the domain's named functions, the dimension statuses from the `dimensions` rule set, the demand from the views. The prototype's own comments record why (pass 1 DR-6).
4. **Both tenants, every screen.** A screen's Playwright test runs on ACS and Wellesmere, and the Wellesmere checklist assertions for that screen are in the same file.
5. **The visual system is ported once.** `DESIGN.md`'s tokens, type roles, marks and named rules become `packages/ui` in B0.12 and every screen composes from it; the `impeccable` and `web-design-guidelines` skills that governed the prototype's phases are the review tools for the port sessions (they are not required by this planning pass, but the quality floor they carry is the one the school has already seen).

### 6.2 The screen inventory by phase

From the router at `index.html:9003` to `:9099` and `roleNav` at `:1898`. The 98 route-states `PHASES.md` P0 verified are the completeness count; a phase's port is done when its share of them renders on both tenants without a console error.

| Role | Route key(s) | Prototype function(s) | Phase | What changes in the port |
|---|---|---|---|---|
| Counselor | `cohort-schedule`, `cohort`, `cohort-all`, `today` | `vSheet` | B3 (the shadow-mode list before G-REAL; the four modes after) | one screen, four modes; the run from `week_run()`; overnight from `tier_yesterday`; the `expected_cadence` panel with reason coverage; the counselor log row; ordered by tier then time raised only |
| Counselor | `command`, `si-cases` | `vCommand` | B3 | one screen with a filter |
| Counselor | `si-interventions` | `vWork` | B3 | |
| Counselor | `si-why` | `vIntel` | B3 | matrix coloured by domain level; the cohort tiles stay as descriptive aggregates; nothing ranks |
| Counselor | `flags` | `vFlagInbox` | B3 | attach, dismiss, merge; the routed vocabulary |
| Counselor | `meetings` | `vMeetings` | B3 | |
| Counselor | the ten `DIMS` keys | `vDim` | B3 (attendance, punctuality, academic, engagement, behaviour), B5 (application progress, list balance, deadlines, documents and references, personal statements) | one component; the three hard-wired dimensions become rule-set outputs |
| Counselor | `files`, a file id | `vFiles`, `vFileOne` | B1 (record tabs), B3 (list and case tabs) | `FILE_ACCESS` on open; the visibility panel rendered from `auth.role_permission` |
| Counselor | an open case (`S.open`) | `vCase` | B3 | level, breadth and window replace strength, confidence and severity; the evidence chain from `evidence_item`; the access panel; escalation |
| Counselor | `thresholds` | `vThresholds` | B2 | versions, propose and activate, the what-if run replaces the estimate |
| Counselor | `arch` | `vArch` | B3 | rewritten to describe the sweep and the pull pipeline truthfully |
| Counselor | `app-season`, `app-flow` | `vUni`, `vFlow` | B5 | |
| Counselor | `app-letter` | `vLetter` | B5 (human-written versions), later (generation) | the evidence panel from citation rows |
| Counselor | `psreviews` | `vPSReviews` | B5 | sectioned per `ref.statement_format`; no "AI read" score and no cohort average (F66) |
| Counselor | `ib-selections`, `ib-demand`, `ib-cas`, `ib-ee` | `vSelQueue`, `vSelStudent`, `vSelDemand`, `vCasCohort`, `vCasDetail`, `vCoordEE`, `vEEStudent` | B6 | the coordinator is the `ib_coordinator` capability; CAS and EE over the ManageBac mirror where the school keeps ManageBac; the CAS cohort is not sorted by gap |
| Counselor | `approvals` | `vApprovals` | B8 | amend and send-back with structured reasons (the prototype has neither) |
| Counselor | `copilot` | `vCopilot` | B7 | plan, execute, write; the boundaries card's promises as tests; the label from the generation row |
| Counselor | `prove`, `pilot` | `vProve`, `vPilot` | B8 | figures from `events.event` only; "targets, not measurements" until events exist |
| Staff (new) | admin: Studio, People, Memberships and capability grants, Caseload and cover, Parents, Mentors, Route and category routes, Audit, Notices; the support console as its own app | none | B1, B3, B4, B5, B8 | surfaces the prototype does not have, for the `school_admin` role and capability holders; designed from passes 2 and 4 |
| Teacher | `class`, `tclasses`, `tlog`, the flag modal | `vTClass`, `vTMyClasses`, `vTLog`, `tflagModal` | B3 | rosters from imports only, plus subject and supervision students; the `safeguarding` kind as a referral; the disclosure rule in the modal; `routed_key` phrases that never reveal a case |
| Teacher | `classapprovals` | `vTSelReview`, `vTSelCard` | B6 | HL picks of own subjects in an open round, through `ib.sign_off()` from `ib.v_signoff_card`: name, prerequisite mark, the student's reason, and no "Personal band" (F58) |
| Teacher | `ee` | `vTEE`, `vTEECard`, `vTEEConfirm`, `vTEEStudent` | B6 | load on every card |
| Student | `discover` | `vDiscoverFlow`, `vOnboardQuiz`, `vDiscoveryChat`, `vPathwayProposals`, `vGamifiedRoadmap` | B8 | deterministic mode first; model mode behind the policy and both gates; tags the student can see and correct; "Closest fit" |
| Student | `guide`, `explore`, `progress`, `targets`, `pslab`, `apexams` | `vHSGuide`, `vPathExplorer`, `vStuProgress`, `vStuTargets`, `vPSLab` and its four sub-views, `vApExamsStandalone` | B5 | shown by the tenant's portal rules; no probability and no admit rate; the statement lab sectioned with no score; the synthetic alumni content labelled |
| Student | `stuclass` | `vStuSubjects`, `vStuSubjectRecord`; at the choosing stage `vSelForm`, `vSelChooser`, `vSelSubmittedPanel` | B5, B6 | the choosing stage from the tenant, not "Grade 10" |
| Student | `stucas` | `vStuCAS`, `vStuCore`, `vStuTOK`, `vStuEE`, `vStuEEPropose` | B6 | TOK described, not tracked; the mirror's rows where ManageBac is the record; the essay under `ee_2027` |
| Student | `mentors`, `network` | `vStuMentors`, `vStuNet` | B8 | sessions in person on school premises only (C6) |
| Student (new) | "I need to talk to someone" | none | B5 | the student's reporting channel; an `urgent_request` reflection, never extracted, reaching a person |
| Parent | overview, messages, guide, grades, uni, pathway, apexams, meetings | `vParent` with its tabs | B5 | the child switcher over activated links only; tabs from the tenant's portal rules; own threads only; no meeting notes, no admit rates, no XP; the mentor line shows only that a pairing exists |
| Mentor | `requests`, `mentees` | `vMentorReq`, `vMentorMentees` | B8 | passkey sign-in; no contact columns |
| Cross-cutting | the ⌘K palette | `palCommands` | B3 (staff), B5 (student, parent) | role-scoped search through the same policies; `DIRECTORY_SEARCH` for whole-school search |
| Cross-cutting | role switcher, grade and track preview | `roleBar()`, `gradeSwitcher()` | not ported | demo apparatus (CONTEXT §11.18) |

### 6.3 What is not ported

The passes found these in the prototype and ruled them out. The frozen demo keeps showing them; the demo script must not read them as promises.

| Prototype behaviour | Where | Why not, and which pass |
|---|---|---|
| Signal strength 0 to 100, the meter, "confidence 86%", the severity tile, signed evidence weights | `vCase`, `caseCard`, `vIntel`, the `arch` page | undefined numbers; replaced by level, breadth and window (pass 3 §9) |
| "Every new grade … evaluated immediately, never in a nightly batch", "gradebook webhook fired" | `vArch` | no webhook exists; the sweep and the pull pipeline are the truth (passes 2 and 3 §0.2) |
| Admission probability per target, with meters | `vCase`, `vFileOne`, `vStuTargets` | traces to nothing; replaced by the sourced requirement, the rule-computed classification with its inputs, and a labelled institution-wide rate (pass 5 §2) |
| The escalation modal's "what will be sent": name, year, headline, attachments | `escalateModal` | the email carries a reference only (pass 4 §5.3) |
| The escalation reason and student names in audit text | `logAudit` calls | names never enter `audit.entry`; the reason lives on the escalation row (pass 4 §0.2) |
| "Minimum necessary visibility" rows for Year head and Head of school | the 360° file | roles that do not exist; the panel renders from `auth.role_permission` (pass 4 §0.2) |
| "Contact consent · On file" for every family; a fabricated default guardian | `parentCard`, `parentOf()` | `consent_status` renders its real value; no guardian is synthesised (passes 2 and 4) |
| "Sessions … are recorded" | mentor and student copy | CAROS records nothing (pass 4 §2.3) |
| Co-pilot answers that rank ("highest structural risk in the cohort") | `CPQ` | invariant 2; the answer schema has no comparative field (pass 5 §0.2) |
| Archetype milestone claims (test-score medians, "filters 80%", salary figures, a Stanford GSB undergraduate programme) | `ARCHETYPES` | unsourced; a claim renders only with a source (pass 5 §2.5) |
| Priya's "platform activity after 01:00", Layla's fourteen-month baseline, Ahmed's "22 months" | authored series | removed or reworded; the window is at most twenty weeks and per section (pass 3 §0.2) |
| "Estimated effect: current false-positive rate 14%" | `vThresholds` | replaced by the what-if run over stored snapshots (pass 3 §10.4) |
| The twelve-character reason box | the escalation form | a structured form with minimums ACS sets (pass 4 §5.3) |
| The pathway probability start-plus-increments | the student pathway view | fabricated in code (`PRODUCT.md`) |
| "190 counselor hours returned per term" | the letter engine copy | not computed until real letters are drafted and timed (pass 5 §3.10) |
| `METRICS` as figures | `vProve` | every figure is a target; the page says so until events exist (CONTEXT §11.2) |
| The stored `weeks[]` run, the per-track hard-coded AP timetable, the three hard-wired dimensions | data and `getStudentClasses`, `dimPunctuality`, `dimEngagement`, `dimBehaviour` | derived from facts and rule sets (pass 1 DR-6, pass 3 §9.3) |
| The Friday-and-Saturday weekend, "Grade" as a literal, ACS staff names in code | `buildNovemberCalendar`, many views | tenant fields (pass 1 DR-8) |
| The role switcher, grade preview, track preview | `roleBar()`, `gradeSwitcher()` | demo apparatus (CONTEXT §11.18); one acting role per session (pass 4 §2.4) |
| The sheet's within-tier sort by "the loudest run", and the CAS cohort sorted by gap | `index.html:3366` to `:3373`, `:6328` | a ranking of students against each other (invariant 2); lists order by tier then time raised, and the ordering test pins it (F50) |
| The Personal Statement Lab's "AI read: N/100" score, its cohort average and its value in the `PS_SUBMITTED` audit entry | `index.html:7631` to `:7649`, `:7877`, `:7905`, `:7925`, `:9643` | an undefined number and a cohort comparison (CONTEXT §11.4); completeness per section instead (F66) |
| "Strongest match" and "Why this matched you" on pathway proposals; "asked to be matched with an alumnus" in the parent portal | `index.html:7052`, `:7192`, `:8100` | the word is reserved for reach, match and safety (invariant 11); "Closest fit", "Why this fits what you said" and "asked for a mentor introduction" (F51) |
| The "Personal band" on the teacher's sign-off card | `index.html:5473` | an engine output shown to a teacher (F58) |
| Per-target admission rates on student and parent screens | the targets views | an institution-wide rate reads as a personal chance; counselor-only, labelled (F77) |
| The coordinator persona "Mr. A. Diaz" | `index.html:4407`, `:4539` to `:4545`, `:4821` | the surname of the real ACS person PRODUCT.md names; the seed uses a fictional coordinator and the name is on the blocklist (F29); the frozen prototype keeps it until Davide decides |
| The CAS "Ask to join" action | `index.html:9311` | it writes nothing today and has no table or audit action; joining an opportunity is recorded as a CAS entry once it happens (F72) |
| The three mandatory reflection sessions as the only Extended Essay model | `EE_MILESTONES`, PRODUCT.md | both current ACS cohorts sit under the 2027 guide's single reflective statement; `ee_2018` for resits only (F62) |

### 6.4 The shell, the tokens, the marker and the letterhead

`packages/ui` carries `DESIGN.md`'s three token layers (material, semantic, letterhead) with the letterhead rule enforced by the same grep the prototype uses (no `--school-*` token inside a tier, track or domain definition); the tier stamps and letters, the run cells, `tally`, `empty`, `head` with two arguments and no eyebrow; the six type roles; the night ledger gated to the student surface as the prototype gates it; reduced motion zeroing duration and delay; the 375px floor for the parent and student surfaces. The demonstration marker is a component bound to `core.school.demonstration_marker`, a generated column no flag can switch off, so it shows on the synthetic ACS tenant in every demo and training session and never on the real one. The letterhead reads `core.school.brand`; the safeguarding role label, the counselor title, the year-group labels and the SIS name read from the tenant everywhere a string would otherwise be typed.

### 6.5 The demos: when the first genuinely demoable build exists, and what it shows

**Demo 1 · the rest of B3, during shadow mode (planning assumption: May 2027, before the first reveal session; under the fallback, before the August 2027 G-REAL).** The first build a school should be shown, because it does things the prototype cannot: a counselor signs in with Google on the synthetic ACS tenant and sees the caseload sheet with the run and "what changed overnight" computed by the real engine over the history and the weekly packs; opens a case and reads the evidence chain with source labels and as-of dates, the baselines with the band, the interventions and notes; a teacher signs in, logs a concern from the register, and the counselor sees it attach and the case move within five minutes; a teacher's safeguarding flag reaches the CPO persona as a referral; the counselor escalates through the structured form, sees their own reporting countdown, and the CPO persona receives an email in the staging mailbox that names no child and no person, signs in and acknowledges; the access panel shows who looked; the thresholds page runs a what-if; then the same product signed into as the Wellesmere school: Years, a Designated Safeguarding Lead who teaches, form tutors, no IB, no "ADEK", no marker. The engine's stories are the ones its rules produce (pass 3 §7.2: 8 of the 15 authored tiers match the prototype's), and the script tells them as they are. What it does not show, and the script says so: parents, students, the university and Diploma modules, the AI features, and any real data. It is shown on the pinned demo revision of staging with a deploy freeze during the booked demo (§7.2). Until Demo 1 exists the frozen prototype is the sales demo and its script skips §6.3's lines. Before Demo 1 there is a **Studio demo** at the end of B1 (an import with its preview and security section, a second approver, a rollback), which is real and new but is for ACS's IT and registrar, not for a counselor.

**Demo 2 · the end of B6 (assumption: during the 2027-28 year, B5's half in the autumn).** Adds the parent portal (activation per contact, two children, a meeting request and its content-free confirmation, the transcript and the requirements page with sourced figures), the student surfaces (subjects, progress, targets with the classification and its inputs, the Personal Statement Lab in UCAS's three sections, the lexicon screen's response with the CPO-confirmed helplines), and the Diploma (a selection submitted, signed off, the conversation recorded and approved; the demand sheet; CAS and the Extended Essay over the ManageBac mirror; the over-capacity assignment recorded). Shown on the synthetic tenants; to ACS staff, also on their real tenant for the surfaces they already hold rights to, in person or on the school's own video platform.

**Demo 3 · the end of B8 with B7 (assumption: 2028).** Adds the co-pilot answering the prototype's five questions with citations, its compiled filter shown, and refusing the sixth; a meeting brief; a parent draft landing in-app; Extended Essay feedback under the 2027 guide with its labels kept off the supervisor's decision screen; the discovery flow in both modes with the approval gate; the mentor path with in-person sessions; and the reporting page with real events. On synthetic data only, with the AI label and the "built from N records" line on every output.

---

## 7. Operations

### 7.1 The deploy pipeline

Three workflows, all in `.github/workflows/`, authenticating to Azure by OIDC with no stored secret.

- **`ci.yml`** on every pull request and on `main`: §5.8.
- **`deploy-staging.yml`** on every merge to `main`: build the `web` and `worker` images once, scan, attest, push to the registry in UAE North; `terraform plan` and, from `main` only, `apply` to staging; run the migration job (`drizzle-orm/migrator` as a Container Apps Job, `lock_timeout = 5s`); swap the revision in single-revision mode, which keeps the old revision live until the new one passes its startup and readiness probes ([Container Apps revisions](https://learn.microsoft.com/en-us/azure/container-apps/revisions)); smoke: `/health`, `/health/sweep`, a sign-in as the synthetic counselor on a CAROS test domain, the staging isolation check; refresh the seed for the synthetic tenants at the anchor date, **except the demo tenant, which no deploy, reseed or drill touches**; the Playwright smoke against staging. **The demo slot** (F43, decided as "start cheap"): a pinned Container Apps revision label serves booked sales demos on staging against the untouched demo tenant, and the pipeline refuses to move that label during a booked demo (a deploy freeze read from `docs/ops/demo-calendar.md`); a separate demo environment is split out only if demos become frequent.
- **`deploy-production.yml`** on a tag `vYYYY.MM.DD-n`: the `production` environment requires Davide's approval and accepts tags only; pre-checks that the tag's commit has been on staging for at least 24 hours with a completed nightly sweep, that the drill log is under a quarter old, and that no P1 or P2 incident is open; then the same image, migration job, revision swap, smoke and a thirty-minute watch during which any §7.4 alert rolls back. Rollback is activating the previous revision, which Container Apps retains (by default up to 100 inactive revisions, same page); the database is forward-compatible by construction, so no migration is ever reverted.

Deploys are not scheduled during the sweep window or on a school morning: the pipeline refuses a production deploy between 01:00 and 08:00 in the timezone of any tenant holding data, and during the school's declared freeze windows (question 142).

### 7.2 Environments

Pass 1 DR-9 as revised by pass 8: Local (Compose with PostgreSQL 17, Azurite, Mailpit; the synthetic tenants); CI (a service container; migrations from zero; the three synthetic tenants including `wellesmere-shifted`); **Staging** (`caros-staging`, UAE North, `B2ms` without HA, no WAF, synthetic tenants only with `purpose = 'demo'`, deploy on merge, **Google domains mapped only for CAROS test domains**, never a school's, so no real identity enters the environment shown to other schools, F43; the pinned demo revision and the untouched demo tenant of §7.1); **Drills** (their own subscription, where the restore drills, the offboarding drill and the onboarding dry run run, so no drill touches the demo, F43); **Production** (`caros-prod`, UAE North with UAE Central access, DR-1 topology, deploy on tag), holding **only real tenants and the canary pair**: two synthetic, unbranded tenants with `purpose = 'canary'`, AI off, no deliverable email addresses, excluded from every report, indicator, cost board and alert by `purpose` (pass 6 decision C6, F42); no branded demo or training tenant ever sits in production, so counselors train on staging's synthetic ACS tenant (open decision O34 closed as "train on staging"); and the isolated single-school deployment from the same module with `deployment_mode = "single"`. The **maintenance window**: Azure Database for PostgreSQL flexible server lets a production server choose a custom one-hour maintenance window by day and start time, notifies five days ahead, and applies custom-window updates at least seven days after system-managed ones ([planned maintenance](https://learn.microsoft.com/en-us/azure/postgresql/flexible-server/concepts-maintenance)); production uses a custom window on Saturday at 09:00 Asia/Dubai (a choice: outside the sweep window and outside school hours), staging a system-managed one so it sees updates first.

### 7.3 Migrations in production

Pass 1 DR-9's expand-and-contract, applied: a release may add tables, columns, indexes, policies and functions (an index built `CONCURRENTLY` goes in a `-- no-transaction` file run by the migration job's second step, because `drizzle-orm/migrator` runs every pending migration in one transaction; at pilot scale none is needed, F40); it never drops or renames what the previous revision still reads; the drop lands two releases later as its own migration with a `contract` tag in its name, reviewed by Fable 5.1 (§4.2). The migration job runs before the revision swap with `lock_timeout = 5s` and fails fast; a failed job stops the deploy and pages; the previous revision keeps serving. A merged migration is immutable (the hook and a CI check); a wrong migration is fixed by a new one. `drizzle-kit check` guards the history; the nightly drift job diffs production against the snapshot and pages on any difference, because a hand change to production (which no person can make without a break-glass credential and a second person, pass 4 §3.7) is exactly the class of event that must not be silent. The `caros_migrator` role is the only login that can run DDL, and `pgaudit` writes `DDL` and `ROLE` events to a workspace the application identity cannot write to (pass 4 §4.2).

### 7.4 Monitoring and alerting

The sweep first, then everything else. **Every alert carries counts, never student ids or names** (pass 4 §9.4, F60); every rule keys on `core.school.purpose`, so a canary or demo tenant never pages as a customer and a customer is never excluded by accident (F42). Severities follow pass 4 §9.6: P1 exposure of student data, an audit-chain mismatch, a referral that could not be delivered; P2 suspected exposure, a leaked credential, a plausible path to data; P3 a vulnerability without exposure; P4 availability. Delivery is one Azure Monitor action group per severity: P1 and the sweep alerts go by SMS and voice to the on-call and email to both people; P2 by SMS and email; P3 and P4 by email. The providers are named in the sub-processor list: Azure Monitor action groups for SMS, voice and email, whose processing is `Global` (regional processing is not offered in the UAE), which is acceptable only because the payload holds counts and no personal data; there is no third-party chat channel in the pilot. Email delivery status is polled from Azure Communication Services, not taken from Event Grid (F60).

| Alert | Condition | Severity | Runbook |
|---|---|---|---|
| Sweep not completed | no `sweep.completed` log line by 05:30 local for a school holding data with its sweep enabled (scheduled query every 15 minutes) | sweep (pages) | `sweep-failed.md` |
| Sweep partial or slow | `status = 'partial'`, or duration over two hours, or `students_failed > 0` | sweep (pages at 05:30, otherwise email) | `sweep-failed.md` |
| Worker silent | heartbeat line absent for 20 minutes | sweep (pages) | `worker-down.md` |
| Watchdog job failed | the external Container Apps Job did not run or reported a missing run | sweep (pages) | `sweep-failed.md` |
| Engine anomalies | `domain.quiet`, `budget.exceeded`, the operations count of students outside band in one domain (a prompt to declare a calendar period, never an engine input, F26), tier churn over 10% of open cases in one night, a DR-6 assertion failure | P4 by email; churn and assertions page | `engine-anomaly.md` |
| Cadence missed | `import.late` for an agreed cadence | P4 email to Davide and in-app to the `school_admin` | `import-late.md` |
| Connector paused | three consecutive failures | P4 email; in-app to the `school_admin` | `connector-paused.md` |
| Directory read failed | the hourly staff read did not complete, or returned a truncated list | P3 email; two consecutive failures page | `directory-read.md` |
| Safety alert unopened | a student safety alert with no disposition past the route's window | the reference-only notice to the CPO (pass 4 §5.8); P3 email to on-call if the notice itself fails to send | `safety-alert.md` |
| Outbox stuck | a queued row older than 30 minutes, or any `safeguarding_escalation` or `safety_alert_notice` row not sent within 5 minutes | the safeguarding cases are P1 (pages); otherwise P4 | `outbox-stuck.md` |
| Audit chain mismatch | the weekly verification finds a break | P1 (pages) | `incident-response.md` |
| Key access failure | Resource Health or Activity Log reports the server or a storage scope cannot reach its key | P1 (pages) | `key-access-failure.md` |
| Tenant canary failed | the nightly canary job sees a row carrying another school's id, reaches a non-canary tenant, or finds a query plan without `school_id` | P1 (pages) | `incident-response.md` |
| Drift | production schema differs from the snapshot | P2 (pages) | `schema-drift.md` |
| Sign-in anomalies | over 20 `SIGN_IN_DENIED` for a domain in a day; a staff session from a new country or AS; more than 5 failed magic-link requests per address per hour | P3 email; the AS and country case in-app to the person and the `school_admin` | `sign-in-anomaly.md` |
| Access anomalies | a teacher opening more than 3× their roster in an hour; a counselor above 3× caseload; export volume | P2 email; visible to the `caseload_lead` | `access-anomaly.md` |
| WAF | blocked-request spike, rule-set errors in prevention mode | P4 email | `waf.md` |
| Certificates and DMARC | certificate expiry within 14 days; DMARC aggregate failures | P4 email | `certificates.md` |
| AI | refusal rate over 1% on a feature; validator refusals over 5% in an hour (pauses the feature); `refused_post_hoc` (suspends the feature); a final-scan hit; cost per generation doubling; the circuit breaker open over 30 minutes | P2 for geo and scan; P3 otherwise | `ai-incident.md`, `model-promotion.md` |
| Safety screen | lexicon alert volume at 3× expected for the school (a lexicon problem owned by the CPO, question 129) | P3 email; in-app to the CPO | `safety-alert.md` |
| Support grants | any grant created or executed; a write grant; a `safeguarding_flag` grant | email to CAROS's on-call; in-app to the school's approver (decision C3: no email to the school); monthly review | `grant-approval.md` |
| Availability | `/health` and `/health/sweep` availability tests from Azure Monitor fail | P4 email; P2 during school hours | `deploy-and-rollback.md` |

Dashboards, three: the sweep board (per school: last run, duration, partials, students failed, alert history), the ingest board (cadence, last as-of, previews waiting, rejections, connector runs), and, from B7, the AI board (pass 5 §10.6's production signals). The school's own view is the monthly report (head hash, grants, rotations, sweep completion, the AI lines), never the dashboards.

### 7.5 On-call for a very small team

On-call means one thing above all: **the sweep window, 05:30 to 07:30 local on school days**, when a page means a counselor may arrive to a sheet that lies. Everything else has a slower clock.

- **Rota, solo mode.** While Davide is the only builder, he is the on-call; the second person is whichever teammate or contracted engineer has agreed to be the escalation contact for the sweep window, named in `docs/ops/rota.md`. When the full process switches on (§2.5), the rota becomes two people alternating weeks. **Escalation is a job, not an SMS reply** (F45): Azure Monitor SMS supports no acknowledgement (its replies are only DISABLE, ENABLE, STOP, START and HELP), so a small Container Apps job, run every five minutes, pages the second person by SMS and voice if a sweep or P1 alert is still firing fifteen minutes after it first paged, and records who was paged when; acknowledgement is a click in `/ops/ack` that the job reads.
- **What a page means in the sweep window:** open `sweep-failed.md`; the watchdog has already put the banner on every counselor's sheet ("tiers are as of the sweep of …"), so no counselor is misled; re-run `sweep.run` with trigger `manual` and a reason; if it cannot complete by 07:30, message the school's agreed morning contact through the agreed channel (question 147) with the one sentence the runbook gives; write the incident note by 09:00.
- **Outside the window:** P1 pages at any hour; P2 within business hours the same day; P3 and P4 the next working day. Weekends and school holidays: P1 only; the sweep still runs, and a failed sweep on a holiday is a P4 until the last holiday night.
- **What on-call is not:** it is not 24×7 support for the school. The DPA states response targets for breaches (24 hours, pass 4 §1.4) and the school's IT knows the morning contact; nothing else is promised. Every runbook is written so that the second person, who did not build the thing, can follow it.
- **Load:** at pilot scale the expected page rate is near zero after B2.17's seven quiet nights; a month with more than two sweep pages triggers a review of the cause, not a tougher rota.

### 7.6 Backups and restore drills

Pass 1 DR-9 and pass 4 §7.3 and §9.7 as written: 35-day point-in-time restore; geo-redundant backup to UAE Central enabled at creation; weekly vaulted backups with yearly copies for the horizon counsel sets (three proposed); Blob ZRS with soft delete, versions and a nightly GRS copy; the immutable raw-import container; backups encrypted under the CMKs of both regions; every restored server runs the RLS suite and the erasure replay before use. The optional out-of-country cold copy is a real choice (F22): either EU ciphertext with key material held outside the UAE by named people, a named restore region and trigger, an EU restore drill on synthetic data and ADEK's consent (F04), or in-country only with the country-level risk stated in the DPA; until the school and counsel choose (O15), nothing is copied abroad and nothing changes in the drills.

The drill calendar: the first full run of all five drills in B4 before the first real record (§1.5); quarterly afterwards, alternating which drill is run in full and which are smoke-run; every run in the drill subscription, its checks run as jobs in UAE North, recorded in `docs/ops/drill-log.md` with date, operator, witness, recovery time and point achieved, the erasure replay result and every surprise; a failed drill opens a blocking issue; a stale log blocks the next production deploy (the CI check). The single-tenant extraction (drill 4) is rehearsed once a year in full because it is the procedure for "restore school X without touching school Y" and the first time it is needed will not be a drill.

### 7.7 Runbooks

`docs/runbooks/`, each one page, each tested by someone who did not write it: `sweep-failed`, `worker-down`, `engine-anomaly`, `import-late`, `import-rollback`, `connector-paused`, `outbox-stuck`, `escalation-route-change`, `grant-approval`, `key-access-failure`, `schema-drift`, `deploy-and-rollback`, `restore-drill` (with the erasure replay as its last step), `erasure-and-restore-replay`, `offboarding-and-key-destruction`, `incident-response` (with the evidence pack and the 24-hour clock), `credential-rotation`, `tenant-onboarding` (pass 2 §9's twenty steps as revised, with the Google Workspace, safeguarding-route and category-route checklists), `quarterly-data-office-check` (pass 4 §1.1), `model-promotion` and `ai-incident` (pass 5 §9.5 and §12), `sign-in-anomaly`, `access-anomaly`, `waf`, `certificates`, `directory-read` (with the leaver step "revoke in CAROS"), `safety-alert`, `teacher-referral`, `demo-freeze`, `canary-failed`. The index is generated from the directory.

---
## 8. Gates where outside help is required

Placed on the sequence, each with who supplies it, what it blocks, and where its evidence is recorded (`docs/PLAN.md`'s gate table). Lead times are assumptions and should be written down as answers arrive. **§2.3 back-schedules every gate on the April path from T** and gives each its latest date; this table says what each gate is.

| # | Gate | Who | Placed at | Blocks | Evidence |
|---|---|---|---|---|---|
| G1 | GitHub Team organisation; Azure subscriptions (staging, production, drills) with PIM; the UAE Central access request; the ZDR organisation for build sessions (C7) | Davide; Microsoft; Anthropic | B0 week 1 | protections on a private repository; drill 2 and the geo-restore; every build session | `PLAN.md` week-one table with ticket numbers |
| G2 | Written consent to apply the ACS name and crest to synthetic data, **naming training and third-party sales demos to other schools** (question 49; F29, F43) | ACS leadership | B0 week 1 | every demo and training session under the name; already a liability | the letter in the DPA folder; the tenant's `branding_mode` |
| G3 | The KHDA directory check of "Wellesmere" | Davide | B0 week 1 | the second tenant's name | a note in `docs/decisions/` |
| G4 | The sign-in client's Google brand verification; sensitive-scope verification only if an OAuth client ever requests such a scope (Google states "3-5 business days" for sensitive-scope verification, [sensitive scope verification](https://developers.google.com/identity/protocols/oauth2/production-readiness/sensitive-scope-verification); brand verification is described as a few business days in secondary guides and is an assumption here) | Google | B0 week 1 | the sign-in consent screen for an external client; nothing else, because the Classroom and Directory reader is a domain-wide-delegated account the school's admin authorises | the Cloud project's verification status |
| G5 | The zero-data-retention arrangement for the product's Anthropic organisation, in writing, and a second for the fallback route (pass 5 R15) | Anthropic sales | conversation opened B0 week 1; needed before G-AI | any student-derived generation on a real tenant; the pilot runs with the AI off until then | `ai.provider_account.zdr_confirmed_at`, `zdr_evidence_ref` |
| G6 | A DPO appointed by CAROS (may be external) | Davide | by 8 January 2027 | G-REAL (pass 4 §1.1, §9.13) | the appointment letter; the name in the DPA |
| G7 | The DPA drafted from pass 4 §1.4, with the ADEK Digital Policy 7.1.3.a non-disclosure clause, the free-text truth and the school-side responsibilities annex, reviewed by counsel, signed by ACS with its annexes | counsel; ACS leadership and DPO | drafting from B0; signed by 12 March 2027 | G-REAL, including the pseudonymised sample and the history import (question 46) | the signed agreement; Annex 1 generated |
| G8 | Counsel's answers to the questions of passes 4 to 6 (C1 to C33, §14.8), with the retention numbers set (C17) | counsel | engaged week 1; C25 by 27 November 2026; the G-REAL answers by 12 February 2027 | G-REAL for those §14.8 tags G-REAL; G-AI for the AI questions; the letter engine for C20 | `privacy.retention_class.set_by_counsel_at`; the decisions log |
| G9 | The school's legal-basis attestation and the three approved notices | ACS DPO and leadership | notices approved by 19 March 2027; attestation at G-REAL | the tenant leaving `onboarding` (the D45 trigger); student and parent sign-in (pass 4 §9.12 h) | `core.school.legal_basis_attestation`; `privacy.notice_version` |
| G10 | Google Workspace configuration by ACS IT: CAROS set to Limited for the staff and student units, the under-18 designation confirmed, **the Directory scope approved on the delegated reader account**, each domain mapped on DNS proof (questions 19, 110, 141; question 126 on RISC is withdrawn, F18) | ACS IT | onboarding step 4, by 19 March 2027 | any staff or student sign-in on the real tenant; the hourly Directory read | the onboarding checklist record |
| G11 | The school's IT security review and its sign-off, with the pack organised by the regulator profile's checklist (for ACS, ADEK 5.5.1 a to h) and whichever questionnaire the school uses (questions 36, 113) | ACS IT | pack by 5 March 2027; sign-off by 26 March | G-REAL | the pack in the DPA folder; the sign-off |
| G12 | The penetration test: an independent firm (CREST-accredited or DESC Cyber Force listed), scoped to the web application, API, authentication and tenant isolation, **run against production while it holds only the canary tenants** (pass 6 decision C5), highs closed and retested within 30 days (application-layer fixes on staging, production-layer fixes on production before G-REAL); yearly afterwards under written rules of engagement (canary tenants only, no real-row exfiltration, redacted evidence, the firm named in the DPA; F41, F61) | the firm | booked by 17 December 2026; fieldwork from 15 February 2027 | G-REAL | the report, the retest record and the rules of engagement in the pack |
| G13 | The incident tabletop with ACS IT and the CPO; yearly after | ACS | the week of 8 March 2027 | G-REAL | the exercise record |
| G14 | Cyber-liability insurance (pass 4 §9.12 f; C16) | a broker | bound by 19 March 2027 | G-REAL | the policy |
| G15 | The CPO and the delegate named, holding `safeguarding_lead` on any staff role; the route configured with a co-signature; the **category routes** for student safety alerts (leadership contact, CPO) confirmed with the CPO (F09); the helpline list confirmed with its verified dates (F11); a test referral delivered and acknowledged (questions 118 to 121, 129, 151) | ACS CPO, HS Principal | by 19 March 2027 | G-REAL; the escalation, teacher referrals and the lexicon screen | `core.school.safeguarding_route`, `safety_alert_routes`, `safeguarding_contacts`; the test referral's audit entries |
| G16 | The elicitation session with the four counselors (pass 3 §10.5), including Part F's two G-LIVE floors, pre-registered in the pilot agreement | ACS counselors | the week of 25 January 2027 | shadow mode's first configuration and G-LIVE's bar | ACS's `engine.thresholds` version 1 note; the pilot agreement |
| G17 | The weekly export pack agreed as a condition of the pilot, with its delivery path (Veracross's Data Export Package over password SFTP, SFTP by key, or upload) and the retrospective mode named as the fallback (pass 2 §6.4, questions 70, 73, 81) | ACS IT and registrar | agreed by 17 December 2026 | the design point of the whole engine | `ingest.expected_cadence` |
| G18 | At least twenty weeks of history before shadow's first sweep, two academic years where ACS has them, and, if they exist, the counselors' dated handled-case records (questions 46, 83, 103) | ACS registrar and counselors, under the DPA | feasibility by 17 December 2026; the import after G-REAL (onboarding step 10); the backtest at shadow week 2 | warm baselines on day one; the backtest | the import records; the backtest report or the statement that history cannot carry it |
| G19 | The Extended Essay guide per round (`ee_2027` for both current cohorts, `ee_2018` for resits), the criteria wording and the word limits and marks confirmed by the coordinator; whether ManageBac's EE template is enabled (questions 78, 133) | ACS IB coordinator | before B6.9 runs for a real cohort | the essay half of B6 | `ib.ee_round.ee_guide_key`; the confirmation columns |
| G20 | Labelling sessions: twenty questions per counselor rewritten in-tenant onto synthetic personas and the 30-item reviews (question 132); the coordinator's 80 EE labels (question 133); the pseudonymiser recall corpus written in-country by staff outside the team (question 164) | ACS counselors, IB coordinator | before each feature's acceptance in B7 | G-AI per feature | `ai.eval_run.signed_off_by` |
| G21 | Per-feature AI acceptance in writing, including the residual-retention disclosure (both periods) and R8 for the student-text features (question 137) | ACS DPO and leadership | after G-LIVE, G5 and G26, in pass 5 §9.6's order | G-AI per feature | `ai.feature_policy.accepted_at`, `school_accepted_residual_retention_at` |
| G22 | The out-of-country cold copy decision as a real choice: EU ciphertext with key material held outside the UAE by named people, a named restore region and an EU restore drill, plus ADEK's consent; or in-country only with the risk stated (question 62; counsel C12; F22) | ACS leadership; counsel; ADEK | before the DPA is signed, because it is a clause either way | nothing in the build; the DPA text; any copy abroad | the decisions log; the DPA clause |
| G23 | Fairness labels: **none collected in the pilot** (decision C11); the audit runs only once pooled cross-school data gives it power; the pilot documents say fairness is untested (question 106) | Davide; ACS DPO | the pilot agreement | nothing in the pilot | the pilot documents |
| G24 | SOC 2 Type I, then Type II; ISO/IEC 27001 (timing assumptions: six, eighteen and twenty-four months from the first real record; a Type I report is a point-in-time opinion and a Type II covers an observation period of months, per audit-firm guidance ([Drata](https://drata.com/learn/soc-2/type-1-vs-type-2), [Vanta](https://www.vanta.com/collection/soc-2/soc-2-audit-timeline)); the AICPA sets no fixed period) | an audit firm | engaged in B9 | nothing in the pilot; later schools' reviews | the reports |
| G25 | Engagement domain enablement in writing and the Classroom reader account (questions 14, 84); the Veracross OAuth application and the ManageBac read-only token with its scopes (questions 78, 81) | ACS IT and leadership | before B6.16 and B9.2 to B9.4 run for the real tenant | the mirror, the connectors and the engagement domain | `core.school_module 'engagement'`; the source-system rows with `CONNECTOR_VERIFIED` |
| **G26** | **ADEK's consent under School Digital Policy 7.1.3.a** before a contractor shares personal data inside or outside the country (F04): first ask ACS how it has handled 7.1.3.a with Veracross and ManageBac (question 149), then ACS submits the request with CAROS's evidence pack (question 150); counsel says whether in-country Microsoft hosting is itself "sharing" (C25) | ACS, to ADEK; counsel | raised with ACS in week 1; if C25 says yes, submitted by 15 January 2027 and a G-REAL gate; otherwise before G-AI | G-AI; any EU cold copy; mentor video (Digital Policy 6.4, decision C6); **G-REAL if counsel says in-country hosting is sharing** | `ai.provider_account.adek_consent_ref`; the decisions log |
| **G27** | **The data protection impact assessment** (PDPL Article 21), drafted by CAROS for ACS as controller, accepted by the school (F37) | Davide; ACS DPO | draft by 29 January 2027; accepted by 19 March 2027 | G-REAL (pass 4 §9.13) | the DPIA in the DPA folder |

## 9. Pilot-ready checklists

What each of the three people at the school would need satisfied before real students are in the system. Every line maps to a gate above, a test in §5 or a runbook in §7.7; the lists are meant to be handed over as they stand.

### 9.1 The IT team

- The DPA signed with its annexes, including the ADEK Digital Policy 7.1.3.a non-disclosure clause and the annex of school-side responsibilities (exports and screenshots taken by school staff, device policy); the sub-processor list (Azure in UAE North and UAE Central with Customer Lockbox, Azure Communication Services Email, Azure Monitor action groups carrying counts only, Anthropic under ZDR for the AI features only and only after ADEK's consent, GitHub for code, and the AI coding assistant if counsel says so under C23); the data sources named with their hosting (pass 4 §1.3).
- The hosting statement: all student data at rest in UAE North with in-country redundancy in UAE Central, customer-managed keys in Key Vault, 35-day point-in-time restore, weekly vaulted backups; the cold-copy position as decided (in-country only, or an EU copy with keys held abroad and ADEK's consent); whether in-country Microsoft hosting needed ADEK's consent (counsel's C25) and, if so, the consent.
- The Google Workspace configuration done and recorded: CAROS Limited for the staff and student units; under-18 designation confirmed; the Directory scope approved on the delegated reader account and the hourly staff read running; each domain mapped on DNS proof; 2-Step Verification enforced on their side (question 112); the leaver runbook's "revoke in CAROS" step added to their own process.
- The Chrome policy line for staff devices (enhanced spell check and grammar extensions off for CAROS, F76), and that CAROS loads no fonts or scripts from third parties.
- The security review pack by the regulator profile's checklist (ADEK 5.5.1 a to h), with their chosen questionnaire answered; the ASVS 5.0 Level 2 self-assessment with links to the passing tests; the penetration test report, run against production before it held a real record, with highs closed; the SOC 2 timeline in writing; the data protection impact assessment.
- The incident process and contacts (24-hour notification to the school's named contacts, updates every 24 hours, the evidence pack, the PDPL 9(2) and ADEK 6.6.1 conflict flagged for the school's decision each time); the tabletop held.
- The restore drill record and the sweep-completion metric definition; the uptime checks; the maintenance window.
- The delivery path tested end to end: upload, the Data Export Package over password SFTP, or SFTP by key; the weekly pack's seven files run once; the first four previews to be approved by their `school_admin`, with a second `school_admin` approving every access-changing batch; the cadence panel showing "on time" and the reason coverage.
- The support-grant path exercised once with them, so they have seen that CAROS cannot read their data without their approval, that each grant names its operator, tables and location, and that every read is logged and visible to them.
- The retention schedule as configured, the SAR and erasure procedures, the offboarding procedure with key destruction and the destruction certificate.
- The two `school_admin` holders: the first named in the DPA with its signatory, the second confirmed by the signatory; four eyes on every capability grant and nobody granting themselves.
- The change-freeze windows agreed (question 142) and the deploy calendar's respect for them.

### 9.2 The Child Protection Officer

- The safeguarding route configured with at least two recipients and oversight, co-signed, with the clock (continuous or school hours) and the SLA chosen (question 119); the **category routes** for student safety alerts (who is told at once for suicidal ideation or severe substance use, for maltreatment, for self-harm) confirmed by them (question 151); a test referral raised, delivered, acknowledged and closed by them.
- The referral email reviewed: a reference, the raiser's role, the time and a link, and nothing about the child and no person's name (question 120).
- The referral form's fields and minimums matched to their policy (question 60); the outcome vocabulary matched to how they record outcomes (question 121); **each reporter's own 24-hour countdown shown from the moment of escalation**, labelled as the reporter's duty, with the direct-report route beside it from the start; `immediate_risk = yes` showing 999 and the Principal; never a claim that CAROS reported; no clause numbers on screen.
- The direct-report contacts confirmed as the school's own list, each with a last-verified date: the Ministry of Education's Child Protection Unit line 80085, the Family Care Authority 800444, Police 999.
- **Teacher referrals**: a teacher's safeguarding flag reaches the route as a referral, and the flag modal tells every teacher to report a disclosure to the coordinator at once; the lexicon also reads teacher flag text.
- Who sees an escalation, and for how long (recipients while open and 90 days after; oversight sees existence and status only; support never without their separate approval); the subject-access default that excludes anything linked to an escalation, and requests about an escalated student routed to them (decision C4).
- The teacher-identity rule: a reporting teacher is identified to the CPO inside CAROS and redacted from every family-facing output (Wadeema Article 44).
- The student's "I need to talk to someone" channel and the fixed message a student sees on a safety flag, written by the school, with the helplines split into "someone to talk to now" (24/7 lines only) and "to report that a child is being harmed", each with hours and a verified date, confirmed by them before any student sees it (question 129); the reference-only "alert unopened" notice they receive when a counselor has not opened an alert, which is never an automatic referral (F12); the screen is a lexicon with no model, and every hit reaches a person.
- Their acceptance that shadow mode records inferences nobody acts on for about ten school weeks, and who at the school may see those records (question 105).
- The mentor programme's vetting policy if and when mentors are enabled (question 122); that sessions are in person on school premises only and never recorded by CAROS.
- After one term of the lexicon: its false-positive rate by entry, and their view on whether an add-only model layer should be proposed (B7.9).

### 9.3 The counselor

- The elicitation session done and the first thresholds written from their answers, with every departure from the default traced to something they said; a copy of the mapping; the two G-LIVE floors they named, pre-registered.
- Their caseload exactly right: each counselor sees their own students, the cover policy chosen (questions 65, 66, 127), the `caseload_lead` named; narrow-only visibility until they answer question 65.
- What the screen shows during shadow mode: teacher flags and referrals live, no tiers; the thirty-second daily log and the Friday prompt; the reveal sessions at shadow weeks 4 and 8 booked, where they judge engine cases mixed blind with matched students; the go-live bar as pooled, pre-registered intervals (at least 40 judged cases), not a point threshold.
- Training on the synthetic ACS tenant on staging, with the demonstration marker visible, before they touch the real one (question 144); the morning ritual rehearsed after Demo 1: sign in, the sheet, "what changed overnight", open a case, accept, intervene, note, escalate.
- What parents and students can and cannot see, rendered from the matrix inside their own notice; that a subject-access pack excludes their notes by default and the school includes an item only as an explicit act (decision C4, question 128).
- The escalation rehearsed with the CPO's test referral: the form, the step-up, their own reporting countdown, what the CPO receives, what acknowledgement means.
- The sweep-failed banner's meaning ("tiers are as of the sweep of …") and the morning contact channel (question 147).
- Later, for the AI features: their twenty questions each, rewritten onto synthetic personas in the tenant, and the 30-item reviews, and the knowledge that every AI output is labelled, cites its records, never ranks, and never sends anything outside the platform.

---

## 10. Risks, ranked

Likelihood and impact are judgements, not measurements. Ranked by expected cost to the pilot. Numbers are stable: R19 to R21 are new in pass 8.

| # | Risk | Likelihood | Impact | Mitigation | Owner, when |
|---|---|---|---|---|---|
| R19 | **The April 2027 date does not hold**: one builder at eight sessions a week has a margin of about a tenth, and the external gates (DPA, pen test, IT review, possibly ADEK) have lead times CAROS does not control | medium to high | high: a school year's delay to live use | the committed cuts; the contingent cuts in order; three checkpoints with the August 2027 fallback named in advance and told to ACS at the checkpoint, not later (§2.3) | Davide, CP1 to CP3 |
| R20 | **ADEK's consent under Digital Policy 7.1.3.a** is needed before G-REAL (if counsel says in-country Microsoft hosting is "sharing") or before G-AI, and ADEK's response time is unknown | medium | high if before G-REAL; medium if before G-AI | ask ACS in week one how it handled 7.1.3.a with Veracross and ManageBac (question 149); counsel's C25 by 27 November 2026; the request and evidence pack ready by 15 January 2027 (G26) | Davide, ACS, counsel |
| R1 | ACS can only deliver termly data, so the academic, attendance and behaviour domains run in retrospective mode and the morning ritual is driven by teacher flags and the university rules alone | medium | high: the differentiator is not demonstrated on real data | the weekly pack as a condition of the pilot agreement, Veracross's Data Export Package as its first delivery path, the retrospective mode named as the fallback (pass 2 §6.4); the mode switch is data, not a deploy | Davide, by CP2 (G17) |
| R2 | The engine's precision on real data is unknown until shadow mode, and the detuned pilot defaults are quiet and slow (pass 3 §11.1: few series-driven check-ins; §7.2: 8 of the 15 authored tiers reproduce), so the pre-registered floors are not met in one term | medium | high: no go-live, or a go-live nobody trusts | shadow mode on the live lifecycle with twenty weeks of history; the blinded judgement against matched controls; the reveal protocol; the levers of pass 3 §11.1 in order; the backtest if history exists; shadow extended to the pre-registered maximum rather than the bar lowered | Davide and the counselors, shadow weeks 4 to 10 |
| R7 | Key-person risk: Davide holds every stream, the infrastructure, the external gates and the on-call | high | high | runbooks tested by someone who did not write them; the paging escalation job with a named second person; the plan file and briefs written so anyone can pick up any task; Fable 5.1 as a second reader of the critical code; the trigger that staffs the streams (§2.5) | Davide, continuous; reviewed at each checkpoint |
| R3 | The legal basis, the consent procedures or ADEK Records 1.5.1 permission are not settled, and the tenant cannot leave `onboarding` | medium | high: no real data | counsel engaged in week one on C26 and C13; the attestation mechanism supports both the provider-list and the per-family notice; the trigger makes the gate impossible to skip rather than easy to forget | Davide and counsel, by T minus 7 weeks (G8, G9) |
| R21 | **Usage limits**, not money, cap the build: the ZDR organisation's rate-limit tier for Opus 5.5 sessions and the review account's weekly limit for Fable 5.1 at max effort | medium | medium: the checkpoints slip | raise the organisation's tier in week one; measure both rates from week one; a week that hits a limit is recorded; Fable reviews are batched and never block more than one task at a time | Davide, from week one |
| R4 | Google Workspace is not configured for under-18 users, or the school's edition lacks the control, and students cannot sign in | medium | medium: student surfaces wait; staff surfaces unaffected | the configuration as an onboarding gate with a written checklist; question 141 on the edition; students have nothing to see before B5 anyway | ACS IT, onboarding step 4 (G10) |
| R8 | The silent overwrite or the drifting documentation happen again | medium | high | the ruleset with an empty bypass list; the wholesale-replacement check; immutable migrations; generated docs, including §4 from the task tables; `docs:verify`; the phase-end rewrite of `CLAUDE.md`; worktrees per session | Davide, B0.3 and B0.14 |
| R9 | The frontend port drops a rule the prototype enforced | medium | medium to high (invariant 6 is such a rule) | the rule ledger before every screen; rules in the domain with tests; Fable 5.1 review of the gate ports; both tenants on every screen | every port |
| R10 | The penetration test, run against production in February, finds an authorization hole late, delaying G-REAL | medium | medium | the RLS and matrix suites from B0; the test booked by CP2 with a retest window; authorization findings fixed at max effort with a Fable review | Davide, CP2 to G-REAL (G12) |
| R5 | Anthropic does not grant ZDR for the product's organisation, or slowly | medium | low for the pilot, high for the AI features | the pilot runs with the AI off by design; the deterministic modes exist for every feature; open decision O55 on the fallback routes | Davide, from week one (G5) |
| R11 | Teachers do not log concerns, and the corroboration rule has nothing to corroborate | medium | medium | the register leads the teacher surface; the 90-second flow at 375px; the routed phrase closes the loop within the school day; the per-teacher rate report for the lead | the caseload lead, from shadow week 1 |
| R6 | A country-level event destroys both UAE regions | low | catastrophic | the real choice put to ACS and counsel (G22): an EU copy with key material held abroad, a named restore region and an EU drill, with ADEK's consent, or in-country only with the risk stated in the DPA; the Oracle fallback in the infrastructure module (pass 1 DR-1) | ACS leadership and counsel, before the DPA |
| R18 | The synthetic ACS tenant, shown under the school's name, is mistaken for real data in a screenshot, or shows a real staff member's name | low | high: reputational and legal | the marker as a generated column bound to the letterhead; the written consent naming sales demos (G2); a fictional coordinator and the real-names blocklist (F29); separate tenants for demo and real; the classification and purpose triggers | Davide, B0 and every demo |
| R12 | The ManageBac mirror does not cover what the coordinator needs (no CAS reflection text, no TOK), or Maia stays the record for university lists and the CAROS module duplicates work | medium | medium | ManageBac stays the record with a read-only v2p3 mirror (C10) and the counselor views it lacks; the Maia half of the decision stays open (O83); the modules can be off | the IB coordinator and counselors, at onboarding (questions 9, 78, 163) |
| R13 | An Extended Essay round is tracked under the wrong guide | low | medium | `ee_2027` by default for both current ACS cohorts, `ee_2018` for resits only; the coordinator confirms the guide, the word limits and the marks per round (G19) | the coordinator, B6 |
| R14 | The complete schema tempts the build to migrate everything early, and the first restore drill takes hours | medium | low | the migration map as the single source; a phase never creates a table it does not read and test | Davide, every migration review |
| R15 | Tooling churn: Vitest 5 is weeks old, Drizzle 1.0 is a release candidate, Opus 5.5 was days old at planning time | medium | low to medium | pins; upgrades as decision records after two GA minor releases; the model registry and the promotion protocol; the cooldown on every dependency | Davide, B9.8 |
| R16 | No second regular builder joins in 2027-28, and B5 to B9 stretch over years | medium | medium | the sequence of §2.5; the trigger for the full process; B5 and B6 timed to the school year's needs (the application deadlines, the selection round) | Davide, reviewed after G-LIVE |
| R17 | The AI features' prose quality disappoints even when the bars pass, because the bars measure honesty, not charm | medium | low | the human sample is the shipping gate; the effort lever tuned on the suites; the counselors write the questions | Davide and the counselors, B7 |

---

## 11. Challenges

**Decisions recorded 2026-09-24 (Davide).** All four positions below are accepted. Passes 7 and 8 plan on them and do not reopen them. Davide's decisions on the pass 7 review then changed three of this pass's own positions: **F39** set the target (G-REAL in the first fortnight of the spring term 2027, Davide building alone, §2.3) and corrected C2's backfill wording; **C7** replaced this pass's C3 (Opus 5.5 under the ZDR organisation writes every task, Fable 5.1 reviews diffs, the B0 bake-off); **C8** replaced the four-stream process with solo mode until a second regular builder joins (§2.4, §2.5).

- **C2 · Pilot timing.** Accepted: G-REAL aims at the first fortnight of a term. Open decision O33 is closed, and F39 names the term: the spring term 2027. **Corrected by F39:** the first version said earlier weeks go to the historical backfill and the backtest, but the history is real data and needs G-REAL first; weeks before G-REAL go to onboarding steps 1 to 6, the elicitation and the CPO's set-up, and the history import follows G-REAL (§1.11).
- **C4 · Sales demo.** Accepted: from Demo 1, sales demos use the Next.js staging build on the synthetic ACS tenant, on a pinned demo revision with a deploy freeze during booked demos (F43); the Vercel prototype stays deployed as the specification only. Open decision O11 is closed. Under the April plan Demo 1 falls during shadow mode, so the prototype remains the sales demo until then.
- **C5 · Penetration test target.** Accepted: the external test runs against production before it holds a real record. Open decision O35 is closed. **Refined by F41:** application-layer fixes are retested on staging, production-layer fixes (WAF, TLS, network, keys) on production while it still holds only the canary tenants; yearly tests after G-REAL follow written rules of engagement (B4.14).
- **C6 · Canary tenants in production.** Accepted: production holds two synthetic, unbranded canary tenants for the isolation canary and the nightly sweep path, excluded from every report. Pass 8 amended pass 1 DR-9 and made the exclusion a mechanism: an immutable `core.school.purpose`, and the canary as a Container Apps job in UAE North (F42).

Each names the decision or the brief it touches, the alternative, its cost and benefit, and what this pass planned on.

**C1 · The thin slice is four phases with additions.** *The brief's proposed first phase.* Argued in §1.13. Planned on the split and the five additions; nothing from the proposed list is dropped.

**C2 · "No fixed deadline" meets a school calendar.** *CONTEXT §2's "optimise for long-term correctness over speed to first demo".* Every phase still ends in tests rather than dates. But the pilot has a calendar the decision did not price: shadow mode needs weeks of weekly data in term time, and a shadow period that straddles a long holiday is dead time. The first version planned on the build's own pace and recommended aiming G-REAL at the first fortnight of a term. **Superseded by F39:** Davide chose the term, the spring term 2027, and §2.3 prices it: the cuts, the checkpoints, and August 2027 as the fallback. The first version's advice to spend early weeks on the historical backfill was wrong, because the backfill is real data and needs G-REAL first.

**C3 · The tool that builds the safeguards is a Covered Model.** *Pass 4's rule R2 and the way this plan is executed.* The first version planned on Fable 5.1 writing the reserved tasks, on the argument that an agent session holds no student data. **Superseded by decision C7:** Opus 5.5 under the zero-data-retention organisation writes every task and Fable 5.1 reviews diffs of code and synthetic data only, so that if real data ever did reach a build session (F23 found routes the first version had not designed out), no Covered Model would retain it; the B0 bake-off tests whether that costs quality in the categories Fable was reserved for, and moves a category back only where Fable is clearly better. The discipline stays: synthetic data only in the repository, hooks that refuse reads outside the repository and commands against production, a blocklist of every real name, no support grant or production credential on any machine that runs an agent. Counsel's question C23, revised, asks whether the coding assistant is a sub-processor the DPA must name.

**C4 · From Demo 1, the sales demo should be the Next.js build, not the frozen prototype.** *CONTEXT §3 "index.html stays deployed on Vercel for demos".* The prototype shows nine things the backend will not do (§6.3). Until Demo 1 exists it is the only demo and the script must skip those lines; from Demo 1, demonstrating it to a school describes a product that will not ship. The alternative, keeping the prototype as the sales demo, costs a false promise at every showing; switching costs nothing in the build. Recommendation, not a departure: after Demo 1 passes, sales demos use the staging build on the synthetic ACS tenant, and the Vercel deployment stays up as the specification. Davide decides (§13).

**C5 · The penetration test targets production, not staging.** *Pass 4 §9.12 d and pass 1 DR-9's staging without a WAF.* A test against staging tests no WAF, no production network and no production key configuration. Planned on: production exists from B4 and holds only the canary tenants until G-REAL, so the external test runs against production before the first real record, and retests run against staging. The cost is that production is provisioned a few weeks earlier than the first real record needs; it buys a test of the thing that will be attacked.

**C6 · Two synthetic tenants live in production.** *Pass 1 DR-9's "real tenants only".* Pass 4's nightly isolation canary needs them, and the sweep path should run in production before it runs for a child. Planned on a canary pair, unbranded, with `purpose = 'canary'`, excluded from every report by that column; pass 8 amended DR-9.

**C7 · The model and review rule is written into the plan file, not left to the session.** *The brief's "mark which work must be reviewed by a different model".* A session cannot judge whether its own task is subtle. Planned on naming the tasks in the Model and Rev columns and generating §4 from them (F38), carrying the model and the reviewer's verdict on the task line and the pull request template, so a wrong model or a missing review is visible. The cost is some rigidity; it buys an audit trail of which model wrote and reviewed what, which a school's reviewer may one day ask for.

---

## 12. For the revision pass

Inconsistencies between passes 1 to 5 that the first version of this pass planned around. Pass 7 checked all eighteen (its A.5) and Davide accepted each; **pass 8 resolved every one**. Each item keeps its first-version text, followed by how it was resolved and where (the changelog has the detail).

1. **Sweep job names and the watchdog hour.** Pass 1 DR-2 has `sweep.tick` and a watchdog at 06:00; pass 3 §13.2 has `sweep.schedule` and 05:45. Planned on pass 3. *Resolved in pass 1 (session 1): `sweep.schedule` and 05:45 everywhere; pass 2's job names aligned in session 3.*
2. **`caros_owner` cannot log in.** Pass 1 creates it `NOLOGIN` while DR-4 has migrations run as it. Planned on a `caros_migrator LOGIN` role that runs `SET ROLE caros_owner` (B0.5). *Resolved in pass 1 (session 1), inside the per-deployable login roles of F01.*
3. **Loose ends in pass 1's DDL.** `core.touch_updated_at()` attached to nothing; `audit.entry` and `events.event` without a school foreign key; the settings comment. *Resolved in pass 1 (session 1).*
4. **Pass 2's onboarding contradicts pass 4 twice** (`sso_jit`; who registers the OAuth client). Planned on pass 4. *Resolved in pass 2 (session 2), including Google's under-18 rule.*
5. **Pass 2's internal counts** (six against seven saved queries; two against three retrospective domains; §7.8 for §7.9; `const`). *Resolved in pass 2 (session 2).*
6. **Pass 4's widening rule is stated three ways.** The first version planned on §3.3's reading while B0.7 tested §3.1's (F47). *Resolved by the decision on F47: narrow-only by default; `allow_counselor_widening` as the ready option; pass 4 §3.1, D36, B0.7 and §5.4 now say the same (session 1 and this revision).*
7. **Column names differ between body and D-table in pass 4.** *Resolved in pass 4 (session 1).*
8. **Feature keys differ between passes 1, 4 and 5.** *Resolved in passes 1 and 5 (sessions 1 and 2): one list, without `headline_rephrase`, `safety_screen` or `letter_draft`.*
9. **`covered_model` and `zdr_eligible` moved** to pass 5's `ai.model`. *Resolved in pass 4 (session 1): R2 points at the registry.*
10. **Production holds two synthetic canary tenants** against pass 1 DR-9. *Resolved in pass 1 (session 1) with `core.school.purpose` (F42).*
11. **The narrative seed layer and `strength`.** *Resolved in pass 1 (session 1): no `strength` in any seed; the narrative layer written against pass 3's schema.*
12. **The Extended Essay's 2027 guide.** *Resolved in passes 1 and 5 (sessions 1 and 2): rounds name their guide, `ee_2027` by default for both current ACS cohorts (F62).*
13. **ADEK Student Protection Policy section numbers.** *Resolved in pass 4 (session 1): re-cited as v1.1; no clause numbers in UI text (F55).*
14. **Google verification is unstated in pass 4.** *Resolved in pass 4 (session 1), with RISC dropped (F18) and step-up by `auth_time` (F19).*
15. **Blob SFTP in UAE North** stays a portal check. *Resolved in pass 2 (session 2), with the $0.30 an hour cost and Veracross's password-only path.*
16. **Audit action keys** are one list. *Resolved in passes 1 to 5 (sessions 1 to 3): `PREVIEW_ACCESS` and `sweep.completed` seeded once; `allowed_tiers` on every key.*
17. **Reveal weeks.** The pilot agreement counts from shadow's first sweep. *Resolved in pass 3 (session 2) and §1.11 (this revision), counting from the first nightly sweep after the caseload lead enables it.*
18. **D15 is subsumed; D11, D56, D58 and D60 land with their tables.** *Resolved by F40: §1.12 is the single source; passes 2 to 5 record the phase each delta lands in (sessions 2 and 3).*

---
## 13. Consolidated open decisions

Every open decision from passes 1 to 5, plus this pass's own, deduplicated and ordered by what blocks what, as they stand after pass 8's three sessions. The source column gives the pass and its decision number (`P3#4` is pass 3's fourth); ACS questions and counsel questions are cited where the decision waits on them. O-numbers are stable: a closed decision keeps its number in the closed list, and new decisions are numbered from O87. `README.md` in this folder carries the same list in brief.

**Tier A · blocks B0 and the first commits. Davide decides, mostly in week one.**

| # | Decision (source) | Options | Recommendation | Who decides |
|---|---|---|---|---|
| O1 | Infrastructure tool (P1#5) | Terraform/OpenTofu with `azurerm`; Bicep | Terraform/OpenTofu | Davide |
| O2 | RPC layer (P1#4) | tRPC 11; oRPC | tRPC; Hono with zod-openapi for the connector API when a non-TypeScript caller exists | Davide |
| O3 | PostgreSQL major (P1#6) | 17; 18 | 17 at pilot; 18 at year 1 once the extension list is clean | Davide |
| O4 | Test runner (P1#18) | Vitest 5; Vitest 4.1 | 5 | Davide |
| O5 | Second synthetic school's name and regulator (P1#14; Q69) | Wellesmere (KHDA) after a directory check; a Sharjah school | Wellesmere, checked | Davide |
| O6 | Repository plan (pass 6) | GitHub Team; Free with weaker protections | Team, for rulesets and environments on a private repository; code-owner review is enforced only when the full process switches on (C8, §2.5) | Davide |
| O8 | XLSX at pilot (P2#13) | accept, read as text; CSV only | accept | Davide |
| O9 | Delivery mechanism and the SFTP implementation (P2#2, P2#17; F54, F71) | Studio upload; Veracross's Data Export Package over password SFTP with IP allow-listing and 90-day rotation; Blob SFTP by key; a container | upload for the first four weeks; the Data Export Package if ACS licenses it; Blob SFTP ($0.30 an hour per account) if UAE North shows it in the portal, else the container | Davide, ACS IT |
| O10 | Grace period before a withdrawn person's status becomes `left` (P2#14) | 0, 14, 30 days | 14 | Davide |
| O13 | Frozen copies of the passes in the backend repository (pass 6 §3.6) | `docs/spec/` copies with the commit hash; links to the prototype repository | copies | Davide |
| O14 | Production maintenance window (pass 6 §7.2) | custom, Saturday 09:00 local; system-managed | custom | Davide |
| O97 | The fictional coordinator's name, checked against ACS's staff directory (P4#22; F29) | any fictional name; one checked | checked before the seed is shown | Davide |
| O103 | The bake-off verdict: which categories of work, if any, move back to Fable 5.1 as writer (C7) | none; named categories | decided by B0.16's tests and cross-review; Opus 5.5 by default | Davide |
| O104 | The account that runs Fable 5.1 reviews, and the ZDR organisation's usage tier for build sessions (C7, F39) | a separate API organisation; a subscription | whichever gives four max-effort review sessions a week; recorded in week one | Davide |
| O105 | Whether the frozen prototype and PRODUCT.md change: a fictional coordinator name in `index.html` (F29) and PRODUCT.md's "three mandatory reflection sessions" (F62) | leave both; change both | change both, as the decisions ask; the prototype is frozen, so it needs Davide's explicit go-ahead | Davide |

**Tier B · blocks G-REAL, the first real record (target April 2027; the latest dates are §2.3's). ACS leadership and DPO, counsel, the CPO and IT, with Davide.**

| # | Decision (source) | Options | Recommendation | Who decides |
|---|---|---|---|---|
| O15 | Out-of-country cold copy of backups (P1#1, P4#1; Q62; counsel C12, C25; F22, F04) | in-country only, the country-level risk stated in the DPA; EU ciphertext with key material held outside the UAE by named people, a named restore region and trigger, an EU restore drill, and ADEK's consent | decide before the DPA is signed; the EU copy only if ACS, counsel and ADEK agree in writing | ACS leadership, counsel, ADEK, Davide |
| O16 | Every retention number, per-school overrides, the vaulted horizon and CAROS's tombstoned audit copy (P1#16, P4#7, P4#15, P4#16; Q33, Q64, Q116; counsel C9, C10, C17, C29) | as proposed; shorter; longer; 1, 3 or 5 yearly copies; keep 7 years or delete at termination | as proposed, 3 years; the audit copy pseudonymised and subject to counsel (Article 5(7), 8(4)); counsel sets the numbers | counsel |
| O17 | Legal basis for the core processing, the consent procedures and the ADEK Records 1.5.1 mechanism (pass 4 §1.5; Q109; counsel C1, C13, C26; F37) | the bases with the provider list in enrolment terms; consent; a mix per strand | no recommendation until counsel answers C26: the contract basis needs the data subject as a party, the legal-obligation basis covers federal law, not ADEK policy, and ADEK Digital 7.1.2 and Mental Health 3.6 require consent procedures | ACS DPO, counsel |
| O18 | Parent activation mode (P4#2; Q124) | out-of-band code; knowledge-based | codes | ACS, Davide |
| O19 | Escalation SLA, clock, the referral form's minimums and the category routes (P4#19; Q60, Q119, Q151; F09) | 120 minutes continuous; a school-hours clock; 40 and 20 characters or ACS's numbers; category routes by default from the regulator profile | 120 minutes continuous; 40 and 20 unless ACS says otherwise; the reporter's own countdown shown from escalation in every case; category routes confirmed by the CPO | ACS CPO |
| O22 | Break-glass cover parameters (P4#18; Q127; F16) | practising counselors only, two in seven days, the lead confirms the same school day or it lapses; other values | as decided; ACS may set stricter numbers | ACS counselors |
| O24 | The delivery cadence ACS commits to, and the history import (P2#1; Q70, Q73, Q83; F54, F24) | weekly pack (the Data Export Package first); nightly Data API; fortnightly; termly with the retrospective mode named | weekly for the pilot with at least twenty weeks of history before shadow; never termly without naming the mode in the agreement | ACS IT and registrar, Davide |
| O28 | Google Classroom as a source and the engagement domain (P1#10, P2#7, P3#2, P4#14; Q14, Q84) | off; on with pass 4 §6.6's seven conditions, with or without Classroom | off until ACS answers and approves the notice; then on with Classroom if accepted | ACS, pass 4's conditions |
| O30 | Certification order (P4#13) | SOC 2 Type I then II then ISO 27001; ISO first | SOC 2 first | Davide |
| O31 | Cyber-liability insurance (pass 4 §9.12 f; C16) | bind before G-REAL; later | before G-REAL | Davide |
| O36 | Who may author and activate profiles after onboarding (P2#10; F35) | CAROS only; `school_admin` with CAROS review of a new import kind for a year | the school's `school_admin` activates; CAROS authors only under a school-approved write grant and reviews, never writes, a new import kind for the first year | Davide |
| O100 | A student with no date of birth under the under-13 check (pass 2 §2.5; Q156; F56) | treated as possibly under 13; treated as 13 or over | possibly under 13, a cautious reading Davide may relax once counsel answers C32 | Davide, counsel |
| O101 | The minimum Workspace administrator role for the delegated reader account (Q110; F18, F74) | a custom role with directory read; a domain administrator | the narrowest role that passes the Directory read and, for Classroom, a teacher of each course or a domain administrator; confirmed with ACS IT | ACS IT |
| O106 | Who confirms the second `school_admin` if the DPA signatory has no CAROS account (pass 4 §2.5) | written confirmation recorded under the bootstrap grant; an account for the signatory | written confirmation recorded on the grant | Davide, ACS |

**Tier B2 · blocks the first shadow sweep. ACS counselors and registrar, with Davide.**

| # | Decision (source) | Options | Recommendation | Who decides |
|---|---|---|---|---|
| O32 | Who activates a threshold version in shadow (P3#15) | caseload lead with the engineer's acknowledgement; any counselor; the engineer | the lead with the acknowledgement | Davide, ACS counselors |
| O37 | Event-triggered evaluation for flags, context and enrolment changes (P3#1; Q67) | nightly only; the hybrid with the daytime asymmetry | the hybrid; shadow includes event runs (F24), so it is set before the first shadow sweep | Davide; ACS counselors on whether daytime raises are welcome |
| O38 | Commit-triggered evaluation (P2#12, P3#8) | never; the scheduled pack only; every commit | the scheduled pack only during shadow; per school policy after | Davide, ACS counselors |
| O41 | The engine may propose `urgent` for the corroboration pattern (P3#6; Q89, Q104) | proposes; stops at `checkin` with a route flag | proposes | ACS CPO and counselors |
| O43 | `shock_opens_monitor` (P3#9) | true; false | true, measured in shadow | ACS counselors |
| O44 | Grade 8 history for Grade 9 entrants (P3#10; Q99) | import from the same SIS; do not | import if ACS exports it | ACS registrar |
| O46 | The counselor log's cadence (P3#13; Q96) | daily; weekly | daily, thirty seconds | ACS counselors |
| O47 | The shadow-exit bar (P3#14; Q92, Q93, Q108; F24, C9) | pre-registered floors on the one-sided 95% lower bound of a pooled, blindly judged precision proxy, primary at `review` or above; the counselors' own floors | defaults 25% at `review` or above and 50% at `checkin` or above, at least 40 judged cases, lift over matched unflagged students; the counselors name the floors in the elicitation | ACS counselors |
| O49 | Recovery weeks (P3#17; Q98) | 2 to 6 | 3 | ACS counselors |
| O50 | Whether a risk-adding context may ever lower a tier (P3#18; Q91) | never; the school's choice per kind | never | ACS counselors and CPO |
| O52 | Case lifecycle defaults (P1#11) | 90-day window, 14-day auto-review, 02:00 sweep; others | the prototype's numbers as version 1 | ACS counselors |
| O90 | Whether relapse fires at weak while attendance is held (P3#21) | yes; no, `relapse_watch` opens a `monitor` case | `relapse_watch` (elicitation card 11) | ACS counselors |
| O91 | When attendance is released from weak (P3#22; Q157) | a measured reason-code coverage of 90%; another bar | 90%, cited in a configuration version | ACS registrar, counselors |
| O92 | The planning constants behind the floors (P3#23; Q159) | `p_typ = 0.05` and the other constants as assumed; values measured from ACS's history | measure from the history import before the first shadow sweep; never computed from other students | Davide, after the history import |
| O94 | The longest shadow period if 40 cases are not judged (P3#25; Q162) | one term; the school year; another | one term, pre-registered | ACS counselors, leadership |

**Tier C · blocks G-LIVE, the shadow exit. ACS counselors and the CPO, with Davide.**

| # | Decision (source) | Options | Recommendation | Who decides |
|---|---|---|---|---|
| O39 | Relapse uplift (P3#4; Q90) | 0, 1, 2 tiers | 1 | ACS counselors |
| O42 | Threshold scope (P3#7) | per school; per-counselor overrides | per school; revisit after the elicitation | Davide |
| O45 | Inheritance across a level change (P3#11) | off; on | off | ACS coordinator |
| O48 | The retrospective and termly-backfill tier cap (P3#16; F25, F31) | review; monitor | review, and it beats relapse's minimum when all fresh evidence is termly | ACS counselors |
| O51 | The scale estimator (P3#19) | MAD; Qn if the backtest shows lead time lost | MAD | Davide, after the backtest |
| O54 | Widening counselor visibility (P1#8; Q65, Q66; F47) | narrow-only, caseload plus cover; `allow_counselor_widening`, set by CAROS after the school asks, widening counselor `signal` and `case_note` reads to school scope, never `safeguarding` | narrow-only until ACS answers question 65 | ACS counselors |
| O93 | Going live with shadow case state (P3#24) | live cases fresh, linked to shadow history; carry shadow cases over | fresh, linked | Davide, ACS counselors |
| O102 | Whether the sales and demo narrative follows the engine's generated tiers (8 of 15 match) or the counselors' elicitation retunes the defaults first (F06) | the engine's stories as they are; retune first | the engine's stories as they are, told honestly; retuning is the counselors' act in shadow | Davide |

**Tier D · blocks G-AI, per feature. Davide, the ACS DPO, counsel, ADEK, the CPO and the counselors.**

| # | Decision (source) | Options | Recommendation | Who decides |
|---|---|---|---|---|
| O55 | If Anthropic does not grant ZDR, or not in time (P5#2) | run with the AI off; Enterprise Frontier Safeguards when available; an in-country route | off, because shadow mode runs AI-off anyway; the others only with counsel | Davide |
| O56 | Provider and inference location (P5#1; Q138; C3, C21; F04) | Claude API, US-pinned ZDR; an in-country route for another provider's models after evaluation | the Claude API for v1, with ADEK's consent; the year-1 review with counsel's answers and the eval scores | Davide, ACS DPO, counsel, ADEK |
| O57 | Legal basis for the AI transfer (P4#4; C3, C18, C33; F37, F04) | a contract drafted to PDPL Article 23; express custodian consent per family; necessity | no recommendation until counsel answers C3: EU-style standard clauses do not meet Article 23; ADEK's consent is needed in any case | ACS DPO, counsel |
| O58 | AI features during shadow mode (P4#5) | off; on for staff-only features | off | Davide, ACS |
| O59 | Default models (P5#3) | Opus 5.5 writes, Sonnet 5 classifies; Sonnet 5 everywhere; Opus 5 anywhere | as designed; revisit when Sonnet 5.5 and Haiku 5.5 pass the promotion protocol | Davide, after the first eval runs |
| O60 | Covered Models after Enterprise Frontier Safeguards (P4#3, P5#19) | keep the exclusion; re-evaluate when EFS documents where retained data lives | keep; a dated review when EFS is generally available | Davide |
| O61 | Counselor note bodies in meeting briefs (P4#6, P5#11) | excluded; included by school policy | excluded | ACS counselors, DPO |
| O63 | Discovery conversation shape (P5#5) | the scripted bank with model phrasing; free-form | scripted | Davide, ACS counselors |
| O65 | Envelope delivery to the model (P5#7) | a JSON user turn; a read-only tool result | measure both in the AI phase's first week; keep the better | Davide |
| O66 | Refusal fallback mechanism (P5#8) | client-side to a named row; the server-side array form | client-side for v1 | Davide |
| O67 | Embeddings (P5#9) | none; pgvector in UAE North for lookup only | none until a measured need | Davide |
| O69 | The parent draft's language (P5#12; Q131) | bilingual pair with English as the record; target language with a translator; English only | bilingual pair | ACS |
| O70 | What the student is told about AI (P5#13) | the plain-language label and the "your counselor can read this" line; a longer notice | as designed, inside the student notice | ACS DPO |
| O71 | Eval labelling by the school (P5#14; Q132) | the school labels, paid or in kind; CAROS alone | the school labels | ACS leadership |
| O72 | The closest-fit margin and minimum tags (P5#15; F51) | 2 points and 3 tags; other values | as assumed until the discovery eval tunes them; the key is `discovery.closest_fit_margin` and "match" never appears | Davide |
| O73 | The alumni precedent's small-cell threshold (P5#16) | 5; 10 | 5 | ACS DPO |
| O74 | Daily AI cost ceiling per school (P5#17; F70) | $10 at pilot; another figure | $10 at pilot, three times the expected daily spend from the second school | Davide |
| O75 | Activation order of the features (P5#18; Q137) | pass 5 §9.6's order; the school's own | pass 5's order | ACS |
| O76 | UK course-level offer rates (P5#20) | none, university pages only; buy the UCAS Courses Data Service | none for the pilot | Davide |
| O95 | How far C1's "no model reads student text" reaches (P4#20) | the safety screen only; every feature that reads student text | the safety screen only; discovery, pathway analysis and EE feedback still send student text under R8 | Davide |
| O98 | Extended Essay feedback where ManageBac holds the essay (P5#21; Q163) | off; on the rationale only; on the draft through the mirror | decide with the coordinator after B6.16 | ACS IB coordinator, Davide |
| O99 | Thinking allowances per AI feature (P5#22; F70) | the assumed allowances; measured values | measured in B7.5 and written into `ai.model_config` | Davide |

**Tier E · blocks the module builds (B5, B6, B8), year 1 and later. Davide, with ACS.**

| # | Decision (source) | Options | Recommendation | Who decides |
|---|---|---|---|---|
| O79 | When to re-evaluate AWS `me-central-1` (P1#2) | never; when AWS resumes billing and publishes a restoration statement | the latter; the module keeps the option cheap | Davide |
| O80 | Built-in PgBouncer (P1#12) | never; at year 3 after the RLS suite passes under transaction pooling | year 3 | Davide |
| O81 | OneRoster profile family (P2#15) | in the thin slice; after the pilot | after the pilot | Davide |
| O82 | Veracross path for a connector (P2#16; Q81; F54) | the Data Export Package (no connector); Data API v3; API Plus | the Data Export Package for the weekly pack; the Data API later, with `verify()` refusing the student-alert and Health scopes; API Plus only if ACS already licenses it | Davide, ACS |
| O83 | Connector order (pass 2 §13; Q77, Q81) | Veracross, ManageBac rosters and grades, Classroom in the order ACS's answers dictate | decided after four green weeks; the ManageBac CAS and EE mirror is B6's (C10) and the Directory read B1's (F18) | Davide, ACS |
| O84 | In-country inference (P5#1 option c) | keep the US route; move more in-country | review at year 1 with counsel's C21 and the scores (moving the safety screen is moot: it has no model, C1) | Davide, ACS DPO, counsel |
| O85 | Letter engine generation (CONTEXT §3 deferred; C20) | after real letters exist to draft and time; never | after, and only once counsel answers C20 and voice samples exist with consent | Davide |
| O86 | Certification timing (pass 4 §9.12 c) | SOC 2 Type I at six months from the first real record; later | six months (assumption; the audit firm sets it) | Davide |
| O87 | Day rule where a school keeps no master register (P2#18) | `half_days`; `all_periods`; `majority_periods` | `half_days` for AM/PM registers, as Wellesmere's fixture | Davide; each school at onboarding |
| O88 | An attendance-reason family for a Dubai school (P2#19) | the KHDA's codes when a real Dubai school signs; the DfE family as a starting lookup | the school's own codes from its regulator profile | Davide, the second school |
| O89 | Extended Essay reflection text in the ManageBac mirror (P2#20) | dates only; the text | dates only unless the coordinator needs the text | ACS IB coordinator |
| O96 | The mentor invitation channel (P4#21) | a code handed over by the coordinator; a content-free email | the handed-over code | Davide, ACS |
| O53 | Who is in the Diploma cohort in the choosing year (P1#15; Q68) | every Grade 10 student; only those who declared | every Grade 10 at ACS, confirmed | ACS |

**Decided for now, reopened only by the named answer:** O20, subject-access defaults, decided by C4: professional records (notes, engine evaluations, AI outputs, anything linked to an escalation or safety alert) are excluded from the default pack and the school includes items one by one; reopens on counsel's C7 and C8. O21, parental access at 18, decided by C5: it continues by default and the student may object; reopens on question 148 or counsel's C6.

**Closed by pass 8 (2026-09-24), listed so they are not reopened:** O7 (strength and confidence removed; level, breadth and window shown); O11 (from Demo 1 the sales demo is the Next.js staging build on the synthetic ACS tenant, with a pinned demo revision and a deploy freeze, F43); O12 (Davide is the only builder; the streams are staffed when a second regular builder joins, C8); O23 (the Directory read is mandatory, hourly for staff and nightly for students; RISC dropped for Workspace accounts, F18); O25 (auto-approval for fact kinds only; access-changing kinds never auto-approve and need a second approver, F57); O26 (one person and one set of links per SIS contact, never merged, per-contact activation, F57); O27's CAS and Extended Essay half (ManageBac stays the record, a read-only v2p3 mirror built in B6, C10; the Maia half is now within O83); O29 (no fairness labels in the pilot, C11); O33 (G-REAL in the first fortnight of the spring term 2027, F39); O34 (train on staging; production holds only customer tenants and the canary pair, F42); O35 (the pen test runs against production before the first real record, retests split by layer, pass 6 decision C5 and F41); O40 (the common-cause cap withdrawn; only the operations count and the calendar-period prompt remain, F26); O62 (the reader has its own policy row and extracts lazily for three record kinds, F21); O64 (the safety screen is a lexicon only in v1, C1; the add-only layer is revisited after one term, B7.9); O68 (the headline rephrase is dropped from v1, F52); O77 (no SMS to anyone at a school in v1, C3); O78 (no mentor video in v1; in person on school premises, C6).

**Closed by an earlier pass:** WAF timing (P1#3) is "from the first real data", in detection mode from the day production exists (pass 4 §9.9); the session privilege model (P1#7) is one acting role per session (pass 4 §2.4); the audit hash chain (P1#13) is on, anchored weekly (pass 4 §4.2); `strength` and `contribution` (P1#17) are removed (pass 3 §9); the escalation reason (P1#9) is a structured form (pass 4 §5.3; the minimums are O19); counselor visibility (P1#8) is caseload plus explicit cover (pass 4 §3.3; the widening option is O54); staged-row and raw-file retention (P2#9) are 30 and 90 days under pass 4's `raw_import` class (pass 4 §7.1; pass 2's D14 withdrawn); rollback authority (P2#11) is the `school_admin`, or support under a school-approved write grant (pass 4 §3.6, F35); shadow-row retention (P3#20) follows live signals (pass 4 §7.1); mentor session recording by CAROS (P4#9) is never.

---
## 14. Consolidated questions for ACS, and for counsel

### 14.1 to 14.7 · Questions for ACS: now one list, in `ACS-IT-QUESTIONS.md`

The first version of this section consolidated questions 1 to 147 here, grouped by who answers. Pass 8 session 3 merged every pass's "Questions for ACS" into `backend-plan/ACS-IT-QUESTIONS.md` instead, under that file's own headings, without duplicates, so that there is one list and not two that drift apart (the failure this repository has already suffered twice). The merged file keeps every original number from 1 to 165 exactly once (a merged entry lists all the numbers it answers), marks each question added by a pass with its source, records question 126 (the RISC receiver) as withdrawn, and **tags every question with the gate it blocks** (F78): `now`, G-REAL, `shadow` (before the first shadow sweep), G-LIVE, G-AI, B5/B6, B9 or `none`.

The questions pass 6 itself added are there as 141 to 147 (Workspace edition, a staging tenant for ACS IT, change-freeze windows, training on the synthetic tenant, shared devices, a Veracross sandbox, the morning contact). Session 3 added **question 165**, ACS's calendars for 2026-27 and 2027-28, because §2.3 back-schedules every gate from the first school day after the 2027 spring break. The two questions the pass 7 decisions created are pass 4's **148** (parental access when a student turns 18, decision C5) and **149** (how ACS has handled ADEK Digital Policy 7.1.3.a with Veracross and ManageBac, decision F04, followed by **150**, the request to ADEK and its evidence pack).

**Ask now**, for an April 2027 start (answers by 30 October 2026, §2.3): 49 (the consent letter for the ACS name, naming sales demos), 149 and 150 (ADEK 7.1.3.a), 165 (calendars), 30 and 31 with 114 (the DPA's template, signatory and data protection lead), 36 with 113 (the IT security review), 2a with 70, and 73 (the weekly pack and the Data Export Package), 83 with 100 (the history export), 19 with 141 (Google app approval and the Workspace edition), 32 with 109 (ADEK Records 1.5.1 permission). By gate, the merged file's 132 entries divide as: 5 needed now, 53 blocking G-REAL, 28 blocking the first shadow sweep, 2 blocking G-LIVE, 8 blocking G-AI, 31 blocking the module builds, 4 blocking B9, and the withdrawn 126.

### 14.8 Questions for counsel

Counsel's questions live in passes 4 to 6 (pass 4 holds C1 to C17 and C24 to C32; pass 5 holds C18 to C22 and C33; this pass holds C23). The first version of this line said "C1 to C22 stand"; C24 to C33 were added by pass 8. Each is listed with the gate it blocks; 24 of the 33 block G-REAL outright and two more (C5, C12) conditionally, and **C25 is the most urgent**, because a "yes" makes ADEK's consent a G-REAL gate (§2.3, G26).

| # | Where | Question, in brief | Blocks |
|---|---|---|---|
| C1 | 04 §1.5 | Is the core basis sound under PDPL Article 4, or must the school rely on consent, and how for a minor (weaknesses in C26) | G-REAL |
| C2 | 04 §1.1 | Is CAROS a "digital platform" under the Child Digital Safety Law; any education exemption (extended by C32) | G-REAL |
| C3 | 04 §6.1 | Which Article 23 case, if any, covers US inference on pseudonymised data | G-AI |
| C4 | 04 §6.5 | The basis for fairness labels | year 1 (no labels in the pilot, C11) |
| C5 | 04 §1.1 | Are SEN or medical fields health data under the ICT Health Law (extended by C30) | year 1; G-REAL if such a column is imported |
| C6 | 04 §1.5, §3.5 | May parental access continue at 18 by default | G-REAL (decision C5's default holds meanwhile) |
| C7 | 04 §7.4 | Does Article 13 reach counselors' notes and a student's own words | G-REAL (C4's default excludes meanwhile) |
| C8 | 04 §5.6, §7.4 | May a subject-access response exclude safeguarding referrals, and who decides | G-REAL (C4's default excludes meanwhile) |
| C9 | 04 §4.4 | May CAROS keep a tombstoned audit copy after termination, given Article 8(4) | G-REAL (the DPA's termination clause) |
| C10 | 04 §7.1 | Limitation periods for claims by a former minor | G-REAL |
| C11 | 04 §1.1 | Must CAROS and the school appoint a DPO | G-REAL |
| C12 | 04 §9.7 | Is an EU copy with key material abroad an Article 23 transfer, and what makes it lawful | G-REAL only if the EU copy is chosen |
| C13 | 04 §1.5 | Does ADEK Records 1.5.1's "parent's permission" reach a vendor | G-REAL |
| C14 | 04 §5.6 | Does Wadeema Article 44 permit showing the reporter's identity to the CPO in CAROS | G-REAL |
| C15 | 04 §5.1, §5.3 | Does notifying the CPO leave the Article 42 duty with the educator; is the reporter's countdown sufficient | G-REAL |
| C16 | 04 §8 | Exposure for a breach of welfare data, and what insurance | G-REAL |
| C17 | 04 §7.1 | Sign off or amend every retention number | G-REAL |
| C18 | 05 §8.5 | Is ZDR's flagged-content retention (2 years; scores 7 years) a further transfer the notice must describe | G-AI |
| C19 | 05 §3.6 | Does AI feedback on an EE research question create an IB disclosure duty | G-AI |
| C20 | 05 §3.10 | Is the student's consent required before a letter draws on CAS and activity records | year 1 (letter engine deferred) |
| C21 | 05 §8.7 | If inference moved in-country, what changes in the DPA and notices | year 1 |
| C22 | 05 §6 | Must the notices describe the lexicon screen; does the Child Digital Safety Law's reporting-channel duty bear on it (the DPIA half is answered: one is required) | G-REAL (the screen ships with the first student screen) |
| C23 | this pass, §11 C3 | **Revised by decision C7.** Is the AI coding assistant a sub-processor the DPA must name: Opus 5.5 writing code under a zero-data-retention organisation, and Fable 5.1 reviewing diffs of code and synthetic data under a separate account, neither ever given tenant data? Must the development-tooling controls (hooks, no production credential on agent machines, the blocklist) appear in the DPA's security annex? | G-REAL (the sub-processor list) |
| C24 | 04 §1.1 | Which PDPL article sets the record-of-processing duty | G-REAL |
| **C25** | 04 §1.1, §6.1 | **Is in-country hosting by Microsoft, as sub-processor, "sharing" that needs ADEK's consent under Digital Policy 7.1.3.a?** Needed by 27 November 2026 | **G-REAL** (a "yes" makes G26 a G-REAL gate) |
| C26 | 04 §1.5 | What basis each strand of processing a minor rests on, and what consent procedure the school must run | G-REAL |
| C27 | 04 §1.1, §9.6 | PDPL 9(2) breach notice to data subjects against ADEK Digital 6.6.1 | G-REAL |
| C28 | 04 §1.1 | Does holding child-protection records in a vendor platform fit ADEK SPP v1.1 4.2 | G-REAL |
| C29 | 04 §4.4, §7.3 | Do pseudonymised tombstones and the erasure register satisfy Article 5(7) | G-REAL |
| C30 | 04 §1.1, §7.1 | If counselors sit in a DoH-licensed centre, are their notes health data | G-REAL |
| C31 | 04 §3.7 | Is a CAROS operator's viewing of tenant data from outside the UAE a cross-border transfer | G-REAL (onboarding review from step 10) |
| C32 | 04 §1.1, §1.5 | Does the under-13 consent rule reach the import of a child's record | G-REAL |
| C33 | 05 §8.3, §3.9 | Does sending a student's free text abroad need a basis beyond the pseudonymised features | G-AI |

---

## Sources

Retrieved 2026-09-23 by this pass. Facts taken from passes 1 to 5 are cited to those passes, whose own Sources sections carry the vendor, regulatory and research pages they verified. **Pass 8 session 3 (2026-09-24) retrieved no new source.** The facts its revision adds rest on pages pass 7 checked on 2026-09-23 and 2026-09-24 and lists in its A.6 citation register, and each is cited to its finding where it is used: that GitHub does not let an author approve their own pull request (F46, the code-owners page below); that Azure Monitor action-group SMS replies are only DISABLE, ENABLE, STOP, START and HELP (F45, the action-groups page below); that `drizzle-orm/migrator` runs every pending migration in one transaction (F40, read in the package source); that Google sends no Cross-Account Protection events for Workspace accounts (F18); that Google's OpenID Connect documents no `prompt=login` (F19); ADEK School Digital Policy 7.1.3.a (F04); Fable 5.1's list price of $10 and $50 per million tokens (pass 7 A.6, pass 6 row). Dates in §2.3 (T, Ramadan, the checkpoints) are planning assumptions, not sourced facts; question 165 asks ACS for the real calendar.

- GitHub: about rulesets https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-rulesets/about-rulesets; about protected branches https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches/about-protected-branches; about code owners https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/about-code-owners; managing environments https://docs.github.com/en/actions/how-tos/deploy/configure-and-manage-deployments/manage-environments; about GitHub Advanced Security https://docs.github.com/en/get-started/learning-about-github/about-github-advanced-security; about GitHub-hosted runners https://docs.github.com/en/actions/using-github-hosted-runners/using-github-hosted-runners/about-github-hosted-runners
- Microsoft Learn: authenticate to Azure from GitHub Actions by OpenID Connect https://learn.microsoft.com/en-us/azure/developer/github/connect-from-azure-openid-connect; Azure Container Apps revisions https://learn.microsoft.com/en-us/azure/container-apps/revisions; Azure Monitor action groups (SMS and voice country lists) https://learn.microsoft.com/en-us/azure/azure-monitor/alerts/action-groups; SFTP support for Azure Blob Storage https://learn.microsoft.com/en-us/azure/storage/blobs/secure-file-transfer-protocol-support; planned maintenance for Azure Database for PostgreSQL flexible server https://learn.microsoft.com/en-us/azure/postgresql/flexible-server/concepts-maintenance
- pnpm settings, dependency resolution (`minimumReleaseAge`, added in v10.16.0, default 1440 minutes since v11) https://pnpm.io/settings/dependency-resolution
- Drizzle Kit `check` https://orm.drizzle.team/docs/drizzle-kit-check
- OWASP Application Security Verification Standard repository https://github.com/OWASP/ASVS (the 5.0.0 release date and level structure are taken from secondary summaries; the project page returned HTTP 404 to this pass)
- Claude Code documentation: memory and CLAUDE.md https://code.claude.com/docs/en/memory; hooks https://code.claude.com/docs/en/hooks; common workflows (worktrees) https://code.claude.com/docs/en/common-workflows
- Google: sensitive scope verification https://developers.google.com/identity/protocols/oauth2/production-readiness/sensitive-scope-verification; manage access to unconfigured third-party apps for users designated as under 18 https://support.google.com/a/answer/13288950
- CREST, Dubai Cyber Force programme https://www.crest-approved.org/membership/dubai-cyber-force-program/
- SOC 2 Type I and Type II descriptions (secondary): Drata https://drata.com/learn/soc-2/type-1-vs-type-2; Vanta https://www.vanta.com/collection/soc-2/soc-2-audit-timeline
- Claude API pricing https://platform.claude.com/docs/en/about-claude/pricing (Opus 5.5 and Sonnet 5 prices as verified by pass 5 on 2026-09-23; the Fable 5.1 figure as carried in the bundled Claude API skill's table dated 2026-06-24, not re-fetched)
- The prototype: `index.html` at commit `fb28217` (the router at lines 9003 to 9099, `roleNav` at 1898, `bind()` at 9141, `PRIO` at 1300, the view functions listed in §6.2); `PHASES.md` (the 98 route-states, the model and effort line format)
- The plan: `backend-plan/CONTEXT.md`, `PRODUCT.md`, `backend-plan/ACS-IT-QUESTIONS.md`, `backend-plan/out/01-architecture-and-data-model.md`, `02-ingest-and-integrations.md`, `03-signal-engine.md`, `04-security-privacy-compliance.md`, `05-ai-design.md`, and for the revision `07-review.md` (the findings and Davide's decisions) and `08-changelog.md`
