# CAROS backend · Pass 8 · Changelog

Pass 8 revises passes 1 to 6 to apply Davide's 107 decisions on the pass 7 review (`07-review.md`, decisions recorded 2026-09-24). It runs in three sessions. This file records every change each session makes, the finding or judgment call that caused it, and the file and section it touched, and hands forward what later sessions still owe.

**Conventions.** F01 to F78 are pass 7's findings; §12.1 to §12.18 are the items of its A.5 (pass 6's inconsistency list); C1 to C11 are its judgment calls. "01 §2.4" means section 2.4 of `01-architecture-and-data-model.md`, and so on. "Decision on F09" means the `Decision (2026-09-24)` line under that finding. Pass 6's own judgment calls (its C2 to C6) are written "pass 6 decision C5" to keep them apart from pass 7's. From session 2, a C-number that belongs to a pass's own list says so: "pass 3 Challenge C11"; questions for counsel (C1 to C33) are always written "counsel question" or listed as such.

---

## Session 1 (2026-09-24): passes 1 and 4

Session 1 revised `01-architecture-and-data-model.md` and `04-security-privacy-compliance.md` in place, and changed no other file. Pass 7's section B ("Keep") was respected: every item it lists still stands in both files, and where a decision changed a kept design (the RLS layers, permissions as data, the reference-only escalation email, Google sign-in, parent magic links, mentor controls, the support principle, the AI rules, the demonstration marker), the change is the one the finding names and nothing wider.

### How the decisions were applied

These notes record where applying a decision needed a reading, so that Davide can check it.

1. **No new D-numbers.** Pass 4's deltas D33 to D61 are revised in place, so pass 6's migration map keeps its keys. Changes to pass 1's own tables, and new tables, are written into pass 1's DDL, which is the design source; 01 §0.3 lists them, and pass 6 must place them (handoff).
2. **A `staff` role** (01 §2.4, 04 §0.1 item 7). F16 makes `school_admin` a role and gives oversight a capability with no data grants, and F10 lets any staff role hold `safeguarding_lead`. A Principal or a CPO in the leadership team is neither counselor nor teacher, so a role with no data access of its own was needed to carry those capabilities. CONTEXT §3 allows "more roles later if needed".
3. **C1's words "no model reads student text"** are applied to the safety screen, which is what the judgment call framed. A literal reading would also switch off discovery chat, the statement read and Extended Essay feedback, all v1 features in CONTEXT §3. This is recorded as pass 4 open decision 20 for Davide to confirm.
4. **Mentor invitations are handed over off-email** (04 §2.3). C3 lists the staff emails kept and sends everything else in-app, but a mentor has no account before the invitation, and F16 forbids a credential in the outbox. The coordinator therefore hands the code over, as the school does for parent activation. Recorded as pass 4 open decision 21.
5. **Break-glass "never self-assigned"** (F16) is read as: break-glass stays a practising counselor's own emergency act, but is confirmed by the lead the same school day or lapses, is rate-limited, and nobody who administers cover (a lead or a school admin) can assign cover or a capability to themselves.
6. **F35's point 3** (two CAROS operators could activate a grant without the school) is applied by removing the CAROS-only emergency approval entirely: during an incident with the school unreachable, CAROS works on infrastructure and aggregates until a school approver responds.
7. **The leadership contact under F09** is told by the same reference-only safety-alert email C3 keeps for counselors, and oversight of unacknowledged referrals (a Principal) is in-app, because C3 lists no oversight email.
8. **"Never names" (decision on F60) applies to the escalation email too.** The CPO's email no longer names the raising counselor; the fallback line points to the counseling office. Section B keeps the email "as a reference only, rendered from an allowlist with a content test", which this preserves.
9. **A teacher's safeguarding referral opens a case** if none is open (01 C23), so that an escalation still always hangs off a case and the engine's pin applies; the teacher never sees the case.
10. **F31's "different timezone and weekend"** is applied as a third, CI-only synthetic tenant (`wellesmere-shifted`, `Europe/London`, Friday and Saturday weekend), rather than giving the fictional Dubai school a timezone it could not have.
11. **F14's reads under the escalation scope** are never included in a subject-access pack, even by the school's per-item act; every other professional record can be included item by item, as C4 says.
12. **F38 against C7.** F38 asked for B2.9 to be Fable-written; C7, decided the same day, has Opus 5.5 write every task and Fable review diffs only. Session 1 made no pass 6 change; the handoff applies F38 as "add a Fable review" under C7.

**Numbering used, for sessions 2 and 3 to continue from.** Questions for ACS 148 to 155 (the next is 156); questions for counsel C24 to C32 (the next is C33); pass 4 open decisions 20 to 22; pass 1 case transitions C23 to C25; pass 4 threat T21. Pass 4 question 126 is withdrawn (F18).

### Changes to `01-architecture-and-data-model.md`

| # | Change | Caused by | Section |
|---|---|---|---|
| 1.1 | Revision note under the title; §0.1 declares two additions (the `school_admin` and `staff` roles; outbound email per C2 and C3) and corrects the cold-copy framing | F16, F10, C2, C3, F22, F04 | header, §0.1 |
| 1.2 | New §0.3 summarising what pass 8 changed | all | §0.3 |
| 1.3 | Escalation reason row notes pass 4's structured form | pass 4 D46 | §0.2 |
| 1.4 | AWS facts corrected: disruption began 1 March 2026; UAE loss limited to data only in `mec1-az2`; Bahrain region-wide, and the country-level precedent; "relevant" billing operations | F73 | DR-1 context, §5.1, Sources |
| 1.5 | Cost table: HA storage billed twice; seven lines added (Blob SFTP, Defender for Storage, Key Vault HSM keys and the UAE Central vault, Entra ID P2, NAT gateway, support plan for Customer Lockbox); totals recomputed (pilot about $1,341 production, $1,547 with staging); two prices re-read from the Retail Prices API, three marked assumptions | F71, F61 | DR-1 cost |
| 1.6 | Residency consequence: a copy abroad needs key material abroad and ADEK consent | F22, F04 | DR-1 consequences |
| 1.7 | Migrations: Drizzle's migrator runs one transaction; a second non-transactional step for `-- no-transaction` files; `CONCURRENTLY` not needed at pilot scale | F40 | DR-2, DR-9 |
| 1.8 | Sweep job names and watchdog hour follow pass 3 (`sweep.schedule`, 05:45); scheduling keys on schools holding data via `core.schools_due_for_sweep()` | §12.1, F36 | DR-2 |
| 1.9 | pg-boss under its own login `caros_jobs` with no tenant privilege; `jobs.enqueue()` validates payloads as ids; completed jobs deleted after 7 days; registered as a non-tenant store; cross-school job test | F36 | DR-2, §2.2, §2.21 |
| 1.10 | Review discipline: solo mode while Davide builds alone; CODEOWNERS and two approvals from the second regular builder | C8, F46 | DR-3, §4 |
| 1.11 | One login role and managed identity per deployable (`caros_web`, `caros_worker`, `caros_support`, `caros_migrator`, `caros_jobs`), all `NOINHERIT`; `caros_owner` cannot log in and the migrator uses `SET ROLE` | F01, §12.2 | DR-4, §2.2 |
| 1.12 | The tier is evaluated in the policy expression (`current_user`) and passed to `auth.allowed()`; system and support honoured only for their tiers; user claims only for the membership's tier | F01 | DR-4, §2.4 |
| 1.13 | Separate teacher tier `caros_t_teacher`; the AI tier `caros_t_ai` listed and granted to web and worker | F75, F44 | DR-4, §2.2 |
| 1.14 | Policies take a per-row class, a row kind, addressee and pairing subjects, and accept the table's verb in insert and update policies | F02, F15 | DR-4, §2.4 |
| 1.15 | RLS tests added: web identity cannot `SET ROLE` to system, support or owner; `NOINHERIT` test; every verb end to end; every qualifier's negative fixture | F01, F02 | DR-4 tests |
| 1.16 | Reporting as per-tenant aggregate tables under RLS with one writer, never materialised views; convention test fails on a materialised view; `reporting` schema created | F34 | DR-6, §2.1, §2.2 |
| 1.17 | Case transitions: engine never lowers or closes a case with an unreleased escalation (C4, C11, C21); person's tier change releases the pin (C10); merge only within one student (C12); escalation uniqueness is one open per case plus an idempotency key, with `previous_escalation_id` (C15); acknowledgement by `safeguarding_lead` on any staff role (C16) | F07, F10 | DR-7.1 |
| 1.18 | New transitions C23 (a teacher's safeguarding referral, opening a case if none), C24 (information added to an open escalation), C25 (link cases across students); C13 writes `routed_key` | F10, F07, F58 | DR-7.1 |
| 1.19 | Selection: `ib.sign_off()` only, HL picks in an open round, signed as the acting teacher; sign-off card without the band; S6 records `review_meeting_version`; S7 guarded by `ib.check_approval`; definer transition functions; column-level updates; imports never write picks; IB-only engine stated as a known limitation | F17, F58, F65 | DR-7.3 |
| 1.20 | Extended Essay: rounds name their guide; ACS defaults to `ee_2027` (one reflective statement), `ee_2018` for resits; E7 and E9 read the guide; ManageBac as read-only record where kept, E1 to E10 disabled there | F62, §12.12, C10 | DR-7.4, §2.10 |
| 1.21 | Seed: fictional coordinator replaces the real ACS name; CPO on a `staff` membership; consent covers third-party sales demos; `purpose 'demo'`; regulator profile applies | F29, F43, F42, F31, F10 | DR-8 |
| 1.22 | Seed: no `strength` in any seed; narrative layer written against pass 3's schema | §12.11 | DR-8, §2.9 |
| 1.23 | Seed: `CONCERN_SUBMIT` mapped to `TEACHER_FLAG`; the `AI_DRAFT` letter entry dropped | F72 | DR-8 |
| 1.24 | Seed: `seed://` numbers never render ("not on file"); `seed://` sources staging only; production check | F30 | DR-8, §2.7 |
| 1.25 | Seed: `COURSE_CATALOG` to per-school rows, not global `ref.*`; archetype steps by programme family | F65 | DR-8, §2.7 |
| 1.26 | Seed: statements as sectioned versions; `PS_SYSTEMS` guidance and the "AI read" score not ported | F32, F66 | DR-8 |
| 1.27 | Seed: EE rounds on the 2027 guide; word limit and marks from the guide, confirmed by the coordinator | F62, F77 | DR-8 |
| 1.28 | `engine-reproduces-seed` runs over both tenants with generated expected-result lists; Wellesmere's eight cases have results from the start | F06, F31 | DR-8 |
| 1.29 | Wellesmere: explicit empty KHDA profile; tutor and Head of Year columns imported into pastoral assignments; a restricted contact in the fixture; DSL on a teacher membership | F31, F33, F57 | DR-8 |
| 1.30 | Second-school checklist: portal from the school's rule set; per-destination deadlines ("UCAS-only" removed); no "ADEK" on any Wellesmere screen, template or email; CI-only `wellesmere-shifted` tenant for timezone and weekend | F31, F65 | DR-8 |
| 1.31 | Environments: staging maps only CAROS test domains; pinned demo revision with deploy freeze and an untouched demo tenant; drills in their own subscription; production holds only real tenants and the unbranded canary pair | F43, F42, pass 6 decision C6 | DR-9 |
| 1.32 | Canary as a Container Apps job in UAE North, refusing non-canary tenants and asserting its own school id on every row; no production credential on agent machines | F42, F23 | DR-9 |
| 1.33 | Cold copy reframed as the real choice (EU ciphertext with keys abroad and an EU drill, or in-country only), ADEK consent first; restore-drill checks run in UAE North | F22, F04, F23 | DR-9 |
| 1.34 | Conventions 11 to 15: column-level writes from `writable_columns`; definer functions never executable by `PUBLIC`; no materialised views; `updated_at` by trigger; vocabularies instead of CHECK lists | F15, F17, F75, F34, §12.3, F65 | §2.1 |
| 1.35 | `core.school`: `purpose` (customer, canary, demo), immutable; one status model (onboarding, shadow, live, suspended, offboarding, offboarded) with a transition trigger; rows never deleted; classification, purpose and branding immutable; a synthetic tenant's letterhead is CAROS-only | F42, F36, F67 | §2.3 |
| 1.36 | `core.person`: guardians sharing an email stay separate persons (partial unique index) | F57 | §2.3 |
| 1.37 | `auth` schema rewritten: tiers; scopes `esc`, `addressee`, `lead_cover`, `own_pairing`, `year_group`, `tutor_group`; `auth.scope_rank()` for the narrowing rule; data classes split and added (`contact_record`, `safety_alert`, `guardian_link`, `xp`, `membership`, `session`, `notification`, `support_grant`, `privacy_request`); roles `school_admin` and `staff`; capabilities by allowed roles, with `safeguarding_oversight`, `year_lead`, `tutor`, `ucas_adviser`; `role_permission` key includes scope and `row_kind`; the first version's inline default rows withdrawn in favour of pass 4 §3.6 | F02, F15, F16, F13, F77, F33, F10, F47 | §2.4 |
| 1.38 | `auth.capability_grant` (four eyes, never to oneself); caseload validity in timestamps; `auth.pastoral_assignment` | F16, F33 | §2.4 |
| 1.39 | Pre-tenant lookups as ID-only definer functions (`auth.school_for_domain`, `auth.school_for_magic_link`, `core.schools_due_for_sweep`, `core.schools_holding_data`, `notify.schools_with_due_outbox`) | F36, F35 | §2.4, §2.18 |
| 1.40 | Scope predicates: timestamps in `in_caseload`; activation and restriction in `is_guardian_of`; new `is_pairing_mentor`, `esc_covers`, `lead_cover_active`, `in_led_year_group`, `in_tutor_group`, `support_grant_covers` (operator and table); diploma cohort through `ref.programme_family` | F57, F59, F02, F33, F35, F65 | §2.4 |
| 1.41 | `auth.allowed()` rewritten (tier argument, table, row kind, addressee, pairing, support branch by operator and table); `auth.protect()` rewritten (class expression, kind, addressee, pairing, verbs) | F01, F02, F35 | §2.4 |
| 1.42 | `notify.enqueue()`: the one way a tier writes the outbox; template and sender checks; no credential in a payload | F02, F16 | §2.4 |
| 1.43 | Definer helpers owned by `caros_policy`, `EXECUTE` revoked from `PUBLIC`; only `auth.allowed()` granted to tiers | F75 | §2.4 |
| 1.44 | Comment on transaction settings corrected (six settings plus the actor label and `SET LOCAL ROLE`) | §12.3 | §2.4 |
| 1.45 | New §2.4a: every qualifier in pass 4 §3.6 mapped to its mechanism and negative test; the matrix generated from one data file for rows and tests | F02 | §2.4a |
| 1.46 | `ref.programme_family` table replaces the ENUM (adds `ib_cp`, `igcse`, `btec`); `sis.enrolment_programme` for more than one programme; `stage_key` no longer drives the portal | F65 | §2.5 |
| 1.47 | `sis.tutor_group` and `sis.tutor_group_member`; FKs for pastoral assignments | F33 | §2.5 |
| 1.48 | `engagement.activity_event.source` and `ingest.source_system.kind` become vocabularies | F65 | §2.6, §2.16 |
| 1.49 | `ref.destination_system` table replaces the ENUM (adds `uc`); columns retyped | F65 | §2.7, §2.11 |
| 1.50 | IGCSE scale keys | F65 | §2.7 |
| 1.51 | `ref.statement_format` and `ref.reference_format` (UCAS three questions; UCAS reference sections; `generation_forbidden`) | F32 | §2.7 |
| 1.52 | `ref.regulator_profile` (contacts with hours and verified dates, reporting window and owner, category-route defaults, citations, outcome vocabulary, indicators, retention minimums, attendance reason family, review-pack checklist); empty profile for a regulator not yet analysed | F31, F09, F11, F55, F77 | §2.7 |
| 1.53 | Rule set `portal`; vocabularies `guide_course`, `engagement_source`, `source_system_kind`, `teacher_flag_routed` | F65, F58 | §2.8 |
| 1.54 | Vocabulary versions and an append-only `config.vocabulary_history`; engine snapshots attributes into rule hits | F48 | §2.8 |
| 1.55 | Flag kind `safeguarding` | F10 | §2.9 |
| 1.56 | `signal.case.shadow`: shadow cases run the live lifecycle, one open live and one open shadow case per student, read as `signal_shadow`; `evidence_item.evaluation_id` | F24 | §2.9 |
| 1.57 | Headline is never model output; `headline_generation_id` removed | F52 | §2.9 |
| 1.58 | `signal.case_escalation_pin` trigger (no engine lowering or auto-close while an escalation is unreleased; merge only within one student) | F07 | §2.9 |
| 1.59 | `signal.case_link` table | F07 | §2.9 |
| 1.60 | `signal.escalation`: `UNIQUE (school_id, case_id)` removed (replaced in D46); comment rewritten; `signal.escalation_update` table | F07 | §2.9 |
| 1.61 | Teacher flags: `routed_key` vocabulary that never reveals a case; teacher tier cannot read `case_id` or `status`; the modal's disclosure rule; flags for roster, subject and EE students; `escalation_id`; idempotency key; flag text screened by the lexicon | F58, F10, F07, C1 | §2.9 |
| 1.62 | Student reflections: the student never reads `safety_flag` | F13 | §2.9, §2.22 |
| 1.63 | `core.meeting` classed `contact_record`, staff-only; the coordinator records review conversations for the whole cohort | F15, F02 | §2.9, §2.22 |
| 1.64 | `signal.week_run()` reads the engine's `outside` flag instead of recomputing from the band | F40 | §2.9 |
| 1.65 | `ib.selection.review_meeting_version`; `ib.check_approval` trigger; sign-off through `ib.sign_off()` and `ib.v_signoff_card` | F17, F58 | §2.10 |
| 1.66 | CAS entries may be `managebac_mirror` rows; EE word limit from the guide; reflection comments for both guides; `v_diploma_cohort` through programme families | C10, F62, F77, F65 | §2.10 |
| 1.67 | Statements per destination and entry year, sectioned; `uni.statement_complete()`; references with a format, centre statement, predicted-grade snapshot; `uni.centre_statement`; `uni.school_deadline`; application pack reports completeness, not characters | F32 | §2.11 |
| 1.68 | XP ledger in its own class `xp`, student-only | F77 | §2.12, §2.22 |
| 1.69 | Guardian links: one person per SIS contact, activation per contact, restriction flags, guardians never write links; family messages scoped by sender and recipient | F57, F15 | §2.13, §2.22 |
| 1.70 | Mentors: subjects through the pairing; own-pairing scope; parents see only the pairing and headline; coordinator creates pairings; `mentor.session.mode` in person on premises only | F59, C6 | §2.14, §2.22 |
| 1.71 | Files read under their own class and kind; raw exports for the school admin only, never support | F15, F35 | §2.15, §2.22 |
| 1.72 | Access-changing imports need a different approver and never auto-approve | F57 | §2.16 |
| 1.73 | AI feature keys aligned with pass 5, less `headline_rephrase` and `safety_screen`, and without `letter_draft`; model flags on pass 5's registry | §12.8, §12.9, F52, C1 | §2.17 |
| 1.74 | `ai.reidentify()` in place of reading the pseudonym map | F44 | §2.17, §2.22 |
| 1.75 | Templates listed per pass 4 §5.8; `sender_roles`; send-time idempotency by outbox id; tokens minted at send time | F28, F02, F77, F16 | §2.18 |
| 1.76 | Audit taxonomy is one list (dedupe `PREVIEW_ACCESS` and `sweep.completed`); `allowed_tiers` on actions and event names; `audit.record()` and `events.emit()` check the caller's tier; school FKs on `audit.entry` and `events.event` | §12.16, F75, §12.3 | §2.19, §2.20 |
| 1.77 | `writable_columns` in the registry; `privacy.retention_override`; `privacy.non_tenant_store` | F15, F17, F65, F36 | §2.21 |
| 1.78 | Grants rewritten: no table-wide user writes; teacher tier column limits; no `signal` schema grant for students; safety-flag columns revoked from student, guardian and mentor tiers; contact columns only through a view; support tier on masked views only | F13, F15, F17, F35, F44, F58, F75 | §2.22 |
| 1.79 | `core.school` write policy uses the new `auth.allowed()` signature; projection views listed as the only definer views | F01, F02 | §2.22 |
| 1.80 | Every `protect()` call revised: derived subjects for child tables, shadow classes, verbs, kinds, addressees, pairings; attendance with its section subject; new tables protected; `updated_at` triggers attached; `PUBLIC` revoked on every definer | F02, F13, F15, F16, F24, F59, F75, §12.3 | §2.22 |
| 1.81 | Closing paragraph on defaults rewritten (narrow-only until question 65) | F47 | §2.22 |
| 1.82 | ER summary and module map updated (pastoral assignments, shadow cases, escalations, formats, aggregates, regulator profiles, owners after C8) | F33, F24, F07, F32, F34, F31, C8 | §3, §4 |
| 1.83 | Challenges 5.1 (dates, keys abroad, ADEK), 5.3 (settled by C2), 5.4 (roles revised), 5.5 (pass 4 adopted the form), 5.7 (no framework caching), new 5.8 (known limitations) | F73, F22, F04, C2, F60, F16, F10, F75, F65 | §5 |
| 1.84 | Open decisions 1, 7, 8, 9, 13, 15, 17 updated or closed | F22, F04, F01, F47, F29 | §6 |
| 1.85 | §7 points to this changelog's handoff; question 62 rewritten; question 56 names the role | F22, F16 | §7, §8 |
| 1.86 | Sources: AWS date, Register URL with `/5296830`, re-checked prices, migrator behaviour | F73, F71, F40 | Sources |

### Changes to `04-security-privacy-compliance.md`

| # | Change | Caused by | Section |
|---|---|---|---|
| 4.1 | Revision note; clause numbers now ADEK Student Protection Policy v1.1 | F55 | header |
| 4.2 | §0.1 rewritten: declares the departures the decisions make (content-free parent email; declared staff safety emails; the CPO notice route; teacher referrals; per-item SAR inclusion; two roles; lexicon-only screen; two AI features out); activation code not emailed; model flags on the registry | C2, C3, F28, F12, F10, C4, F14, F16, C1, F52, F77, §12.9 | §0.1 |
| 4.3 | §0.2 rows: escalation email names a role; Year head as data; safeguarding lead on any staff role; routed label; mentor sessions in person | F60, F33, F10, F58, C6 | §0.2 |
| 4.4 | §0.4 points 2, 3, 5, 6 revised (Directory read; database verbs and per-deployable identities; each reporter's 24-hour duty; the truth about free text; ADEK consent) | F18, F01, F02, F09, F20, C1, F04 | §0.4 |
| 4.5 | New §0.5 summarising the revision | all | §0.5 |
| 4.6 | Regulator: Data Office merged into the Federal Authority for AI and Data; regulations still unissued | F37 | §1.1, §1.4, §9.6, Sources |
| 4.7 | PDPL corrections: Article 9(2) conflict with ADEK 6.6.1; Article 13 a right to information with refusal grounds; Article 21 is the DPIA (CAROS drafts it; G-REAL item); Article 23's contract route narrower; Articles 5(7) and 8(4) added | F37, F14 | §1.1 |
| 4.8 | Wadeema Article 44 quotation marked as shortened; teachers' internal referral noted | F37 (citation register), F10 | §1.1 |
| 4.9 | ADEK Student Protection Policy re-cited as v1.1 with renumbered clauses; 24-hour duty is each staff member's; 3.7 emergencies; question on vendor holding case reports | F55, F09, F37 | §1.1 |
| 4.10 | Records Policy corrected (1.5.1 under transfers; five-year clause scope; 2.1.2.b clinic records) | F37 | §1.1, §7 |
| 4.11 | Digital Policy: 7.1.3.a (ADEK consent before a contractor shares data) and 6.4 (live virtual visitors) added; 7.1.2 consent procedures; ADEK consent gate and the ask-ACS-first sequence | F04, C6, F37 | §1.1 |
| 4.12 | Student Mental Health Policy cited: 3.5.1(a) and (b), 3.6; category routing as school configuration confirmed with the CPO | F09, F37 | §1.1 |
| 4.13 | Guidance Policy: four indicators, counted over one population, in the ADEK profile | F77, F31 | §1.1 |
| 4.14 | Hosting paragraph rewritten on 7.1.3.a | F04 | §1.1 |
| 4.15 | Child Digital Safety Law: under-13 gate reaches the record; consent at import; Cabinet Resolution No. 106 of 2026 noted | F56 | §1.1, §1.5, D58 |
| 4.16 | Cybercrimes Article 6; Penal Code 431 and 432 corrected | F37 (citation register) | §1.1, T7 |
| 4.17 | ICT Health Law: counselors in DoH-licensed centres may produce health data; 25-year retention; no SEN label in the pilot | F37, C11 | §1.1 |
| 4.18 | SDPC figure corrected (more than 222,000 agreements) | F37 (citation register) | §1.2 |
| 4.19 | Sub-processors: email polled not via Event Grid; Azure Monitor action groups (counts only); Microsoft engineers under Customer Lockbox; Anthropic only after ADEK consent; coding assistant; GitHub runners never read production; cold copy needs keys abroad and ADEK consent | F60, F61, F04, F23, C7, F22 | §1.3 |
| 4.20 | DPA outline: confidentiality with managed devices and location; cross-border clause with ADEK consent and the free-text truth; breach clause names the new regulator and the 9(2) conflict; DPIA row; retention row with overrides, portal delivery, pseudonymised tombstones; demonstration consent covers sales demos; fairness audit not in the pilot; school-side responsibilities annex | F61, F04, F20, F37, F65, F76, F29, F43, C11 | §1.4 |
| 4.21 | Lawful basis: three weaknesses flagged to counsel; under-13 processing; status model gate; notices state inference in the US, the free-text truth, both retention periods, no model on the screen, mentors abroad; C5 default and question 148; only the student requests after 18 | F37, F56, F36, F76, F20, F63, C1, C5, F14 | §1.5 |
| 4.22 | Google binding by email only for a person with no stored subject; Google brand verification stated; RISC dropped for Workspace; Directory read mandatory and hourly for staff; leaver runbook step | F75, §12.14, F18 | §2.1 |
| 4.23 | Parents: activation per contact enforced in the database; later links pending until approved; one person per SIS contact, restrictions suspend one contact only; reactivation enforced | F57 | §2.2 |
| 4.24 | Mentors: ADEK SPP v1.1 and Digital 6.4 cited; invitation code off-email; own pairing only, first names by default, mentors abroad; pattern holds without a classifier; grooming email to the coordinator, in-app to the lead; sessions in person only | F55, F04, C3, F16, F59, F76, C1, C6 | §2.3 |
| 4.25 | Step-up: `auth_time` through `claims`, ten-minute rule, B1 spike, staff passkey fallback; revocation list without RISC | F19, F18 | §2.4 |
| 4.26 | Identity administration: school admin as a role; bootstrap grant; domain mapping on DNS proof, approval and audit; four eyes for every capability, never for oneself; no welfare access | F16, F35 | §2.5 |
| 4.27 | Permission model: scope and row kind in the key; new capabilities; the widening rule stated once (narrow-only; the counselor widening option kept ready behind `allow_counselor_widening`); sensitivity for new classes | F02, F16, F33, F10, F47, F13, F15, F77 | §3.1 |
| 4.28 | Enforcement: tier in the policy; verbs enforced by the database; transition functions and column grants; worker's cross-tenant reads only through ID-only functions; jobs under their own login | F01, F02, F15, F17, F36 | §3.2 |
| 4.29 | Cover: timestamps; never self-assigned; break-glass only for practising counselors, two in seven days, lead confirms the same day; pastoral assignments; narrow-only until question 65 | F16, F33, F47 | §3.3 |
| 4.30 | Teachers: own tier; codes only; flag view; sign-off card without the band; routed label vocabulary; `ib.sign_off()`; concern paths for subject and EE students; the disclosure rule; the safeguarding flag as a referral | F75, F58, F10 | §3.4 |
| 4.31 | Students and parents: no meeting notes, contact log, safety alerts or XP for parents; messages by sender and recipient; own link rows only; mentor view; C5 at 18 | F15, F13, F77, F59, C5 | §3.5 |
| 4.32 | Permission matrix rewritten: new scope codes, the school admin role column, a pastoral column, new classes, per-kind qualifiers and projection views; rules for the `staff` role, oversight and leadership contacts; the "no access" symbol written as "none" | F02, F10, F13, F15, F16, F33, F58, F59, F77, style | §3.6 |
| 4.33 | Support path: every CAROS write under a school-approved write grant; request names mode, tables, location; no activation without the school; separate console app; grants bound to operator and table; masked views only; real data shown only in person or on the school's platform; Customer Lockbox; access from abroad and counsel question | F35, F61 | §3.7 |
| 4.34 | Tests: verbs positive and negative through RLS; every qualifier's negative fixture; flagged fixture per role; narrowing rule; deployable roles; support by operator and table; cross-school jobs | F02, F13, F15, F47, F01, F35, F36 | §3.8 |
| 4.35 | Audit: pass 8 action keys; tier allowlists; migration review under C8; SAR access history excluded by default; tombstoned audit copy is pseudonymised, a counsel question | F07, F09, F10, F12, F16, F35, F36, F75, F77, C8, F46, F14, C4, F37 | §4.1 to §4.4 |
| 4.36 | Escalation defined against v1.1; the reporter's own countdown and direct-report route from the start; contacts from the regulator profile; no clause numbers in UI text; teachers can escalate | F09, F55, F31, F10 | §5.1 |
| 4.37 | Route: any staff role holds `safeguarding_lead`; oversight holds `safeguarding_oversight` and gets in-app notices; leadership and emergency contacts; `safety_alert_routes` confirmed by the CPO; nightly route validation | F10, F16, C3, F09, F77 | §5.2 |
| 4.38 | The act: teacher referrals; `immediate_risk = yes` shows 999 and the Principal and notifies the whole route; one open escalation per case plus idempotency; linked later escalations; the pin; `notify.enqueue()`; the email names a role only; reminders and deputy by email, oversight in-app; the 24-hour banner restates the reporter's duty without clause numbers; updates while open; outcome vocabulary from the profile; after closing | F10, F09, F07, F02, F60, C3, F55, F31 | §5.3 |
| 4.39 | Content test covers every §5.8 email, no names, role-only senders | F60, F12, F28 | §5.4 |
| 4.40 | Channels: emails content-free; delivery status polled; no SMS in v1; ops payloads counts only | F60, C3 | §5.5 |
| 4.41 | Who sees an escalation: v1.1 numbering; status view; SAR requests about an escalated student go to the safeguarding lead; default exclusions | F55, F14, C4 | §5.6 |
| 4.42 | Student disclosures rewritten: lexicon only; every hit reaches a person; alerts staff-only and explainable; routing by category; safety-alert email; the CPO's reference-only "alert unopened" notice; helpline list split, draft until the CPO confirms, verified dates; teacher flag text screened; urgent requests never extracted | C1, F03, F13, F48, F09, F12, F11, F10, F21 | §5.7 |
| 4.43 | New §5.8: every outbound message, with recipient, channel, trigger, content allowlist and basis | F28, C2, C3, F60, F12, F77, F16 | §5.8 |
| 4.44 | AI position: Article 23 corrected, no settled basis until counsel; ADEK consent a named gate before G-AI | F37, F04 | §6.1 |
| 4.45 | Rules: R2 points at the registry; R3 both retention periods; R4 name rule tested on Arabic names, script and nicknames, independent recall, fingerprint stripped, statements not pseudonymisable, no user id, `ai.reidentify()`; R6 as columns with the free-text truth and an allowlisted AI tier; R7 nightly AI as the case owner; R8 lexicon screen and C1's scope; R9 column name | §12.9, F63, F20, F76, F44, F21, C1, §12.7 | §6.2 |
| 4.46 | Features: safety screen no model; "AI read" not ported; headline rephrase dropped | C1, F66, F52 | §6.3 |
| 4.47 | Allowlist: discovery keys; headline row removed; safety screen as lexicon; quarantined reader row allowlisted per record kind, extracting lazily | §12.8, F52, C1, F21 | §6.4 |
| 4.48 | Fairness audit: no labels in the pilot; "safeguarding" definition quoted correctly; pre-registered, pooled comparisons | C11, F55, F68 | §6.5 |
| 4.49 | Engagement flag column name | §12.7 | §6.6 |
| 4.50 | Retention: per-school overrides; Records Policy scope; safety alerts in `welfare`; `xp` row; fairness labels none in the pilot; law list with 5(7), 8(4), ICT Health 25 years | F65, F37, F48, F77, C11 | §7, §7.1, §7.2 |
| 4.51 | Erasure: non-tenant stores; tombstones are pseudonymised, a counsel question | F36, F37 | §7.3 |
| 4.52 | Subject access rewritten: narrower Article 13 reading; only the student after 18; escalated students routed to the safeguarding lead; professional records and escalation-linked items excluded by default; per-item inclusion as an explicit act | C4, F14, F37 | §7.4 |
| 4.53 | Offboarding: exit pack in the portal; `offboarding` status; school row kept as `offboarded`; audit copy subject to counsel | F76, F36, F37 | §7.5 |
| 4.54 | Threat model: T1 (no framework caching; aggregate tables; canary in UAE North keyed on `purpose`); T2; T4 (step-up proof; Directory read); T7 (articles; grants bound; Lockbox); T8 (different approver); T11 (content-free emails); T12 (own pairing; pattern holds; in person); T14 (C8); T16 (immutability; blocklist of real names; sales-demo consent); T20 (free text; both retention periods; ADEK); new T21 (development tooling) | F75, F34, F42, F10, F19, F18, F37, F35, F61, F57, F60, F59, C1, C6, C8, F67, F29, F43, F20, F63, F04, F23 | §8 |
| 4.55 | Browser leaks: self-hosted fonts, `font-src 'self'`, spell check off on sensitive fields, Chrome policy in the IT checklist | F76 | §9.1 |
| 4.56 | Key backups restore only in-geography, hence keys abroad for the cold copy | F22 | §9.2 |
| 4.57 | Veracross token scopes refuse alerts and health; Data Export Package password rotation | F54 | §9.3 |
| 4.58 | Ops alerts carry counts, not student ids | F60 | §9.4 |
| 4.59 | Incidents: new regulator; 9(2) against 6.6.1 flagged each time; on-call paging escalation job | F37, F45 | §9.6 |
| 4.60 | Cold copy rewritten as the real choice, ADEK consent first | F22, F04 | §9.7 |
| 4.61 | SFTP: password path for Veracross's scheduled export with IP allow-listing and 90-day rotation; cost noted | F54, §12.15, F71 | §9.10 |
| 4.62 | Controls a reviewer can see: canary results; qualifier fixtures; verb and narrowing suites; no-ADEK test; blocklist of every real person's name; development-tooling controls | F42, F02, F13, F47, F31, F29, F23 | §9.11 |
| 4.63 | Review pack from the regulator profile; production pen test before real data; retest split; yearly rules of engagement | F31, pass 6 decision C5, F41, F61 | §9.12 |
| 4.64 | Pre-real-data list: DPIA; 7.1.3.a clause; notices' free-text truth; Directory scope instead of RISC; step-up spike; verb suite; category routes and helplines confirmed by the CPO; ADEK consent for AI | F37, F04, F20, F18, F19, F02, F09, F11 | §9.13 |
| 4.65 | Deltas revised in place: D33 (new class sensitivities; pass 3 D30 keys), D35 (capability roles), D36 (widening rule; `allow_counselor_widening`), D37 (DNS proof, approval, definer-only writes), D39 (step-up evidence), D42 (per-contact links, restriction, no co-holder), D43 (never self-assigned, lead confirmation, lapse), D44 (kind, mode, write functions, school approval, location), D45 (status gate, category routes, verified contacts), D46 (idempotency, partial unique index, previous escalation, pin release, reporter deadline, teacher referral), D47 (§5.8 templates, role not name), D48 (pass 8 keys, `PREVIEW_ACCESS` removed, tier lists), D49 (inclusion reasons; outcomes from the profile), D50 (`xp`; overrides), D53 (flags to the registry; ADEK consent reference; reader kinds), D55 (allowlist), D56 (no hard-coded timezone), D57 (in-person sessions; coordinator pairings), D58 (under-13 consent), D59 (teacher tier), D61 (tests); note that D42, D44 and D46 land with `auth.allowed()` | F13, F15, F16, F77, F10, F47, F35, F43, F19, F57, F61, F36, F09, F11, F07, F28, F60, §12.16, F75, C4, F31, F65, §12.9, F04, F21, F44, C6, F59, F56, F02, F29, F42 | §10 |
| 4.66 | Sources: ADEK SPP v1.1 and Mental Health Policy URLs; Digital Policy clauses re-read; regulator merger | F55, F09, F04, F37 | Sources |
| 4.67 | Challenges 5, 7, 8, 9, 12 revised | C6, F22, F04, F16, F47, C4, F14 | Challenges |
| 4.68 | Open decisions 1, 4, 8, 9, 10, 11, 12, 17, 18 updated or closed; new 20 (C1's scope), 21 (mentor invitation channel), 22 (fictional coordinator name) | F22, F04, F37, C3, C6, C5, C4, F18, C11, F16, C1, F29 | Open decisions |
| 4.69 | "For other passes" points to this handoff; RISC deliverable replaced by the Directory read | F18 | For other passes |
| 4.70 | Questions for ACS 110, 114, 118 to 121, 123, 126 (withdrawn), 128 revised; 148 to 155 added | F18, F35, F10, C3, F60, F09, C6, C4, C5, F04, F57, F37 | Questions for ACS |
| 4.71 | Counsel questions C1, C2, C3, C5, C7, C8, C9, C12, C13, C15 revised; C24 to C32 added | F37, F56, F04, F14, F22, F55, F09, F61 | Questions for counsel |

### Decided items that needed nothing in 01 or 04

Their changes belong to other files and are all in the handoff below: F05, F08, F25, F26, F38, F39, F49, F50, F51, F53, F64, F68 (its engine parts; its fairness part is in 04 §6.5), F69, F70, F74, F78, §12.4, §12.5, §12.17, §12.18 (applied through F40), C9. C7 appears in 04 only as a fact (the coding assistant runs under a ZDR organisation); its build rule is pass 6's.

---

## Session 2 (2026-09-24): passes 2, 3 and 5

Session 2 revised `02-ingest-and-integrations.md`, `03-signal-engine.md` and `05-ai-design.md` in place, and changed no other plan file except this changelog. It applied every decided item that touches the three files, including several the session 1 handoff did not list (in pass 2, F05, F06, F18, F36, F56 and F62; in pass 3, F32, F34, F37, F42, F60 and C11; in pass 5, F01, F32, F36, F42, F65, F66, C2 and C3), and checked each file against the revised passes 1 and 4. The three files were revised in parallel and then reconciled against each other (note 36 below). Pass 7's section B was respected: everything it keeps in these passes still stands, and where a decision changed a kept design, the change is the one the finding names. That includes cadence honesty; pull-only ingest with mapping profiles as data, a closed transform set, the preview's security section, rosters treated as permissions, idempotent re-import and rollback; no middleware; the pure, versioned engine with its scenario suite; personal median and MAD with floors, a one-sided CUSUM, and combination by level, breadth and persistence; the removed numbers; the queue ordered by tier and time only; cold start without other students' history; episode exclusion and recorded suppression; shadow mode with the counselor log and reveal sessions; the AI rules; the quarantined reader and the closed co-pilot grammar; and the Extended Essay guide as versioned data.

### How the decisions were applied

These notes record where applying a decision needed a reading, so that Davide can check it. The first three are consequences rather than readings, and they change what the pilot will look like.

1. **The detuned engine is quiet and slow** (F05, F06, C9; 03 §3.6, §11.1, Challenge C11). Run on its own level function (`calibrate_tiers.py`, seed 20260924), the pilot defaults put about 0.01 new check-in cards and 0.5 new review cards a week on an 87-student caseload under normal noise (0.08 and 1.8 under heavy-tailed noise), against the first version's 0.7 and 11. The price is detection: a genuine change in two domains reaches `review` 56% of the time by week 8 and `checkin` 9% of the time by week 8 (30% by week 16); a single declining subject reaches `review` 20% of the time by week 8. Releasing the attendance hold raises two-domain check-in detection to 17% by week 8. Pass 3 names the levers (§11.1); the elicitation session (§10.5) is where the counselors choose.
2. **The demo stories no longer all come out of the engine** (F06; 03 §7.2). With the attendance and punctuality series re-authored in days and late days (no register produces the prototype's weekly percentages or an "on-time percentage"), twenty weeks of imported history and the pilot defaults, 8 of the 15 authored tiers match. Maryam's `good` arrives two weeks late, Ahmed and Priya land one tier lower, Hana's relapse does not fire (she gets a `monitor` case), and Zayd, Maya and Fahad get no case. The expected results are generated by the rules, as F06 requires, so the staging demo will show these tiers, not the prototype's.
3. **The AI bill roughly doubles once thinking tokens are counted** (F70; 05 §11.3). The variable cost is about $69, $103 and $118 per school-month at pilot, year 1 and year 3 (about $31, $47 and $54 without thinking), and $312, $184 and $126 all-in with evals. At year 3 the AI line is about three quarters of per-school infrastructure, so "small next to infrastructure" now holds at pilot only.

**Pass 2**

4. **Access-changing imports** (F57). Roster, staff, contacts and caseload imports (pass 1 §2.16, pass 4 T8), pass 2's new `pastoral` kind, and any batch whose security section is not empty (a mirror batch that moves an Extended Essay supervisor changes who reads the essay) need an approver other than the uploader and never auto-approve. Fact imports may still be uploaded and approved by one `school_admin` and may auto-approve after four green weeks. Pass 4 §3.6's matrix cell reads wider; see the handoff.
5. **Imports wait for `shadow`** (F36, pass 4 §1.5). "Real imports are refused until the attestation exists" is applied as one status check: no import into a real tenant until it is `shadow` or `live`, because D45's trigger already refuses `onboarding → shadow` without the attestation. So the pseudonymised onboarding sample now follows G-REAL, the onboarding calendar is entered by hand, and a sample import never commits.
6. **The under-13 check** (F56) runs per student at import. Consent comes from a mapped column, a consent already on the student, or a `school_admin`'s recorded decision (`ingest.consent_decision`, placed in D2); until then every row about the student is rejected. A student with no date of birth is treated as possibly under 13, a cautious reading Davide may relax. Question 156.
7. **Days from a per-period register** (F05) are stored as derived day rows marked with the declared rule (`derived_rule`, in D4: `half_days`, `all_periods` or `majority_periods`) and flagged in the preview, not computed in a view, so every signal can name the rule its day came from (invariant 4). Pass 2 open decision 18 picks the default.
8. **Pastoral data** (F33) arrives as its own `pastoral` import kind, because the iSAMS fixture carries tutor and head-of-year columns in its forms and year-group files rather than in the staff file. An imported assignment grants nothing until a `school_admin` grants the matching capability under four eyes.
9. **Guardians are never merged in CAROS** (F57), including through the merge path; only a SIS contact id the SIS itself has retired may be linked by a candidate decision.
10. **The ManageBac mirror** (F27, C10). One `ib.cas_entry` per experience per strand; ManageBac's hours required are compared with the school's expectation and never written into it; Extended Essay mirror rows carry the research question, supervisor, status and reflection dates, not reflection text (pass 2 open decision 20). The first version's `managebac_readonly_import` value is withdrawn, since a one-time import is what `caros` already means. "Earlier than B9" is read as B6, with the `ib.*` tables the mirror writes. The mirror's new columns sit in D10.
11. **A KHDA absence-reason family** (F65) is pass 2 open decision 19, not an ACS question, because Wellesmere is fictional and the question belongs to a real Dubai school. The DfE family stays as a starting lookup, labelled English statutory guidance.
12. **Consistency fixes not caused by a finding.** Rung 1 of the identity ladder checks `privacy.erased_external_identity`, as pass 4 §7.3 says pass 2 does; ManageBac tokens rotate each school year (pass 4 §9.3); `after_commit` marks final results for pass 3's F64 rule. "Mr Diaz" stays once in 02 §7.4 as the ACS coordinator whose paper process is described: a person to ask, not demo data, which F29 does not cover.

**Pass 3**

13. **"Floors at the full typical noise"** (C9) is read as: every measure family's scale floor is one full unit of a typical student's per-point noise (percentage attainment 3.0 to 4.0 points; attendance from a day-level binomial; proportions and counts at the larger of the student's own rate and the typical one), and no minimum meaningful change sits below that unit.
14. **The day-level floor** is `sqrt(n̄ · p · (1 − p))` with `p = max(p_own, p_typ)`, `p_typ = 0.05` and `n̄ = 10` days a block: 0.69 days for a student missing under 5% of days. `p_typ` is a configured constant, a planning assumption until ACS's history is imported (pass 3 open decision 23, question 159), and never computed from other students (invariant 2).
15. **Fortnight blocks** (F05, F25, F68). Attendance is counted in non-overlapping blocks of ten school days with the minimum meaningful change at 1.5 days, so one day never fires and two can. With a weekly count and a floor above one day, a student missing a day every week could never signal. The cost is that attendance moves at most once a fortnight.
16. **The z cap is 3.0**, the value F05 suggests. **One persistence definition** (F06): consecutive weeks of data with the CUSUM signal; C9's three weeks is `persist = 3`.
17. **Strong shocks through their own rule** (F05). A strong shock (z ≥ 5 and at least two floors) is moderate alone and strong when the previous point of the same measure was outside. The first version's condition, a strong shock with the CUSUM already at `h`, would have been almost unreachable under the cap.
18. **Exit and recovery** (F05). Exit needs level 0 over the last two weeks of data and the mean deviation since the case opened below the floor; recovery needs level 0 for three weeks of data and the same mean. The chart restarts when a case opens, and a triggering measure's baseline is frozen while the case is open, which also settles F78's S2 point.
19. **The attendance hold and the policy floor** (F05). The domain stays at weak until a configuration version cites the measured reason-code coverage (default bar 90%, question 157, pass 3 open decision 22). The chronic-absence policy floor is a rule, not a level, so it is not held; it now reads the year to date rather than a trailing four weeks, to match the convention it cites. That goes beyond F05's letter.
20. **`relapse_watch`** is a design addition. While attendance is held at weak a relapse on it cannot reach moderate, so a weak level on a closed case's triggering measure inside its window opens a `monitor` case showing the history. Whether relapse should fire at weak during the hold is pass 3 open decision 21.
21. **`min_history_full` is withdrawn** (F25: "the self-starting band instead of the cap"); attainment history is counted in assessments; two-year courses are declared in the thresholds rule set (D31), not as a column.
22. **Termly precedence and results day** (F31, F64). The termly cap of `review` wins over relapse's minimum of `checkin` when all fresh evidence is termly; live evidence keeps the minimum. University rules run on calendar days outside every `sis.term`, so pass 1's `sis.calendar_kind` needs no new value.
23. **G-LIVE's primary endpoint is `review` or above** (F24, C9), because the detuned engine produces too few check-ins to judge at pilot scale; check-in precision is gated only if 40 such cases exist. The criteria are at least 40 blindly judged cases; a one-sided 95% Wilson lower bound at or above a pre-registered floor (defaults 25% at review, 50% at check-in, named by the counselors before shadow); engine cases judged "yes" more often than matched unflagged students; and a longest shadow of one term (pass 3 open decision 25, question 162). Going live starts live cases fresh, each linked to its shadow history (pass 3 open decision 24).
24. **The backtest's key custodian** (F23) is named as Davide, CAROS's data custodian during the pilot.

**Pass 5**

25. **C1's scope** follows session 1 (pass 4 open decision 20): no model reads student text in the safety screen; discovery chat, pathway analysis and Extended Essay feedback still send student text to the model under R8, pending Davide's confirmation.
26. **Audit keys** (F12). Beyond `AI_SAFETY_ROUTED` becoming pass 4's `SAFETY_ALERT_ROUTED`, pass 5 renamed `AI_SAFETY_FLAG` and `AI_SAFETY_REVIEWED` to `SAFETY_ALERT_RAISED` and `SAFETY_ALERT_DISPOSED`, since no model performs those acts. Pass 4's three keys are used, not seeded twice.
27. **One open alert per student** (F12). Further hits append to an alert that has no disposition, adding recipients and raising urgency only, so three flagged messages are one alert rather than three routings. This is a design reading, not decision text.
28. **Admit rates** (F77) are dropped from student and parent screens, the first of the two options F77 offered. The institution-wide rate stays an input and appears, labelled, on the counselor's file only.
29. **The reader extracts three record kinds** (F21): teacher flags, parent messages and Extended Essay rationales, the kinds an enabled v1 writer reads under pass 4 §6.4. Besides `mentor.message`, D69 also drops student reflections and statement drafts.
30. **Nightly pre-warming as the case owner** (F44). Pass 1's `auth.allowed()` refuses any non-user actor under the AI tier, so pre-warming sets `actor_kind = 'user'` with the owner as person and records `initiated_by = 'prewarm'` (D63), so the audit shows the worker started it. Pass 1's test wording disagrees with its own function; see the handoff.
31. **The name rule** (F20): no length floor, Arabic script and transliteration, school-recorded nicknames, and kinship and role cues for people outside the directory. Recall bars are proposed at 0.99 for directory names (a gate before G-AI) and 0.90 for others, measured on sentences written by staff outside the team (question 164).
32. **Teacher flag text** (F10). A lexicon hit on a `concern`, `positive` or `note` flag raises an alert about the student and shows the student nothing; a hit on a `safeguarding` flag raises no second alert, because the flag is already a referral.
33. **Helplines and categories** (F11, F09). Pass 5's re-check found that the Family Care Authority's page states no hours for 800444, so it renders "hours not published" until the CPO confirms; 800988 rests on a 2020 Dubai Media Office release and is left to a Dubai school's CPO. Lexicon categories map to pass 4 §5.2's example route keys, with exploitation and grooming under `maltreatment` (question 151).
34. **Thinking allowances** (F70) use the review's illustrative range per feature until B7.5 measures them (pass 5 open decision 22). The daily ceiling stays $10 at pilot and scales to three times the expected daily spend from the second school, because a flat $10 would pause pre-warming on a peak-month day at year-1 volumes.
35. **Naviance and the ManageBac essay** (F69, C10). A re-read on 2026-09-24 found the Scattergrams sentence on "College Planning in Naviance"; the plan attributes it there and paraphrases instead of quoting. Extended Essay feedback where ManageBac holds the essay is pass 5 open decision 21, with question 163.

**Reconciliation across the three files**

36. Pass 2 said the class register fed punctuality; pass 3 now counts one lateness measure in late days from the master register and reads the class register only as a fallback, so pass 2 follows pass 3, including the fixture's re-authored series (2.59). Pass 3's new daily-register question duplicated pass 2's question 71, so 71 absorbed it. Pass 5's copy lint would refuse a pass 2 sentence using "matched" (2.58). The new questions were numbered in file order.

**Numbering used, for session 3 to continue from.** Questions for ACS 156 to 164 (156 in pass 2; 157 to 162 in pass 3; 163 and 164 in pass 5; the next is 165); question for counsel C33 (pass 5; the next is C34). Pass 2 open decisions 18 to 20 (4, 5 and 11 closed); pass 3 open decisions 21 to 25 (5, 12 and 20 closed), pass 3 Challenge C11, scenarios S24 to S26 and a Wellesmere expected-result list W1 to W8; pass 5 open decisions 21 and 22 (4, 6 and 10 closed). No D-number was added: D1 to D16, D17 to D32 and D62 to D75 were revised in place, and D15 is kept as a withdrawn key so pass 6's map does not shift. No section was renumbered; four pass 5 headings were retitled with their numbers kept (§3.7, §6.2, §6.3, §6.6).

### Changes to `02-ingest-and-integrations.md`

| # | Change | Caused by | Section |
|---|---|---|---|
| 2.1 | Revision note under the title, in the form session 1 used | all | header |
| 2.2 | The termly consequence covers behaviour too; the revision adds no departure and narrows the incumbents challenge (ManageBac's API exposes CAS and projects; ManageBac stays the record, CAROS mirrors) | §12.5, F27, C10 | §0.1 |
| 2.3 | `s17`'s dash described in words (em dash removed); attendance rate in days from the master register; one guardian person per SIS contact and pass 4 §1.5 for `unknown` consent; CAS history as mirror rows at a ManageBac school | style, F05, F57, C10 | §0.2 |
| 2.4 | New §0.4 summarising the revision | all | §0.4 |
| 2.5 | Fixtures load only from a CI-signed manifest; `fixture_unsigned` | F57 | DR-10 |
| 2.6 | `const` is a binding, not a transform (nineteen transforms); `school_admin` is a role; the engineer works under a school-approved onboarding grant and saves profiles under a write grant (`WRITE_GRANT_EXECUTED`) | §12.5, F16, F35 | DR-11 |
| 2.7 | Guardians: one person per SIS contact, never merged; merges are for students and staff | F57 | DR-12 |
| 2.8 | Three SIS-fed domains arrive late under termly delivery; capped term backfill for dated rows, retrospective mode for aggregates (pass 3 §4.8); Data Export Package as unattended delivery | §12.5, F25, F54 | DR-14 |
| 2.9 | `ingest.precedence` cross-reference §7.8 → §7.9 | §12.5 | DR-15 |
| 2.10 | Middleware rejection also rests on ADEK Digital Policy 7.1.3.a (a further party needs ADEK consent) | F04 | DR-16 |
| 2.11 | State table: real-tenant imports refused until `status IN ('shadow','live')`; access-changing imports approved by someone other than the uploader, never auto-approved (`approver_is_uploader`); support rollback only under a school-approved write grant; sample imports always end `discarded`; connector auto-approval covers facts only | F36, F57, F35, F23 | §2.1 |
| 2.12 | Upload and approval by the `school_admin` role with pass 4's `ingest` rights (the first version's counselor-capability row withdrawn); different approver for access-changing kinds, so two `school_admin` holders; OneRoster cross-reference §7.7 → §7.8; raw exports read by the `school_admin` only, under step-up, never on a support grant; raw files, staged and rejected rows never leave the tenant | F16, F57, §12.5, F15, F35, F23 | §2.2 |
| 2.13 | DDL: new staged entity kinds (enrolment programme, CAS experience, caseload, tutor groups, pastoral); `needs_decision` covers under-13 consent; new `ingest.consent_decision`; external-ref kinds for tutor groups, CAS experiences, EE projects; `precedence.field_key` examples aligned with §7.9 and its cross-reference §7.8 → §7.9; conflict keys `selection.level` and `cas.hours_required`; `ingest.import.fixture_manifest_sha256` and CHECK `import_approver_differs`; `sis.attendance_event.derived_rule` | F33, F35, F56, F27, §12.5, F17, F57, F05 | §2.3 |
| 2.14 | Jobs: `validate` runs the under-13 check and derives day rows; `diff` computes reason coverage; commit order adds guardian links, tutor groups, pastoral and caseload assignments; `after_commit` enqueues the backfill with cadence and marks committed final results for pass 3's offer check; connector tick lists schools through `core.schools_holding_data()` and runs only for `shadow` or `live` | F56, F05, F33, F35, F25, F64, F36 | §2.4 |
| 2.15 | Validation: `erased_subject` at rung 1 as pass 4 §7.3 expects; the under-13 check (mapped consent column or a Studio decision; the student's other rows rejected until then); cross-row checks for SIS email change (reactivation), imported restrictions, unknown counselor, register disagreement | pass 4 §7.3, F56, F57, F35, F05 | §2.5 |
| 2.16 | Reason codes: `unknown_counselor`; `conflict:` cross-reference §7.8 → §7.9 with `conflict:selection.level`; pass 8 codes listed | §12.5, F17, F56, F57, F05 | §2.6 |
| 2.17 | Preview: Consent, Attendance (register source, reason coverage) and Approval lines; guardian links gained listed one by one, with restrictions and reactivations; caseload, pastoral and EE-supervisor lines; figures marked illustrative | F57, F56, F05, F33, F27 | §2.7 |
| 2.18 | Rollback by the `school_admin` role under step-up, or by support only under a school-approved write grant naming the function | F35 | §2.10 |
| 2.19 | Studio: the `school_admin` role; source kinds from the vocabulary; signed fixtures; lookups pre-seeded from the regulator profile's family; activation under step-up; second approver; consent decisions beside identity decisions; reason coverage on Cadence; `PREVIEW_ACCESS` (D9) instead of "pass 4 decides"; raw rows under step-up | F16, F65, F57, F31, F56, F05, §12.16 | §2.11 |
| 2.20 | Profile example: the em dash in `null_tokens` written as the JSON escape `\u2014` | style | §3.1 |
| 2.21 | Natural keys: enrolment programmes, teacher-only staff memberships, guardians and links by SIS contact, the `DAY` row rule, CAS mirror rows, EE mirror rows, caseload, tutor groups and members, pastoral assignments | F65, F16, F57, F05, F27, C10, F35, F33 | §3.4 |
| 2.22 | Composite import kinds `pastoral` and `caseload`; enrolment carries its programmes | F33, F35, F57, F65 | §3.6 |
| 2.23 | Profile tests also run for the CI-only `wellesmere-shifted` tenant | F31 | §3.7 |
| 2.24 | Ladder: erased check at rung 1; rung 3 for staff and students only, never to a `left` or `erased` person or one whose stored Google subject differs; sign-in binding as pass 4 §2.1; guardians on the SIS contact id alone | pass 4 §7.3, F75, F57 | §4.2 |
| 2.25 | Sessions end with the last active membership, not "at `left`" | pass 4 §2.1 | §4.4 |
| 2.26 | Guardians are never merged in CAROS; the split and late merge reversal need the school's `school_admin` or a school-approved write grant | F57, F35 | §4.6 |
| 2.27 | Guardians rewritten: one person and one set of links per SIS contact (the one-person-per-email recommendation withdrawn); imported restrictions suspend one contact; links written at commit after a second approval; `reactivation_required` on SIS email change; `unknown` consent per pass 4 §1.5; `lives_with` unused | F57 | §4.7 |
| 2.28 | Staff rewritten: sign-in binding per pass 4; `teacher` memberships only for teaching staff, other roles and capabilities granted in CAROS; the `pastoral` import of tutor groups and heads of year, inert without the capability; staff access ends through the mandatory hourly Directory read, the 14-day grace only guarding against a truncated file | F75, F16, F10, F33, F18 | §4.8 |
| 2.29 | IGCSE scales added (labelled an assumption); the Faria "around April 30" date withdrawn as the Group 6 upload deadline | F65, F74 | §5.1 |
| 2.30 | Attendance: `derived_rule`; master register feeds the rate in days, the class register punctuality and lesson-level lateness; declared day rule (`half_days`, `all_periods`, `majority_periods`) with every derived day flagged; the reason family from the regulator profile, the DfE family a starting lookup only; vocabulary history; ADEK the family ACS's profile names; reason-code coverage measured on every attendance import; lateness measures left to pass 3 | F05, F31, F65, F48, F25, F68 | §5.2 |
| 2.31 | Security section covers every access-changing kind; a roster batch is never auto-approved, whatever its security section; tests for second approver, individual links, restricted contact, inert pastoral assignment | F57, F33 | §5.4 |
| 2.32 | Canonical `restriction` on guardian links | F57 | §5.5 |
| 2.33 | The v2p2 academic-years path marked as read, the adapter pinning v2p3 | F27 | §5.6 |
| 2.34 | Termly: dated rows run as a capped term backfill, aggregates as retrospective mode; the three late domains named; what the pipeline hands the engine (cadence and granularity), with no pass 3 threshold restated | F25, §12.5 | §6.2 |
| 2.35 | Cadence table: termly split into dated rows and aggregates only; mode chosen from cadence and granularity; shadow comparison includes backfill runs | F25, F24, F31 | §6.3 |
| 2.36 | Weekly pack: seven queries, four weekly (was "six"); the Data Export Package (schedule, twice a day, five-minute limit, overwrite, password SFTP); master versus class attendance; programmes, under-13 consent date, restrictions, caseload feed; termly fallback in two modes; the history import (at least twenty weeks before shadow, exported month by month) | §12.5, F54, F05, F65, F56, F57, F35, F25, F24, F06 | §6.4 |
| 2.37 | Fixtures: ACS history import first; Wellesmere history plus both termly modes; engine results generated by the rules for both tenants | F06, F25, F31 | §6.5 |
| 2.38 | Interface: `SourceKind` becomes `AdapterKind` (source kinds a vocabulary, `maia_csv` dropped, `google_directory` added); import kinds `pastoral`, `caseload`, `cas_mirror`; `ACCESS_CHANGING` set | F65, F18, F33, F35, F27, F57 | §7.1 |
| 2.39 | Connector runs: schedule per school holding data; auto-approve facts only, the schema refusing `true` for access-changing kinds and mirror batches that move a supervisor held; `verify` also refuses Veracross alerts and Health; Monitor alerts carry counts, not student ids | F36, F57, F54, F60 | §7.2 |
| 2.40 | Veracross: API Plus has Rostering and Gradebook only and speaks the API format; Data Export Package; read scopes on student alerts and Health refused; `OAuth_App_Admin` attributed to Veracross's pages, not Edlink's; rate limit 300 per 3 minutes per access token; Data API public on Stoplight; 1EdTech listing expired; the watcher moves each overwriting export into the immutable container; master and class rows mapped by use | F74, F54, F05 | §7.3 |
| 2.41 | ManageBac: v2p3 pinned; per-token scopes; `deleted_since`, `modified_since` and paging corrected; Projects expose CAS experiences, hours required and EE projects (TOK and CAS reflection text still absent); a roster never writes an IB pick (conflict row instead); CAS and EE mirror rows; three record-system values (`managebac_readonly`, `caros`, `off`); ACS on `ee_2027`, `ee_2018` for resits; the mirror adapter built with the IB module; subject selection CAROS-owned; adapter plan with mirror resources; token rotation per pass 4 §9.3 | F27, C10, F74, F17, F62 | §7.4 |
| 2.42 | iSAMS: a Dubai school's codes are its own, not English law; tutors and heads of year imported; AM/PM registers use `half_days` | F65, F33, F05 | §7.5 |
| 2.43 | Classroom: reader must be a domain administrator or a teacher of each course; the mandatory Directory read (hourly for staff, nightly for students, outside the preview path); `engagement_source` vocabulary and `occurred_on` from the tenant timezone; pass 4 §6.6 is the engagement rule | F74, F18, F65, pass 4 D56 | §7.6 |
| 2.44 | OneRoster: API Plus is read by its adapter, not by the CSV profile family | F74 | §7.8 |
| 2.45 | Precedence: staff membership note corrected; `selection.level` CAROS-only; attendance day status from the master register; `cas`, `ee` mirrored at a ManageBac school; `pastoral` row; "never maintained twice" at a mirror school | F16, F17, F05, C10, F33 | §7.9 |
| 2.46 | Fixture intro: `expected.json` written from planted defects; signed manifests; pipeline oracle and engine oracle kept apart, the engine's generated by the rules | F57, F06, F31 | §8 |
| 2.47 | ACS fixture: `2025-26-history/` backfill pack; twenty in-band weeks for every authored case; punctuality measure read from pass 3; reason mix; multiple programmes; `Restriction` column; defect 6 now two persons; defects 19 to 25 (restriction, reactivation, individual link, under-13, register disagreement, approver, unsigned fixture) | F06, F05, F65, F57, F56 | §8.1 |
| 2.48 | Wellesmere fixture: history pack; Michaelmas dated rows and Lent term grades only; `wellesmere-shifted`; `ContactRestriction`; pastoral import from forms and year groups; half-day rule and the school's own codes; gradebook `AssessmentType`, `DueDate`, `Submitted`; DSL as one teacher membership with the capability granted in CAROS; inert Head of Year; restricted contact | F31, F25, F57, F33, F05, F65, F10, F16 | §8.2 |
| 2.49 | What fixtures prove: consent and attendance lines asserted; three tenants; both tenants' engine results generated; `selection-conflict.spec.ts` | F31, F06, F17, F05 | §8.3 |
| 2.50 | Onboarding intro: one status model (`onboarding` → `shadow` at G-REAL before step 7 → `live` at G-LIVE); CAROS writes only under the bootstrap or a school-approved write grant; real data seen on managed devices, shown only in person or on the school's own video platform, never in tickets or coding sessions | F36, F35, F61, F23 | §9 |
| 2.51 | Onboarding steps: 1 attestation; 3 tenant under the bootstrap grant with `purpose = 'customer'` and immutable classification and branding; 4 CAROS owns the OAuth client, Limited and under-18 configuration, DNS-proved domains, Directory scope; 5 the school's two `school_admin` holders grant counselor memberships and `caseload_lead` under four eyes; 6 calendar by hand; 7 G-REAL gate, sample with free text blanked, discarded, kept in the tenant; 8 profiles under a write grant; 9 password SFTP for the Data Export Package; 10 at least twenty weeks of history, second approver, F61; 11 masked review; 12 binding per pass 4; 13 caseload as a Studio import; 14 shadow since step 7; 16 fact-only auto-approval; 17 canary results, no tests in the school's tenant, audit read per pass 4 §4.3; 18 no just-in-time provisioning, memberships by source, under-18 access, off-email activation codes; 19 pooled G-LIVE criteria; 20 `shadow → live`, `sweep_hour`, watchdog at 05:45; CAROS human-step list updated | F36, F35, F42, F67, §12.4, F18, F16, F57, F23, F54, F24, F61, F75, §12.1 | §9 |
| 2.52 | Deltas revised in place with a landing-phase column: D1 status gate; D2 `ingest.consent_decision`; D3 manifest hash and approver constraint; D4 `derived_rule`; D7 vocabulary rows for source kinds and engagement sources; D9 `UNDER13_CONSENT_RECORDED`, `CONFLICT_RESOLVED`, tier lists, `PREVIEW_ACCESS` here only, pass 1's import keys not repeated; D10 `managebac_readonly` (replacing `managebac_readonly_import`) and the mirror's columns on `ib.cas_entry` and `ib.ee_essay`; D12 `security_invoker`; D15 withdrawn as a row, key kept; D16 `writable_columns`; disagreements with pass 6's task tables noted on D8, D11, D13, D14, D15 | F40, §12.18, F36, F56, F57, F05, F65, F17, §12.16, F75, F27, C10, F16 | §10 |
| 2.53 | Challenges: 11.1 counts, Data Export Package, termly fallback in two modes; 11.2 narrowed (Maia, TOK and CAS reflection text remain); 11.3 ADEK 7.1.3.a and question 149; 11.7 tightened to never auto-approve access-changing imports | §12.5, F54, F25, F27, C10, F04, F57 | §11 |
| 2.54 | Open decisions: 1 reordered with the Data Export Package as (a); 2 password path; 3 narrowed to facts; 4 closed (one person per SIS contact); 5 closed (ManageBac record, CAROS mirror); 10 activation by the school, CAROS review not a write; 11 closed; 17 portal check, $0.30 an hour, password path; new 18 (day rule), 19 (Dubai reason family), 20 (EE reflection text in the mirror) | F54, F57, F27, C10, F35, §12.15, F71, F05, F65 | §12 |
| 2.55 | For other passes: framing note; pass 3 additions (day rows, coverage input, mode inputs, history, fixtures, mirror rows); pass 4 answered items and the reader's minimum role still open; pass 6 exceptions (mirror with the IB module, Directory read early), fact-only auto-approval, coverage on the cadence panel | F05, F25, F24, F06, F31, C10, F74, F54, F35, F57, F18, F56, F40, F27 | §13 |
| 2.56 | Questions for ACS: intro; 70 (Data Export Package), 71 (official daily register, reason share), 73 (password SFTP), 78 (scoped token, CAS in Projects, EE template, reflection text), 81 (no alerts or health scopes), 83 (twenty weeks, month-by-month export), 84 (Directory scope, reader role), 85 (two people to upload and approve) revised; 156 added | F54, F05, F27, C10, F24, F74, F57, F56, F18 | §14 |
| 2.57 | Sources: a note that every added vendor fact comes from the pass 7 review, by finding; 1EdTech listing expired; Edlink annotations corrected (rate limit per token; no `OAuth_App_Admin`); Faria date annotation; Directory API page added as pass 4 cites it | F54, F74, F27, F18 | Sources |
| 2.58 | Note for pass 5: unmapped subjects "cannot be compared with" offer conditions, not "matched" (session lead, after pass 5 flagged it) | F51 | §13 |
| 2.59 | Lateness aligned with pass 3's one measure (session lead): late days from the master register, the class register read only for a late mark in the first registration period and for the teacher's register view; the ACS fixture's authored series in days missed and late days per pass 3 §7.2; question 71 also asks whether the daily register records lateness and half days | F05, F06, F68 | §0.2, §0.4, §5.2, §6.4, §7.3, §8.1, §13, §14 |

### Changes to `03-signal-engine.md`

| # | Change | Caused by | Section |
|---|---|---|---|
| 3.1 | Revision note under the title | all | header |
| 3.2 | §0.1 declares one departure in detail (attendance in days from the master register, not CONTEXT §3's "per-session attendance") and names the detuned pilot defaults | F05, C9 | §0.1 |
| 3.3 | §0.2 rows: Priya's series re-authored as late days; Ahmed's history printed in assessments; run cells read the engine's `outside`; Yousef's mock judged against his band; broken `§9.3` reference fixed | F06, F78, F40, F31 | §0.2 |
| 3.4 | §0.4 "The engine on one page" rewritten for the revised engine | F05, C9, F25, F26, F07, F08, F10, F24, F48 | §0.4 |
| 3.5 | New §0.5 summarising the revision | all | §0.5 |
| 3.6 | "New item" defined as an entry into a tier (occupancy separate); queue size per weekly pack | F06 | §1.1 |
| 3.7 | Point targets for the pilot exit replaced by pre-registered pooled intervals over at least about 40 blindly judged cases | F24, C9 | §1.4 |
| 3.8 | Balfanz, Herzog and Mac Iver: the 60% is for four flags (re-read in the paper, 77% and 12,972 confirmed); Faria et al.: 10% against 14%, 21% against 26%, −0.26 and −0.17, no +0.23; AEI: unexcused share of absences 35% to 55%, *What Stories* re-read with authors and date; chronic-absence threshold regulator-neutral | F69, F31 | §2.1 |
| 3.9 | ARLs computed by the appendix script (one-sided 931 and 335; with the cap 974 and 346; two-sided about 465 and 168); SigmaXL no longer credited with 465; count floors at the student's own rate; "about 4%" replaced by 7.9% computed | F69 (A.6), F78 | §2.3 |
| 3.10 | Points per assessment (attainment), per week, per fortnight block (attendance); "weeks of data" defined; provisional blocks | F25, F05, F68 | §3.1 |
| 3.11 | Window in weeks of data; attainment carried across a two-year course; episode exclusion extended to open cases (S2 consistency); `min_history_full` withdrawn; twenty weeks of history imported before shadow | F25, F78, F06, F24 | §3.2 |
| 3.12 | Scale widened by `sqrt(1 + 1/H)`; scale floors at the full typical noise (percentage 3.0 → 4.0 points; day-level binomial for days missed; own-rate floors for proportions and counts); the reading of "full typical noise" stated; minimum meaningful change 1.5 days per block, conduct 3 points per week | F05, C9, F25, F68 | §3.3 |
| 3.13 | Detectors: ordinal one-step hold across two assessments; strong shock through its own rule; z capped at 3 inside the CUSUM; one persistence definition (consecutive weeks of data with the CUSUM signal); chart restart when a case opens; `outside` written on every point | F05, F06, F68, F40 | §3.4 |
| 3.14 | Levels at `persist` 3; three caps (self-starting, attendance held at weak, second assessment); the "thirteen check-ins a week" claim withdrawn as a finding; scale-free rates from the script | F05, C9, F06 | §3.5 |
| 3.15 | Calibration table, detection figures and defaults regenerated from `calibrate_tiers.py`; the slower detection stated | F06, C9 | §3.6 |
| 3.16 | Scale table: percentage floor 4.0; ordinal hold with computed rates (17.48% → 5.04% outside, 0.28% moderate); days-missed row | F68, C9 | §3.7 |
| 3.17 | Missing data per assessment; seasonality without the common-cause catch; CUSUM reset and carry-over; two-year course inheritance | F25, F26 | §3.8 to §3.10 |
| 3.18 | Academic: per-assessment attainment; mock judged against the section's band (feeds a level); missing count per fortnight block; evidence sentence prints the real history | F25, F31, F78 | §4.1 |
| 3.19 | Attendance rewritten: master register in days, fortnight blocks, register/unit/window per measure, held at weak until reason-code coverage is measured, one lateness measure, unexplained-days rule, year-to-date policy floor, class register only as a lateness fallback | F05, C9, F25, F68 | §4.2 |
| 3.20 | Engagement reads `occurred_on` only | pass 4 D56 (session 1) | §4.3 |
| 3.21 | Behaviour: weekly floor and weekly shock | F68 | §4.4 |
| 3.22 | Teacher concern: a `safeguarding` flag is a referral, not an engine input; vocabulary attributes and versions snapshotted; no model headline summary | F10, F48, F52 | §4.5 |
| 3.23 | `offer_condition_check` on final results; university clocks in calendar days outside term (`sis.term`) | F64 | §4.6 |
| 3.24 | Recovery on levels and the mean since opening; no recovery on a pinned case; relapse only from person-closed cases; relapse example made consistent; `relapse_watch` during the hold; termly cap wins over relapse's minimum | F05, F07, F78, F31 | §4.7 |
| 3.25 | Cadence: termly files with dated rows run as a backfill capped at review; retrospective rules compare within a subject over three prior terms and need a two-step or held drop; the screen says which domains are building a baseline | F25, F31 | §4.8 |
| 3.26 | Cold start: self-starting from three points, band-phase cap withdrawn, history import of at least twenty weeks, two-year carry | F25, F06, F24 | §5 |
| 3.27 | Suppression: common-cause input withdrawn; authorised absence in days and blocks; vocabulary snapshots in suppression checks | F26, F05, F48 | §6.1, §6.2 |
| 3.28 | Breadth: the three-week claim from an unpublished script marked as such; block evidence window | F06 | §7.1 |
| 3.29 | Combination rows revised (row 2 safeguarding note, row 8, rows 9 and 11 defined, row 10 results day, row 12 not "alone", row 14 as a cap with precedence, row 15 incl. `relapse_watch`, row 16 pin); seed reproduction re-traced with re-authored series; expected lists generated by running the rules; 8 of 15 match, 7 differ, with reasons | F10, F68, F78, F64, F31, F07, F05, F06 | §7.2 |
| 3.30 | Persistence: one definition, `persist` 3, computed rates; the first version's 0.65% explained | F06, C9 | §7.3 |
| 3.31 | Recovery summary: levels, mean since opening, never on a pinned case | F05, F07 | §7.4 |
| 3.32 | Event runs never lower a pinned case; shadow event runs | F07, F24 | §7.5 |
| 3.33 | Common-cause cap withdrawn (heading kept); operations count and prompt as the successor, never an engine input | F26 | §7.6 |
| 3.34 | Tier table: urgent never lowered or closed by the engine while an escalation is unreleased; university clock outside term | F07, F64 | §8.1 |
| 3.35 | Movement rules: the escalation pin, a second concern on an escalated case, merge within one student, rate limit on lowerings and reversals only (never a raise into checkin or urgent) | F07, F08 | §8.2 |
| 3.36 | Hysteresis: exit on levels and the mean since opening (no `h_exit`); pin and reversal limit in `lower(case)` | F05, F07, F08 | §8.3 |
| 3.37 | Auto-review never closes a pinned case or one inside the `ESC` window | F07 | §8.4 |
| 3.38 | Statement dimension from `uni.statement_complete()`; `stmt_watch_below` withdrawn | F32 | §9 |
| 3.39 | Parameters: `measures.<key>` (register, unit, window), `cusum.z_cap`, `attendance.max_level`, `typical_absence_share`, `persist` 3, ordinal hold, two-year carry; tiering adds `termly_backfill_cap`, reversal cap; withdrawn `min_history_full`, `exit_h_factor`, `common_cause_share` | F68, C9, F05, F25, F26, F08 | §10.1 |
| 3.40 | Pilot scale about 350 students | F78 | §10.4 |
| 3.41 | Elicitation cards and Part B in days; card 11 answers open decision 21; Part F collects the G-LIVE floors | F05, F24 | §10.5 |
| 3.42 | §11.1 rewritten from the script: native-unit students, entries, occupancy, cause breakdown, genuine-change detection table, levers | F06, C9, F05 | §11.1 |
| 3.43 | Queue: three weeks before a card; common-cause re-run removed; the prototype's within-tier sort not ported and tested | C9, F26, F50 | §11.2 |
| 3.44 | Fairness test: not run in the pilot; pooled across customer tenants only; pre-registered, pooled across quarters, 95% intervals, Bonferroni; what still runs without labels | C11, F68, F42 | §12.2 |
| 3.45 | Privacy tension: no labels in the pilot, D26 unused; nationality not confirmed as a PDPL category; the DPIA | C11, F69 (A.6), F37 | §12.4 |
| 3.46 | Engine input: restricted domains (pass 4 D58), carry-over, cadence and register, shadow flag, escalation pin, recent moves, vocabularies; no field for a school-wide share | F07, F08, F24, F26, F48, pass 4 D58 | §13.1 |
| 3.47 | Orchestration: `sweep.schedule` and the watchdog through `core.schools_due_for_sweep()` (holds data); finalize counts after writing, reporting aggregates not materialised views; backfill for termly and history imports; job names and 05:45 match pass 1 | F36, F26, F34, §12.1, F25, F78 | §13.2 |
| 3.48 | Idempotence: one evaluation per run, student and evaluated week; `outside` flags written | F40 | §13.3 |
| 3.49 | `sweep.partial` carries counts, not ids; provider named | F60 | §13.4 |
| 3.50 | Re-runs: termly and history backfills; shadow runs of every trigger; example dates on school days; shadow overnight columns | F25, F24, F78 | §13.5, §13.6 |
| 3.51 | Alerts: holds-data schools; counts only through Azure Monitor action groups; every count and engine-quality alert keyed on `purpose` | F36, F60, F42 | §13.8 |
| 3.52 | Shadow rewritten: shadow cases on the live lifecycle, all run triggers, history first, escalations never shadow, going live; reveal weeks counted from the first shadow sweep; blinded judgement with matched unflagged students; G-LIVE on pre-registered pooled intervals with at least 40 cases; fairness criterion withdrawn; DPIA | F24, C9, F75, §12.17, C11, F37 | §14 |
| 3.53 | Scenario suite: S1 to S23 revised (S1, S2, S3, S7, S8, S9, S10, S11, S12, S13, S14, S15, S19, S20, S21, S22, S23), new S24, S25, S26; Wellesmere expected results W1 to W8; regulator-neutral test; property tests (strict no-comparison, within-tier order, pin, vocabulary snapshots) | F05, F06, F07, F08, F10, F25, F26, F31, F48, F50, F64, F68, F78 | §15.1 |
| 3.54 | Backtest as a UAE North job, key custody named, aggregates only; measures the planning constants and coverage; master register; no fairness screen | F23, F37, F05, C11 | §15.2, §15.3 |
| 3.55 | Signal records: example dates on Mondays, revised features and inputs, `vocabulary_used`, headline rephrase removed, evidence and evaluation fields, `triggering_state` | F78, F48, F52, F24, F07, F05 | §16 |
| 3.56 | Deltas revised in place: D17 unique key and no seed migration, D19 note, D20 cadence and phases, D22 `triggering_state`, D24 class, D25 rewritten with `security_invoker`, D26 unused in the pilot, D27 absence reasons and versions, D28 without `sweep.completed` and with new events, D30 pass 4's keys, D31 schema additions and removals; tests list | F40, §12.11, F24, F05, F75, C11, F48, §12.16, F68, F25, F26, F07, F50 | §17 |
| 3.57 | Appendix replaced by the published `calibrate_tiers.py` and its output | F06 | Appendix |
| 3.58 | Sources corrected, completed and re-found | F69, A.6 | Sources |
| 3.59 | Challenges: C5 closed; C7, C8, C9 revised; new C11 (the cost of the detune) | F26, F10, C9, F06 | Challenges |
| 3.60 | Open decisions: 5, 12, 20 closed; 14, 16 revised; new 21 to 25 | F26, C11, F24, F25, F31, F05 | Open decisions |
| 3.61 | "For other passes" rewritten | F52, F24, F06, F05, F23, F40, F36 | For other passes |
| 3.62 | Questions 95, 97, 100, 106, 108 revised; 157 to 162 added; the daily-register question folded into pass 2's question 71 by the session lead | F26, C9, F05, C11, F24, F64, F25 | Questions for ACS |

### Changes to `05-ai-design.md`

| # | Change | Caused by | Section |
|---|---|---|---|
| 5.1 | Revision note under the title paragraph | all | header |
| 5.2 | §0.1 declares two withdrawals (headline rephrase; the safety screen's model layer), matching 04 §0.1 items 8 and 9; ADEK consent under Digital Policy 7.1.3.a as a gate in item 1; C1 read as the safety screen only (04 open decision 20, reading (a)) | F52, C1, F03, F04 | §0.1 |
| 5.3 | Institution-wide offer and admit rates are an input and counselor-only, never printed per target on student or parent screens | F77 | §0.1, §2.2 |
| 5.4 | §0.2: discovery row (keyword floor, student corrections, no session end); new rows for "match" copy not ported and the "AI read" score not ported | F53, F51, F66 | §0.2 |
| 5.5 | §0.2 EE row: both current ACS cohorts on the 2027 guide, `ee_2018` for resits; criteria B to E names marked an assumption | F62, F69 | §0.2 |
| 5.6 | §0.2: parent draft row gains the content-free `parent_update_waiting` notice; letter row follows the UCAS reference format; audit row notes the dropped seeded `AI_DRAFT`; co-pilot promises row | C2, F60, F32, F72 | §0.2 |
| 5.7 | §0.3 records the withdrawn hand-offs (headline rephrasing, `headline_generation_id`, the model half of safety routing) | F52, C1 | §0.3 |
| 5.8 | §0.4 "on one page" rewritten for the revised design (two gates, AI tier allowlist, lazy reader, shown filter, tag floor, lexicon-only safety, admit rates off student screens, cost with thinking) | all | §0.4 |
| 5.9 | New §0.5 summarising the revision | all | §0.5 |
| 5.10 | §1 feature table: co-pilot filter shown and raw-text search; discovery floor and code-only session end; pathway "closest fit", programme family, submit any path; EE guide, confirmed limits, labels off decision screens; headline row dropped; safety row lexicon-only; reader lazy per kind with role counts; statement read sectioned with no score; letter engine per format; brief note bodies through a view | F53, F51, F65, F62, F77, F52, C1, F21, F32, F66, F20, F44 | §1 |
| 5.11 | Naviance: first quotation kept (verified); second re-attributed to "College Planning in Naviance" and paraphrased; UCAS offer rate calculator paraphrased and marked an assumption | F69 | §2.1 |
| 5.12 | §2.2: institution rate line counselor-only and labelled institution-wide; capped classification shown to families as "a highly selective institution" with no figure; NYUAD example dated | F77, F69 | §2.2 |
| 5.13 | §2.4 citations: UCAS Tariff per entry year with the 2027 IB values; Scorecard definition source corrected; McGill quote corrected; AUS SAT scope; `seed://` rows never render | F69, F30 | §2.4 |
| 5.14 | §2.5: archetype steps conditioned on programme family; UCAT tip and AAMC caveat corrected | F65, F69 | §2.5 |
| 5.15 | §3.0 contract: ADEK consent and canary checks in policy; `ai` class scope per F15; AI tier granted only to `caros_web` and `caros_worker`, `NOINHERIT`, grants generated from the allowlist; pre-warming as the case owner, `actor_kind = 'user'`, schools holding data only; fingerprint stripped; no stable user id; `ai.reidentify()` at render | F04, F42, F15, F01, F44, F36, F20, F76 | §3.0 |
| 5.16 | Envelope: `school` block reduced to vocabulary keys and date, source labels by kind, `mentions_others` as counts; rules (f) fingerprint and (g) single-student envelope | F20, F77 | §3.0.1, §3.0.2 |
| 5.17 | Review table: EE labels off decision screens; headline row withdrawn; reader row; safety screen noted as not a model feature | F53, F52, C1 | §3.0.5 |
| 5.18 | Audit keys: `SAFETY_ALERT_RAISED`, `SAFETY_ALERT_DISPOSED`, and pass 4's `SAFETY_ALERT_ROUTED`, `SAFETY_ALERT_NOTICE_SENT`, `ALERT_UNOPENED_NOTICE_SENT` in place of `AI_SAFETY_*` | F12, C1 | §3.0.6 |
| 5.19 | Co-pilot: compiled filter rendered above every answer and editable; text search over raw text; "meet these conditions" copy | F53, F51 | §3.1 |
| 5.20 | Brief pre-warming as the meeting's counselor under `caros_t_ai`, schools holding data, never canaries | F44, F36, F42 | §3.2 |
| 5.21 | Parent draft: CONTEXT's feature name kept, delivered in-app; parent's messages through the reader; content-free `parent_update_waiting` email per 04 §5.8; siblings never tokenised | C2, C3, F28, F60, F21, F77 | §3.3 |
| 5.22 | Discovery turn: lexicon first, keyword floor plus model additions with spans, student corrections, `answer_kind` descriptive only, session end by code or the student | F53, C1 | §3.4 |
| 5.23 | Pathway: "Closest fit" and `discovery.closest_fit_margin`; "Why this fits what you said"; any archetype submittable; programme-family roadmap; two stale cross-references fixed | F51, F53, F65 | §3.5 |
| 5.24 | EE feedback: `ee_2027` default for both cohorts; coordinator-confirmed limits and marks; rationale through the reader; labels off supervisor and coordinator decision screens; behaviour when ManageBac holds the essay | F62, F77, F21, F53, C10 | §3.6 |
| 5.25 | Headline rephrase section kept with a "dropped from v1" body | F52 | §3.7 |
| 5.26 | Safety screen not a model; reader lazy | C1, F21 | §3.8 |
| 5.27 | Statement read per section of `ref.statement_format`, completeness by `uni.statement_complete()`, no score, treated as not pseudonymisable | F32, F66, F20 | §3.9 |
| 5.28 | Letter engine data per `ref.reference_format`: centre statement never generated, extenuating circumstances `generation_forbidden`, predicted-grade snapshot, internal deadlines, voice matched per format and section | F32 | §3.10 |
| 5.29 | Retrieval: raw-text search, `text_search.kinds` by record kind, filter rendered, "returned because" and "meet these conditions" copy | F53, F51 | §4.2 to §4.4 |
| 5.30 | Discovery flow: state table without model safety calls, `chatIsDone` code-only, submit from any path, `paused_safety` exits on a person's disposition; "Closest fit"; a copy lint for "match"; review screen shows tag sources, picks outside the top three and `answer_kind` | F53, C1, F51 | §5.1 to §5.3 |
| 5.31 | Deterministic mode names the `discovery_chat` and `pathway_analysis` rows and filters steps by programme family | §12.8, F65 | §5.6 |
| 5.32 | §6.1 rewritten: the screen is a lexicon, every hit reaches a person, alert load owned by the CPO; citations corrected (Holmes wording, Character.AI dates) | C1, F03, F69 | §6.1 |
| 5.33 | §6.2 retitled "The lexicon screen, the only layer in v1": screened kinds include teacher flag text and EE rationales; Arabic script; categories mapped to route keys; urgency from severity; stored lexicon version and span; urgent requests; teacher-flag rules | C1, F10, F48, F21, F09 | §6.2 |
| 5.34 | §6.3 retitled "Layer 2: not in v1": what was removed, what replaced it, the add-only revisit terms after one term; Broadbent, Swaminathan and Weber citations corrected | C1, F03, F69 | §6.3 |
| 5.35 | §6.4: new template variables; helplines split into "talk now" (24/7 only) and "report harm", each with hours; no "any time"; Estijaba removed; contacts from the regulator profile and school; Dubai lines; draft until the CPO confirms; last-verified dates | F11, F31, F69 | §6.4 |
| 5.36 | §6.5 rewritten: `safety_alert` class; one open alert per student; routing by category as school configuration; CPO "alert unopened" notice, never a referral; disposition by a person only; explainability; false-positive rate for the revisit | F09, F12, F13, F48, C1 | §6.5 |
| 5.37 | §6.6 retitled "What the screen never does, as tests": no-model, every-hit, routing, unopened-notice, per-role flagged fixture, templates, helplines, teacher-flag, urgent-request and explainability tests | C1, F03, F11, F12, F13, F21, F48, F31 | §6.6 |
| 5.38 | §6.7 corpus measures the lexicon; §6.8 mentor messages through the lexicon, grooming email to the coordinator | C1, F13, F21, C3 | §6.7, §6.8 |
| 5.39 | §7.1 threat goals (screen cannot be persuaded; reflections and mentor messages off the writer list) | C1, F21 | §7.1 |
| 5.40 | §7.2: the reader's own policy row, lazy extraction for three kinds (table), never-extracted list, consumer test, role counts; Hines, design-patterns paper and NCSC citations corrected | F21, F77, F69 | §7.2 |
| 5.41 | Injection corpus expectations under the lexicon and for unread surfaces | C1, F21 | §7.5 |
| 5.42 | §8.1: AWS date 1 March; Bedrock in-region only in `me-central-1`; Vertex quotation re-attributed to Anthropic's page; Google Middle East offer; Foundry egress scoped | F73, F69 | §8.1 |
| 5.43 | §8.2: R1 to R12 as revised by pass 8, ADEK consent; R2 flags only on the registry | F04, §12.9, F20, F44, F63, F76, C1 | §8.2 |
| 5.44 | §8.3: single-student envelopes never tokenise a second student; fingerprint; name rule with no length floor, Arabic script, nicknames, people outside the directory; recall measured on an independent corpus; columns versus free text; statements not pseudonymisable; leak scanner scope; `ai.reidentify()` | F20, F77, F44 | §8.3 |
| 5.45 | §8.4: AI tier grants and accessors from one allowlist; note bodies through a view | F44 | §8.4 |
| 5.46 | §8.5: family notice with both retention periods, "flagged" explained, "inference in the United States", mentors abroad; AI generations out of the default subject-access pack | F63, F76, F20, C1, C4, F14 | §8.5 |
| 5.47 | §8.6: ADEK consent as the second blocking gate and its sequence; §8.7: in-country safety-screen item moot, discovery chat first | F04, C1, F20 | §8.6, §8.7 |
| 5.48 | §9.1: registry flags stated as the only home; effort table without headline and safety rows, with a thinking allowance column and larger `max_tokens`; thinking and `tool_choice` claims re-cited; launch-guidance claim marked an assumption | F52, C1, F70, §12.9, F69 | §9.1 |
| 5.49 | §9.4 to §9.6: settings (`closest_fit_margin`, reader kinds, no headline key); sign-off and candidate bars without a safety model; activation order of the five features behind both gates | F51, F21, F52, C1, F04 | §9.4 to §9.6 |
| 5.50 | §10.1 and §10.5: counselors' questions collected in the tenant and rewritten onto synthetic personas in-country; no-real-data test covers student names; lexicon review | F23, C1 | §10.1, §10.5 |
| 5.51 | §10.2 suites: co-pilot questions; discovery floor checks; headline suite dropped; safety suite is the lexicon; reader kinds and counts; pseudonymiser recall corpus | F23, F53, F52, C1, F21, F77, F20 | §10.2 |
| 5.52 | §10.3 profiles P1, P5, P6, P9 revised | F51, C1, F21, F65, F31 | §10.3 |
| 5.53 | §10.4 pass bar: pseudonymiser recall bar (proposed, gate before G-AI); lexicon bar replacing the model bar; P5; human sample | F20, C1, F03 | §10.4 |
| 5.54 | §10.6 production signals: alerts by category, false-positive rate by lexicon entry | C1 | §10.6 |
| 5.55 | §11.1 assumptions: removed lines, reader volume as an upper bound, thinking allowances, eval volume derivation | F70, F52, C1, F21 | §11.1 |
| 5.56 | §11.2 price table relabelled as list prices; the 1.1x sentence attributed to the pricing page; the "stack" quotation removed | F70, F69 | §11.2 |
| 5.57 | §11.3 results recomputed from the script; four observations rewritten (thinking roughly doubles the bill; 20% of infrastructure at pilot, about 73% at year 3) | F70 | §11.3 |
| 5.58 | §11.4 ceiling headroom recomputed; ceiling scales with the school from the second school | F70 | §11.4 |
| 5.59 | §12.2 refusal categories re-found (five plus null); safety screen unaffected by refusals | F69, C1 | §12.2 |
| 5.60 | §12.3 to §12.6: timeout rows for the reader; breaker without safety layer 2 or headline jobs; ceiling behaviours; `max_tokens` stops counted | C1, F52, F21, F70 | §12.3 to §12.6 |
| 5.61 | §12.8 degraded-mode matrix rewritten (reader column; safety screen always unaffected; gates row) | C1, F52, F21, F04 | §12.8 |
| 5.62 | D62 drops nothing from `ai.model_config`; FK on pass 1's `provider` column; agreement with passes 1 and 4 stated | §12.9 | §13 |
| 5.63 | D63 `output_kind` without `headline` and `safety`; `initiated_by` for pre-warming | F52, C1, F44 | §13 |
| 5.64 | D66 tag entries carry their source; `outside_top_three` on proposals | F53 | §13 |
| 5.65 | D68 rewritten: `signal.safety_alert` without `layers`, with lexicon version, hits, one-open index, disposition CHECK, class `safety_alert`; `urgent_request`; `safety.lexicon` rule set; `open_24_7` on contacts; helpline confirmation columns; `safety_response` variables | C1, F03, F11, F12, F13, F21, F48 | §13 |
| 5.66 | D69 extraction columns on three kinds only; revoked from teacher, student, guardian, mentor tiers; reader policy row and kinds | F21, F13, F77 | §13 |
| 5.67 | D70 `ee_2027` default, resits on `ee_2018`, coordinator confirmation columns | F62, F77, §12.12 | §13 |
| 5.68 | D71 safety keys renamed; pass 4's keys not re-seeded; `allowed_tiers` | F12, C1, §12.16, F75 | §13 |
| 5.69 | D72 voice samples carry `format_key` and `section_key` | F32 | §13 |
| 5.70 | D73 admission statistics counselor-only; archetype steps' programme families; `uni.classification` joins the `rule_set_version` key CHECK; §2.3 reference fixed | F77, F65 | §13 |
| 5.71 | D74 feature list equals pass 1's CHECK; `closest_fit_margin`; headline key withdrawn; ADEK consent and canary conditions | §12.8, F52, C1, F51, F04, F42 | §13 |
| 5.72 | D75 tests revised and added | C1, F13, F20, F21, F44, F51, F53, F65, F76, F77 | §13 |
| 5.73 | Sources: re-check note; new and corrected entries (College Planning in Naviance, refusals page, Opus 5.5 "What's new", Tariff 2027, Scorecard documentation, Dubai Media Office 800988, Holmes citation, AWS date) | F69, F11, F73 | Sources |
| 5.74 | Challenges 1, 3, 5, 6, 9, 10 revised | C1, F04, F52, F21, F70, F62 | Challenges |
| 5.75 | Open decisions 4, 6, 10 closed; 1, 15, 17 revised; 21 (EE feedback where ManageBac holds the essay) and 22 (thinking allowances) added | F21, C1, F52, F51, F70, C10, F04 | Open decisions |
| 5.76 | "For other passes": pointer to the changelog handoff; pass 6 build order; EE settled; pass 3 note; alert evidence keeps its class | F52, C1, F04, F70, F62, F13 | For other passes |
| 5.77 | Questions for ACS 129, 130, 132, 133, 137 revised in place; 163 and 164 added | F11, C1, F23, F62, F77, F04, C10, F20 | Questions for ACS |
| 5.78 | Counsel C18 and C22 revised in place; C33 added | F63, F37, C1, F20 | Questions for counsel |
| 5.79 | Appendix cost script revised and re-run; output reproduced exactly from the plan's copy | F70, F52, C1, F21 | Appendix |

### Decided items that needed nothing in 02, 03 or 05

F02, F19, F22, F38, F39, F41, F43, F45, F46, F47, F49, F58, F59, §12.2, §12.3, §12.6, §12.7, §12.10, §12.13, §12.14, C5, C6, C7 and C8: their changes belong to passes 1, 4 and 6; session 1 made the pass 1 and pass 4 parts, and the pass 6 parts are in the handoff below. F29: nothing in the three files presents a real person as demo data (pass 2 still names the real ACS coordinator as the person to ask, which F29 does not cover). F55: none of the three cites a Student Protection Policy clause.

---

## Handoff to session 3

**Completed in session 3** (2026-09-24): every item below was applied, in 06, in passes 1 and 4, in `CONTEXT.md` and in `ACS-IT-QUESTIONS.md`, except those listed under "Left undone after pass 8" at the end of this file; the Session 3 section lists every change. Every change another file still needs, with its finding number. Written by session 1; **updated by session 2** (2026-09-24), which completed the items for passes 2, 3 and 5 (marked *Done in session 2* below), added what the revisions of those passes need from pass 6, and listed the inconsistencies it found in passes 1 and 4, which it could not edit.

### `02-ingest-and-integrations.md`

**Completed in session 2.** Every item below was applied to `02-ingest-and-integrations.md`; the changes named after each item are the principal ones, and the Session 2 table lists every change by finding. Nothing further is owed here.

- **F16.** D15's row `('counselor','config','configure','school','school_admin')` becomes a row for the `school_admin` role, which is no longer a counselor capability; D15 is subsumed by pass 4's matrix in any case (§12.18). *Done in session 2 (2.12, 2.19, 2.45 and others).*
- **F17.** An import never writes a pick: a ManageBac or SIS level that differs from an approved pick becomes a conflict row for the coordinator (02 §7.4, reconcile 02:863 with 02:877). *Done in session 2 (2.13, 2.16, 2.41, 2.45, 2.49 and 2.52).*
- **F23.** Onboarding sample rows keep no free-text columns, or are processed only in-country (02 §9). *Done in session 2 (2.11, 2.12, 2.50 and 2.51).*
- **F25.** When a termly file carries per-assignment rows, describe delivery so the engine can backfill weekly (with pass 3). *Done in session 2 (2.34, 2.35 and others).*
- **F27, C10.** Add `managebac_readonly` to D10's `cas.record_system` and `extended_essay.record_system`; design the read-only mirror in §7.4 and §7.9 over the v2p3 Projects resources (CAS experiences with approval status, hours per strand, supervisor, CAS-project flag and dates; hours required; PBL projects including the DP Extended Essay template for first assessment 2027; no TOK endpoint, no CAS reflection text); mirror rows arrive as `source = 'managebac_mirror'` (01 §2.10); pin v2p3 (v2p2 is being retired); ask ACS question 78 whether the EE PBL template is enabled; correct the Challenges and open decision 5. *Done in session 2 (2.33, 2.41 and others).*
- **F31.** Fix the Wellesmere fixture so its eight authored cases can be produced (gradebook due dates and missing flags; the mock dip as a level input); import `TutorStaffId` and `HeadOfYear StaffId` into `sis.tutor_group*` and `auth.pastoral_assignment` (01 §2.4, §2.5, F33); support the CI-only `wellesmere-shifted` variant (01 DR-8). *Done in session 2 (2.23, 2.48, 2.49 and others).*
- **F33.** The staff import (§4.8) creates pastoral assignments; §8.2's Wellesmere columns are imported. *Done in session 2 (2.13, 2.22 and others).*
- **F35.** Onboarding (§9 steps 3, 5, 8, 11, 17; §2.10): the tenant is created under the bootstrap grant (pass 4 §2.5, D44); mapping profiles are authored under a write grant; the caseload CSV loads through the Studio with a security section and preview; support rollbacks only under a write grant. *Done in session 2 (2.18 and others).*
- **F40, §12.18.** Note the phase each delta lands in (with pass 6's map). *Done in session 2 (2.52 and 2.55).*
- **F54.** Add Veracross's Data Export Package as delivery option (a) in open decision 1 and question 70 (scheduled, up to twice a day, 14 runs a week, status emails; five-minute query limit; each export overwrites the last); support password SFTP with IP allow-listing and rotation for that path (pass 4 §9.10); the Veracross `verify()` refuses the alerts and health scopes; the rate limit is 300 requests per 3 minutes per access token. *Done in session 2 (2.54, 2.56, 2.57 and others).*
- **F57.** List guardian-link gains individually in the preview's security section; uploader and approver differ for rosters, staff, contacts and caseloads; access-changing imports never auto-approve (reconcile with pass 4, which now agrees); fixtures only from a signed manifest; one person per SIS contact, never merged (supersedes open decision 4's option (b)); import restriction flags into `family.guardian_link.restriction` (D42); a new link for an activated contact is pending until the import is approved. *Done in session 2 (2.5, 2.7, 2.17, 2.26, 2.27, 2.31 and others).*
- **F61.** Onboarding screen-shares show real data only in person or on the school's own video platform (02:1062). *Done in session 2 (2.50 and 2.51).*
- **F65.** Attendance reasons for Wellesmere from a KHDA reason family, or a question, not DfE register codes; source systems and engagement sources as vocabularies; programme families as `ref.programme_family` (with `igcse`, `btec`, `ib_cp`) and `sis.enrolment_programme` for more than one programme; the IGCSE scale in §5.2. *Done in session 2 (2.21, 2.29, 2.38, 2.42 and others).*
- **F74.** Correct §7.3 to §7.8: API Plus has Rostering and Gradebook, no Resources, API format not OneRoster CSV; the 1EdTech listing is expired (one OneRoster 1.1 entry from 2022); ManageBac `deleted_since` covers students, parents and teachers only, `modified_since` is missing on attendance, grades, term grades and Projects, gradebook grades page at 10, tokens carry configurable scopes; a Classroom verified teacher sees only their own courses; the Edlink article does not mention `OAuth_App_Admin`; fix the §7.8 and §7.9 cross-references and 02:113 (§7.7 should be §7.8). *Done in session 2 (2.40, 2.43, 2.44 and others).*
- **F75.** D12's views are `security_invoker`; align §4.2's staff binding with pass 4 §2.1 (bind by email only when the person has no stored Google subject). *Done in session 2 (2.24, 2.28, 2.51 and 2.52).*
- **§12.4.** Step 18 no longer uses `sso_jit`; step 4: CAROS owns the OAuth client and the school configures app access; add Google's rule that users designated under 18 are blocked from unconfigured third-party apps. *Done in session 2 (2.51).*
- **§12.5.** Internal counts: six against seven saved queries (02:735, 02:1107, 02:738 to 02:746); DR-14's two against three retrospective domains (02:66, 02:718); §7.8 cited for §7.9 (02:70, 02:249, 02:328); `const` as transform and binding (02:50, 02:376). *Done in session 2 (2.2, 2.6, 2.8, 2.9, 2.16, 2.36 and others).*
- **§12.15.** Blob SFTP in UAE North remains a portal check; $0.30 an hour per account; Veracross's export offers password SFTP only. *Done in session 2 (2.54).*
- **§12.16.** `PREVIEW_ACCESS` stays in D9; pass 4 D48 no longer seeds it. *Done in session 2 (2.19 and 2.52).*
- **Style.** Two em dashes remain in 02; remove them when the file is revised. *Done in session 2 (2.3 and 2.20).*

### `03-signal-engine.md`

**Completed in session 2.** Every item below was applied to `03-signal-engine.md`; the changes named after each item are the principal ones, and the Session 2 table lists every change by finding. Nothing further is owed here.

- **F05, C9.** Attendance in days from the master register; a day-level floor; a minimum meaningful change above one day; unexplained absences counted in days over non-overlapping windows; z capped inside the CUSUM (for example at 3) with strong shocks through their own rule; the chart restarted when a case opens; exit and recovery on levels and the run-mean since the case opened, not on S; attendance held at weak until ACS's reason-code coverage is measured; detune before the pilot (day-level attendance, floors at the full typical noise, three weeks of persistence); re-trace Maryam and S3. *Done in session 2 (3.2, 3.4, 3.12, 3.13, 3.14, 3.19 and others).*
- **F06.** At least twenty in-band weeks in the fixture, or imported prior-year attendance; the expected diff generated by running the rules; a punctuality measure the authored series could come from, or late counts; one persistence definition (consecutive weeks with S ≥ h); publish `calibrate_tiers.py`; define "new items"; state the floor; regenerate §3.6, §7.3, §11.1 and the appendix from the engine's own level function. *Done in session 2 (3.3, 3.6, 3.15, 3.28, 3.30, 3.42 and others).*
- **F07.** §8.1 to §8.4: the engine may raise and never lower or auto-close a case with an unreleased escalation (pass 1's `signal.case_escalation_pin`); after the outcome a person sets the tier; add scenarios S24 (escalated case, quiet data, 60 days) and S25 (second concern on an escalated case). *Done in session 2 (3.32, 3.34, 3.35, 3.37, 3.46 and others).*
- **F08.** The rate-limit hold applies to lowerings and repeated reversals only; a raise into `checkin` or `urgent` is never held; add the case to S19. *Done in session 2 (3.4, 3.35, 3.36, 3.39, 3.46 and 3.53).*
- **F10.** §4.5 and §7.2 rows 2, 7, 8, 12: a flag of kind `safeguarding` is a referral that goes to the route, not a row-12 engine input (pass 1 C23). *Done in session 2 (3.4, 3.22, 3.29, 3.53 and 3.59).*
- **F23.** §15.2: the backtest runs as a job in UAE North, only aggregates leave, and the pseudonymisation key's holder is named. *Done in session 2 (3.54 and 3.61).*
- **F24, C9.** Shadow keeps its own case state as `signal.case` rows with `shadow = true` (pass 1 §2.9) and stores evidence against the evaluation; include backfill and event runs; import at least twenty weeks of history before shadow; replace point thresholds with a pre-registered interval pooled across counselors and a minimum of about 40 cases; blind the judgement with matched unflagged students; fix F05 first; rewrite D25's `v_shadow_queue` to read shadow cases and all run triggers (§14.1, §14.3, §14.5, 03:100 and 03:936). *Done in session 2 (3.7, 3.43, 3.52 and others).*
- **F25.** Carry academic baselines across the year within a subject for two-year courses; count history in assessments; use the self-starting band instead of the cap; a termly per-assignment file runs the weekly engine as a backfill capped at `review`; working grades need a two-step drop or a drop held across two windows; non-overlapping windows for counts; the screen says which domains are still building a baseline. *Done in session 2 (3.10, 3.11, 3.17, 3.18, 3.25, 3.26 and others).*
- **F26.** Drop the common-cause automatic cap; keep the ops note and the prompt to declare a `calendar_period`; keep the property test strict (§7.6, C5, `sweep.finalize`, O40). *Done in session 2 (3.27, 3.33, 3.59, 3.60, 3.62 and others).*
- **F31.** A Wellesmere expected-result list; resolve the precedence of termly delivery against relapse; the mock dip feeds a level; ordinal scenarios not IB-only; §7.2 rows 1 and 14 and §15.1 regulator-neutral. *Done in session 2 (3.3, 3.8, 3.18, 3.24, 3.25, 3.29 and others).*
- **F36.** §13.2 `sweep.schedule` and the sweep-not-completed alert key on schools holding data (`status IN ('shadow','live')`) through `core.schools_due_for_sweep()`, not "active". *Done in session 2 (3.47, 3.51 and 3.61).*
- **F40.** The engine writes `outside` on every point of `feature_snapshot.series` (pass 1's `week_run()` now reads it); resolve backfill writing several evaluations per run against `UNIQUE (school_id, sweep_run_id, student_id)`. *Done in session 2 (3.3, 3.13, 3.48, 3.56 and 3.61).*
- **F48.** Snapshot the vocabulary attribute values used into `rule_hits` and the evidence snapshot, with a test (pass 1 adds `config.vocabulary_history`). *Done in session 2 (3.4, 3.22, 3.27, 3.46, 3.53, 3.55 and others).*
- **F50.** §11.2 orders a tier by time raised; add the test that changing other students' data never moves a row within its tier (with pass 6). *Done in session 2 (3.43, 3.53 and 3.56).*
- **F52.** Remove the headline rephrase wording (03:1082); the rule headline stands alone. *Done in session 2 (3.22, 3.55 and 3.61).*
- **F64.** An `offer_condition_check` rule on the import of final results; calendar-day SLAs for university rules, or a results-period calendar kind (if chosen, pass 1's `sis.calendar_kind` needs the value). *Done in session 2 (3.23, 3.29, 3.34, 3.53 and 3.62).*
- **F68, C11.** One-step ordinal moves stay inside the band unless held across two assessments; row 8 needs the other domain at moderate or a weak held two weeks; register, unit and window per measure in the thresholds schema; for fairness, pre-registered comparisons pooled across quarters with intervals and a multiple-comparisons rule, and no labels collected during the pilot (§12.4, D26 unused in the pilot). *Done in session 2 (3.16, 3.21, 3.39, 3.44, 3.45 and others).*
- **F69.** AEI: replace the 1.6% to 4.1% figure with the report's statistic (unexcused share of absences, 35% to 55%); Faria et al. 2017: chronic absence −0.26 (primary model), course failure −0.17, no 0.23; Balfanz, Herzog and Mac Iver 2007: the 60% is for four flags (attendance, behaviour, mathematics, English); re-check every quoted sentence and number in pass 3 and mark any claim that cannot be re-found as an assumption. *Done in session 2 (3.8, 3.9, 3.45 and 3.58).*
- **F75.** D25's view is `security_invoker`. *Done in session 2 (3.52 and 3.56).*
- **F78.** Hana's attendance consistent between §4.7 and §7.2; Ahmed's floor 3.0; the impossible "14 weeks of history"; pilot scale about 350; example dates on the week start; S2 against §3.2 exclusion; rows 9 and 11 define "L = 1"; §2.3's "about 4%" is 6.2%. *Done in session 2 (3.40, 3.55 and others).*
- **§12.16.** Drop `sweep.completed` from D28 (pass 1 §2.20 seeds it). *Done in session 2 (3.56).*
- **§12.17.** Count reveal weeks from shadow's first sweep; the pilot agreement says so. *Done in session 2 (3.52).*
- **D30.** Use pass 4's keys `signal_shadow`, `counselor_log`, `fairness_label`. *Done in session 2 (3.56).*

### `05-ai-design.md`

**Completed in session 2.** Every item below was applied to `05-ai-design.md`; the changes named after each item are the principal ones, and the Session 2 table lists every change by finding. Nothing further is owed here.

- **F03, C1.** Remove the model layer and `cleared_by_model`; the lexicon is the only screen and every hit reaches a person; drop `fiction_or_quote` and `means_or_plan_mentioned`; rewrite §6.1 to §6.3, the pass bar (05:885), C22, and D68's `layers`; remove the `safety_screen` model feature from D74; revisit an add-only layer after one term. *Done in session 2 (5.26, 5.32, 5.33, 5.34, 5.37, 5.38 and others).*
- **F04.** The AI rules and §8 rest on ADEK consent under Digital Policy 7.1.3.a, a gate before G-AI. *Done in session 2 (5.15, 5.43, 5.47 and others).*
- **F09.** Alert routing by category is school configuration confirmed with the CPO (pass 4 §5.2, §5.7). *Done in session 2 (5.33 and 5.36).*
- **F10.** The screen reads teacher flag text (§6.2, 05:521). *Done in session 2 (5.33).*
- **F11.** §6.4's message split into "someone to talk to now" (24/7 lines only: 800-SAKINA; DFWAC for Dubai) and "to report that a child is being harmed" (116111, 800444, 800988 for Dubai), each with hours; no "any time" unless every line is 24/7; Estijaba removed; the CPO confirms the list before any student sees it and each number stores its last-verified date (question 129, D68's `safety_response`). *Done in session 2 (5.35, 5.37, 5.65, 5.73 and 5.77).*
- **F12.** Option (b): the CPO receives a reference-only "alert unopened" notice, never an automatic referral; §6.5 and D68 rewritten; `safety_alert_notice` and `alert_unopened_notice` join the content test; reconcile D71's `AI_SAFETY_ROUTED` with pass 4's `SAFETY_ALERT_ROUTED`. *Done in session 2 (5.18, 5.36, 5.37, 5.65 and 5.68).*
- **F13.** D68's alert is class `safety_alert` (staff-only), not `student_voice`; the student and mentor tiers cannot read `safety_flag` or extraction columns; a per-role flagged fixture. *Done in session 2 (5.36, 5.37, 5.38, 5.65, 5.66, 5.72 and others).*
- **F14, C4.** §8.5: AI generations are excluded from the default subject-access pack. *Done in session 2 (5.46).*
- **F20.** §8.3, the leak scanner and the family notice: exclusions hold for columns, free text can carry any class; the name rule tested on Arabic names, Arabic script and nicknames; recall measured on a corpus written by people who did not build the pseudonymiser; the school fingerprint stripped from the envelope; personal statements treated as not pseudonymisable. The in-country safety-screen item is moot after C1. *Done in session 2 (5.16, 5.44, 5.53 and others).*
- **F21.** The reader and the screen have their own policy rows allowlisted per record kind; extract lazily, only for kinds an enabled writer reads; drop `mentor.message` from D69; never extract urgent requests; a test fails when an extraction column fills with no consuming feature enabled; §7.2 and D69 agree. *Done in session 2 (5.40, 5.66, 5.75 and others).*
- **F23.** Question 132: counselors' questions collected in the tenant and rewritten onto synthetic personas in-country by a named person; §10.2 eval cases; the no-real-data test covers student names. *Done in session 2 (5.50, 5.51 and 5.77).*
- **F37.** C22's impact-assessment question is answered: a DPIA is required (pass 4 §1.1). *Done in session 2 (5.78).*
- **F44.** §3.0 and §8.4: the AI tier's grants generated from `ai.feature_policy.allowed_fields`; nightly brief pre-warming runs as the case owner under `caros_t_ai`; re-identification through `ai.reidentify()`. *Done in session 2 (5.20, 5.45 and others).*
- **F48.** `signal.safety_alert` keeps its lexicon version and evidence span, retained with the alert (the model and prompt version no longer apply under C1). *Done in session 2 (5.33, 5.36, 5.37 and 5.65).*
- **F51.** Rename the "Strongest match" stamp and the `discovery.strongest_match_margin` key ("closest fit"); lint UI copy for "match" (§3.5, §5, §9, D74). *Done in session 2 (5.23, 5.49, 5.52 and others).*
- **F52.** Drop the headline rephrase from v1 (05:11, 05:246, 05:355, 05:828); remove its D74 row and the `headline_rephrase_requires_review` key. *Done in session 2 (5.2, 5.7, 5.25, 5.48, 5.63, 5.76 and others).*
- **F53.** Keyword tags as a floor the model may add to with quoted spans, never remove; the student sees and corrects the tags and may submit any archetype; `answer_kind` never ends a session; the compiled co-pilot filter shown above every answer, with raw text searched too; `too_broad` and `doubtful` kept off the supervisor's decision screen. *Done in session 2 (5.4, 5.10, 5.17, 5.19, 5.22, 5.29 and others).*
- **F62.** Default ACS's rounds to the 2027 guide, `ee_2018` for resits; re-ask question 133 about the May 2027 and May 2028 cohorts (§0.2, Challenge 9, D70). *Done in session 2 (5.5, 5.24, 5.67 and others).*
- **F63.** The notice and C18 state both periods (flagged content up to 2 years, classification scores up to 7) and what "flagged" means; drop "pseudonymised fragment" as an overstatement. *Done in session 2 (5.43, 5.46 and 5.78).*
- **F69.** Correct the Naviance quotation and its attribution (05:84); re-check every quoted sentence and number in pass 5. *Done in session 2 (5.11, 5.13, 5.59, 5.73 and others).*
- **F70.** A thinking-token allowance per feature in the cost script, measured in B7.5's smoke test; relabel the §11.2 price table. *Done in session 2 (5.55, 5.56, 5.57, 5.58, 5.79 and others).*
- **F76.** No user id, or a random one per generation, in request metadata; the notice says "inference in the United States"; mentors abroad in the consent text (§8). *Done in session 2 (5.15, 5.43, 5.46 and 5.72).*
- **F77.** Drop per-target institution-wide admit rates from student and parent screens, or label them; fix `mentions_others` putting a second student into a single-student envelope; the coordinator confirms Extended Essay word limits and marks. *Done in session 2 (5.3, 5.12, 5.70 and others).*
- **§12.8.** D74's feature list without `headline_rephrase` and `safety_screen`; pass 1's CHECK now matches. *Done in session 2 (5.31 and 5.71).*

### `06-build-sequence.md`

- **F01, C7.** B0.5 to B0.8: the per-deployable login roles, `NOINHERIT`, the tier in the policy expression, `caros_migrator`; B0.8 tests that the web identity cannot `SET ROLE` to system, support or owner; the B0 bake-off runs on this rewrite (Opus 5.5 and Fable 5.1 on the same brief, judged by acceptance tests and cross-review).
- **F02.** B0.7 seeds the matrix through pass 1 §2.4a from `matrix.json`; §5.4 adds positive end-to-end tests for every verb through RLS.
- **F04, F39.** ADEK consent as a named gate before G-AI and before any EU cold copy (G5, G7, G21, G22); ask ACS about 7.1.3.a with its other vendors first (question 149), then the request and evidence pack (question 150), raised this term.
- **F05, F06.** B2.9, B2.16 and §5.2 acceptance on the corrected fixture and published script.
- **F07.** B3.4's brief and the backend `CLAUDE.md` (06:606): the pin, one open escalation per case, updates, linked escalations; scenarios S24 and S25.
- **F09, F55.** B3.5 and §9.2 (06:1031, 06:1032): the reporter's countdown from the moment of escalation, category routing configuration, 999 and the Principal on `immediate_risk`, no clause numbers in UI text, 80085 labelled as the Ministry of Education's line.
- **F10.** B3.14: the flag modal's disclosure rule, the safeguarding flag kind as a referral, the lexicon on teacher text, flags for subject and supervision relationships.
- **F11, F12.** B5.12 and B3.8: the helpline split confirmed by the CPO; templates `safety_alert_notice` and `alert_unopened_notice` with the content test.
- **F13.** A per-role flagged fixture test.
- **F14, C4.** B4.3 rewritten with an escalated-student fixture and the default exclusions; §9.3.
- **F15, F16.** B3.3 and B3.6: break-glass for practising counselors with the lead's same-day confirmation, capability four eyes, the `school_admin` role, `notify.enqueue()`.
- **F17.** B6.3 tests the approval trigger (student, kind, held_at window, notes, version); the backend `CLAUDE.md` line (06:608) names the trigger.
- **F18.** Remove RISC from B1's scope (06:43, 06:107), B1.15, §1.13's thin slice, the §9.1 G-REAL checklist and O23; the Directory read is mandatory, hourly for staff; the leaver runbook step.
- **F19.** A B1 spike before B1.14: `auth_time` through `claims`, the ten-minute rule, a live-session test; the staff passkey fallback.
- **F20, F21, F44, F53.** The B7 tasks follow pass 5's revision.
- **F22.** G22 and R6: the cold copy's real choice, with key material abroad and an EU restore drill on synthetic data.
- **F23.** C3 (06:1095), §2.4 and the hooks (06:679, 06:680): Read and Bash hooks refusing paths outside the repository and `az` or `psql` against production; no production credential on agent machines; §5.8's canary off GitHub-hosted runners; the backtest, canary and restore checks as jobs in UAE North.
- **F24, C9.** §1.11 shadow on the live engine; at least twenty weeks of history before shadow; G-LIVE bars as pooled pre-registered intervals.
- **F25, F26.** B2.4 and O40 follow pass 3's revision.
- **F27, C10.** B9.3, R12, O27: the ManageBac CAS mirror moves earlier than B9.
- **F28, C2, C3.** B3.8, B5.4, B6.14 and §7.4 (06:949) build exactly pass 4 §5.8's templates; everything else in-app.
- **F29.** B0.11 seeds the fictional coordinator; the blocklist covers every real person in PRODUCT.md and the prototype; G2's consent letter names third-party sales demos.
- **F30.** B5.2: `seed://` rows in staging only, a production check, "not on file".
- **F31.** B2.16, B3.4's brief, B8 (06:328, 06:345), §9.2: the regulator profile; Wellesmere's expected results; the no-ADEK test; the CI-only shifted tenant; remove "UCAS-only" from the Wellesmere assertion.
- **F32.** B5.6 and B5.7: sectioned statements, reference formats, the centre statement, the predicted-grade snapshot, internal deadlines.
- **F33.** Place pastoral assignments and tutor groups.
- **F34.** B8.12 and §1.12 (06:405): per-tenant aggregate tables under RLS.
- **F35.** B1.13 (staging maps only CAROS domains), B4.12, §1.11 (06:384): the bootstrap grant and write grants.
- **F36.** §1.11 (06:379 to 06:385): ACS moves `onboarding → shadow` at G-REAL and `shadow → live` at G-LIVE; §7.4 (06:932) alerts key on holding data; pg-boss under its own login.
- **F38, C7.** Regenerate §4.1 to §4.3 from the task tables; add a Fable review to every silent-error task F38 lists (B2.9, B2.13, B2.8, B2.15, B4.15, B4.6, B7.5, B7.16, B7.18, B8.12, B0.12, B0.13, B1.5, B1.17, B3.7, B5.11, B6.8, B6.11, B8.7, B8.11); under C7 Opus 5.5 writes every task and Fable 5.1 reviews diffs only, so F38's "make B2.9 Fable-written" becomes "Fable reviews B2.9"; a category where the bake-off shows Fable clearly better moves back to Fable.
- **F39, C8.** Replace week counts with D-session counts on the critical path; target G-REAL in the first fortnight of the spring term 2027 (April 2027), Davide building alone; map the phases onto ACS's 2026-27 and 2027-28 calendars; back-schedule every external gate (pen-test booking, DPA, DPO, insurance, ADEK consent, the ACS IT review, and now the DPIA) from April 2027; plan against subscription usage limits, not dollars; drop the ownership rebalance until a teammate exists; state plainly what must be cut or deferred for April to hold; fix C2's backfill wording.
- **F40.** Make §1.12 the single source; D61 in B0 and D75 in B7; regenerate B1.1, B2.10 and B7.1; fix the placement gaps (`core.meeting`'s FKs, `uni.destination` read by B8, `ai.generation`, `ai.model_config`, `ai.pseudonym_map`); dedupe `PREVIEW_ACCESS` and `sweep.completed`; the migrator's non-transactional step; D42, D44 and D46 land with `auth.allowed()` in B0; place pass 8's new pass 1 objects: `ref.programme_family`, `ref.destination_system`, `ref.statement_format`, `ref.reference_format`, `ref.regulator_profile`, `sis.enrolment_programme`, `sis.tutor_group`, `sis.tutor_group_member`, `auth.capability_grant`, `auth.pastoral_assignment`, `config.vocabulary_history`, `signal.case_link`, `signal.escalation_update`, `uni.centre_statement`, `uni.school_deadline`, `privacy.retention_override`, `privacy.non_tenant_store`, the `reporting` and `jobs` schemas, and the projection views of pass 1 §2.4a.
- **F41, F61.** B4 and B4.14 follow pass 6 decision C5; application-layer retests on staging, production-layer retests on production while it holds only canaries; yearly tests' rules of engagement; G12.
- **F42.** B4's acceptance (06:230), §5.4, §5.8, §7.2 and O34: the canary pair unbranded with `purpose = 'canary'`, a Container Apps job in UAE North; a test that every report keys on `purpose`; close O34 as "train on staging".
- **F43.** §7.1, §7.2, B4.7, B4.10, B4.11, §9.3, G2: the pinned demo revision and deploy freeze; drills in their own subscription; staging maps only CAROS test domains.
- **F45.** §7.5: a job that pages the second person if an alert is still firing after 15 minutes.
- **F46, C8.** §2.4 and §2.5: solo mode, review service level, CODEOWNERS when a second builder joins.
- **F47.** §12 item 6, B0.7, O54 and §5.4: narrow-only by default, the `allow_counselor_widening` option, tests on pass 4 §3.1's rule.
- **F49.** The backend `CLAUDE.md` quotes CONTEXT §4 verbatim, with the backend's mechanisms beneath each invariant.
- **F50.** Add the prototype's sorts (the sheet's "loudest run", the CAS cohort by gap) to §6.3; bind B3.9 to tier then time; replace the DOM grep with the ordering test, on B3.9.
- **F51.** Add "Strongest match" and "asked to be matched" to §6.3; O72.
- **F52.** Remove the headline rephrase from B7.11.
- **F54.** B4.9 and B9.2 follow pass 2's revision.
- **F56.** B5.13: the under-13 gate reaches the record, with consent checked at import.
- **F57, F58, F59.** The parent import rules; §6.2 (06:853) the teacher sign-off view without a band; the mentor tasks.
- **F60.** §7.4 (06:928): the ops channel receives counts, not ids; email status polled.
- **F62.** B6 (06:262), G19, R13: the 2027 guide by default.
- **F65.** B5 (06:237), B6.6 (06:276), §6.2 (06:856, 06:861): per-school portal rules, the catalogue per school, archetype steps by programme, the IB-only limitation documented.
- **F66.** §6.2's `psreviews` row (06:846) and §6.3: the "AI read" score is not ported.
- **F70.** B7.17: the thinking allowance.
- **F72.** Move O11, O33 and O35 to the closed list; the audit key count is 37 with computed actors; map `CONCERN_SUBMIT` and drop the `AI_DRAFT` letter entry (pass 1 DR-8 now does); place the CAS "Ask to join" action (`index.html:9311`) in B6 or record that it is not ported.
- **F75, F76.** No framework caching on authenticated routes; the IT checklist's Chrome policy line; self-hosted fonts (§9.1).
- **F77.** B8.12 defines ADEK's four indicators over one population.
- **F78.** §4.4 (06:743) and §14: tag each consolidated ACS question with the gate it blocks.
- **§12.1 to §12.18.** Mark §12 items 1, 2, 3, 6, 7, 8, 9, 10, 11, 12, 13, 14 and 16 as resolved by pass 8 in passes 1 and 4; items 4, 5 and 15 go with pass 2's revision; item 17 goes to the pilot agreement; item 18 is F40.
- **C5, C6, C11.** Add question 148 to §14; mentor sessions in person only in v1; the pilot documents state that no fairness audit runs.
- **New ACS questions and counsel questions.** Add pass 4's questions 148 to 155 and counsel questions C24 to C32 to §14's consolidated lists; record question 126 as withdrawn.

**Added by session 2.** What the revisions of passes 2, 3 and 5 add for pass 6. The session 1 items above still stand; where an item here makes one of them concrete, it says so.

- **F40, §12.18 (§1.12, B0, B1.1).** Pass 2 §10 now records the phase §1.12 lands each of D1 to D16 in, and marks where the task rows disagree: B1.1 lists D8, D11, D14 and D15, which §1.12 lands in B0, B5, B0 and B0 (as matrix rows); B0's spine creates `doc.file` "with D13 kinds", which §1.12 lands in B1. Pass 3's B2.10 line ("D17 to D32") needs the same check against §1.12.
- **F56 (§1.12, B1.5, B1.12, B5.13).** The import's under-13 check runs in B1 and reads pass 4 D58's `sis.student.under13_custodian_consent_at`, which §1.12 lands in B2: land that column (or all of D58) in B1, with `ingest.consent_decision` (pass 2 D2) and the Studio's consent decisions in B1.12. Makes session 1's F56 item concrete.
- **F27, C10 (B9.3, B6, R12, O27).** The ManageBac CAS and Extended Essay mirror (v2p3; D10's mirror columns) moves from B9.3 to B6, with the `ib.*` tables it writes. B9's scope text naming `/v2p2/auth/permissions` (06:355) says v2p3. R12's mitigation and O27's CAS and EE half close under C10; the Maia half stays open.
- **F57 (B1.4, B1.6, B1.9, B1.11, O25, §1.11).** B1.4's acceptance ("the shared-email guardian pair becomes one person with two contacts") becomes two persons, one per SIS contact; B1.6's security section lists guardian links one by one, restrictions, reactivations, caseload and pastoral changes; B1.9's fixtures load only from a signed manifest; B1.11 refuses approval by the uploader for roster, staff, contacts, caseload and pastoral batches; O25 closes as "facts only"; §1.11's week 3 row limits auto-approval to fact kinds.
- **F06, F25, F31 (B1.9, B2.9, B2.16, §5.2, §5.5).** Fixture generators add the `2025-26-history/` packs for both tenants, the attendance and late-day series pass 3 §7.2 re-authors, Wellesmere's gradebook columns (`AssessmentType`, `DueDate`, `Submitted`) and a Lent term-grades-only gradebook, the `wellesmere-shifted` `expected.json`, and ACS defects 19 to 25. `engine-reproduces-seed` runs over ACS, Wellesmere and `wellesmere-shifted` against generated lists. **The expected ACS result is pass 3 §7.2's, not "thirteen match"** (06:162, 06:772): 8 of 15 authored tiers match under pilot defaults, Maryam two weeks late, Ahmed and Priya one tier lower, Hana at `monitor`, Zayd, Maya and Fahad with no case. B2.9 commits `calibrate_tiers.py` from pass 3's appendix and reproduces its output within rounding, with a Fable review (F38 under C7). Makes session 1's F05 and F06 item concrete.
- **F05, C9, F25, F68 (B1.8, B2.1 to B2.5, B2.8, the B3 cadence panel).** B1.8's attendance normaliser gains the declared day rule, derived day rows, `register_disagreement` and reason coverage in `import.summary`. The engine tasks take pass 3's revised measures: per-assessment attainment points; attendance in days from the master register over fortnight blocks; floors at the full typical noise; the z cap; one persistence rule at three weeks; the chart restart; the ordinal hold; the two-year carry; attendance held at weak until coverage is measured, with a test that only a configuration version citing measured coverage releases it; the year-to-date policy floor; `relapse_watch`; D31's schema additions (`measures.<key>` with register, unit and window, `cusum.z_cap`, `attendance.max_level`, `typical_absence_share`) and removals (`min_history_full`, `exit_h_factor`, `common_cause_share`). The cadence panel shows coverage.
- **F26 (B2.4, O40; 06:159, 06:936).** Remove the common-cause guard from suppression; S12 as revised; the strict no-comparison property; close O40 (only the operations prompt remains; question 95 revised). Makes session 1's F25 and F26 item concrete.
- **F07, F08 (B2.6).** The escalation pin in the engine and the domain layer, including shadow cases; the rate limit on lowerings and reversals only; exit and recovery on levels; scenarios S19, S24 and S25, and S26 for results day (F64).
- **F40, F75, §12.16 (B2.10).** D17's new unique key, D20, D22's `triggering_state`, D25 rewritten as `security_invoker`, D26 unused in the pilot, D28 without `sweep.completed`, D30 with pass 4's keys.
- **F36, F34, F60, F42 (B2.12, B2.13).** `sweep.schedule` and the watchdog through `core.schools_due_for_sweep()`; reporting aggregates written per tenant, never materialised views; alerts carry counts only and key on `purpose`. New tests: the attendance-hold release, no regulator name in engine templates, the pin property, the vocabulary-snapshot property.
- **F24, C9, §12.17 (B4.15, §1.11, G-LIVE, §10.5 elicitation).** The comparison over shadow cases and every run trigger; blinded judgement with matched unflagged students (question 161); G-LIVE per pass 3 §14.5, with **the primary endpoint at `review` or above** because the detuned engine produces too few check-ins to judge at pilot scale, pre-registered floors, at least 40 blindly judged cases, lift over matched controls, and no fairness criterion; at least twenty weeks of history imported before the first shadow sweep; shadow until 40 judged cases (a planning estimate of five to eleven weeks) or the pre-registered maximum (question 162); Part F of the elicitation also collects the two G-LIVE floors. Makes session 1's F24 item concrete.
- **F18, §12.4 (B1.15, §1.13, §1.11 week 0, O23).** The Directory read is a `google_directory` resource on the delegated reader account, hourly for staff, built in B1 before the first real staff sign-in, not in B9 with the other connectors. Week 0 registers the Directory scope instead of a RISC receiver; CAROS owns the OAuth client and the school sets Limited and under-18 access.
- **F35, F16, F33, F65 (B1 scope, B1.2, B1.6, B1.8, B1.10, B1.16, B5, §1.11 week 0).** `school_admin` is a role, not a capability, in B1's scope; profile authoring by the onboarding engineer is a write grant; the caseload arrives as a `caseload` import through the Studio, and pastoral data as a `pastoral` import, with their targets in B1; the tenant, the first `school_admin`, the first domain and the route are created under the bootstrap grant; the `igcse_9_1` and `igcse_a_g` scale rows; `sis.enrolment_programme` written by the enrolment import.
- **F36, F23 (§1.11 week 0).** No import before a real tenant is `shadow`, so the pseudonymised sample follows G-REAL; the sample script blanks free-text columns and the sample never commits.
- **F54 (B4.9, B9.2, O24).** Makes session 1's F54 item concrete: B4.9 adds the password path for Veracross's Data Export Package (an Azure-generated password in Key Vault, 90-day rotation, Veracross's IPs allow-listed) and a watcher that moves each overwriting export into the immutable container within fifteen minutes; B9.2's `verify()` refuses the student-alert and Health scopes and rate-limits per access token; O24 lists the Data Export Package first.
- **F64 (B2).** `after_commit` marks students with committed final grades for pass 3's `offer_condition_check`.
- **B1.5.** The reason-code list grows: `unknown_counselor`, `under13_consent_missing`, `erased_subject`, `reactivation_required`, `restriction_imported`, `register_disagreement`, `day_derived:<rule>`, `fixture_unsigned`, `approver_is_uploader`, `conflict:selection.level`, `conflict:cas.hours_required`.
- **C1, F03, F09 to F13 (B7.9, B5, B3.8, B5.12).** The safety screen has no model, so B7.9 can ship with the first student-facing screen (B5) instead of waiting for B7 and G-AI: the lexicon, routing by category from `safety_alert_routes`, the "alert unopened" notice, one open alert per student, the per-role flagged fixture, teacher flag text, and the split helpline list confirmed by the CPO. 06:294, 06:308, 06:762 and 06:1217 still describe a model "layer 2", `cleared_by_model` and `means_or_plan_mentioned`. The content test covers `safety_alert_notice`, `alert_unopened_notice` and `parent_update_waiting`. Makes session 1's F11 and F12 item concrete.
- **B7.1 (F21, F52, C1, F12, F62, F32).** Pass 5's deltas as revised: D63's `initiated_by` and smaller `output_kind`; D68 rewritten (no `layers`, one open alert per student, class `safety_alert`, `urgent_request`, the `safety.lexicon` rule set, helpline confirmation, `open_24_7`); D69 on three record kinds; D70's confirmation columns and defaults; D71's renamed keys, without pass 4's three; D72's voice-sample format; D68 and D73 extend `config.rule_set_version.key`; D74 without a headline key.
- **B7.2 (F20, F77).** The name rule (no length floor, Arabic script, nicknames, people outside the directory); the school fingerprint stripped; single-student envelopes never carry a second student; the recall corpus written in-country by people outside the team (question 164), with its 0.99 bar as a gate before G-AI.
- **B7.4 (F04, F42, F76, F01, F44, F36).** The policy step checks `adek_consent_ref` and `purpose <> 'canary'`; no `metadata.user_id`; the AI tier only under `caros_web` and `caros_worker`; grants generated from the allowlist, with the CI test; pre-warming as the case owner (`actor_kind = 'user'`, `initiated_by = 'prewarm'`), only for schools holding data.
- **B7.5, B7.17 (F70).** Measure thinking tokens per feature (p50 and p99) on staging, replace the allowances in the cost script, set `max_tokens` from the measured p99; the cost board shows the AI line at about three quarters of per-school infrastructure at year 3. Makes session 1's F70 item concrete.
- **B7.7, B7.8, B7.10, §5.8 (F52, C1, F20, F21, F23).** No headline suite and no safety model suite; the safety suite measures the lexicon; the pseudonymiser recall corpus joins; counselors' questions arrive only through the in-tenant rewrite; the reader's policy row with `allowed_record_kinds`, lazy extraction and the extraction-consumer test; injection expectations under the lexicon.
- **B7.12 to B7.16, B7.18, B7.19 (F44, F53, C2, F21, F62, F77, C10).** Brief pre-warming as the owner; the compiled co-pilot filter above every answer, and raw-text search; the parent draft through the reader, delivered in-app with the content-free `parent_update_waiting` notice; Extended Essay feedback on `ee_2027` with coordinator-confirmed limits, model labels off decision screens, and the ManageBac-mirror path (pass 5 open decision 21, question 163); the degraded-mode matrix as revised; no safety model in promotions; labelling sessions for questions 129, 132, 133 and 164.
- **B8 discovery, O72, §6.3 (F53, F51, F65, F66, F52).** Keyword tags as a floor with student corrections, any archetype submittable, `answer_kind` descriptive only, "Closest fit" and `closest_fit_margin`, the copy lint for "match", programme-family steps. §6.3's not-ported list adds "Why this matched you" beside session 1's items.
- **G-AI, G21 (F04, F20, F21, F52).** Both gates (ZDR in writing, ADEK consent) plus the pseudonymiser recall bar; per-feature school acceptance covers five features and the reader's record kinds.
- **C11.** The pilot documents also state that D26 is unused and the fairness audit does not run (session 1's C11 item).
- **§14.** Add questions 156 to 164 and counsel question C33 to the consolidated lists; record the questions revised in place (pass 2: 70, 71, 73, 78, 81, 83, 84, 85; pass 3: 95, 97, 100, 106, 108; pass 5: 129, 130, 132, 133, 137) and counsel questions C18 and C22 (C22's impact-assessment half is answered: a DPIA is required).
- **Stale in 06 after session 2** (line numbers as of 2026-09-24): `common_cause` (06:159, 06:936), `cleared_by_model` and `means_or_plan_mentioned` (06:308), "layer 2" (06:294, 06:308, 06:762, 06:1217), `v2p2` (06:355), "thirteen" (06:162, 06:772).

### `01-architecture-and-data-model.md` and `04-security-privacy-compliance.md` (found by session 2)

Session 2 could not edit these files. Each item is a small inconsistency between a revised pass 2, 3 or 5 and a revised pass 1 or 4; the finding it traces to is given where there is one.

- **01 §2.10** (F27, C10). `ib.ee_essay` has no `source` column, though DR-7.4 says mirror rows arrive with `source = 'managebac_mirror'`; `ib.cas_entry` has no columns for ManageBac's approval status, the CAS supervisor or the CAS-project flag. Pass 2 D10 adds them; pass 1's DDL should carry them.
- **01 §2.16** (F57). The `ingest.import.uploaded_by` comment names roster, staff, contacts and caseload imports as needing a different approver; it should also name `pastoral`.
- **04 §3.6 against 01 §2.16 and 04 T8** (F57). The school admin's `ingest` cell reads "approve an import another person uploaded" without limiting it to access-changing kinds; pass 1, T8 and pass 2 limit the rule to them. Davide to say which is meant. T8's mitigations cell also says both "no auto-approval when non-empty (pass 2 §5.4)" and "such imports never auto-approve (F57)"; the first clause should go.
- **04 §2.5 rules 1 and 3** (F16). Nothing says how the second `school_admin` membership is created and confirmed when four eyes needs two people.
- **01 §2.4** (F36, F18). `core.schools_holding_data()` cannot list `onboarding` tenants, whose `school_admin` holders the hourly Directory read also covers, and it includes `suspended` and `offboarding`, where connectors must not run (pass 2 filters inside the tenant).
- **04 §2.1, §9.3** (F74, F18). The minimum Workspace administrator role the delegated reader account needs for the Directory read and for every Classroom course is not stated (pass 2 §7.6 leaves it to the school and question 110).
- **01 DR-8** (F36, F06). The seeded status of the synthetic tenants is not stated; `core.school.status` defaults to `onboarding`, and the sweep visits only `shadow` or `live` schools, so staging sweeps need the seed to set one of those. ACS's seeded thresholds version 1 carries the prototype's `acad 2`, which maps to `shock.z = 2.0`, below pass 3's bound of 2.5; the seed should start from the platform defaults.
- **01 §2.9** (F40, F05, F24). `signal.evaluation`'s DDL keeps `UNIQUE (school_id, sweep_run_id, student_id)`, which pass 3 D17 replaces; `signal.week_run()` aligns series by array position, which breaks with per-assessment and fortnight-block series (align by `week_start`); `signal.case_escalation_pin` looks up escalations on the case's own id, so it does not cover the student's shadow case (pass 3 pins shadow cases in the engine); `signal.feature_snapshot`'s comments name measure keys and an eight-week window that no longer exist.
- **04 §3.6** (F24). Confirm that `signal_shadow` covers shadow cases and their evidence items, not only evaluations; pass 3 places the blind judgements in `counselor_log`.
- **01 DR-4 tests and 04 §3.8** (F01). Both say a `caros_web` connection that sets `app.actor_kind = 'system'` under a user tier "sees exactly what that user sees"; 01 §2.4's `auth.allowed()` returns false for any non-user actor under a user or AI tier, so it sees nothing. Pass 5 plans on the function.
- **01 §2.9, §2.7, §2.8, §2.11** (F13, F11, F32). `signal.student_reflection` has no `urgent_request` column, which 04 §5.7 relies on; `ref.regulator_profile.reporting_contacts` and pass 4 D45's contacts have no field saying a line is 24/7; `config.rule_set_version.key`'s CHECK lacks `uni.classification` and `safety.lexicon` (convention 15 may prefer a vocabulary); `uni.author_voice_sample` has no format. Pass 5 D68, D72 and D73 add them.
- **04 D55** (C1). "The safety screen reads a mentor message through a single-row function" under the AI tier is stale: the screen is in-process code and never uses the AI tier.
- **04 §5.8, §5.7, §5.2** (F10, F09). `safety_alert_notice` says "a message from a student", but it is also sent for a hit on a teacher's flag ("a student safety alert needs your attention" fits both); §5.7's list of texts the lexicon reads lacks Extended Essay rationales, which pass 5 also screens; §5.2's route keys are an example, and pass 5 folds exploitation and grooming into `maltreatment`, which pass 4 does not state.

### `CONTEXT.md` (in `backend-plan/`, outside the scope of sessions 1 and 2)

- **F28, C2, C3.** Section 3's "Outbound notifications" row: content-free transactional email to parents (magic links and role-only notices); declared reference-only safety emails to staff (the counselor's safety alert, escalation reminders and deputy routing to the CPO's route, the CPO's "alert unopened" notice, grooming alerts to the mentor coordinator); everything else in-app.
- **F16, F10.** Section 3's "Roles" row: `school_admin` as a role and a `staff` role carrying capabilities, beside the five.
- **C1, F52.** Section 3's "AI features in v1": the safety screen is a lexicon with no model; no headline rephrase.
- **C6.** Invariant 11's note: v1 mentor contact is CAROS messaging and in-person sessions on school premises.
- **F05 (added by session 2).** Section 3's "Data cadence" row names "per-session attendance" in the weekly pack. The pack still carries the per-period register, but the engine now counts every attendance measure in days from the school's master (daily) register (pass 3 §0.1, §4.2); the row should say "per-assessment grades, and attendance per day from the master register (with the lesson register where kept)".
- **C10 (added by session 2).** Section 3's "Incumbent systems" row and section 10's ManageBac line: ManageBac stays the record for CAS and the Extended Essay, and CAROS mirrors them read-only through the v2p3 API (pass 2 §7.4); subject selection and its conversation gate stay CAROS-owned.

### `ACS-IT-QUESTIONS.md` (in `backend-plan/`, outside the scope of sessions 1 and 2)

- It carries the numbering the passes continue. Pass 6 §14 consolidates the questions, so adding 148 to 164 (session 2 added 156 to 164), and marking 126 withdrawn, there is optional; whoever does it keeps each pass's wording. Pass 3's question about the daily register was folded into pass 2's question 71 rather than added.

### Outside `backend-plan/` (Davide's decision; no session edits these)

- **F29.** `index.html` still names the prototype's coordinator "Mr. A. Diaz", the surname of the real ACS staff member PRODUCT.md names; the decision asks for a fictional name in the prototype too. The file is frozen as the sales demo, so this needs Davide's explicit go-ahead.
- **F62.** PRODUCT.md describes "the three mandatory reflection sessions" of the Extended Essay; both current ACS cohorts follow the 2027 guide's single reflective statement.
- **F06 (added by session 2).** Under the pilot defaults the engine reproduces 8 of the prototype's 15 authored tiers (pass 3 §7.2), so the Next.js demo seeded from generated results will tell some stories differently from the frozen `index.html` (Hana at `monitor` rather than `urgent`; Zayd, Maya and Fahad with no case). Whether the sales narrative follows the engine, or the counselors' elicitation retunes the defaults first, is Davide's call; nothing in `index.html` needs to change.

---

## Session 3 (2026-09-24): pass 6, the cross-pass check, the merged questions and the index

Session 3 revised `06-build-sequence.md` in place, applying every decided item that touches it and every item of the handoff above. It fixed the inconsistencies session 2 found in passes 1 and 4, applied the `CONTEXT.md` items, checked passes 1 to 6 against each other and fixed what disagreed. It merged every pass's questions into `../ACS-IT-QUESTIONS.md` with a gate tag on each, and wrote `README.md`. Nothing outside `backend-plan/` was edited. Pass 7's section B was respected: every kept design still stands, and where a decision changed one (the RLS layers, the thin slice, the anti-overwrite discipline, the canary, both synthetic tenants in every test), the change is the one the finding names.

The session was run by one lead (this changelog, 06, the README) with three helpers: one fixed 01, 04 and `CONTEXT.md` and then the contradictions found across passes 1 to 5; one compiled the open decisions and counsel questions and searched passes 1 to 5 for contradictions (read only); one merged the ACS questions. A final read-only check compared the revised 06 with passes 1 to 5.

### How the decisions were applied

These notes record where applying a decision needed a reading, so that Davide can check it. The first four change what the next six months look like.

1. **April 2027 is planned in sessions, against an assumed date** (F39). T, the first school day after ACS's 2027 spring break, is assumed to be Monday 5 April 2027 and B0 to start Monday 28 September 2026; question 165 asks for the real calendar. With one builder there is one critical path, so "D-session counts" became a single count: 116 build sessions before G-REAL, plus a 20% rework allowance (about 139), of which about 130 must be done by the code freeze at T minus 7 weeks for the pen test. At an assumed eight sessions a week over 18 working weeks (144) the margin is about a tenth. The plan says plainly: April holds at eight a week with rework at or under 20%; at seven only with every contingent cut; below that, or if a checkpoint fails, August 2027.
2. **What is cut for April** (F39). Nothing on pass 4 §9.13's list. Because shadow mode shows counselors no tiers, the counselor's morning (the sheet's four modes, the case file, cover, the command centre, `si-why`, the dimension pages, meetings) and Demo 1 move behind G-REAL, with the case file, the what-if run and the comparison report due by the first reveal session; the offboarding drill and the load test move before G-LIVE; the Profiles editor moves to B9. B5 and B6 no longer land during shadow but in 2027-28; B7 waits for G-LIVE, ZDR and ADEK. Six contingent cuts (about nine sessions) are named in order. G-DEMO therefore follows G-REAL, and under C4 ("from Demo 1") the frozen prototype stays the sales demo until then.
3. **"Plan against subscription usage limits"** (F39) **against C7's ZDR API organisation.** A ZDR organisation is metered per token, not a subscription, and Fable 5.1 cannot run under ZDR. The reading: Opus 5.5 build sessions are limited by the organisation's rate-limit tier and spend limit, Fable 5.1 reviews by a separate account's weekly usage limit (the tighter one), and Davide's attention caps both; eight builds and four reviews a week are the assumption, measured from week one. Open decision O104.
4. **Three checkpoints and the fallback.** CP1 on 23 October 2026 (measure the rate), CP2 on 17 December 2026 (B1 complete, engine green, pen test booked, counsel's C25, the DPA draft back, ACS's answers), CP3 on 15 February 2027 (the G-REAL scope code-complete, production up, pen test starting). CP2 or CP3 failing moves G-REAL to the first fortnight of 2027-28; §1.11 describes what that costs (shadow in the application season, B5 for the 2028-29 cycle).
5. **C7 in the task tables.** Every former `F max` task is now `O max` with a Fable review; the Rev column is the review rule; §4 is generated from the tables and a CI check fails when they differ (F38). The bake-off (B0.16) uses the first version's six reserved categories as its categories. The attempt counter's third attempt runs on Opus 5.5 at max effort with a Fable review of the two failed diffs, because Fable writes no task.
6. **C8's "human review never blocks a merge"** is read as: no required approvals in the ruleset (GitHub does not let an author approve their own pull request, F46), while the Fable review of an `F` task is recorded before merge and a "request changes" verdict blocks until addressed or overruled in writing. That review is a model's, not a person's, and C8 keeps "different-model review on the reserved list".
7. **The trigger for the full process** (C8) is defined as a person other than Davide authoring merged pull requests in three of any four consecutive weeks, or committing in writing to two days a week for a phase.
8. **§14 now points to `ACS-IT-QUESTIONS.md`** instead of keeping a second consolidated list. The handoff asked for questions 148 to 164 to be added to §14's lists; the task asked for all of them to be merged into `ACS-IT-QUESTIONS.md` with gate tags. Keeping both would recreate two lists that drift, so §14 keeps the "ask now" list, the counts by gate and the full counsel list with gates, and the questions live in one file.
9. **New gates G26 (ADEK consent) and G27 (the DPIA)**, because both are named gates in the decisions (F04, F37) and §8 had no row for either. G26 is a G-REAL gate only if counsel answers C25 "yes"; otherwise it precedes G-AI and any EU cold copy.
10. **Placements the single-source map (F40) forced**: `core.meeting` moves from B0 to B3 (its foreign keys' tables); `doc.file` to B1 (with D13); `uni.destination` to B8 (B8 reads it); all of D58 to B1 (the import's under-13 check reads it); scope predicates land in the migration of the table they read, as pass 4 asks for D42, D44 and D46; D14 is withdrawn (staged rows are pass 4's `raw_import` class).
11. **Consistency readings made by the helper that edited passes 1 to 5**, confirmed by the lead: the synthetic tenants are seeded `live` so their sweep runs; `core.schools_with_accounts()` (every status but `offboarded`) serves the Directory read; the nightly sweep starts only when the caseload lead enables it after the backfill sweep (`sweep_enabled_at`, `SWEEP_ENABLED`), so shadow weeks count from that sweep; `shadow → live` requires a recorded `g_live_signoff`; a suspended tenant returns only to its previous status; the bootstrap approval names two different people, the DPA signatory and the first `school_admin`, and the signatory confirms the second (`SECOND_ADMIN_CONFIRMED`); retention keys are pass 4's 27, seeded by pass 1 and updated by D50; staged rows 30 days; `mentor_coordinator` stays counselor-only; pass 4 §5.8 governs messages to people at a school, not CAROS's own on-call pages.
12. **The example `docs/PLAN.md`** in §3.2 uses placeholders, never invented progress figures.

**Numbering used.** Question for ACS 165 (the next is 166); no new counsel question (C23 revised in place; the next is C34); open decisions O87 to O106 in 06 §13 (the next is O107); gates G26 and G27; risks R19 to R21; tasks B0.16, B6.16 (the ManageBac mirror, moved from B9.3) and B9.10 (the Profiles editor, moved from B1.10); 06 §0.5 new. Audit actions added in passes 1 and 4: `SWEEP_ENABLED`, `G_LIVE_SIGNED_OFF`, `SECOND_ADMIN_CONFIRMED`. No section, D-number or question was renumbered.

### Changes to `06-build-sequence.md`

| # | Change | Caused by | Section |
|---|---|---|---|
| 6.1 | Revision note under the title | all | header |
| 6.2 | §0.1 declares one departure from CONTEXT §2 (a dated target, one builder); the narrowings revised: the map as single source, the matrix through pass 1 §2.4a, the thin slice split for April | F39, C8, F40, F02 | §0.1 |
| 6.3 | §0.2 audit-key count corrected to 37; `CONCERN_SUBMIT` mapped and the `AI_DRAFT` letter entry dropped | F72 | §0.2 |
| 6.4 | §0.3 notes what pass 8 changed in the requests (RISC withdrawn, D61, no safety model) | F18, F40, C1 | §0.3 |
| 6.5 | §0.4 rewritten: the phase table (Directory read, S1 to S26, B3 split around G-REAL, the canary, the drills subscription, the ManageBac mirror in B6, both AI gates), the April target paragraph, the overlay, the demos, the streams | F39, C8, F18, F24, C1, C10, F04, F42, F43 | §0.4 |
| 6.6 | New §0.5 summarising the revision | all | §0.5 |
| 6.7 | Conventions: streams as package boundaries in solo mode; `O` writes every task and `F` in Rev is the Fable review; the map as the single source; `wellesmere-shifted`; the no-ADEK assertion; the "cut or deferred" marker | C7, C8, F40, F31, F39 | §1.0 |
| 6.8 | B0 rewritten: per-deployable login roles with `NOINHERIT`; the tier in the policy expression; the matrix with every qualifier's mechanism and negative test, verbs through RLS, narrow-only widening with `allow_counselor_widening`; ID-only lookups; `notify.enqueue()`; per-tier audit allowlists; pg-boss under `caros_jobs`; `core.school.purpose` and the status model; regulator profiles; the spine's new tables; `core.meeting` and `doc.file` moved out; the fictional coordinator and the real-names blocklist; the solo ruleset; the Read and Bash hooks; the drill subscription; self-hosted fonts; OIDC for staging only; D61's tests in B0.8; B0.16 the bake-off; estimate 22 sessions | F01, F02, F16, F33, F34, F36, F40, F42, F47, F48, F65, F67, F75, F76, F23, F29, F43, F31, C7, C8 | §1.1 |
| 6.9 | B1 rewritten: the Directory read replaces the RISC receiver (B1.15); the step-up spike and `auth_time` or a staff passkey (B1.14); `school_admin` as a role; DNS-proved domains and CAROS-only domains on staging; a second approver for access-changing imports; signed fixture manifests; the shared-email guardians stay two persons; the under-13 check with D58 in B1; `caseload` and `pastoral` kinds; the day rule and reason coverage; B1.1's list from the map; the Profiles editor moved to B9.10; the support console bound to operator and table with write grants; Fable review on B1.5, B1.6 and B1.17 | F18, F19, F16, F35, F43, F57, F56, F05, F33, F40, F38 | §1.2 |
| 6.10 | B2 rewritten on pass 3's revised engine: days and fortnight blocks, floors at typical noise, the z cap, `persist` 3, the chart restart, the attendance hold, no common-cause cap, the pin, raises never held, shadow cases on the live lifecycle, S24 to S26 and W1 to W8, `calibrate_tiers.py` committed, expected lists generated for three tenants (8 of 15 for ACS), thresholds version 1 from the platform defaults, `schools_due_for_sweep()`, counts-only alerts keyed on `purpose`, reporting aggregates; the what-if cut behind G-REAL; Fable review on B2.8, B2.9, B2.13, B2.15 | F05, F06, F07, F08, F24, F25, F26, F31, F34, F36, F40, F42, F48, F60, F64, F68, C9, F38, F39 | §1.3 |
| 6.11 | B3 split into before and after G-REAL and rewritten: teacher referrals, the disclosure rule, flags for subject and supervision students, `routed_key`; one open escalation per case with an idempotency key, updates and links, the pin (S24, S25), C23 to C25; the reporter's countdown from escalation, 999 and the Principal, category routes, no clause numbers, 80085 labelled; role-only emails for exactly pass 4 §5.8's templates, send-time idempotency, polled delivery status; break-glass rules; meetings staff-only; the ordering test replaces the DOM grep; `spellcheck="false"`; Fable review on B3.7 | F07, F09, F10, F12, F15, F16, F28, F50, F55, F58, F60, F76, F77, C2, C3, F39, F38 | §1.4 |
| 6.12 | B4 rewritten: the pen test against production with split retests and yearly rules of engagement; the DPIA; the `onboarding → shadow` gate; the SAR pack under C4 with an escalated-student fixture; erasure over non-tenant stores with pseudonymised tombstones; per-school retention overrides; the canary as a UAE North job keyed on `purpose`; Customer Lockbox; drills in their own subscription; onboarding under the bootstrap grant with two named people, `enable_sweep()` and the G-LIVE sign-off; the Directory scope in place of RISC; B4.7, B4.11 and B4.15 cut behind G-REAL; B4.15 with the blinded queue and the pooled statistics; Fable review on B4.6 and B4.15 | F41, C5, F37, F36, F14, C4, F42, F23, F43, F61, F35, F18, F24, F39, F38 | §1.5 |
| 6.13 | B5 rewritten: `seed://` rows never render; the catalogue per school; admit rates counselor-only; statements sectioned per format; UCAS reference formats, the centre statement, extenuating circumstances never generated; content-free nudge, magic-link and confirmation emails; chases in-app; activation per SIS contact (co-holder dropped); portal rules; the lexicon-only safety screen moved here from B7.9 with category routes, the "alert unopened" notice and the split helplines; Fable review on B5.11; scheduled for 2027-28 | F30, F65, F77, F32, F60, C2, C3, F57, C1, F03, F09, F11, F12, F13, F66, F38, F39 | §1.6 |
| 6.14 | B6 rewritten: `ib.check_approval`, `review_meeting_version`, `ib.sign_off()` from a card without the band, imports never write picks, definer functions and column grants; the ManageBac read-only mirror moved here from B9.3 as B6.16; `ee_2027` by default and the coordinator's confirmation of limits and marks; the IB-only limitation stated; "Ask to join" not ported; Fable review on B6.8 and B6.11 | F17, F58, F27, C10, F62, F77, F65, F72, F38 | §1.7 |
| 6.15 | B7 rewritten: the ZDR and ADEK gates; the policy step's checks; the name rule, the fingerprint stripped, single-student envelopes; the AI tier's grants from the allowlist; pre-warming as the case owner; `ai.reidentify()`; no `metadata.user_id`; thinking tokens measured; the reader lazy for three kinds with the extraction-consumer test; five features; B7.9 now only the add-only layer's revisit; B7.11 dropped; the co-pilot's filter shown; EE labels off decision screens; Fable review on B7.5, B7.16, B7.18 | F04, F20, F21, F42, F44, F52, F53, F70, F76, C1, C2, F38 | §1.8 |
| 6.16 | B8 rewritten: keyword tags as a floor with spans, "Closest fit", any archetype submittable; programme-family steps; XP in its own class; mentors in person on premises, coordinator-made pairings, off-email invitations, the grooming alert; reporting as per-tenant aggregates; the regulator profile's four indicators over one population; `purpose`; Fable review on B8.7, B8.11, B8.12 | F51, F53, F65, F77, C6, F59, F34, F31, F42, C3, F38 | §1.9 |
| 6.17 | B9 rewritten: the Data Export Package needing no adapter; `verify()` refusing alert and Health scopes; the per-token rate limit; v2p3; the mirror moved to B6; the Classroom reader's role; the second school's regulator profile; B9.10 the Profiles editor | F54, F74, F27, C10, F18, F31, F39 | §1.10 |
| 6.18 | The pilot overlay rewritten against the 2026-27 and 2027-28 calendars: the status model, the sweep enabled after the backfill, twenty weeks of history, reveal weeks from the first shadow sweep, G-LIVE on pooled criteria with a recorded sign-off at the start of 2027-28, B5 and B6 in 2027-28, AI after both gates, no fairness labels; the fallback's consequences | F36, F24, C9, §12.17, F39, C11 | §1.11 |
| 6.19 | The migration map rewritten as the single source: the migrator's non-transactional step; predicates with the tables they read; pass 8's new pass 1 objects placed; D61 in B0 and D75 in B7; D14 withdrawn; D58 in B1; `core.meeting` B3, `doc.file` B1, `uni.destination` B8; `sweep_enabled_at` and `g_live_signoff` in B0; duplicate seed keys once | F40, §12.16, §12.18, F56, F36 | §1.12 |
| 6.20 | Thin slice: the Directory read replaces the RISC receiver; a fifth change for April | F18, F39 | §1.13 |
| 6.21 | §2 retitled; §2.1 streams unstaffed in solo mode; §2.2's notify seam through `notify.enqueue()`; **§2.3 replaced by the April 2027 schedule** (capacity, the session table, what the date holds on, committed and contingent cuts, what cannot be cut, three checkpoints, every external gate back-scheduled, the August fallback); §2.4 the solo ruleset, three review layers, secrets and development tooling; §2.5 rewritten as "Davide alone, and when that changes" with the trigger | F39, C8, F46, F23, C7, F04, F37 | §2 |
| 6.22 | §3: the brief example rewritten (one open escalation per case, teacher referral, the reporter's deadline); the attempt counter under C7; the `PLAN.md` example (Directory read, checkpoints, external gates, placeholders); `CLAUDE.md` quotes CONTEXT §4 verbatim with the mechanism under each invariant, plus the model and real-data lines; hooks `guard-prod` and `guard-paths` | F49, C7, F23, F07, F18, F39 | §3.1 to §3.4 |
| 6.23 | §4 regenerated from the task tables: writers and efforts, `xhigh` withdrawn, the review rule and the bake-off, the 98 reviewed tasks including every F38 task, capacity by usage limits and cost for completeness | F38, C7, F39 | §4 |
| 6.24 | §5: the suites (deployable roles, qualifiers, verbs, narrowing, jobs, `purpose`; S1 to S26 and W1 to W8; generated seed lists; §5.8 templates; the lexicon safety suite; AI-tier allowlist, extraction-consumer and recall tests); §5.2 rewritten; §5.4 verbs, narrowing, the canary job, the Wellesmere checklist without "UCAS-only" and with the no-ADEK test; §5.5 defects 19 to 25; §5.6 evals without headline or safety model suites; §5.7 the sweep's gate; §5.8 nightly jobs off the runners | F02, F47, F42, F23, F31, F06, F57, C1, F52, F20, F36, F29 | §5 |
| 6.25 | §6.2 rows revised; §6.3 eight rows added (the within-tier and CAS sorts, the "AI read" score, the "match" strings, the band on the sign-off card, per-target admit rates, the coordinator's name, "Ask to join", the three reflection sessions); §6.5 demos re-timed and re-scripted | F50, F66, F51, F58, F77, F29, F72, F62, F39, F43, F06, F10 | §6 |
| 6.26 | §7: the demo slot and deploy freeze; staging maps only CAROS domains; the drills subscription; production holds only customer tenants and the canary pair (O34 closed); the migrator's step; counts-only alerts keyed on `purpose`, providers named, polled delivery status; Directory-read and safety alerts; support-grant notices in-app to the school; the paging escalation job; the cold copy as a real choice; new runbooks | F43, F42, F40, F60, F45, F22, F04, F18, C3 | §7 |
| 6.27 | §8 gates revised (G1, G2, G7, G8, G10, G12, G15 to G23, G25) and G26 (ADEK consent) and G27 (the DPIA) added | F04, F29, F43, F18, C5, F41, F09, F11, F24, F54, F62, F20, F23, F22, C11, F37 | §8 |
| 6.28 | §9 checklists rewritten for the IT team, the CPO and the counselor | F04, F18, F76, F37, F09, F10, F11, F12, F55, C4, C6, F24, F47, F57 | §9 |
| 6.29 | §10: R19 (the date), R20 (ADEK consent), R21 (usage limits) added; R1, R2, R6, R7, R10, R12, R13, R16, R18 revised; reordered by expected cost | F39, F04, C7, F54, F05, F06, C9, F22, C8, C5, C10, F62, F29 | §10 |
| 6.30 | §11: the decisions paragraph names F39, C7 and C8; C2's backfill wording corrected; C4 to C6 refined; C3 superseded by C7; C6 and C7 challenge text | F39, C7, C8, F43, F41, F42, F38 | §11 |
| 6.31 | §12: every item marked resolved, with where | A.5 | §12 |
| 6.32 | §13 rebuilt by gate (a new tier before the first shadow sweep); 17 closed with their reasons; O20 and O21 decided for now; 20 rows changed; O87 to O106 added | F72, and the decisions each row names | §13 |
| 6.33 | §14 replaced by a pointer to `ACS-IT-QUESTIONS.md`, the "ask now" list and counts by gate; §14.8 lists C1 to C33 with the gate each blocks; C23 revised under C7 | F78, C7, F04 | §14 |
| 6.34 | Sources: the revision retrieved no new page; the facts it adds are cited to pass 7's findings; §2.3's dates marked as assumptions | F69's discipline | Sources |
| 6.35 | After the final cross-check against passes 1 to 5: D29 (`sweep_hour`) moved to B0, since `core.schools_due_for_sweep()` reads it; `signal.teacher_flag` and `signal.v_last_contact` moved to B3 with the tables they reference; the sweep's reporting aggregates named (`reporting.agg_tier_daily`, `reporting.agg_event_weekly`) and created in B2, `reporting.agg_indicator` in B8; B0.5's migration files and the tier roles' `NOLOGIN NOBYPASSRLS`; D14 removed from the spine; B3.1 and B8.1 aligned with the map; `uni.destination` defined as introduced by this pass; pass 3 D24's `signal.shadow_judgement` placed in B4; B3.8's template list aligned with pass 4 §5.8 (no `meeting_confirmation` or `parent_activation`; `grooming_alert` named; the reference-and-URL test only for the escalation emails; six variables with `raised_by_role` among them); a C-number convention in §1.0 and the colliding labels fixed; G17's questions (70, 73, 81); R2's citation; §14.8's count | F40, F34, F28, F60, F78, consistency (session 3) | §1.0, §1.1, §1.3, §1.4, §1.5, §1.9, §1.12, §8, §10, §14.8 |

### Changes to `ACS-IT-QUESTIONS.md`

| # | Change | Caused by | Section |
|---|---|---|---|
| Q.1 | Every "Questions for ACS" section of passes 1 to 6 merged under the file's own headings, without duplicates: 116 numbers added (50 to 165) as 132 entries, 30 entries listing more than one number, every number from 1 to 165 present once (checked by script); each added question marked with its source pass; question 126 recorded as withdrawn; a reading guide added | task 5 | all |
| Q.2 | Every question tagged with the gate it blocks (`now`, G-REAL, `shadow`, G-LIVE, G-AI, B5/B6, B9, `none`); twelve entries marked "ask now" for an April 2027 start | F78, F39 | all |
| Q.3 | Question 165 added: ACS's calendars for 2026-27 and 2027-28 | F39 | §1 |
| Q.4 | Question 78: whether IT would restrict the ManageBac token to a fixed IP address (the NAT gateway's timing) | consistency (session 3) | §1 ManageBac |
| Q.5 | Question 49: the consent letter names training and demonstrations to other schools; the demo's coordinator is fictional | F29, F43 | §7 |
| Q.6 | The questions the decisions created are present and clearly worded: 148 (parental access at 18, C5), 149 and 150 (ADEK 7.1.3.a with other vendors, then the request, F04) | C5, F04 | §4 |


### Changes to passes 1 to 5 and `CONTEXT.md`

Made by the helper that edited those files in three rounds (the handoff items from session 2 and `CONTEXT.md`; the contradictions the cross-pass search found; alignment with the rewritten pass 6), each confirmed by the lead. Numbering continues each file's earlier tables; `CONTEXT.md` rows are numbered X.

### Changes to `01-architecture-and-data-model.md`

| # | Change | Caused by | Section |
|---|---|---|---|
| 1.87 | A line under the revision note: session 3 made further consistency fixes, listed in 08-changelog.md Session 3 | consistency (session 3) | header |
| 1.88 | RLS test corrected: a `caros_web` connection that sets `app.actor_kind = 'system'` under a user tier or the AI tier sees nothing, because `auth.allowed()` refuses any non-user actor under those tiers (the first wording said it "sees exactly what that user sees") | F01 | DR-4 |
| 1.89 | Facts layer follows pass 3 §7.2: Ahmed's last eight Mathematics assessment points on a history of 20 assessments with a median of 90.5 (the "eight weekly working-grade points" and the band of 82 to 95 no longer exist); attendance and punctuality authored in days and late days over twenty weeks of imported history | F06; consistency (session 3) | DR-8 |
| 1.90 | New paragraph on the synthetic tenants' seeded status and purpose: every synthetic tenant is inserted at `live` so the sweep visits it (demo tenants on staging and in CI, and the CI-only `wellesmere-shifted`, with `purpose 'demo'`; the production canary pair with `purpose 'canary'`); why `live` and not `shadow`; the move trigger governs updates and the attestation applies to real tenants only | F36, F06 | DR-8 |
| 1.91 | `S.thresh` row: ACS's `engine.thresholds` version 1 starts from pass 3's platform defaults; the prototype's values are not ported (`acad 2` maps to `shock.z = 2.0`, below pass 3's range of 2.5 to 4.5), and the version's note records this | F36, F06, F05 | DR-8 |
| 1.92 | `core.school.status` comment: account housekeeping keys on "has accounts" (every status but `offboarded`), through `core.schools_with_accounts()` | F36, F18 | §2.3 |
| 1.93 | `core.schools_holding_data()` returns exactly `shadow` and `live` (was also `suspended` and `offboarding`); a comment names its users (connector tick, data alerts such as `import.late`, AI pre-warming) and why suspended, offboarding and onboarding tenants are excluded | F36 | §2.4 |
| 1.94 | New ID-only definer `core.schools_with_accounts()` (every status but `offboarded`) for the hourly staff and nightly student Directory read and session expiry, which can only remove access; granted to `caros_worker` with the other pre-tenant lookups | F18, F36 | §2.4 |
| 1.95 | `ref.regulator_profile.reporting_contacts` gains `open_24_7`, which a `talk_now` contact must have true (pass 5 D68) | F11 | §2.7 |
| 1.96 | `config.rule_set_version.key` CHECK gains `uni.classification` (pass 5 D73) and `safety.lexicon` (pass 5 D68); a comment says it stays a CHECK rather than a vocabulary under convention 15, because each key names a schema a CAROS package publishes and a school can add versions, not keys | F13, F77, C1 | §2.8 |
| 1.97 | `signal.feature_snapshot` comments: pass 3's measure keys (`academic.<section>.attainment`, `attendance.days_missed`, `attendance.late_days`, `engagement.active_days`), units in days, `window_weeks` as the baseline window of 20 weeks of data, and the series point fields including `outside` and the block's `week_start` | F05, F68, F40 | §2.9 |
| 1.98 | `signal.evaluation` gains `backfill_of` and pass 3 D17's key `evaluation_one_per_week UNIQUE NULLS NOT DISTINCT (school_id, sweep_run_id, student_id, backfill_of)` in place of `UNIQUE (school_id, sweep_run_id, student_id)`; a comment says the table is created with D17's key and column because D17 lands with it in B2 | F40 | §2.9 |
| 1.99 | `signal.signal.contribution` comment: dropped by pass 3 D18 (level and polarity), never populated, instead of "pass 3 defines the semantics or removes it" | consistency (session 3) | §2.9 |
| 1.100 | `signal.case_escalation_pin()` also pins a shadow case when any unreleased escalation exists for the same student (escalations are never shadow and hang off the live case), so shadow behaves as live would, as pass 3 §14.1 says | F24, F07 | §2.9 |
| 1.101 | `signal.student_reflection` gains `urgent_request boolean NOT NULL DEFAULT false` (pass 5 D68; pass 4 §5.7's request, never extracted); the `safety_flag` comment says it points at the alert and is staff-only | F13, F21 | §2.9 |
| 1.102 | `signal.week_run()` rewritten to align series by calendar week (`week_start`), not array position: it builds the last `p_weeks` Mondays, lets a fortnight-block point cover both its weeks (pass 3 D20's `cadence`), and counts measures, not points, per week | F40 | §2.9 |
| 1.103 | `ib.cas_entry` gains pass 2 D10's mirror columns `approval_status`, `supervisor_person_id` (with its composite foreign key to `core.person`) and `is_cas_project` | F27, C10 | §2.10 |
| 1.104 | `ib.ee_essay` gains pass 2 D10's `source text NOT NULL DEFAULT 'caros' CHECK (source IN ('caros','import','managebac_mirror'))`, which DR-7.4 already assumed | F27, C10 | §2.10 |
| 1.105 | `uni.author_voice_sample` gains pass 5 D72's `format_key` (references `ref.reference_format`) and `section_key`; a sample with no format is not used | F32 | §2.11 |
| 1.106 | `ingest.import.uploaded_by` comment names pastoral imports and any batch whose security section is not empty beside roster, staff, contacts and caseload | F57 | §2.16 |
| 1.107 | NAT gateway line: from B6 if the school's ManageBac token is IP-restricted to a static egress address (an assumption to confirm with question 78), otherwise from B9 | F27; consistency (session 3) | DR-1 cost |
| 1.108 | The sweep schedule also requires the school's nightly sweep to be enabled (`core.school.sweep_enabled_at`) | F24, F36 | DR-2 |
| 1.109 | Facts layer: an ACS `2025-26-history/` pack delivered as one backfill batch, as in pass 2 §8.1, so every student has at least twenty weeks of history before the first sweep; Ahmed's per-assessment points end 91 ... 41 after at least twenty prior assessments carried from year 1 (median 90.5); no new numbers beyond passes 2 and 3 | F06, F24, F25 | DR-8 |
| 1.110 | `weeks[]` row: at least twenty in-band weeks of per-day attendance and per-assessment grades from the history pack, read by `signal.week_run()` by `week_start` | F06, F24, F40 | DR-8 |
| 1.111 | Synthetic tenants are seeded with `sweep_enabled_at` set as well as at `live` | F24, F36 | DR-8 |
| 1.112 | `core.school` gains `status_before_suspension`, `sweep_enabled_at` and `sweep_enabled_by_person_id` (set once by the caseload lead through `core.enable_sweep()` after the step-15 backfill sweep is reviewed, audited `SWEEP_ENABLED`; shadow week 1 counts from it) and `g_live_signoff jsonb` (who, when, the rule-set version whose note records the result; written by `core.record_g_live_signoff()`, audited `G_LIVE_SIGNED_OFF`), with CHECKs | F24, F36 | §2.3 |
| 1.113 | `core.check_status_move()` refuses `shadow → live` without `g_live_signoff`, records the status a suspension came from, and lets a suspended tenant return only to that status | F24, F36 | §2.3 |
| 1.114 | `core.schools_due_for_sweep()` also requires `sweep_enabled_at IS NOT NULL`, so `sweep.schedule`, `sweep.watchdog` and the outside alert all wait for it | F24, F36 | §2.4 |
| 1.115 | `ib.ee_essay`'s `proposed` CHECK applies only to rows with `source = 'caros'`: mirror rows carry no rationale or target (pass 2 open decision 20) | F27, C10 | §2.10 |
| 1.116 | Retention seed rewritten as one list with pass 4 §7.1's keys: all 27 classes seeded (adds `directory`, `attendance`, `behaviour`, `signal`, `discovery`, `xp`, `family`, `mentor`, `mentor_messages`, `documents`, `ai_pseudonym`, `counselor_log`, `fairness_label`, `staff`, `notice_ack`, `consent`); `academic` narrowed to grades, assessments, transcripts and IB records; `application` `delete`, as in §7.1; `ai_generation` 90 days; `raw_import` names staged rows (30 days); `after_expiry` CHECK gains `tombstone` and `keep`; a comment says D50 updates and inserts nothing | consistency (session 3); F77 | §2.21 |

### Changes to `02-ingest-and-integrations.md`

| # | Change | Caused by | Section |
|---|---|---|---|
| 2.60 | Staged rows purged 30 days after commit (was 90), or 14 days after `failed`/`discarded`, in pass 4's `raw_import` class through its `proposed_rule`; raw files keep 90 days | consistency (session 3) | §2.2 |
| 2.61 | `ingest.after_commit` enqueues `sweep.backfill` per import and `sweep.event` per student whose facts changed, which are pass 3's job names (was `sweep.run` with trigger `event`) | §12.1; consistency (session 3) | §2.4 |
| 2.62 | `ingest.connector.tick`: `core.schools_holding_data()` returns only `shadow` and `live`, never `suspended` or `offboarding`; the in-tenant status check is kept as defence in depth | F36 | §2.4 |
| 2.63 | The Directory read lists tenants through `core.schools_with_accounts()` (every status but `offboarded`, so onboarding tenants are covered) | F18, F36 | §7.6 |
| 2.64 | Status paragraph: entering `shadow` does not start the nightly sweep, which waits for step 15's enabling act; `shadow → live` only on step 19's recorded sign-off | F24, F36 | §9 |
| 2.65 | Step 3: the DPA names two different people, the signatory (school leadership) and the first `school_admin`; if it would name only one, the step cannot start | F16, F35 | §9 |
| 2.66 | Step 5: the second `school_admin`'s grant is confirmed by the DPA signatory and audited `SECOND_ADMIN_CONFIRMED` | F16 | §9 |
| 2.67 | Step 14: no nightly sweep has run yet, because the sweep is not yet enabled | F24 | §9 |
| 2.68 | Step 15: the first sweep is `sweep.backfill` over the history import (or `sweep.run` with trigger `manual` and a reason), started explicitly and not gated; after review the caseload lead enables the nightly sweep (`sweep_enabled_at`, `SWEEP_ENABLED`), and shadow week 1 counts from the first nightly sweep after it | F24, §12.1 | §9 |
| 2.69 | Step 19: the caseload lead activates the first live version and records the G-LIVE sign-off against it (`g_live_signoff`, `G_LIVE_SIGNED_OFF`) | F24 | §9 |
| 2.70 | Step 20: pass 1's trigger refuses `shadow → live` without step 19's sign-off | F24 | §9 |
| 2.71 | D14 withdrawn (no `staged_row` class; key kept so pass 6's map does not shift) | consistency (session 3) | §10 |
| 2.72 | Open decision 9 closed: 90 days for raw files, 30 for staged rows, both in `raw_import` (pass 4 §7.1) | consistency (session 3) | §12 |
| 2.73 | Handoff to pass 4: the reader account's role is answered by pass 4 §2.1, with the Directory read's minimum role left to question 110 | F74, F18 | §13 |
| 2.74 | The table's preamble: pass 6's revision made §1.12 the single source and its task rows agree with it; rows that recorded a disagreement say it is resolved (was "the row says so; pass 6 settles it") | consistency with pass 6's revision | §10 |
| 2.75 | D8's "Lands in": the first version's B1.1 also listed D8; resolved in pass 6's revision (was "disagreement") | consistency with pass 6's revision | §10 (D8) |
| 2.76 | D11's "Lands in": the first version's B1.1 also listed D11 before the `uni` tables existed; resolved in pass 6's revision (was "disagreement") | consistency with pass 6's revision | §10 (D11) |
| 2.77 | D13's "Lands in": the first version's B0 spine created `doc.file` with D13's kinds; resolved, `doc.file` lands in B1 (was "disagreement") | consistency with pass 6's revision | §10 (D13) |
| 2.78 | D14's "Lands in": the first version's B1.1 listed D14; pass 6's revision records it as withdrawn in §1.12 and B1.1 does not list it | consistency with pass 6's revision | §10 (D14) |
| 2.79 | D15's "Lands in": the first version's B1.1 also listed D15; resolved in pass 6's revision (was "disagreement") | consistency with pass 6's revision | §10 (D15) |

### Changes to `03-signal-engine.md`

| # | Change | Caused by | Section |
|---|---|---|---|
| 3.63 | Retention of fairness labels is pass 4's `fairness_label` class (deleted once each report is accepted, and within one year), not "the shortest class" | consistency (session 3) | §12.4 |
| 3.64 | `sweep.schedule` also requires `core.school.sweep_enabled_at`; synthetic tenants are seeded at `live` with the sweep enabled | F24, F36 | §13.2 |
| 3.65 | The outside alert: a shadow school whose nightly sweep is not yet enabled is not expected to run and raises nothing | F24, F36 | §13.8 |
| 3.66 | Idempotence: the evaluation key and `backfill_of` are in pass 1's DDL (D17) | F40 | §13.3 |
| 3.67 | History first: the nightly shadow sweep starts when the caseload lead enables it after the backfill sweep is reviewed (pass 2 §9 steps 14 and 15), not at G-REAL | F24 | §14.1 |
| 3.68 | Going live names pass 1's `g_live_signoff` and `G_LIVE_SIGNED_OFF`; the trigger refuses `shadow → live` without it, and a school suspended from shadow returns only to shadow | F24, F36 | §14.1 |
| 3.69 | Shadow weeks count from the first nightly shadow sweep after the enabling act; the reviewed backfill sweep is not counted | F24, §12.17 | §14.4 |
| 3.70 | The caseload lead records the G-LIVE sign-off against the first live version | F24 | §14.5 |
| 3.71 | D17 no longer adds `backfill_of` or swaps the constraint: both are in pass 1's DDL, because D17 lands with the table in B2 | F40 | §17 (D17) |
| 3.72 | "For other passes", pass 1: the D17 key is now in pass 1; the seeded status, sweep gate and G-LIVE sign-off are closed | F40, F24, F36 | For other passes |
| 3.73 | Reveal sessions: pass 6's overlay (§1.11) uses the same count and calls them shadow weeks 4 and 8 (was "counts from G-REAL, calls the same sessions weeks 6 and 10") | consistency with pass 6's revision | §14.4 |
| 3.74 | "For other passes", pass 6: migrations D17 to D32 land in the phases of pass 6 §1.12 (D27, D28 and D30 with B0's global seeds, the rest in B2), not "in order after pass 2's D1 to D16"; the common-cause cap's removal from B2.4 and O40 is marked done | consistency with pass 6's revision | For other passes |

### Changes to `04-security-privacy-compliance.md`

| # | Change | Caused by | Section |
|---|---|---|---|
| 4.72 | A line under the revision note: session 3 made further consistency fixes, listed in 08-changelog.md Session 3 | consistency (session 3) | header |
| 4.73 | "Roles come from two sources only": the tenant's second `school_admin` is excepted from confirmation by a second `school_admin` (§2.5 rule 1) | F16 | §2.1 |
| 4.74 | The Directory read runs for every tenant `core.schools_with_accounts()` returns (every status but `offboarded`), so `onboarding` tenants, whose `school_admin` holders already sign in, are covered | F18, F36 | §2.1 |
| 4.75 | New paragraph on the reader account's Workspace role: for Classroom, a domain administrator or a teacher of every course read (pass 2 §7.6); for the Directory read, an administrator privilege that can read users, the minimum to be confirmed with ACS IT under question 110; a custom read-users role, its availability and its sufficiency for `users.list` labelled assumptions | F74, F18 | §2.1 |
| 4.76 | Rule 1: the bootstrap grant is valid while the tenant has fewer than two `school_admin` memberships; the first `school_admin` nominates the second, confirmed in writing by the person named in the bootstrap approval (the DPA signatory's named contact), who is neither nominator nor nominee, recorded on the grant under the bootstrap grant and audited `SECOND_ADMIN_CONFIRMED`; once per tenant (aligned with pass 2 §9 steps 3 and 5, per the coordinator's correction) | F16 | §2.5 |
| 4.77 | Rule 3: four eyes for every capability and every `school_admin` membership after the second, confirmed by an existing holder who is not the grantee; nobody grants anything to themselves; the stale "names a second before onboarding" sentence replaced by a pointer to rule 1 | F16 | §2.5 |
| 4.78 | New audit action `SECOND_ADMIN_CONFIRMED` in §2.5's audited list and in §4.1's pass 8 additions (D48 seeds §4.1's keys, so no delta change is needed) | F16 | §2.5, §4.1 |
| 4.79 | The worker's cross-tenant reads add `core.schools_with_accounts()`; sweep, connectors and data alerts key on `shadow` or `live`, so nothing runs for a `suspended` or `offboarding` tenant; account housekeeping keys on every status but `offboarded` | F36, F18 | §3.2 |
| 4.80 | The school admin's `ingest` cell applies the F57 reading: an access-changing import (roster, staff, contacts, caseload, pastoral, or any batch whose security section is not empty) is approved only by someone other than its uploader; a fact import may be uploaded and approved by one `school_admin` | F57 | §3.6 |
| 4.81 | Rules under the matrix: `signal_shadow` covers shadow sweep runs, evaluations and signals, shadow cases and their tier and stage history, signals and evidence items; pass 3's blind judgements are `counselor_log` rows | F24 | §3.6 |
| 4.82 | Roles test corrected: a user tier or the AI tier with `app.actor_kind = 'system'` sees nothing (was "sees only what the user sees"), matching pass 1's `auth.allowed()` | F01 | §3.8 |
| 4.83 | The route keys are named as pass 5's (`suicidal_ideation`, `self_harm`, `maltreatment`, `severe_substance_use`, `other`); exploitation and grooming map to `maltreatment`, and unrouted categories fall to `other` | F09 | §5.2 |
| 4.84 | The lexicon's screened texts add Extended Essay rationales, and the list is the lexicon version's `screened_kinds` (pass 5 D68) | F10, F09 | §5.7 |
| 4.85 | `safety_alert_notice` wording is "a student safety alert needs your attention", which fits a student's text and a teacher's flag alike; the §5.8 trigger names a hit on a teacher's flag | F10 | §5.7, §5.8 |
| 4.86 | Quarantined reader row: the three v1 record kinds (teacher flags, parent messages, Extended Essay rationales); never `mentor.message`, `signal.student_reflection` or statement drafts, all dropped by pass 5 D69, so an urgent request is never extracted (the row said only "a reflection marked `urgent_request`") | F21; consistency (session 3) | §6.4 |
| 4.87 | T8: the stale clause "no auto-approval when non-empty (pass 2 §5.4)" removed; the bolded rule now names pastoral imports and any batch whose security section is not empty | F57 | §8 (T8) |
| 4.88 | D45: contacts carry `open_24_7` beside `verified_at`, and a `talk_now` contact must have it true (pass 5 D68 adds the field to this schema and to the regulator profile's) | F11 | §10 (D45) |
| 4.89 | D55: "the safety screen reads a mentor message through a single-row function" replaced by: the safety screen is in-process lexicon code with no model and never uses this tier | C1 | §10 (D55) |
| 4.90 | Google service-account key item: the reader account holds the least Workspace role that works (Classroom: a domain administrator or teacher of every course read; Directory read: to be confirmed under question 110) | F74, F18 | §9.3 |
| 4.91 | Rule 1: the DPA names two different people, the signatory (school leadership) and the first `school_admin`; if it would name only one, onboarding step 3 cannot start; the DPA signatory confirms the second `school_admin`'s grant (`SECOND_ADMIN_CONFIRMED`) (replaces round 1's "person named in the bootstrap approval") | F16, F35 | §2.5 |
| 4.92 | The `mentor_coordinator` holder is "a counselor", matching D35's `{counselor}` (was "a staff member") | consistency (session 3) | §2.3 |
| 4.93 | The worker paragraph adds that the nightly sweep also waits for `sweep_enabled_at` | F24, F36 | §3.2 |
| 4.94 | New audit actions `SWEEP_ENABLED` and `G_LIVE_SIGNED_OFF` (seeded by D48 from §4.1) | F24, F36 | §4.1 |
| 4.95 | Scope of the message inventory: it is the complete list of messages to people at a school; CAROS's own on-call pages carry counts and job ids only (F60) and may use SMS and voice; "SMS of any kind" becomes "SMS to anyone at a school" | F60; consistency (session 3) | §5.8 |
| 4.96 | A note above the schedule: its keys are the whole set, pass 1 seeds them all and D50 updates them, and `staged_row` is withdrawn | consistency (session 3) | §7.1 |
| 4.97 | The mentor messages row is keyed `mentor_messages` (was a second `mentor` row) | consistency (session 3) | §7.1 |
| 4.98 | The `raw_import` row names the staged-row rule (30 days after commit, or 14 after `failed` or `discarded`) | consistency (session 3) | §7.1 |
| 4.99 | D33 sets sensitivity on the rows pass 3 D30 inserts and inserts none; the stale "dots ... handoff" clause is removed | §12.16; consistency (session 3) | §10 (D33) |
| 4.100 | D50 updates every row pass 1 seeds (all 27 §7.1 keys, listed) with `proposed_days` or `proposed_rule`, and inserts none | consistency (session 3) | §10 (D50) |
| 4.101 | "For other passes", pass 6: migrations D33 to D61 land in the phases of pass 6 §1.12, with D42, D44 and D46 in the same migration as the predicates that read them (was "D33 to D60 after pass 3's D32, in the order given") | consistency with pass 6's revision | For other passes |

### Changes to `05-ai-design.md`

| # | Change | Caused by | Section |
|---|---|---|---|
| 5.80 | The `safety_alert_notice` wording quoted as "a student safety alert needs your attention; sign in to CAROS", fitting student text and teacher flags alike, matching pass 4 §5.8 | F10 | §6.5 |
| 5.81 | "For other passes", pass 6: the lexicon safety screen is taken out of the AI phase's build order; pass 6 builds it in B5.12 with the first student-facing screen (C1) | consistency with pass 6's revision | For other passes |
| 5.82 | "For other passes", pass 6: migrations D62 to D75 land in the phases of pass 6 §1.12 (D71's keys in B0; D68, D72 and D73 in B5 except D73's archetype columns; D70 in B6; D66 and the archetype columns in B8; the rest in B7), not "after pass 4's D61, in order" | consistency with pass 6's revision | For other passes |

### Changes to `CONTEXT.md`

| # | Change | Caused by | Section |
|---|---|---|---|
| X.1 | A line near the top: updated 2026-09-24 in pass 8 session 3 to reflect Davide's decisions on the pass 7 review, with the changes listed in `out/08-changelog.md` | all | header |
| X.2 | "Data cadence": the weekly pack carries per-assessment grades and attendance per day from the master register, with the lesson register where kept (was "per-session attendance") | F05 | §3 |
| X.3 | "Incumbent systems": ManageBac stays the record for CAS and the Extended Essay, mirrored read-only through the v2p3 API (pass 2 §7.4); subject selection and its conversation gate stay CAROS-owned | C10 | §3 |
| X.4 | "AI features in v1": the safety screen is a lexicon with no model; no model rephrases the rule headline | C1, F52 | §3 |
| X.5 | "Outbound notifications" rewritten: nudges to students; content-free transactional email to parents (magic links and role-only notices); the declared reference-only safety emails to staff; everything else in-app; pass 4 §5.8 the complete list; the first decision kept in brackets | F28, C2, C3 | §3 |
| X.6 | "Roles": `school_admin` as a role and a `staff` role carrying capabilities, beside the five (original text kept) | F16, F10 | §3 |
| X.7 | A note after invariant 11 (invariant wording unchanged): in v1, mentor contact is CAROS messaging and in-person sessions on school premises | C6 | §4 |
| X.8 | Section 10's ManageBac row: ManageBac stays the record for CAS and the Extended Essay, mirrored read-only through the v2p3 API; subject selection and its gate stay CAROS-owned | C10 | §10 |

### Decided items that needed nothing more in session 3

Every decided item was applied by one of the three sessions. Session 3 checked each finding's decision line against the six passes. It found no item that still needed a change after the tables above, except the items listed under "Left undone" below.

---

## Left undone after pass 8

Pass 8 is complete: every decision line in `07-review.md` has been applied where a document can apply it, and passes 1 to 6 have been checked against each other. What remains is outside the plan's reach, or waits for an answer:

1. **Outside `backend-plan/`, Davide's decision (O105).** `index.html` still names the prototype's IB coordinator "Mr. A. Diaz", the surname of the real ACS staff member PRODUCT.md names (F29); the prototype is frozen, so changing it needs Davide's go-ahead. PRODUCT.md still describes "the three mandatory reflection sessions" of the Extended Essay, where both current ACS cohorts follow the 2027 guide's single reflective statement (F62).
2. **The sales narrative (O102, F06).** Under the pilot defaults the engine reproduces 8 of the prototype's 15 authored tiers. The plan tells the engine's stories as they are; whether the counselors' elicitation retunes the defaults before the Next.js build replaces the prototype as the sales demo is Davide's call.
3. **The bake-off verdict (O103, C7).** Scheduled as B0.16; it cannot exist until B0 runs.
4. **The calendar (question 165).** Every date in 06 §2.3 counts back from an assumed T of Monday 5 April 2027; the real first day of ACS's spring term moves them all.
5. **Asking ACS and counsel this term.** The "ask now" questions (49, 149, 150, 165, 30, 31 and 114, 36 and 113, 2a and 70, 73, 83 and 100, 19 and 141, 32 and 109) and counsel's C25 first. Whether ADEK's consent (G26) is a G-REAL gate depends on C25; ADEK's response time is an assumption.
6. **Readings Davide should confirm.** O95 (C1's "no model reads student text" read as the safety screen only); O100 (a student with no date of birth treated as possibly under 13); O104 (the usage-limit reading of F39 against C7); O106 (the DPA signatory's written confirmation of the second `school_admin`); the reading of C8 in session 3 note 6 (a Fable "request changes" verdict blocks an `F` task's merge unless overruled in writing).
7. **Answers that decide open items.** Question 148 and counsel's C6 (parental access at 18, O21); counsel's C7 and C8 (the subject-access default, O20); the rest of 06 §13, ordered by the gate each blocks, with the same list in brief in `README.md`.
